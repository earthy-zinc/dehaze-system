<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pei.dehaze.mapper.SysUserRoleMapper">

    <!-- 根据用户ID获取角色ID集合 -->
    <select id="listRoleIdsByUserId" resultType="java.lang.Long">
        SELECT
            role_id
        FROM
            sys_user_role
        WHERE
            user_id = #{userId}
    </select>

    <!-- 获取角色绑定的用户数 -->
    <select id="countUsersForRole" resultType="java.lang.Long">
        SELECT
            count(*)
        FROM
            sys_user_role t1
            INNER JOIN sys_role t2 ON t1.role_id = t2.id AND t2.deleted = 0
            INNER JOIN sys_user t3 ON t1.user_id = t3.id
            AND t3.deleted = 0
        WHERE
            t1.role_id = #{roleId}
    </select>

    <!-- 批量查询角色关联的活跃用户名（去重，软删用户不参与） -->
    <select id="listUsernamesByRoleIds" resultType="java.lang.String">
        SELECT DISTINCT
            t3.username
        FROM
            sys_user_role t1
            INNER JOIN sys_user t3 ON t1.user_id = t3.id AND t3.deleted = 0
        WHERE
            t1.role_id IN
            <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
                #{roleId}
            </foreach>
    </select>
</mapper>
