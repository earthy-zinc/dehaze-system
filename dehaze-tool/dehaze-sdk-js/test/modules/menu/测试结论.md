发现的后端问题
POST /api/v1/menus 不返回创建的菜单ID - 与其他接口（如 POST /api/v1/dept 返回ID）设计不一致
GET /api/v1/menus/{id}/form 对不存在的菜单返回成功 - 应返回 404 或业务错误
PUT /api/v1/menus/{id} 对不存在的菜单返回成功 - 应返回 404 或业务错误
DELETE /api/v1/menus/{id} 对不存在的菜单返回成功 - 应返回 404 或业务错误
MenuForm.id 类型定义为 string 但后端实际返回 number - SDK 类型定义与后端返回不一致