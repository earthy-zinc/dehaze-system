| 测试用例           | 预期行为               | 实际行为（后端 bug）                  |
|----------------|--------------------|-------------------------------|
| 获取不存在部门的表单数据   | 抛出业务错误 A0400/B0001 | 返回 `{code:'00000'}` 但 data 有值 |
| 缺少必需字段 name    | 抛出参数校验错误           | 创建成功                          |
| 父部门不存在         | 抛出参数校验错误           | 创建成功                          |
| 不能将部门设置为自己的子部门 | 抛出业务错误             | 更新成功（循环依赖未校验）                 |
| 删除后查询          | 抛出错误或返回 null       | 返回成功但 data 有值                 |
| 批量删除后查询        | 同上                 | 同上                            |
| 级联删除后查询        | 同上                 | 同上                            |
