# 2026-07-12 工作日志

## 配置文件整理与迁移

### 问题背景
随着代码更新，dehaze-java 下的部分配置文件已过时或位置不当，需要清理和迁移。

### 完成的工作

1. **docker-compose.yml 清理**
   - 删除 `dehaze-java/docker-compose.yml`（旧版，已被根目录 `docker-compose.yml` 完全替代）
   - 删除 `dehaze-java/.env`（配合旧 docker-compose 的环境变量文件）
   - 根目录 `docker-compose.yml` 补充了 prometheus + grafana 服务（旧文件中有但根目录缺少的）

2. **config 目录迁移**
   - `dehaze-java/config/sql/` → `config/sql/`（4个SQL文件：schema.sql, data.sql, migration_20260711.sql, sys_wpx_file_data.sql）
   - `dehaze-java/config/redis/` → `config/redis/`
   - `dehaze-java/config/prometheus/` → `config/prometheus/`
   - `dehaze-java/config/logstash/` → `config/logstash/`
   - `dehaze-java/config/kube/` → `config/kube/`
   - 这些都是三后端共享的基础设施配置，不是 dehaze-java 专属

3. **代码路径引用更新**（6处）
   - dehaze-go: sys_input_history.go, sys_algorithm_version.go, sys_algorithm.go
   - dehaze-python: 003_add_algo_version_audit_img_history.py, sys_input_history.py, sys_algorithm.py
   - `dehaze-java/config/sql/schema.sql` → `config/sql/schema.sql`

4. **文档更新**
   - `03-数据库设计.md`: 添加 SQL 文件位置说明、补充 sys_input_history 表设计、ER 图、索引清单
   - `06-部署架构.md`: 更新配置文件参考表

### 待用户手动完成
- ~~删除 `dehaze-java/config/` 目录~~ ✅ 用户已删除

### 第二轮：其他过时内容修复

5. **测试 SQL 文件同步**
   - `dehaze-java/src/test/resources/db/schema.sql`: 补充 sys_algorithm_version、sys_input_history 表，sys_algorithm 添加 version/audit_by/audit_time/audit_remark 字段
   - `dehaze-java/src/test/resources/db/h2/sys_algorithm.sql`: 同步新字段
   - 新建 `dehaze-java/src/test/resources/db/h2/sys_algorithm_version.sql`
   - 新建 `dehaze-java/src/test/resources/db/h2/sys_input_history.sql`
   - 三处表数量统一为 19 张

6. **测试配置旧路径修复**（3处）
   - application-test.yml、application-test-h2.yml、application-test-tc.yml
   - 旧路径 `E:/ProgramProject/new-dehaze/dehaze-java/upload` → `./upload`

7. **硬编码密码修复**
   - `FastAutoGeneratorTest.java`: `"123456"` → `System.getenv().getOrDefault("DEHAZE_PASSWORD", "12345678")`
   - 注：`SystemConstants.java` 的 DEFAULT_PASSWORD 是业务层面新用户初始密码，不属基础设施范畴，未修改

### 备注
- `target/test-classes/` 下的旧路径是 Maven 编译产物，下次 build 自动覆盖
- `dehaze-java-cloud/` 和 `dehaze-java-cloud-plus/` 中可能存在类似的硬编码密码问题，未在本次范围内处理
