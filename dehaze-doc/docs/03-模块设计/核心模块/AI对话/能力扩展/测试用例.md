# AI 对话模块测试用例 — 能力扩展 (F-M08-006)

## 1. 测试目标

- [x] 验证 Skills 管理（列表/创建/启停/渐进式加载/执行过程展示）
- [x] 验证工具调用生命周期与可见性（状态展示/审计记录）
- [x] 验证高风险操作确认、连续失败保护
- [x] 验证虚拟文件系统容量限制、任务清单约束
- [x] 验证搜索工具配额与降级、代码执行沙箱安全边界
- [x] 验证能力访问与安全（最小权限/凭据保护/调用审计）
- [x] 验证 MCP Server 管理（外部接入/命名空间/凭据/健康/审计）与 SKILL 市场

## 2. 测试前置

| 数据类型 | 准备内容 | 说明 |
|---------|---------|------|
| **测试 Skill** | 2 个启用 Skill + 1 个禁用 Skill | Skills 管理验证 |
| **测试 Agent** | 1 个带 MCP 命名空间与文件权限配置的 Agent | 能力访问与权限验证 |
| **测试会话** | 含工具调用记录的消息（推理步骤含 thought/tool_use） | 工具调用可见性验证 |
| **Mock LLM** | 可编排工具调用序列（成功/失败/连续失败） | 工具生命周期与失败保护 |
| **Mock 沙箱** | 模拟代码执行（正常/超时/资源超限） | 代码执行安全验证 |

### 2.1 数据清理策略

- 测试 Skill 打标 `source=test`，测试结束后软删除
- 测试会话前缀 `test_mf_`，测试结束后软删除

---

## 3. 用例清单

### 3.1 Skills 管理

| 用例 ID | 测试场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|---------|---------|---------|------|---------|--------|
| `T-MF-080` | Skills 列表 | 含启用与禁用 Skill | GET Skills 列表 | 返回名称、描述、适用场景，启用/禁用状态正确 | P1 |
| `T-MF-081` | Skill 创建（管理员） | — | Markdown 指令 + 可选脚本/模板 | Skill 创建成功，出现在列表中 | P1 |
| `T-MF-082` | Skill 启停 | 已有 Skill | 管理员启用/禁用 | 禁用后 LLM 不再选用该 Skill，启用后恢复 | P1 |
| `T-MF-083` | Skill 渐进式加载 | 启动会话 | 初始化 | 仅加载名称和描述（几十 tokens），LLM 判断需要时才加载完整指令，上下文不膨胀 | P1 |
| `T-MF-084` | Skill 手动指定加载 | 会话进行中 | 用户指定"使用 xxx Skill" | 加载该 Skill 完整指令并按其流程执行 | P2 |
| `T-MF-085` | Skill 执行过程展示 | Skill 按步骤执行 | 观察消息流 | 执行中展示当前步骤与完成状态，与工具调用条目一致 | P2 |
| `T-MF-086` | Skill 删除解绑校验 | Skill 被 Agent 关联 | 管理员删除 | 拒绝删除并提示先解绑；无关联时软删除成功 | P1 |
| `T-MF-087` | Skill 试运行 | 已有 Skill | 输入测试数据试运行 | 返回指令执行效果预览，不入库不推送 | P2 |
| `T-MF-088` | SKILL 市场目录与共享 | 平台维护预设目录 | 查看市场/共享自建 Skill | 市场展示预设 Skill 与启用状态；共享需先启用 | P2 |
| `T-MF-089` | Skill 管理越权 | 普通用户 | 调用创建/启停/删除接口 | 返回 403（A0301） | P0 |

### 3.2 工具调用生命周期与可见性

| 用例 ID | 测试场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|---------|---------|---------|------|---------|--------|
| `T-MF-090` | 工具调用发起展示 | Mock LLM 调用 MCP 工具 | 发送消息触发工具调用 | 消息流出现工具调用条目（工具名 + 参数摘要），默认折叠 | P0 |
| `T-MF-091` | 工具调用状态流转 | 工具执行中 | 观察 SSE | 工具调用条目状态按 running → success 更新，前端徽标变化 | P1 |
| `T-MF-092` | 工具调用失败状态 | Mock 工具返回失败 | 观察 SSE | 状态更新为 error，失败原因展示，不阻塞整个推理 | P1 |
| `T-MF-093` | 工具调用审计记录 | 会话含 3 次工具调用 | GET 消息详情 | 推理步骤含完整工具调用记录（tool/toolInput/observation/status/latencyMs） | P1 |
| `T-MF-094` | 工具调用失败反馈给 LLM | 工具调用失败 | 观察后续推理 | 失败结果作为 tool 消息回传，LLM 决策重试/换工具/跳过 | P1 |
| `T-MF-095` | 工具返回格式校验 | 工具返回与 schema 不符 | 工具返回注入前 | 标准化错误（RETURN_SCHEMA_ERROR）回传，LLM 决策重试/换工具/跳过 | P1 |
| `T-MF-096` | 工具返回完整性校验 | 返回缺必填字段/超大 | 工具返回注入前 | 缺失校验失败返回错误；超大返回截断保护，不阻塞推理 | P1 |
| `T-MF-097` | 工具返回注入拦截 | 工具返回含注入模式 | 工具返回注入前 | 命中注入模式截断该片段，不中断推理（与上下文管理衔接） | P1 |

### 3.3 高风险操作确认

| 用例 ID | 测试场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|---------|---------|---------|------|---------|--------|
| `T-MF-100` | 高风险操作触发确认 | 写操作（覆盖已有文件） | LLM 调用该工具 | 不直接执行，推送 interrupt(type=confirm)，展示确认卡片 | P0 |
| `T-MF-101` | 用户确认后执行 | 确认卡片展示中 | 用户确认 | 按确认参数执行工具，推理继续 | P0 |
| `T-MF-102` | 用户修改参数后执行 | 确认卡片展示中 | 用户修改参数后确认 | 按修改后的参数执行 | P1 |
| `T-MF-103` | 用户拒绝后处理 | 确认卡片展示中 | 用户拒绝 | 推理取消，LLM 收到"用户拒绝"反馈，调整方案或说明 | P1 |
| `T-MF-104` | 只读操作不触发确认 | 读文件/检索/计算 | LLM 调用只读工具 | 直接执行，不弹出确认卡片 | P1 |

### 3.4 连续失败保护

| 用例 ID | 测试场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|---------|---------|---------|------|---------|--------|
| `T-MF-110` | 单次失败不阻断 | 工具首次失败 | 观察推理 | 失败反馈给 LLM，继续推理 | P1 |
| `T-MF-111` | 连续失败达上限剔除工具 | 同一工具连续失败 3 次 | 观察推理 | 工具从当前可用列表剔除，提示 LLM 更换能力 | P1 |
| `T-MF-112` | 新推理轮次恢复工具 | 工具被剔除后 | 用户发送新消息 | 失败计数重置，工具重新可用 | P2 |

### 3.5 虚拟文件系统与任务清单

| 用例 ID | 测试场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|---------|---------|---------|------|---------|--------|
| `T-MF-120` | 工作区文件读写 | 会话进行中 | LLM 写文件后读文件 | 读写成功，内容一致 | P1 |
| `T-MF-121` | 工作区容量超限 | 工作区接近上限 | 写入超限文件 | 工具返回错误，提示清理后再写入 | P1 |
| `T-MF-122` | 会话结束工作区清除 | 会话结束 | 新建会话 | 工作区内容清除，不跨会话残留 | P1 |
| `T-MF-123` | 任务清单创建与更新 | 复杂任务 | LLM 调用 write_todos | 任务列表三态流转正确（pending→in_progress→completed） | P1 |
| `T-MF-124` | 任务清单数量超限 | 任务项达 32 个 | 再添加任务 | 返回错误提示合并或精简 | P2 |
| `T-MF-125` | 已完成任务保留 | 任务完成 | 观察消息流 | 已完成项保留在列表底部作为过程记录 | P2 |

### 3.6 搜索工具与配额

| 用例 ID | 测试场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|---------|---------|---------|------|---------|--------|
| `T-MF-130` | 网络搜索返回结果 | 搜索服务可用 | LLM 调用网络搜索 | 返回标题+摘要+来源链接，单次 Top 5-10 条 | P1 |
| `T-MF-131` | 搜索配额超限 | 已达限频（30 次/小时） | 再次搜索 | 返回错误，提示配额用尽 | P1 |
| `T-MF-132` | 网络搜索降级知识库 | 网络搜索不可用 | 触发搜索 | 降级为知识库检索，回答基于检索内容 | P1 |
| `T-MF-133` | 回答引用溯源 | 基于检索结果回答 | 观察回复 | 回答中引用处标注来源，用户可核查 | P1 |
| `T-MF-134` | 本地文件检索范围限制 | 工作区外文件 | LLM 检索越权路径 | 检索被拒绝，不返回工作区外内容 | P1 |

### 3.7 代码执行沙箱

| 用例 ID | 测试场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|---------|---------|---------|------|---------|--------|
| `T-MF-140` | 沙箱执行 Python 脚本 | 沙箱可用 | LLM 执行 Python | 返回 stdout/exitCode，结果回传 LLM | P0 |
| `T-MF-141` | 沙箱执行超时 | 脚本死循环 | 执行脚本（无超时参数） | 单次执行超时（60s）终止，返回超时错误 | P1 |
| `T-MF-142` | 沙箱资源超限 | 脚本内存/进程超限 | 执行资源密集脚本 | 资源限制生效，执行被终止并返回错误 | P1 |
| `T-MF-143` | 沙箱隔离真实系统 | 沙箱会话 | 尝试访问宿主机路径/敏感网络 | 访问被拒绝，不影响真实系统 | P0 |
| `T-MF-144` | 高风险命令触发确认 | Shell 任意命令 | LLM 执行 Shell 删除命令 | 触发确认流程（见 §3.3），确认后才执行 | P1 |
| `T-MF-145` | 错误信息不泄露内部堆栈 | 脚本异常 | 执行失败脚本 | 返回结构化错误，不含内部堆栈路径 | P1 |

### 3.8 能力访问与安全

| 用例 ID | 测试场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|---------|---------|---------|------|---------|--------|
| `T-MF-150` | 最小权限—未关联能力不可调用 | Agent 未关联某命名空间 | LLM 尝试调用该命名空间工具 | 调用被拒绝，返回越权错误 | P0 |
| `T-MF-151` | 文件权限规则生效 | Agent 配置只读目录 | LLM 写只读目录 | 写入被拒绝 | P1 |
| `T-MF-152` | 凭据不暴露给 LLM | 工具参数含敏感字段 | 观察工具参数与展示 | 敏感字段脱敏，不进入工具参数/展示/日志明文 | P0 |
| `T-MF-153` | 工具调用审计可查询 | 会话含工具调用 | 查询审计记录 | 记录含调用者/时间/工具/参数/结果/耗时，可追溯 | P1 |
| `T-MF-154` | 输出数据量控制 | 工具返回超大数据 | 调用大数据工具 | 返回结果被截断或摘要化，不进入上下文 | P2 |

### 3.9 MCP Server 管理（外部接入中心）

| 用例 ID | 测试场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|---------|---------|---------|------|---------|--------|
| `T-MF-180` | 注册外部 MCP Server | 管理员 | 注册（名称/传输协议/端点/鉴权） | 注册成功并自动拉取工具清单 | P0 |
| `T-MF-181` | Server 工具清单查询 | 已注册 Server | 查询工具清单 | 返回工具名/描述/参数 schema 概要 | P1 |
| `T-MF-182` | Server 启停 | 已注册 Server | 管理员禁用后启用 | 禁用后不参与命名空间预筛选；启用后恢复 | P1 |
| `T-MF-183` | 命名空间配置 | 已注册 Server | 配置工具分组 | 命名空间覆盖式更新，Agent 仅可访问关联命名空间 | P1 |
| `T-MF-184` | 凭据加密存储 | 已注册 Server | 配置 API Key | 加密存储，仅录入/更新不回显明文，不暴露给 LLM | P0 |
| `T-MF-185` | Server 健康探测 | 已注册 Server | 探测连通性 | 返回健康状态与延迟，异常显著标注 | P2 |
| `T-MF-186` | 外部 MCP 调用审计 | 会话调用外部工具 | 查询审计记录 | 记录谁/何时/调用什么/结果/耗时，可追溯 | P1 |
| `T-MF-187` | 市场一键接入 | 市场预设 Server | 一键接入 | 注册成功并拉取工具清单，展示已接入状态 | P2 |
| `T-MF-188` | Server 删除解绑校验 | Server 被 Agent 关联 | 管理员删除 | 拒绝删除并提示先解绑 | P1 |
| `T-MF-189` | MCP 管理越权 | 普通用户 | 调用注册/启停/凭据接口 | 返回 403（A0301） | P0 |

---

## 4. 关键边界与异常

| 用例 ID | 测试场景 | 输入 | 期望行为 |
|---------|---------|------|---------|
| `T-MF-160` | 工具不存在 | 调用未注册工具 | 返回工具不存在错误，LLM 更换能力 |
| `T-MF-161` | MCP 网关不可用 | 调用 MCP 工具 | 预筛选降级（仅标题搜索），工具调用失败反馈给 LLM |
| `T-MF-162` | 所需能力未启用 | 用户请求禁用能力 | LLM 明确告知不可用并给替代建议 |

---

## 5. 性能

| 用例 ID | 测试场景 | 指标 | 期望 |
|---------|---------|------|------|
| `T-MF-170` | MCP 命名空间预筛选开销 | before_model 钩子耗时 | 摘要命中缓存时 < 20ms，未命中 < 200ms |
| `T-MF-171` | 工具调用状态推送延迟 | thought 事件推送 | 工具完成到状态推送 < 1s |
| `T-MF-172` | 沙箱执行启动耗时 | 容器冷启动 | < 2s（复用实例池时 < 500ms） |
