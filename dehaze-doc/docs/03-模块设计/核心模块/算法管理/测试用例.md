# 算法管理模块测试用例

## 1. 测试目标

- [ ] 验证核心功能符合 [需求规格](./需求规格.md)
- [ ] 覆盖正常流程、边界条件、异常分支
- [ ] 验证状态流转与审核机制
- [ ] 验证缓存策略与性能指标
- [ ] 验证权限控制与安全性
- [ ] 验证导入导出功能完整性

## 2. 测试前置

### 2.1 测试数据

| 数据类型 | 数据准备 |
|---------|---------|
| 用户数据 | 超级管理员、算法管理员、算法开发者、普通用户各1个 |
| 角色权限 | 配置 `sys:algorithm:*` 相关权限 |
| 算法数据 | 各状态算法（草稿1/测试中2/待审核3/已发布4/已停用5）各1个 |
| 模型文件 | 有效的 .pth/.onnx/.h5 测试文件 |
| 测试图片 | 雾霾图 + 清晰图各3张 |

### 2.2 测试范围

**包含的功能点**：
- F-M05-001 ~ F-M05-010（算法列表、详情、新增、审核、修改、停用/删除、导入导出、监控、预测、评估）

**不包含的功能点**：
- Python 算法服务内部实现
- MinIO 文件存储底层

---

## 3. 用例清单

### 3.1 算法列表与详情 (F-M05-001, F-M05-002)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-M05-001 | 正常流程：获取算法树形列表 | 已登录 | GET /api/v1/algorithms | 返回树形结构，按类型分类 | P0 |
| T-M05-002 | 正常流程：获取算法详情 | 算法ID存在 | GET /api/v1/algorithms/{id} | 返回完整算法信息 | P0 |
| T-M05-003 | 搜索：按名称模糊搜索 | 存在 "AOD-Net" 算法 | keywords="AOD" | 返回匹配算法列表 | P1 |
| T-M05-004 | 筛选：按类型筛选 | 存在不同类型算法 | type="deep_learning" | 仅返回深度学习类型 | P1 |
| T-M05-005 | 筛选：按状态筛选 | 存在各状态算法 | status=4 | 仅返回已发布算法 | P1 |
| T-M05-006 | 异常：查询不存在的算法 | - | id=999999 | 返回 A0401 请求资源不存在 | P1 |
| T-M05-007 | 权限：普通用户查看 | 以普通用户登录 | GET /api/v1/algorithms | 仅返回已发布状态算法 | P1 |

### 3.2 新增算法 (F-M05-003)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-M05-010 | 正常流程：新增算法 | 有 add 权限 | name, type, description, import_path | 创建成功，状态=草稿(1)，返回算法ID | P0 |
| T-M05-011 | 边界：名称重复 | 已存在 "AOD-Net" | name="AOD-Net" | 返回 A0501 数据已存在 | P0 |
| T-M05-012 | 边界：名称为空 | - | name="" | 参数校验失败 A0400 | P1 |
| T-M05-013 | 边界：名称超长 | - | name=65字符 | 参数校验失败（最大64字符）A0400 | P2 |
| T-M05-014 | 模型上传：有效格式 | - | 文件 model.pth | 上传成功，记录 path | P0 |
| T-M05-015 | 模型上传：无效格式 | - | 文件 model.txt | 返回错误，仅支持 .pt/.pth/.onnx/.h5 | P0 |
| T-M05-016 | 模型上传：超大文件 | - | 600MB 文件 | 返回错误，最大 500MB | P1 |
| T-M05-017 | 权限：无权限新增 | 以普通用户登录 | POST /api/v1/algorithms | 返回 403 | P0 |

### 3.3 审核算法 (F-M05-004)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-M05-020 | 正常流程：审核通过 | 算法状态=待审核(3)，有 audit 权限 | approved=true | 状态→已发布(4)，记录 audit_by/audit_time | P0 |
| T-M05-021 | 正常流程：审核驳回 | 算法状态=待审核(3)，有 audit 权限 | approved=false, remark="原因" | 状态→测试中(2)，记录 audit_remark | P0 |
| T-M05-022 | 边界：驳回原因为空 | 算法状态=待审核(3) | approved=false, remark="" | 返回 B0001 驳回时必须填写审核备注 | P0 |
| T-M05-023 | 边界：非待审核状态审核 | 算法状态=草稿(1) | PUT /api/v1/algorithms/{id}/audit | 返回 B0001 只有待审核状态才能审核 | P1 |
| T-M05-024 | 权限：无审核权限 | 以算法开发者登录 | PUT /api/v1/algorithms/{id}/audit | 返回 403 | P0 |

### 3.4 修改算法与版本管理 (F-M05-005)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-M05-030 | 正常流程：修改基本信息 | 算法状态=草稿 | description="新描述" | 更新成功 | P0 |
| T-M05-031 | 正常流程：创建新版本 | 算法状态=已发布 | version="v2.0.0", changelog | 版本记录保存到 sys_algorithm_version | P0 |
| T-M05-032 | 边界：版本号已存在 | 已存在 v1.0.0 | version="v1.0.0" | 返回 A0501 数据已存在 | P1 |
| T-M05-033 | 正常流程：版本回滚 | 存在历史版本 v1.0.0 | POST /api/v1/algorithms/{id}/rollback?versionId= | 恢复到 v1.0.0，更新 is_current 标记 | P0 |
| T-M05-034 | 边界：回滚到当前版本 | 当前版本 v2.0.0 | versionId=当前版本 | 返回 A0502 状态不允许操作 | P1 |
| T-M05-035 | 缓存：版本更新后缓存失效 | 缓存存在预测结果 | 创建新版本 | 该算法预测缓存全部失效 | P1 |

### 3.5 停用/删除算法 (F-M05-006)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-M05-040 | 正常流程：停用算法 | 算法状态=已发布(4)，有 stop 权限 | PUT /api/v1/algorithms/{id}/status | 状态→已停用(5) | P0 |
| T-M05-041 | 正常流程：启用算法 | 算法状态=已停用(5)，有 stop 权限 | PUT /api/v1/algorithms/{id}/status | 状态→已发布(4) | P0 |
| T-M05-042 | 正常流程：删除算法 | 算法状态=已停用(5)，有 delete 权限 | DELETE /api/v1/algorithms?ids= | 删除成功，级联删除子孙算法 | P0 |
| T-M05-043 | 边界：删除已发布算法 | 算法状态=已发布(4) | DELETE /api/v1/algorithms?ids= | 返回 A0502 状态不允许删除 | P0 |
| T-M05-043a | 边界：删除草稿算法 | 算法状态=草稿(1) | DELETE /api/v1/algorithms?ids= | 删除成功（草稿可删除） | P1 |
| T-M05-043b | 边界：删除已归档算法 | 算法状态=已归档(6) | DELETE /api/v1/algorithms?ids= | 删除成功（已归档可删除） | P1 |
| T-M05-044 | 边界：删除正在使用的算法 | 算法有预测任务进行中 | DELETE /api/v1/algorithms?ids= | 返回 A0504 存在关联数据 | P1 |
| T-M05-045 | 批量删除 | 有 delete 权限 | ids=[1, 2, 3] | 批量删除成功，递归删除子孙 | P1 |

### 3.6 导入/导出算法 (F-M05-007)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-M05-050 | 正常流程：导出算法元数据 | 有 export 权限 | GET /api/v1/algorithm/_export | 返回 .xlsx 文件流（或异步任务 ID） | P0 |
| T-M05-051 | 正常流程：CSV 格式导出 | 有 export 权限 | GET /api/v1/algorithm/_export?format=csv | 返回 .csv 文件流 | P1 |
| T-M05-052 | 正常流程：导入算法元数据 | 有 import 权限，下载模板并填写 | POST /api/v1/algorithm/_import（Excel 文件） | 创建算法（状态=草稿1），返回导入结果 | P0 |
| T-M05-053 | 导入校验：文件格式错误 | - | 上传 .txt 文件 | 返回 A0701 文件格式不支持 | P0 |
| T-M05-054 | 导入校验：必填字段为空 | - | Excel 中 name 列为空 | 返回 A0706 必填字段为空 | P1 |
| T-M05-055 | 导入校验：名称冲突 | 已存在 "AOD-Net" | 导入包含 name="AOD-Net" | 返回 A0707 数据校验失败 | P0 |
| T-M05-056 | 正常流程：下载导入模板 | 有 import 权限 | GET /api/v1/algorithm/template | 返回 .xlsx 模板文件 | P1 |
| T-M05-057 | 边界：导入文件超大 | - | 25MB Excel 文件 | 返回 A0702 文件大小超限 | P2 |

### 3.7 性能监控 (F-M05-008)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-M05-060 | 正常流程：获取监控数据 | 登录用户 | GET /api/v1/algorithms/{id}/monitor | 返回实时监控指标 | P0 |
| T-M05-061 | 正常流程：获取统计报表 | 登录用户 | GET /api/v1/algorithms/{id}/monitor/stats | 返回监控数据（当前复用监控接口） | P1 |
| T-M05-062 | 权限：无监控权限 | 以普通用户登录 | GET /api/v1/algorithms/{id}/monitor | 正常返回（接口未强制权限校验） | P1 |

### 3.8 模型预测处理 (F-M05-009)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-M05-070 | 正常流程：执行预测 | 算法状态=已发布(4) | algorithmId, imageUrl | 返回 logId + status=1(processing)，轮询至 completed | P0 |
| T-M05-071 | 缓存命中：重复预测 | 同图+同算法已预测 | 同输入 | POST 直接返回 status=2(completed) + 结果，不调用Python服务 | P0 |
| T-M05-072 | 异常：算法不存在 | - | algorithmId=999999 | 返回 A0401 | P1 |
| T-M05-073 | 异常：算法非已发布 | 算法状态=草稿(1) | algorithmId | 返回 A0502 状态不允许操作 | P1 |
| T-M05-074 | 异常：图片格式不支持 | - | 上传 .bmp 图片 | 返回 A0701 文件格式不支持 | P1 |
| T-M05-075 | 异常：预测任务超时 | Python服务响应慢 | - | 轮询超时（SDK 120s），任务标记 failed | P1 |
| T-M05-076 | 日志：记录预测日志 | - | 执行预测 | sys_pred_log 记录算法ID、MD5、耗时 | P1 |

### 3.9 效果评估计算 (F-M05-010)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-M05-080 | 正常流程：执行评估 | 预测图+清晰图已上传 | predImageUrl, gtImageUrl | 返回 logId + status=1(processing)，轮询至 completed 返回指标 | P0 |
| T-M05-081 | 缓存命中：重复评估 | 同图对已评估 | 同输入 | POST 直接返回 status=2(completed) + 结果 | P0 |
| T-M05-082 | 异常：缺少清晰图 | - | gtImageUrl 为空 | 返回 B0001 缺少清晰图进行对比 | P0 |
| T-M05-083 | 日志：记录评估日志 | - | 执行评估 | sys_eval_log 记录 result JSON（含5个指标） | P1 |
| T-M05-084 | 指标验证：PSNR 范围 | - | - | PSNR > 0，通常 20-50 dB | P2 |
| T-M05-085 | 指标验证：SSIM 范围 | - | - | SSIM 在 0-1 之间 | P2 |
| T-M05-086 | 指标验证：LPIPS 范围 | - | - | LPIPS 在 0-1 之间 | P2 |

---

## 4. 关键边界与异常场景

| 场景 | 测试点 | 期望行为 |
|------|--------|---------|
| 状态流转 | 草稿(1)→测试中(2)→待审核(3)→已发布(4) | 按规则流转，不允许跳跃 |
| 审核驳回 | 驳回原因必填 | 空原因返回 B0001 |
| 版本回滚 | 回滚到已归档版本 | 返回 A0502 |
| 导入冲突 | 名称与现有算法重复 | 返回 A0707，提示修改名称 |
| 预测缓存 | 相同图片+算法 | 第二次请求命中缓存，不调用 Python |
| 版本更新缓存 | 算法更新版本后 | 该算法所有预测缓存失效 |
| 并发预测 | 100 用户同时预测 | Redis 分布式锁防重，结果正确 |

---

## 5. 性能与并发测试

### 5.1 性能指标

> 目标值引用自 [后端实现文档](./后端实现.md#9-性能目标)

| 指标 | 目标值 |
|------|--------|
| 算法列表查询响应时间 | P99 < 300ms |
| 算法详情查询响应时间 | P99 < 100ms |
| 模型预测响应时间 | P99 < 10s |
| 效果评估响应时间 | P99 < 10s |
| 监控数据查询响应时间 | P99 < 500ms |

### 5.2 并发场景

| 场景 | 并发数 | 预期结果 |
|------|--------|---------|
| 并发查询算法列表 | 100 | 响应时间 < 500ms |
| 并发执行预测 | 50 | 结果正确，无重复推理 |
| 并发创建算法 | 20 | 名称唯一性校验正确 |

---

## 6. 安全性测试

| 测试项 | 验证方式 | 预期结果 |
|--------|---------|---------|
| 权限控制 | 无权限用户调用审核/停用/删除接口 | 返回 403 |
| SQL 注入 | 名称字段注入 `admin'--` | 被参数化查询转义 |
| XSS 攻击 | 描述字段注入 `&lt;script&gt;` | 输出时编码 |
| 文件上传 | 上传 .exe/.sh 文件 | 拒绝，仅允许 .pt/.pth/.onnx/.h5 |
| 越权访问 | 开发者尝试删除他人算法 | 失败，数据范围隔离 |
| 导入包安全 | 上传包含恶意脚本的 .zip | 拒绝或沙箱执行 |

---

## 7. 缓存测试

| 场景 | 缓存 Key 模式 | 验证点 |
|------|--------------|--------|
| 算法列表缓存 | `algorithm:list` | 新增/删除后失效 |
| 算法详情缓存 | `algorithm:detail:{id}` | 修改后失效 |
| 预测结果缓存 | `prediction:{algorithmId}:{imageMd5}` | 版本更新后失效 |
| 监控数据缓存 | `algorithm:monitor:{id}` | 5分钟 TTL 刷新 |

---

**文档版本**: v1.1.0  
**最后更新**: 2026-07-30  
**维护者**: 技术文档团队
