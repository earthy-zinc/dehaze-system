# 菜单管理模块测试用例

## 1. 测试目标

- [x] 验证核心功能符合需求规格（菜单CRUD、显示控制、路由生成）
- [x] 覆盖四种菜单类型的条件字段校验
- [x] 验证树形结构、层级限制（5级）、子菜单检查
- [x] 验证权限标识唯一性、角色菜单关联清理
- [x] 验证并发场景与数据一致性
- [x] 验证缓存策略与性能指标
- [x] 验证权限控制与安全性
- [x] 验证操作日志强制记录

---

## 2. 参考文档

| 文档类型 | 路径 |
|---------|------|
| **需求规格** | [需求规格.md](./需求规格.md) |
| **API 接口** | [API接口.md](./API接口.md) |
| **后端实现** | [后端实现.md](./后端实现.md) |
| **测试架构** | [02-系统架构/05-测试架构.md](../../../02-系统架构/05-测试架构.md) |

> **注**：接口详细参数/响应结构可通过 API 文档 MCP 查询。

---

## 3. 测试前置

### 3.1 测试数据

| 数据类型 | 准备内容 | 说明 |
|---------|---------|------|
| **角色数据** | 系统管理员角色 | 用于权限验证测试 |
| **菜单数据** | 完整的5级菜单树结构 | 用于层级限制、子菜单检查测试 |
| **菜单数据** | 各类型菜单（目录/菜单/按钮/外链） | 用于菜单类型条件字段测试 |

### 3.2 数据清理策略

- 测试菜单数据使用统一前缀 `test_menu_` 标识
- 测试结束后按前缀逻辑删除测试菜单（`deleted=1`）
- 保留系统默认菜单（如系统管理根目录）不做删除操作
- 测试后清理角色-菜单关联表数据

### 3.3 测试范围

**包含的功能点：**
- 菜单列表查询（树形结构、多条件筛选：关键字/权限标识/路由地址/类型/显示状态）
- 菜单新增（四种菜单类型）
- 菜单编辑
- 菜单删除（含级联删除、批量删除、逻辑删除）
- 菜单显示控制
- 菜单排序
- 菜单下拉选项
- 路由列表生成
- 操作日志强制记录验证

**不包含的功能点：**
- 角色管理功能（独立测试）
- 用户角色关联（角色管理模块测试）
- 权限验证中间件（系统架构层测试）

---

## 4. 用例清单

### 4.1 菜单列表查询 (F-MM-001)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-MM-001 | 正常流程：查询菜单树 | 系统存在菜单数据 | - | 返回树形结构，按sort排序，响应时间&lt;500ms | P0 |
| T-MM-002 | 正常流程：关键字搜索菜单 | 存在菜单名称包含"系统"的菜单 | keywords="系统" | 返回名称匹配的菜单树 | P0 |
| T-MM-003 | 边界：空数据查询 | 系统无菜单数据 | - | 接口返回空列表 | P1 |
| T-MM-004 | 边界：特殊字符搜索 | - | keywords="&lt;script&gt;" | 正常返回，无XSS风险 | P1 |
| T-MM-005 | 验证：树形结构正确性 | 存在5级菜单树 | - | 树形层级关系正确，最多5级 | P0 |
| T-MM-006 | 验证：排序生效 | 同一父级下有多个菜单 | - | 菜单按sort字段升序排列 | P1 |
| T-MM-007 | 正常流程：按权限标识筛选 | 存在perm含"user"的菜单 | perm="user" | 返回perm模糊匹配的菜单树 | P1 |
| T-MM-008 | 正常流程：按路由地址筛选 | 存在path含"system"的菜单 | path="system" | 返回path模糊匹配的菜单树 | P1 |
| T-MM-009 | 正常流程：按菜单类型筛选 | 存在各类型菜单 | type=2 | 返回type=2（菜单）的菜单 | P1 |
| T-MM-010 | 正常流程：按显示状态筛选 | 存在显示/隐藏菜单 | visible=0 | 返回visible=0（隐藏）的菜单 | P1 |

### 4.2 菜单新增 (F-MM-002)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-MM-011 | 正常流程：新增目录菜单 | 已登录且有sys:menu:add权限 | type=1, name="test_menu_目录", icon, redirect | 新增成功，返回菜单ID | P0 |
| T-MM-012 | 正常流程：新增菜单类型 | 已登录且有权限 | type=2, name="test_menu_页面", path, component | 新增成功，返回菜单ID | P0 |
| T-MM-013 | 正常流程：新增按钮类型 | 已登录且有权限 | type=4, name="test_menu_按钮", perm | 新增成功，返回菜单ID | P0 |
| T-MM-014 | 正常流程：新增外链类型 | 已登录且有权限 | type=3, name="test_menu_外链", path="https://..." | 新增成功，返回菜单ID | P0 |
| T-MM-015 | 边界：菜单名称唯一性校验 | 同一父级下已存在菜单"测试菜单" | name="测试菜单", parentId相同 | 校验失败，提示"菜单名称已存在" | P0 |
| T-MM-016 | 边界：权限标识唯一性校验 | 系统已存在权限标识"sys:user:add" | perm="sys:user:add" | 校验失败，提示"权限标识已存在" | P0 |
| T-MM-017 | 边界：上级菜单选择按钮类型 | 存在按钮类型菜单 | parentId=按钮菜单ID | 校验失败，提示"上级菜单不能是按钮类型" | P0 |
| T-MM-018 | 边界：上级菜单选择外链类型 | 存在外链类型菜单 | parentId=外链菜单ID | 校验失败，提示"上级菜单不能是外链类型" | P0 |
| T-MM-019 | 边界：菜单类型必填字段缺失（菜单） | - | type=2, path为空 | 校验失败，提示"路由地址不能为空" | P1 |
| T-MM-020 | 边界：菜单类型必填字段缺失（按钮） | - | type=4, perm为空 | 校验失败，提示"权限标识不能为空" | P1 |
| T-MM-021 | 边界：层级限制（5级） | 已存在4级菜单树 | 在第4级菜单下新增 | 校验通过，新增为第5级 | P1 |
| T-MM-022 | 边界：层级超过限制（6级） | 已存在5级菜单树 | 在第5级菜单下新增 | 校验失败，提示"菜单层级不能超过5级" | P0 |
| T-MM-023 | 异常：无权限新增 | 当前用户无sys:menu:add权限 | 有效菜单数据 | 返回403，提示"无操作权限" | P0 |

### 4.3 菜单编辑 (F-MM-003)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-MM-024 | 正常流程：编辑菜单基本信息 | 菜单存在，有编辑权限 | 修改name, sort | 更新成功，信息保存 | P0 |
| T-MM-025 | 正常流程：修改菜单类型 | 菜单存在，有编辑权限 | type从1改为2 | 更新成功，校验类型对应必填字段 | P0 |
| T-MM-026 | 正常流程：修改上级菜单 | 菜单存在，有编辑权限 | 修改parentId | 更新成功，层级关系正确 | P0 |
| T-MM-027 | 边界：修改后菜单名称重复 | 同一父级下已有菜单"测试" | 修改name="测试" | 校验失败，提示"菜单名称已存在" | P0 |
| T-MM-028 | 边界：修改后权限标识重复 | 系统已存在"sys:user:edit" | 修改perm="sys:user:edit" | 校验失败，提示"权限标识已存在" | P0 |
| T-MM-029 | 边界：编辑不存在的菜单 | 菜单已被删除 | id=不存在的ID | 返回404，提示"菜单不存在" | P1 |
| T-MM-030 | 边界：上级菜单设置为自己 | - | parentId=自己的ID | 校验失败，提示"上级菜单不能是自己" | P1 |
| T-MM-031 | 边界：上级菜单设置为自己的子菜单 | A->B->C结构 | 将A的parentId设为C | 校验失败，提示"不能设置自己的子菜单为父菜单" | P0 |
| T-MM-032 | 边界：菜单类型变更必填字段缺失 | 原type=1改为type=4 | 修改type=4，perm为空 | 校验失败，提示"权限标识不能为空" | P1 |
| T-MM-033 | 异常：无权限编辑 | 当前用户无sys:menu:edit权限 | 有效修改数据 | 返回403，提示"无操作权限" | P0 |
| T-MM-034 | 验证：权限变更触发缓存刷新 | 角色已分配权限标识 | 修改菜单权限标识 | 角色权限缓存立即刷新 | P0 |

### 4.4 菜单删除 (F-MM-004)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-MM-035 | 正常流程：删除无子菜单的菜单 | 菜单存在，无子菜单 | id=菜单ID | 删除成功，菜单标记为已删除（deleted=1），数据保留 | P0 |
| T-MM-036 | 正常流程：级联删除子菜单 | 菜单下有2个子菜单 | id=父菜单ID | 删除成功，父菜单及子孙菜单均标记 deleted=1 | P0 |
| T-MM-037 | 边界：删除不存在的菜单 | 菜单已被删除 | id=不存在的ID | 返回404或静默处理 | P2 |
| T-MM-038 | 边界：删除系统预置菜单 | 系统管理是预置菜单 | id=预置菜单ID | 删除失败，提示"系统预置菜单不可删除" | P1 |
| T-MM-039 | 异常：无权限删除 | 当前用户无sys:menu:delete权限 | 有效菜单ID | 返回403，提示"无操作权限" | P0 |
| T-MM-040 | 边界：取消删除 | - | 不提交删除请求 | 不调用删除接口，菜单列表查询返回成功，数据不变 | P1 |
| T-MM-041 | 验证：删除触发角色菜单关联清理 | 菜单已分配给角色 | 删除菜单 | 角色-菜单关联表记录同步删除 | P0 |
| T-MM-042 | 验证：删除触发缓存刷新 | 菜单权限已缓存 | 删除菜单 | 相关权限缓存立即清除 | P0 |
| T-MM-043 | 正常流程：批量删除菜单 | 存在多个无父子关系的菜单 | ids=1,2,3（逗号分隔） | 删除成功，多个菜单均标记 deleted=1 | P0 |
| T-MM-044 | 正常流程：批量删除含父子关系 | ids 含父菜单与其子菜单 | ids=父ID,子ID | 删除成功，去重后级联删除，无"菜单不存在"误报 | P1 |

### 4.5 菜单显示控制 (F-MM-005)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-MM-045 | 正常流程：隐藏菜单 | 菜单状态为显示 | visible=0 | 状态变更为隐藏，接口返回status=0 | P0 |
| T-MM-046 | 正常流程：显示菜单 | 菜单状态为隐藏 | visible=1 | 状态变更为显示，接口返回status=1 | P0 |
| T-MM-047 | 异常：无权限修改状态 | 当前用户无sys:menu:edit权限 | 有效参数 | 返回403，提示"无操作权限" | P0 |
| T-MM-048 | 验证：隐藏菜单权限仍生效 | 用户拥有菜单权限 | 隐藏菜单 | 用户仍可通过接口访问（权限保留） | P1 |
| T-MM-049 | 验证：隐藏父菜单时子菜单一并隐藏 | 父菜单下有子菜单 | visible=0（父菜单） | 路由列表中父菜单及其子菜单均不生成路由 | P0 |

### 4.6 菜单排序 (F-MM-006)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-MM-050 | 正常流程：修改菜单排序 | 菜单存在 | sort=10 | 排序值更新，列表顺序变更 | P1 |
| T-MM-051 | 边界：排序值为负数 | - | sort=-1 | 校验失败，提示"排序值必须大于0" | P1 |
| T-MM-052 | 边界：排序值为0 | - | sort=0 | 排序值更新，排在同级最前 | P2 |
| T-MM-053 | 验证：同级排序生效 | 同一父级下有多个菜单 | 设置不同sort值 | 菜单按sort升序排列 | P1 |

### 4.7 菜单下拉选项 (F-MM-007)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-MM-054 | 正常流程：查询菜单下拉选项 | 系统存在菜单数据 | - | 返回树形结构菜单选项 | P0 |
| T-MM-055 | 验证：按钮类型不显示 | 存在按钮类型菜单 | - | 返回结果不包含按钮类型菜单 | P0 |
| T-MM-056 | 验证：外链类型不显示 | 存在外链类型菜单 | - | 返回结果不包含外链类型菜单 | P1 |
| T-MM-057 | 边界：空菜单列表 | 系统无菜单数据 | - | 返回空列表 | P2 |

### 4.8 路由列表生成 (F-MM-008)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-MM-058 | 正常流程：生成路由列表 | 存在目录和菜单类型菜单 | - | 返回路由列表，包含path、component、meta等 | P0 |
| T-MM-059 | 验证：隐藏菜单不生成路由 | 存在hidden状态的菜单 | - | 返回的路由列表不包含隐藏菜单 | P0 |
| T-MM-060 | 验证：按钮类型不生成路由 | 存在按钮类型菜单 | - | 返回的路由列表不包含按钮类型 | P0 |
| T-MM-061 | 验证：外链类型不生成路由 | 存在外链类型菜单 | - | 返回的路由列表不包含外链类型 | P1 |
| T-MM-062 | 验证：权限过滤路由 | 用户拥有部分菜单权限 | - | 返回用户有权限访问的路由 | P0 |
| T-MM-063 | 验证：路由树形结构 | 存在多级目录菜单 | - | 返回的路由保持层级关系 | P0 |

### 4.9 操作日志 (F-MM-002/003/004)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-MM-064 | 验证：增删改操作日志强制记录 | 管理员执行菜单新增/编辑/删除 | 完成一次新增/编辑/删除操作 | 操作日志记录操作人、时间、菜单ID、变更内容 | P0 |

---

## 5. 关键边界与异常场景

| 场景 | 测试点 | 期望行为 |
|------|--------|---------|
| **菜单树深度** | 层级超过5级 | 禁止新增，提示层级限制 |
| **菜单数量** | 单次返回1000+菜单 | 性能测试：响应时间 < 500ms |
| **并发修改** | 同一菜单被2个请求同时修改 | 乐观锁/事务保护，只一个成功 |
| **层级变更** | 将父菜单的子菜单设为新父菜单 | 校验失败，防止循环引用 |
| **缓存一致性** | 修改菜单后查询 | 缓存立即失效，返回最新数据 |
| **权限唯一性** | 新增菜单权限标识重复 | 校验失败，提示唯一性约束 |
| **角色关联** | 删除已分配给角色的菜单 | 同步删除角色-菜单关联表记录 |
| **路由生成** | 用户无权限访问菜单 | 路由列表不包含该菜单 |
| **隐藏父菜单** | 隐藏上级菜单 | 子菜单在路由列表中一并不生成 |
| **操作审计** | 菜单增删改操作 | 强制记录操作日志，含操作人/时间/菜单ID/变更内容 |

---

## 6. 性能与并发测试

### 6.1 性能指标

> 性能目标参见 [需求规格.md](./需求规格.md) 第 1.3 节

| 指标 | 目标值 | 测试方法 |
|------|--------|---------|
| **菜单列表查询响应时间** | &lt;500ms | 1000菜单数据，树形查询 |
| **表单提交响应时间** | &lt;1s | 单次新增/编辑操作 |
| **树形渲染时间** | &lt;300ms | 5级菜单树前端渲染 |
| **路由生成响应时间** | &lt;300ms | 生成1000+路由 |
| **缓存命中率** | >80% | 查询权限缓存命中率 |

### 6.2 并发场景

| 场景 | 并发数 | 验证点 |
|------|--------|--------|
| **并发查询菜单树** | 50用户 | 响应时间稳定，无错误 |
| **并发新增菜单** | 10请求 | 唯一性校验正确，无重复数据 |
| **并发编辑同一菜单** | 5请求 | 乐观锁正常工作，数据一致 |
| **并发删除菜单** | 5请求 | 子菜单检查正确，关联表清理完整 |

---

## 7. 安全性测试

| 测试项 | 验证方式 | 预期结果 |
|--------|---------|---------|
| **权限控制** | 无权限用户调用新增/编辑/删除接口 | 返回403 |
| **数据权限** | 用户访问无权限的菜单 | 返回403或数据过滤 |
| **SQL注入** | 参数中包含 `admin'--` | 被参数化查询转义，无SQL注入风险 |
| **XSS攻击** | 菜单名称注入 `&lt;script&gt;alert(1)&lt;/script&gt;` | 输出时编码，不执行脚本 |
| **传输安全** | 抓包分析请求 | HTTPS加密传输 |
| **防重复提交** | 快速连续点击提交 | 仅执行一次提交成功 |
| **越权访问** | 修改其他角色的菜单关联 | 失败，仅能操作自己有权限的菜单 |
| **操作日志** | 查询菜单增删改操作日志 | 强制记录操作人、时间、菜单ID、变更内容 |

---

## 8. 数据一致性测试

### 8.1 缓存一致性测试

| 场景 | 验证点 | 期望行为 |
|------|--------|---------|
| **创建菜单后** | 查询菜单列表 | 新菜单立即出现在列表中 |
| **修改权限标识后** | 查询权限缓存 | 新权限标识对应的缓存正确加载 |
| **修改显示状态后** | 查询路由列表 | 隐藏菜单不生成路由 |
| **删除菜单后** | 查询权限缓存 | 相关缓存立即清除 |
| **删除菜单后** | 查询角色-菜单关联表 | 关联记录同步删除 |

### 8.2 事务回滚测试

| 场景 | 触发条件 | 验证点 |
|------|---------|--------|
| **数据库错误** | 模拟DB连接断开 | 操作回滚，数据不变 |
| **业务异常** | 校验失败抛异常 | 事务回滚，数据一致 |
| **Panic触发** | 代码运行时错误 | 事务回滚，数据恢复 |
| **多表操作** | 同时更新菜单表和关联表 | 全部成功或全部回滚 |

### 8.3 数据完整性测试

| 场景 | 验证点 | 期望行为 |
|------|--------|---------|
| **逻辑删除过滤** | 查询菜单列表 | 不返回已删除菜单 |
| **唯一性约束** | 检查权限标识重复 | 包含已删除记录的校验 |
| **子菜单级联** | 删除父级菜单 | 子孙菜单一并逻辑删除（deleted=1） |
| **层级关系** | 修改上级菜单 | 保持树形结构一致性 |

---

## 9. 集成测试场景

### 9.1 角色菜单关联集成测试

| 场景 | 测试点 | 期望行为 |
|------|--------|---------|
| **分配菜单权限** | 为角色分配菜单 | 角色-菜单关联表增加记录 |
| **取消菜单权限** | 清空角色菜单 | 角色-菜单关联表记录删除 |
| **删除菜单** | 删除已分配的菜单 | 角色-菜单关联表记录同步删除 |
| **权限生效** | 修改菜单权限标识 | 用户权限立即更新 |

### 9.2 前端路由集成测试

| 场景 | 测试点 | 期望行为 |
|------|--------|---------|
| **路由生成** | 调用路由列表接口 | 返回前端路由配置 |
| **权限过滤** | 用户无权限访问菜单 | 路由列表不包含该菜单 |
| **隐藏菜单过滤** | 菜单状态为隐藏 | 路由列表不包含该菜单 |
| **路由结构** | 多级目录菜单 | 路由保持层级关系 |
