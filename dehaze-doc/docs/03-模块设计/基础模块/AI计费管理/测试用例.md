# AI 计费管理模块测试用例

## 1. 测试目标

- [x] 验证积分计量换算正确性（Token->credits，按模型计费比例，含缓存折扣、降级计费、bill_type 分离、子智能体计费）
- [x] 验证积分余额账户（余额扣减/回补、余额 vs 限额、充值/赠送/试用/管理员调整/退款补偿）
- [x] 验证配额管理的原子性和准确性（预扣+实扣+退差额、日/月限额、并发不超扣）
- [x] 验证计费预估算法的合理性
- [x] 验证异常检测规则的触发与告警
- [x] 验证计费查询与管理 API（余额查询、计费明细、退款申请/审核、管理员调整）
- [x] 覆盖正常流程、边界条件、异常分支

---

## 3. 测试前置

### 3.1 测试数据

| 数据类型 | 准备内容 | 说明 |
|---------|---------|------|
| **VIP 等级用户** | 4 个测试用户（普通/VIP1/VIP2/SVIP），前缀 `test_billing_` | 各等级已分配对应日/月限额 |
| **模型配置数据** | 3 个模型（含不同输入/输出/缓存计费比例，含降级链配置） | 用于计费比例与降级计费测试 |
| **配额初始值** | 按测试用例需要预置日/月配额已用值 | 每次测试前设置初始值 |

### 3.2 数据清理策略

- 测试用户前缀 `test_billing_` 标识，测试结束后按前缀清理计费记录与配额缓存
- 不退还不清理配额（数据由定时任务处理）
- 测试消息在测试结束后级联删除

### 3.3 测试范围

**包含的功能点：**
- F-MB-001：积分余额账户（余额扣减/回补、余额 vs 限额）
- F-MB-002：积分充值与赠送（积分包/VIP 赠送/试用/管理员调整）
- F-MB-003：退款与补偿
- F-MB-004：积分计量（基本换算、缓存折扣、降级计费、bill_type 分离、子智能体计费）
- F-MB-005：配额管理（日/月限额、预扣实扣、差额退补、并发扣减、配额重置、配额不足三分支）
- F-MB-006：计费预估
- F-MB-007：异常检测与告警
- F-MB-009：计费记录与统计（余额查询、计费明细、账单、退款 API）

**不包含的功能点：**
- 第三方模型供应商账单对账（未实现功能）
- 积分充值支付流程（属订单管理模块）

---

## 4. 用例清单

### 4.1 余额账户与充值退款 (F-MB-001/002/003)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-AB-001 | 余额扣减正确 | 用户余额=10,000，配额充足 | 实扣 actualCredits=3,000 | credits_balance=7,000；sys_ai_credit_log 写入 source=consume 记录，related_id 关联计费记录 | P0 |
| T-AB-002 | 余额 vs 限额分离（限额不足不扣余额） | 日限额已用满 | 发送消息 | 返回配额不足；credits_balance 不变；日/月限额已用计数不变 | P0 |
| T-AB-003 | 积分包充值增加余额 | 用户余额=500 | 购买 1000 积分包，支付成功 | credits_balance=1,500；sys_ai_credit_log 写入 source=recharge；充值积分永久有效 | P0 |
| T-AB-004 | VIP 赠送按月发放与月末清零 | VIP1 用户 | 月初发放赠送积分；月末未用部分 | 月初余额增加（source=vip_gift）；月末未用部分清零（source=vip_gift_expire） | P1 |
| T-AB-005 | 新用户试用赠送 | 新用户注册 | 注册成功 | credits_balance=100；sys_ai_credit_log 写入 source=trial | P1 |
| T-AB-006 | 管理员手动调整积分 | 用户余额=500 | 管理员补扣 200，填写原因 | credits_balance=700；sys_ai_credit_log 写入 source=admin_adjust，含 operator_id 与 reason | P1 |
| T-AB-007 | 管理员越权防护 | 普通用户（无管理员权限）尝试调整他人积分 | 调整积分接口 | 返回 403 无权限；credits_balance 不变；无 source=admin_adjust 流水 | P0 |
| T-AB-008 | 退款补偿回补余额 | 存在误扣计费记录 credits=2,000 | 管理员审核通过退款 2,000 | credits_balance += 2,000；sys_ai_credit_log 写入 source=refund，related_id 关联原计费记录；日/月限额已用计数不重置 | P0 |
| T-AB-009 | 连续配额不足告警 | 用户 24h 内配额不足触发 11 次 | 第 11 次配额不足 | 触发告警（标记"可能异常使用，需人工核查"）；不调整 TPM | P1 |

### 4.2 积分计量 (F-MB-004)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-AB-010 | Token按模型计费比例正确换算为积分 | 用户已登录，选择gpt-4o模型（inputRate=1.0, outputRate=4.0, cachedRate=0.5） | 发送消息"什么是暗通道先验？"；LLM返回inputTokens=1000, cachedInputTokens=200, outputTokens=500 | sys_ai_message.credits=1000×1.0+200×0.5+500×4.0=3100；sys_ai_billing.credits=3100 | P0 |
| T-AB-011 | 不同模型计费比例不同 | 同一用户发送相同消息"什么是PSNR？"，分别用gpt-4o和gpt-4o-mini | 两条消息各1000 input + 500 output | gpt-4o credits=1000×1.0+500×4.0=3000 >> gpt-4o-mini credits=1000×0.2+500×0.5=450 | P0 |
| T-AB-012 | 缓存命中按折扣价计费，credits_saved正确计算 | 首条消息已建立缓存前缀（system_prompt+工具定义）；第二条消息发送 | 第二条消息"RIDCP适用场景"；cachedInputTokens=200 | credits_saved=200×(1.0-0.5)=100；credits=(inputTokens×inputRate)+(cached×cachedRate)+(output×outputRate) | P0 |
| T-AB-013 | 模型不支持Prompt Cache时cached=0, saved=0 | 选择supports_prompt_cache=0的模型simple-model | 发送消息 | cached_input_tokens=0；credits_saved=0 | P1 |
| T-AB-014 | 主模型降级按实际降级模型计费 | gpt-4o（outputRate=4.0）fallback->gpt-4o-mini（outputRate=0.5）；Mock gpt-4o返回429 | 用户选择gpt-4o发送消息，触发降级 | sys_ai_billing.model="gpt-4o-mini"；actual_model="gpt-4o"；credits按gpt-4o-mini比例计算 | P0 |
| T-AB-015 | 多级降级A->B->C，按最终成功模型计费 | A（fallback->B），B（fallback->C）；Mock A和B返回5xx，C成功 | 发送消息 | model="C"；actual_model="A"；credits按C的计费比例 | P1 |
| T-AB-016 | 降级链全部失败不计费 | 所有模型（含fallback链）全部返回5xx | 发送消息 | 无计费记录或credits=0；配额恢复发送前水平 | P1 |
| T-AB-017 | 工具调用中的LLM Token分离记录 | 发送需要工具调用的消息"帮这张图去雾" | ReAct循环产生多步推理 | sys_ai_billing中至少有一条bill_type="chat"；若ReAct observe调用LLM则有bill_type="tool_llm"记录；tool_llm记录的tool_credits>0 | P0 |
| T-AB-018 | 子智能体独立计费+主Agent汇总 | 发送触发2个子智能体并行执行的消息 | 两条子智能体各自LLM调用 | 两条子智能体各自创建计费记录（bill_type=chat/tool_llm，关联同一会话）；主Agent消息credits=SUM(子智能体credits)+主Agent自身credits；配额扣减以汇总credits为准 | P0 |
| T-AB-019 | 知识库注入(kb_inject)按注入Token计费 | 会话命中知识库 RAG 检索，注入 Top-K 片段 | 发送消息触发知识库检索注入 | sys_ai_billing 生成 bill_type=kb_inject 记录；credits 按注入 Token × inputRate 计算；与 chat 类记录独立分别入账 | P0 |
| T-AB-020 | 语音识别(asr)按音频时长计费 | 发送语音消息 | 上传音频并触发 ASR | sys_ai_billing 生成 bill_type=asr 记录；credits 按音频时长（秒）× 对应费率计算 | P0 |
| T-AB-021 | 语音合成(tts)按字符数计费 | 触发 TTS 语音合成 | 输出文本转语音 | sys_ai_billing 生成 bill_type=tts 记录；credits 按字符数 × 对应费率计算 | P0 |

### 4.3 配额管理 (F-MB-005)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-AB-022 | 日限额充足正常发送消息 | 普通用户日限额10,000；Redis日配额已用=0 | 发送消息，预估消耗3,000 | 消息成功发送；Redis日配额已用=实际消耗积分 | P0 |
| T-AB-023 | 日限额不足拒绝发送 | Redis日配额已用=9,000，剩余1,000 | 发送消息，预估消耗3,000 | 返回A0503（配额不足）；Redis日配额已用保持9,000；无sys_ai_billing新记录 | P0 |
| T-AB-024 | 日限额充足但月限额不足，拒绝发送 | Redis日配额已用=0；Redis月配额已用=98,000（月限额100,000，剩余2,000） | 发送消息，预估消耗3,000 | 返回配额不足（月度）；Redis月配额已用保持98,000 | P0 |
| T-AB-025 | 实际消耗<预估消耗，差额退还正确 | 配额充足，剩余10,000 | 预估2,000->预扣2,000->LLM实际1,500->差额退还500 | Redis日配额已用=1,500；pre_deduct=2,000；quota_consumed=1,500；sys_ai_message.credits=1,500 | P0 |
| T-AB-026 | 实际消耗>预估消耗，额外扣减正确 | 配额充足 | 预估1,000->预扣1,000->LLM实际2,000->额外扣减1,000 | Redis日配额已用=2,000；pre_deduct=1,000；quota_consumed=2,000 | P1 |
| T-AB-027 | 预估超过剩余配额直接拒绝 | Redis日配额已用=9,000，剩余1,000 | 预估2,000>剩余1,000->拒绝 | 配额不足；Redis值不变（9,000）；无计费记录 | P0 |
| T-AB-028 | 并发发送不超扣 | 日限额剩余5,000；两条消息预估各3,000 | 同一用户两会话同时发送 | 仅一条成功；另一条返回配额不足；Redis日配额已用≤5,000（不会变为6,000） | P0 |
| T-AB-029 | 日配额每日0点自动重置 | 今日日配额已用=5,000 | 模拟日期变更到下一天，触发重置 | 新日期配额已用=0；旧日期配额记录保留 | P1 |
| T-AB-030 | 月配额每月1日自动重置 | 本月配额已用=50,000 | 模拟日期变更到下月1日，触发重置 | 新月份配额已用=0 | P1 |
| T-AB-031 | VIP升级后新限额立即生效 | 普通用户日限额10,000，已用9,000；用户升级VIP1（日限额50,000） | 升级后发送消息预估3,000 | 3,000≤50,000-9,000=41,000 -> 继续推理；quota不足interrupt解除，resume恢复 | P0 |
| T-AB-032 | 配额不足：等待重置保留上下文 | 用户拒绝升级，选择等待 | 配额检查失败后选择等待重置 | Agent 暂停并保留对话上下文；等待至日配额重置后可继续推理（上下文未清除） | P1 |
| T-AB-033 | 配额不足：放弃清除上下文 | 用户选择放弃 | 配额检查失败后选择放弃 | Agent 暂止；清除对话上下文；用户后续需新建会话 | P1 |

### 4.4 计费预估 (F-MB-006)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-AB-034 | 新会话预估（无历史数据，保守估计合理） | 新用户，无历史对话 | 发送消息"你好"（20字符） | 预估输入Token=历史0×0.8+当前消息Token×1.2+记忆注入200+工具定义500；预估输出Token≤模型max_output_tokens×0.3；预估积分在合理范围 | P1 |
| T-AB-035 | 长会话历史数据自适应预估 | 会话已有10+轮历史，历史平均上下文Token已稳定 | 发送新消息 | 预估输入Token=历史平均×0.8+当前消息Token×1.2+记忆注入200+工具定义500；预估≈历史平均输入Token×inputRate+max_output_tokens×0.3×outputRate | P1 |
| T-AB-036 | 预估范围区间展示 | 模型inputRate=1.0, outputRate=4.0；预估输入=1500, 预估输出=1500 | 获取预估积分 | 基准值=1500×1.0+1500×4.0=7500；展示区间±30%："预计消耗5,250-9,750积分" | P1 |

### 4.5 异常检测与告警 (F-MB-007)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-AB-037 | 单次超高消耗触发告警 | 用户月限额100,000（阈值=10,000） | 消息实际消耗15,000积分 | 消息正常完成；ELK中出现异常标记；前端展示"本次消耗较高，建议简化需求" | P1 |
| T-AB-038 | 突发峰值触发告警与限流 | 用户日限额10,000（阈值=5,000） | 5分钟内连续发送，累计消耗>5,000 | 触发告警 + 日限额限流（不调整 TPM/RPM，TPM 由 API 网关层统一控制）；异常告警记录生成 | P1 |
| T-AB-039 | 空回复高错误警 | 填充长prompt（15,000 token），LLM返回空 | 发送长消息，outputTokens=0 | 异常告警触发（空回复高消耗）；消息正常结束不阻塞对话 | P1 |
| T-AB-040 | 正常消耗不触发异常 | 正常消息对话 | 发送普通消息（消耗<月限额10%） | 无异常标记，无告警指标触发 | P2 |

### 4.6 计费查询与管理 API (F-MB-001/003/009)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-AB-041 | 余额查询接口 | 用户有余额与配额记录 | GET /api/v1/ai-billing/balance | 返回余额、日/月配额使用情况 | P0 |
| T-AB-042 | 退款申请重复-拒绝 | 同一计费记录已提交退款申请 | POST /api/v1/ai-billing/refunds（同一记录） | 返回 A0680（退款申请已存在） | P1 |
| T-AB-043 | 退款审核-原记录已退款 | 原计费记录已退款完成 | POST /api/v1/ai-billing/refunds/{id}/audit | 返回 A0681（退款审核失败） | P1 |
| T-AB-044 | 跨用户查询余额-权限拒绝 | 用户 A 查询用户 B 的余额 | GET /api/v1/ai-billing/balance（用户 B 身份） | 返回 A0403（无权限） | P0 |
| T-AB-044a | 响应信封契约 | 用户有余额与配额记录 | GET /api/v1/ai-billing/balance | 返回 `{code:"00000", msg:"一切ok", data:{...}}`，余额在 `data` 内 | P0 |
| T-AB-044b | 退款金额不合法 | 存在计费记录 | POST /api/v1/ai-billing/refunds（amount=0 或负数） | 返回 A0400（退款积分数必须大于 0） | P1 |
| T-AB-044c | 账单月份不存在 | 该月无任何消费/充值/退款记录 | GET /api/v1/ai-billing/bills/{不存在月份} | 返回 A0401（账单不存在） | P1 |

---

## 5. 关键边界与异常场景

| 场景 | 测试点 | 期望行为 |
|------|--------|---------|
| **计费比例变更** | 管理员更新sys_ai_model.inputRate，缓存过期后生效 | 缓存过期后新消息使用更新后的计费比例，旧消息credits不变 |
| **计费比例缓存失效** | Redis不可用时查询计费比例 | 降级直接查询MySQL，响应时间<100ms |
| **配额缓存不可用** | Redis宕机 | 降级查询MySQL + 内存计数器，继续正常服务（最多1秒延迟） |
| **LLM调用异常时预扣退还** | 发送消息后LLM网络超时 | 预扣1,000->LLM失败->退还1,000->消息status=3->sys_ai_message.credits=0 |
| **用户中途取消流式计费** | 发送消息LLM生成约300 token后点击停止 | credits=inputTokens×inputRate+300×outputRate（已生成token按实际计费） |
| **VIP降级后超额暂停** | 用户VIP到期降级后已用量超过新限额 | 下次发送消息时quota校验失败，提示配额已达上限（需等待重置） |
| **跨月份配额查询** | 1月31日23:59和2月1日00:01的quota key不同 | 跨月正确切换Redis key，查询到对应月份的配额已用 |
| **大量计费记录查询** | 单个用户有100,000+条计费记录 | 分页查询响应时间<500ms |

---

## 6. 性能与并发测试

### 6.1 关键操作性能目标

| 指标 | 目标值 | 测试方法 |
|------|--------|---------|
| **配额校验响应时间** | <50ms | Redis读取+阈值判断 |
| **预扣操作响应时间** | <100ms | Redis DECR×2（日+月）+ 阈值检查 |
| **实扣操作响应时间** | <100ms | Redis INCR差额 + MySQL insert |
| **积分换算响应时间** | <10ms | 本地计算（乘法+加法） |
| **计费记录写入响应时间** | <200ms | MySQL insert single row |
| **计费预估响应时间** | <50ms | Redis读取历史Token均值 + tiktoken编码 |

### 6.2 并发场景

| 场景 | 并发数 | 验证点 |
|------|--------|--------|
| **并发配额预扣** | 10请求/用户 | Redis DECR原子性，不超扣，后续请求正确被拒绝 |
| **并发差额退还** | 5请求/用户 | Redis INCR原子性，退还不丢（不会少退） |
| **并发计费写入** | 20请求 | MySQL insert正确，每条记录credits/inputTokens/outputTokens配对正确 |
| **并发配额查询** | 100请求 | 响应稳定，无超时错误 |

### 6.3 配额缓存同步验证

| 场景 | 验证点 | 期望行为 |
|------|--------|---------|
| **预扣后立即查询** | Redis配额已用+预估 | Redis已用立即反映预扣值 |
| **实扣后立即查询** | Redis配额已用+actual-预估 | Redis已用立即反映实扣后的正确值（差额退补） |
| **Redis宕机降级查询MySQL** | MySQL中近3次计费记录+sum | 降级后配额已用与Redis恢复前差值<一次消息的credit |

---

## 7. 安全性测试

| 测试项 | 验证方式 | 预期结果 |
|--------|---------|---------|
| **用户配额隔离** | 用户A的配额扣减不应影响用户B | 用户B的Redis配额key不变化 |
| **手动绕过配额** | 直接调用LLM不经过计费模块 | 被API网关拦截，未通过计费模块的调用拒绝 |
| **预扣负值注入** | 传入负数预估消耗 | 被校验拒绝，预估消耗必须>0 |
| **Redis配额篡改** | 直接SET Redis配额值 | Redis密码保护，外部不可访问 |
| **管理员调整越权防护** | 普通用户调用管理员调整接口 | 返回 403，余额不变 |
| **跨用户数据访问** | 用户 A 查询用户 B 的余额/账单/记录 | 返回 A0403 |

---

## 8. 配额扣减集成测试

> 本节验证配额扣减在多步推理、欠费熔断等复杂链路下的端到端正确性。

### 8.1 预扣 -> 滚动预算 -> 实扣 -> 差额退补完整流程

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-AB-045 | 预扣->滚动预算->实扣->差额退补 | 用户配额充足，发起多步推理任务 | ① 发送前预扣预估积分 ② 每步执行前滚动预算校验 ③ 完成后实扣实际消耗 ④ 退还预扣差额 | 预扣值≥实扣值；差额正确退补；最终配额已用=实际消耗；余额扣减=实扣积分；pre_deduct 与 quota_consumed 记录一致 | P0 |

### 8.2 欠费熔断 -> 充值 -> 解除熔断完整流程

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-AB-046 | 欠费熔断->充值->解除熔断 | 用户余额耗尽且配额不足 | ① 触发欠费熔断，AI 能力调用返回 A0503 ② 用户充值（购买积分包） ③ 余额恢复后重新调用 | 熔断期间调用被拒；充值后余额增加（source=recharge）；熔断解除，AI 能力恢复正常；期间配额已用计数不重置 | P0 |

### 8.3 多步推理的预算协调测试

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-AB-047 | 多步推理预算协调 | 用户剩余配额仅够 2 步，推理需 3 步 | 执行 3 步推理 | 前两步正常执行（各步预扣通过）；第 3 步滚动预算校验失败，触发中止策略；已完成步骤结果保留并摘要返回；前端提示"积分不足"卡片支持升级后继续 | P0 |

---

## 9. 数据一致性测试

### 9.1 计费记录一致性

| 场景 | 验证点 | 期望行为 |
|------|--------|---------|
| **消息实际credits与配额扣减一致** | sys_ai_message.credits vs sys_ai_billing.quota_consumed | 两者相等 |
| **消息totalTokens与token三字段一致** | sys_ai_billing.input+output vs 消息展示total | 一致 |
| **工具调用token分离正确** | tool_llm 类记录的 credits 不重复计入 chat 类记录 | chat 类 credits + tool_llm 类 credits + kb_inject/asr/tts 类 credits = 会话总消耗 |
| **降级不丢计费记录** | 降级链中每个实际执行的模型都有计费记录 | 最终成功的那一级模型有计费记录，前N-1级无（未实际执行LLM调用） |

### 9.2 事务回滚测试

| 场景 | 触发条件 | 验证点 |
|------|---------|--------|
| **MySQL写入失败** | 模拟sys_ai_billing insert失败 | 配额已扣不退（不一致状态），异常告警触发；重试补偿后恢复一致 |
| **Redis操作失败** | 模拟Redis DECR失败临时不可用 | 降级查询MySQL，不丢失配额信息 |

---

## 10. 高风险用例标注

以下用例若实现错误可能导致**财务损失**（少扣/多扣积分），被标记为高风险，在每次发布前必须全部通过：

| 用例ID | 风险说明 |
|--------|---------|
| T-AB-010 | 积分换算错误->全平台用户计费批量偏差 |
| T-AB-014 | 降级计费错误->用户为服务降级多付积分 |
| T-AB-025 | 差额退还错误->用户多付积分不可撤回 |
| T-AB-028 | 并发超扣->单用户配额被扣超过限额，财务损失 |
| T-AB-031 | VIP升级后quota refresh失败->付费用户权益不生效 |
| T-AB-045 | 预扣实扣差额退补链路断裂->配额与余额不一致 |
