# 推荐管理模块需求规格

> **文档分层说明**：本文档按"已实现（现状）/ 本次改造目标（规划中）/ 远期规划"三层组织。
> - **已实现**：当前代码已落地、可直接验证的功能
> - **本次改造目标（规划中）**：已列入[近期改造计划总览](../../05-改造计划/近期改造计划总览.md)，尚未实现的演进方向
> - **远期规划**：已列入[远期改造计划总览](../../05-改造计划/远期改造计划总览.md)，依赖后续阶段的基础设施
>
> **本次改造说明**：本规格在校准实际实现基础上，补齐多任务推荐场景、可验证效果指标与数据飞轮远期规划。标记为「规划中」的项为已定论但尚未落地的演进方向，不视为当前可用能力。

## 1. 模块概述

### 1.1 模块定位

推荐管理模块根据图像特征分析结果，按用户当前处理的任务类型（去雾/去雨/去雪/低光增强/超分辨率/去噪/图像修复）为用户推荐合适的图像处理算法，并提供推荐规则配置与推荐效果度量能力。该模块为算法选择页面提供"算法推荐"面板入口。

> **说明**：早期需求稿描述"根据用户偏好、算法表现等多维信息推荐"并"替代样例图片的静态推荐"，**当前未实现**——实际仅基于图像特征（场景规则匹配）推荐，样例图片仍为静态配置。多任务推荐（按 `task_type` 过滤候选算法池）为规划中目标（见 §4.2）。

### 1.2 核心价值

| 价值维度 | 具体内容 | 状态 |
|---------|---------|------|
| **降低选择门槛** | 用户无需了解算法差异，上传图片即可获得 Top 3 推荐 | ✅ 已实现 |
| **场景匹配** | 基于图像特征（场景类型）匹配预设规则，推荐理由可解释 | ✅ 已实现 |
| **规则可配置** | 管理员可配置场景→算法的映射规则及权重 | ✅ 已实现 |
| **效果可度量** | 推荐采纳率、覆盖率等基础指标统计 | ✅ 已实现（简化） |
| **数据飞轮** | 用户使用、评价、收藏行为反哺推荐引擎 | ⏳ 规划中 |
| **可扩展** | 推荐能力从算法扩展到参数方案、数据集、增强策略 | ⏳ 规划中 |

### 1.3 业务目标

#### 功能目标

- 基于图像特征分析，推荐 Top N 最合适的算法 ✅ 已实现
- 支持推荐理由的可解释性展示 ✅ 已实现
- 推荐结果随用户反馈和使用数据持续优化 ⏳ 规划中（当前反馈仅记录不参与匹配）
- 支持管理员配置推荐规则和权重 ✅ 已实现

#### 性能目标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 图像特征分析 | < 2s | 上传图片到完成特征提取（真实特征分析为远期规划，当前 Mock） |
| 推荐结果生成 | < 500ms | 特征分析完成后生成推荐列表 |
| 端到端推荐 | < 3s | 用户上传图片到看到推荐结果 |
| 推荐覆盖率 | ≥ 80% | 有推荐结果的请求占比（推荐结果非空的请求数 / 总请求数） |
| 推荐采纳率 | ≥ 40% | 采纳数 / 展示数（需采集采纳信号，规划中） |

> **说明**：早期需求稿的"推荐准确率 ≥ 70%（采纳且满意度≥4星）"依赖未采集的采纳/评分数据，不可验证，已移除；改为可验证的覆盖率/采纳率指标。采纳率依赖采纳信号采集（见 §4.3），当前采纳未记录。

## 2. 用户角色分析

### 2.1 主要用户角色

| 角色 | 描述 | 主要职责 |
|------|------|---------|
| **普通用户** | 需要图像处理但不懂算法选择的用户 | 上传图片获取推荐，一键使用推荐算法 |
| **系统管理员** | 管理推荐规则和权重的运营人员 | 配置推荐规则、查看推荐效果报表 |

> **说明**：早期需求稿中的"进阶用户"角色（查看技术指标）**无独立权限**——特征分析详情对所有登录用户可见（前端折叠面板展开即可查看）。

### 2.2 权限矩阵

| 功能点 | 普通用户 | 管理员 |
|--------|:------:|:------:|
| 获取算法推荐 | ✅ | ✅ |
| 查看推荐理由 | ✅ | ✅ |
| 查看特征分析详情 | ✅ | ✅ |
| 反馈推荐效果（有用/无用） | ✅ | ✅ |
| 配置推荐规则 | ❌ | ✅ |
| 查看推荐效果报表 | ❌ | ✅ |

> 权限标识：`sys:recommendation:rule:view` / `sys:recommendation:rule:edit`（规则管理）、`sys:recommendation:report`（报表）。

## 3. 推荐对象类型

| 类型编码 | 类型名称 | 当前阶段 | 排期 | 说明 |
|---------|---------|:------:|------|------|
| `algorithm` | 算法推荐 | 已实现 | — | 根据图像特征 + 任务类型推荐最合适的算法 |
| `preset` | 参数方案 | 规划中 | 近期（依赖任务类型/算法维度扩展） | 根据图像特征推荐最佳参数组合 |
| `dataset` | 数据集 | 规划中 | 远期 | 根据研究方向推荐合适的数据集 |
| `enhance` | 增强策略 | 规划中 | 远期 | 推荐预处理/后处理策略组合 |

> `preset/dataset/enhance` 的支持依赖 `sys_recommendation.target_type` 扩展与推荐对象维度落地；远期 LLM 智能推荐（自然语言描述需求 → 自动推荐算法/参数/数据集）依赖 AI 对话模块，见 §8。

## 4. 功能需求

### 4.1 图像特征分析 (F-REC-001)

#### 4.1.1 功能描述

对用户选择的图像进行多维特征分析，输出结构化的图像特征向量，作为推荐匹配的输入。**已实现**（当前为确定性 Mock 实现，不执行真实图像分析）。

> **实现说明**：当前 analyze 接口基于 **imageUrl 的 MD5 生成确定性伪随机特征**（同一 URL 多次分析结果一致，不同 URL 结果不同），不下载图片、不调用算法服务提取真实特征。`imageId` 方式**暂不支持**（返回业务异常）。真实图像特征分析待算法服务提供特征提取能力后落地。

#### 4.1.2 分析维度

| 维度 | 说明 | 状态 |
|------|------|:----:|
| 雾霾浓度 | 轻度/中度/重度 + 置信度 | ✅ 已实现（Mock） |
| 场景类型 | 城市/风景/建筑/夜景/逆光/室内 + 置信度 | ✅ 已实现（Mock） |
| 光照条件 | 明亮/正常/偏暗/极暗/逆光 | ✅ 已实现（Mock） |
| 图像复杂度 | 0-1 数值（纹理/边缘/色彩） | ✅ 已实现（Mock） |
| 颜色分布 | 色温 + 饱和度 | ✅ 已实现（Mock） |
| 分辨率 | 标清/高清/超清 | ✅ 已实现（Mock） |
| 噪声水平 | 低/中/高 | ✅ 已实现（Mock） |

> 早期需求稿中的**维度权重（30%/20%/15%/10%/10%/5%/10%）无对应实现**——当前推荐匹配仅使用"场景类型"维度。

#### 4.1.3 分析结果展示

| 字段 | 说明 |
|------|------|
| 雾霾浓度 | 轻度/中度/重度 + 置信度 |
| 场景类型 | 城市/风景/建筑/夜景/逆光/室内 + 置信度 |
| 光照条件 | 明亮/正常/偏暗/极暗/逆光 |
| 复杂度 | 0-1 数值（进度条可视化） |
| 分辨率 | 标清/高清/超清 |
| 噪声水平 | 低/中/高 |
| 颜色分布 | 色温、饱和度（原始数据） |

> 前端折叠面板（"图像特征分析"）展开即展示全部维度，**无用户角色分级展示**（早期需求稿的普通用户/进阶用户分级未实现）。

### 4.2 算法推荐 (F-REC-002)

#### 4.2.1 功能描述

基于图像特征分析结果 + 任务类型，匹配推荐规则表，输出 Top 3 推荐算法列表，包含推荐理由和匹配度。**已实现**（按 `task_type` 过滤候选算法为规划中，当前仅按场景匹配）。

#### 4.2.2 推荐策略（已实现）

推荐算法为**场景规则匹配**：

```
确定场景类型（analysisId 历史 → imageMd5 历史 → 默认 urban）
    ↓
加载启用的规则（sys_recommendation_rule，enabled=1）
    ↓
按场景类型匹配规则（scene_type 相同）→ 按权重降序
    ↓
聚合候选算法（规则 algorithm_ids 并集）→ 过滤已发布算法（status=4）
    ↓
【规划中】按用户当前任务类型 task_type 过滤候选算法（算法 task_type 与请求 task_type 一致）
    ↓
匹配度 = 该算法在匹配规则中的最高权重（归一化 0-100）
    ↓
按匹配度降序 → 截取 Top 3
```

**推荐理由模板**（按场景类型，`算法名 + 理由`）：

| 场景类型 | 推荐理由模板 |
|---------|-------------|
| urban（城市） | 处理速度快，对城市雾霾效果出色 |
| landscape（风景） | 在自然场景下表现稳定，色彩还原度高 |
| building（建筑） | 深度模型，对建筑场景处理能力强 |
| night（夜景） | 低光照增强组合，避免过度暗化 |
| backlight（逆光） | HDR预处理提升暗部细节 |
| indoor（室内） | 室内场景适配，细节保留好 |

> **说明**：
> - 早期需求稿中按"轻度/中度/重度雾霾 + 具体算法倾向（DCP/CAP/AOD-Net 等）"的策略表**不成立**——推荐由管理员配置的规则表驱动，候选算法随规则变化
> - "高噪声图像→去噪预处理"场景**未实现**（噪声维度不参与匹配）
> - 无论是否有匹配结果，均写入 `sys_recommendation` 推荐记录（供反馈关联）

#### 4.2.3 推荐结果展示（已实现）

每个推荐算法展示：

| 字段 | 说明 |
|------|------|
| 算法名称 | 已发布算法的名称 |
| 匹配度 | 百分比（0-100，按规则权重计算） |
| 推荐理由 | 场景模板生成的一句话理由 |
| 算法评分 | 暂不展示（标注"暂无数据"，待真实评分数据采集后展示） |
| 预期效果 | 暂不展示（待真实耗时数据采集后展示） |
| 操作 | 一键使用（`select` 事件，跳转图像处理） |

> **说明**：早期实现的"算法评分固定 3.5 星 / 预期耗时固定 5000ms"为硬编码假数据，具有误导性，已移除展示；待真实评分/耗时数据采集后恢复展示。推荐列表仅按匹配度（matchScore）排序，**无"按评分/按使用频率"排序选项**（后端不支持，前端不展示该选项）。
>
> 早期需求稿中的"查看详情、加入对比"操作**未实现**。

### 4.3 推荐反馈 (F-REC-003)

#### 4.3.1 功能描述

用户对推荐结果进行显式反馈，反馈数据记录用于推荐效果度量。**已实现**（显式反馈）。

#### 4.3.2 反馈方式（已实现）

| 反馈方式 | 说明 | 状态 |
|---------|------|:----:|
| 显式反馈 | "有用"/"无用"按钮，写入 `sys_recommendation.feedback`（1=有用；2=无用） | ✅ 已实现 |
| 采纳推荐 | 用户使用推荐算法完成处理（`adopted_algorithm_id` 字段已建但**未写入**） | ⏳ 规划中 |
| 效果评价 | 处理结果评分（1-5星）关联推荐记录（`sys_rating` 未关联推荐 ID） | ⏳ 规划中 |
| 收藏行为 | 收藏推荐的处理结果作为强正向信号 | ⏳ 规划中 |

> 早期需求稿中"反馈数据用于优化推荐模型"——当前反馈仅记录用于报表统计，**不参与后续推荐匹配**。反馈/采纳/评分/收藏信号回灌推荐引擎为远期规划（采集字段与排期见 §8.1）。

### 4.4 推荐规则管理 (F-REC-004)

#### 4.4.1 功能描述

管理员可配置推荐规则（场景→算法映射）及权重。**已实现**（规则 CRUD + 启用/禁用）。

#### 4.4.2 可配置项

| 配置项 | 说明 | 状态 |
|--------|------|:----:|
| 场景匹配规则 | 新增/编辑/禁用场景→算法映射（rule_name/scene_type/algorithm_ids/weight/enabled） | ✅ 已实现 |
| 规则权重 | 0-100 数值，控制匹配优先级 | ✅ 已实现 |
| 特征维度权重 | 调整 7 个分析维度的权重 | ⏳ 规划中（Mock 分析无权重） |
| 算法评分权重 | 用户评分 vs 处理成功率 vs 处理速度 | ⏳ 规划中（当前无真实评分数据，评分暂不展示） |
| 推荐数量 Top N | 默认 3（TOP_N 硬编码常量） | ⏳ 规划中 |
| 冷启动策略 | 新上线算法的推荐曝光策略 | ⏳ 规划中 |

**规则管理细节**：
- 删除操作实际为**禁用**（`enabled=false`，规则保留可追溯）
- 规则变更后刷新内存缓存（Java 端 CopyOnWriteArrayList，写时复制；Python 端每次实时查询启用规则）
- 权重校验：0-100，超出返回业务异常

## 5. 推荐效果度量

### 5.1 核心指标（已实现，简化）

| 指标 | 实际计算方式 | 说明 |
|------|---------|------|
| 推荐覆盖率 | 有推荐结果的请求数 / 总请求数 | ✅ 可验证（基于 sys_recommendation 记录） |
| 推荐采纳率 | 采纳数 / 展示数（采纳 = 用户使用推荐算法完成处理） | ⏳ 规划中（依赖 `adopted_algorithm_id` 写入，当前未记录） |
| 推荐满意度 | 有用反馈数 / 有反馈总数（useful/feedbackTotal） | ✅ 已实现（简化，有用即满意） |
| 冷启动成功率 | 固定 0.0（硬编码） | ⏳ 规划中（冷启动策略未实现） |
| 趋势 | 按日聚合采纳率（date + adoptionRate） | ✅ 已实现 |

> 早期需求稿的"推荐准确率 ≥ 70%（采纳且满意度≥4星）"依赖未采集数据，不可验证，已移除；改为可验证的覆盖率/采纳率指标。

### 5.2 报表（接口已就绪，前端远期落地）

管理员可通过 `GET /api/v1/recommendations/report` 查询效果报表（接口已就绪），包含：推荐总量、覆盖率、采纳率、满意度、冷启动成功率、按日采纳率趋势。

> 早期需求稿中的"各场景类型的推荐准确率、各算法的被推荐次数和采纳率、用户反馈分布（有用/无用占比）"**未实现**。前端报表页面为**远期规划**（接口与权限标识已就绪，前端页面未落地）。

## 6. 依赖关系

### 6.1 依赖模块

| 模块 | 依赖说明 | 状态 |
|------|---------|:----:|
| **算法管理** | 获取已发布算法列表（status=4）作为候选池 | ✅ 已实现 |
| **文件管理** | 图像特征分析（真实图像分析） | ⏳ 规划中（当前 Mock，不读取图片） |
| **会员管理** | VIP 用户优先推荐更高质量算法 | ⏳ 规划中（无会员差异） |
| **收藏管理** | 收藏行为作为推荐训练正向信号 | ⏳ 规划中 |
| **反馈评价** | 处理结果评分作为推荐效果反馈 | ⏳ 规划中 |

### 6.2 被依赖模块

| 模块 | 依赖说明 | 状态 |
|------|---------|:----:|
| **算法选择** | 推荐结果作为算法选择面板的默认入口（RecommendationPanel 组件） | ✅ 已实现 |
| **图像处理** | 推荐策略指导默认参数方案（多任务）；`recommendedBy` 字段已定义但前端当前未携带 | ⏳ 规划中 |
| **图像输入** | 样例图片的推荐算法动态生成（当前为静态配置） | ⏳ 规划中 |

## 7. 待确认事项

> 早期需求稿"推荐模型选择"一项已定论：当前采用**规则匹配**方案（代码注释明确"后续可演进为机器学习模型"，预留架构扩展点），以下为尚未确认/未实现的规划项：

1. **冷启动策略**：新上线算法如何获得推荐曝光机会？（当前无冷启动机制）
2. **A/B 测试框架**：是否需要支持推荐策略的 A/B 测试能力？
3. **推荐历史**：用户是否需要查看历史推荐记录？
4. **离线分析**：批量图片分析是否在规划范围内？
5. **真实特征分析**：是否接入 Python 算法服务的图像特征提取能力（替换 Mock）？

## 8. 远期规划

> **远期规划**：基于 LLM 的智能算法推荐（自然语言描述需求 → 自动推荐算法/参数/数据集），由 AI 对话模块承载，见[远期改造计划总览 §3](../../05-改造计划/远期改造计划总览.md)（步骤 1 核心交付物之一）。现有规则匹配推荐作为 LLM 推荐的候选能力保留。

### 8.1 数据飞轮闭环（远期规划）

用户行为信号回灌推荐引擎，提升推荐质量：

| 信号 | 采集字段 | 来源 | 状态 |
|------|---------|------|:----:|
| 显式反馈 | `sys_recommendation.feedback`（1=有用/2=无用） | 推荐反馈接口 | ✅ 已采集 |
| 采纳信号 | `sys_recommendation.adopted_algorithm_id` | 用户使用推荐算法完成处理 | ⏳ 规划中（字段已建未写入） |
| 效果评价 | `sys_rating` 关联 `recommendation_id` | 处理结果评分 | ⏳ 规划中（未关联） |
| 收藏行为 | `sys_favorite` 关联推荐结果 | 收藏处理结果 | ⏳ 规划中（未关联） |

> 当前反馈仅记录用于报表统计，**不参与后续推荐匹配**；远期信号回灌推荐引擎后实现个性化排序。
