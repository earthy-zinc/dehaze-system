# 订单管理模块测试用例

## 1. 测试目标

- [x] 验证核心功能符合需求规格（订单创建、支付、退款、状态机）
- [x] 覆盖正常流程、边界条件、异常分支
- [x] 验证并发场景与数据一致性（支付回调幂等、优惠券并发、自动续费）
- [x] 验证缓存策略与性能指标（订单创建 <200ms、回调处理 <3s）
- [x] 验证权限控制与安全性（支付签名、金额校验、幂等）

---

## 2. 参考文档

| 文档类型 | 路径 |
|---------|------|
| **需求规格** | [需求规格.md](./需求规格.md) |
| **API 接口** | [API接口.md](./API接口.md) |
| **数据库设计** | [02-系统架构/03-数据库设计.md](../../../02-系统架构/03-数据库设计.md) §4.5 |
| **测试架构** | [02-系统架构/05-测试架构.md](../../../02-系统架构/05-测试架构.md) |

> **注**：接口详细参数/响应结构可通过 API 文档 MCP 查询。

---

## 3. 测试前置

### 3.1 测试数据

| 数据类型 | 准备内容 | 说明 |
|---------|---------|------|
| **套餐数据** | 在售套餐各若干（不同等级/周期） | 用于下单测试 |
| **用户数据** | 各等级会员用户、普通用户 | 用于不同场景下单 |
| **优惠券** | 不同类型可用券 | 用于订单抵扣测试 |
| **支付配置** | 微信/支付宝沙箱配置、平台余额充值用户 | 用于支付测试 |
| **订单数据** | 各状态订单样本（待支付/已支付/已完成/已取消/退款中/已退款） | 用于列表查询与状态流转 |

### 3.2 数据清理策略

- 测试订单使用统一前缀 `DHTEST` 标识订单号
- 测试结束按前缀清理订单、支付流水、退款记录
- 支付沙箱数据每日重置
- 测试优惠券使用专用批次，便于回收

### 3.3 测试范围

**包含的功能点：**
- 订单创建与支付对接（含余额直付）
- 支付回调处理与幂等
- 订单超时取消
- 退款申请、审核、失败自动重试
- 自动续费
- 后台订单管理与统计
- 余额账户充值全链路（充值下单→渠道回调→余额增加→流水）
- 渠道对账（三类差异：金额不符/系统多单/渠道多单）

**改造目标用例（见 [需求规格 §3.5](./需求规格.md)，本期落地）：**
- 平台余额支付（余额校验/冻结/扣减/乐观锁）
- 组合支付（余额 + 第三方渠道，余额拆分）
- 退款按使用次数比例折算、7 天时限统一、50% 使用量校验、促销套餐不可退
- 退款后权益回收与成长值扣减
- 支付/退款结果通知
- 订单统计环比/同比、退款原因分布、转化率

**不包含的功能点（非本期范围）：**
- 套餐定义与定价（套餐模块测试）
- 会员等级变更（会员模块测试）
- 支付渠道接口细节（渠道沙箱测试）

---

## 4. 用例清单

### 4.1 订单创建 (F-OM-002)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-OM-001 | 正常流程：创建无优惠券订单 | 用户登录，套餐在售 | POST /api/v1/orders, packageId, payMethod=wechat | 订单创建成功，返回订单号+支付参数，响应&lt;200ms | P0 |
| T-OM-002 | 正常流程：创建含优惠券订单 | 用户有可用优惠券 | packageId, couponId, payMethod | 订单创建成功，coupon_amount 正确计算 | P0 |
| T-OM-003 | 正常流程：平台余额创建订单 | 用户已登录 | payMethod=balance | 订单创建成功（创建阶段不支付，由 pay 发起） | P0 |
| T-OM-004 | 验证：create 不触发渠道下单 | 订单已创建（wechat） | 检查支付流水 | 无支付流水记录（pay 时才创建） | P1 |
| T-OM-005 | 验证：订单号格式 | 订单创建成功 | 检查 order_no | 格式为 DH + yyyyMMddHHmmss + 6位随机数 | P0 |
| T-OM-006 | 验证：套餐信息冗余存储 | 订单创建 | 查询订单 | package_name/package_level/period_days 冗余写入 | P0 |
| T-OM-007 | 验证：expire_time 计算 | 订单创建 | 检查 expire_time | = 创建时间 + 30分钟 | P0 |
| T-OM-008 | 验证：优惠券锁定 | 订单使用优惠券 | 检查 sys_user_coupon.status | 变为"已锁定"(4)（支付成功后才核销为 2） | P0 |
| T-OM-009 | 验证：应付金额计算 | 原价1000，促销折扣200，优惠券抵扣100 | 检查 payable_amount | = 1000-200-100=700 | P0 |
| T-OM-010 | 边界：套餐下架校验 | 套餐 status=0 | 尝试下单 | 校验失败，提示"套餐已下架" | P0 |
| T-OM-011 | 边界：优惠券无效 | 券已使用/过期 | 使用此券下单 | 校验失败（锁定条件更新 0 行） | P0 |
| T-OM-012 | 边界：优惠券不属于该用户 | 使用他人优惠券 | 下单 | 校验失败，提示"优惠券不存在"（不暴露归属） | P0 |
| T-OM-013 | 边界：优惠券不适用此套餐 | 券限定了适用套餐，不含此套餐 | 下单 | 校验失败，提示"优惠券不适用此套餐"（价格计算阶段） | P1 |
| T-OM-014 | 边界：非法支付方式 | - | payMethod=unknown | 校验失败，提示"不支持的支付方式" | P0 |
| T-OM-015 | 边界：防重复下单 | 同一用户同一套餐5秒内重复下单 | 第二次下单 | 失败，提示"请勿重复下单" | P0 |
| T-OM-016 | 异常：未登录用户下单 | 未登录 | 创建订单 | 返回401 | P0 |
| T-OM-017 | 异常：参数缺失 | 未传 packageId | 创建订单 | 校验失败 | P1 |

### 4.2 支付对接 (F-OM-003)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-OM-018 | 正常流程：微信支付发起 | 订单已创建 | POST /api/v1/orders/{orderNo}/pay, payMethod=wechat | 返回二维码URL/支付参数（渠道未启用时 mock 返回） | P0 |
| T-OM-019 | 正常流程：支付宝支付发起 | 订单已创建 | payMethod=alipay | 返回支付链接（渠道未启用时 mock 返回） | P0 |
| T-OM-020 | 现状（技术债）：平台余额支付 | 订单已创建 | payMethod=balance | 现状直接置已支付 + 触发权益生效（无余额校验/扣减）；改造目标见 T-OM-021 | P0 |
| T-OM-021 | 改造目标：余额支付全链路 | 余额账户有充足余额 | payMethod=balance | 校验余额→冻结→扣减（乐观锁）→置已支付+权益生效；余额不足返回 B007 | P0 |
| T-OM-022 | 改造目标：组合支付 | 余额账户有部分余额 | payMethod=combined | 余额部分扣减 + 剩余走第三方渠道；渠道未启用时 mock 返回 | P0 |
| T-OM-023 | 边界：订单已取消 | 订单状态=cancelled | 发起支付 | 失败，返回订单状态不允许操作 | P0 |
| T-OM-024 | 边界：订单已支付 | 订单状态=paid | 发起支付 | 失败，返回订单状态不允许操作 | P0 |
| T-OM-025 | 边界：订单超时 | expire_time &lt; NOW() | 发起支付 | 失败，提示"订单已超时" | P0 |
| T-OM-026 | 正常流程：支付成功创建支付流水 | 支付成功 | 检查 sys_payment_record | 生成支付记录（payment_no/channel/amount/status=2/callback_time） | P1 |

### 4.3 支付回调处理 (F-OM-003)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-OM-027 | 正常流程：微信回调成功 | 订单状态=pending，签名有效 | POST /api/v1/orders/payment/wechat/callback | 订单变 paid，记录支付流水，触发权益生效，响应&lt;3s | P0 |
| T-OM-028 | 正常流程：支付宝回调成功 | 订单状态=pending | POST /api/v1/orders/payment/alipay/callback | 同上 | P0 |
| T-OM-029 | 验证：签名校验 | 签名无效 | 回调请求 | 返回失败，不处理业务 | P0 |
| T-OM-030 | 验证：金额校验 | 回调金额 ≠ 应付金额 | 回调请求 | 三端统一抛 A0538，记录日志+返回失败 | P0 |
| T-OM-031 | 验证：幂等处理 | 同一回调重复发送 | 第二次回调 | 直接返回成功，不重复执行业务 | P0 |
| T-OM-032 | 验证：幂等键唯一约束 | 同一 payment_no 重复插入 | 检查唯一索引 | uk_payment_no 阻止重复 | P0 |
| T-OM-033 | 边界：订单已取消收到回调 | 订单状态=cancelled | 回调请求 | 返回失败，记录日志（不执行业务） | P1 |
| T-OM-034 | 边界：订单已支付收到回调 | 订单状态=paid | 回调请求 | 幂等返回成功 | P0 |
| T-OM-035 | 验证：权益生效触发 | 支付成功 | 检查会员记录 | level_code 变更为套餐等级、level_source=package、expire_time 写入（原到期时间叠加） | P0 |
| T-OM-036 | 验证：成长值累积 | 支付成功 | 检查成长值流水 | 生成 consume 类型流水（按支付金额 1:1 累积成长值） | P0 |
| T-OM-037 | 验证：优惠券核销 | 订单使用了优惠券 | 检查 sys_user_coupon | status 变为"已使用"(2)，used_order_id 写入，模板 used_qty+1 | P0 |
| T-OM-038 | 验证：Redis 分布式锁 | 并发回调同一订单 | 同时到达 | 锁保护，仅一个回调执行业务，其余幂等返回成功 | P0 |

### 4.4 支付超时取消 (F-OM-004)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-OM-039 | 正常流程：30分钟未支付自动取消 | 订单 expire_time &lt; NOW()，状态=pending | 定时任务扫描 | 状态变 cancelled，cancel_reason="超时未支付" | P0 |
| T-OM-040 | 验证：释放优惠券 | 订单使用了优惠券 | 取消后 | sys_user_coupon.status 恢复为"未使用"(1) | P0 |
| T-OM-041 | 验证：调用渠道关单接口 | 订单已调用微信下单 | 取消时 | 调用微信关单接口（防延迟支付） | P1 |
| T-OM-042 | 验证：定时任务频率 | 每5分钟扫描 | 检查任务配置 | 单次扫描 &lt;30s | P1 |
| T-OM-043 | 边界：取消后收到延迟回调 | 订单已取消 | 收到支付成功回调 | 记录异常，人工处理 | P1 |

### 4.5 余额账户与充值 (F-OM-015, F-OM-017)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-OM-044 | 正常流程：创建充值订单 | 用户已登录 | 选择充值金额 + 支付方式(微信/支付宝) | 创建充值订单(type=recharge)，返回订单号+支付参数 | P0 |
| T-OM-045 | 正常流程：充值支付回调成功→余额增加 | 充值订单已创建，渠道回调 | 收到渠道回调 | balance += 充值金额，记录余额流水(change_type=recharge)，发送充值成功通知 | P0 |
| T-OM-046 | 验证：充值余额流水完整 | 充值成功 | 检查 sys_balance_log | 记录 user_id/变动类型 recharge/金额/变动后余额/关联订单号/时间 | P1 |

### 4.6 用户端订单管理 (F-OM-013, F-OM-014)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-OM-047 | 正常流程：查询我的订单列表 | 用户有多个订单 | GET /api/v1/orders/my | 返回分页订单列表 | P0 |
| T-OM-048 | 正常流程：按状态筛选 | 存在各状态订单 | status=pending | 仅返回待支付订单 | P0 |
| T-OM-049 | 正常流程：查询订单详情 | 订单存在 | GET /api/v1/orders/{orderNo} | 返回订单完整信息含金额明细 | P0 |
| T-OM-050 | 正常流程：用户主动取消订单 | 订单状态=pending | PUT /api/v1/orders/{orderNo}/cancel | 订单变 cancelled，释放优惠券 | P0 |
| T-OM-051 | 边界：已支付订单不可取消 | 订单状态=paid | 尝试取消 | 失败，提示"已支付订单不可取消" | P0 |
| T-OM-052 | 边界：查看他人订单 | 订单属于其他用户 | GET /api/v1/orders/{orderNo} | 返回 A0530 | P0 |
| T-OM-053 | 验证：状态-动作映射 | 不同状态订单 | 校验各状态可发起动作 | 待支付状态允许发起支付，已支付状态允许申请退款（其余状态不允许对应动作） | P1 |

### 4.7 退款申请 (F-OM-006)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-OM-054 | 正常流程：申请退款 | 订单状态=paid（Python）/paid 或 completed（Java） | POST /api/v1/orders/{orderNo}/refund, reason | 订单变 refunding，创建退款记录 | P0 |
| T-OM-055 | 边界：非本人订单申请退款 | 订单属于其他用户 | 申请退款 | 返回 A0530 | P0 |
| T-OM-056 | 边界：pending状态订单申请退款 | 订单状态=pending | 申请退款 | 失败，返回 A0531 | P0 |
| T-OM-057 | 边界：cancelled状态订单申请退款 | 订单状态=cancelled | 申请退款 | 失败，返回 A0531 | P0 |
| T-OM-058 | 边界：重复退款申请 | 订单已申请退款 | 再次申请 | 失败，uk_order_id 唯一约束 | P0 |
| T-OM-059 | 边界：超过7天申请退款 | 支付成功超7天（Python 端） | 申请退款 | Python：返回 A0534；Java：无校验，可申请（两端待统一） | P1 |
| T-OM-060 | 验证：退款金额计算 | 订单已支付 | 检查 refund_amount | = paid_amount（全额退款，无按使用次数折算） | P0 |
| T-OM-061 | 验证：used_quota 固定为0 | 申请退款 | 检查 sys_refund_record.used_quota | = 0 | P0 |
| T-OM-062 | 验证：退款记录字段完整 | 申请退款 | 检查 sys_refund_record | 含 refund_no/order_id/refund_amount/reason/used_quota/status | P0 |
| T-OM-063 | 验证：退款原因拼接 | customReason 非空 | 申请退款 | reason 字段为 `reason + ":" + customReason` | P1 |

### 4.8 退款审核 (F-OM-007)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-OM-064 | 正常流程：查询退款审核列表 | 存在退款中订单 | GET /api/v1/orders/refunds/page | 返回待审核退款列表 | P0 |
| T-OM-065 | 正常流程：审核通过 | 管理员有 order:refund:approve 权限 | PUT /api/v1/orders/refunds/{id}/approve | 调渠道退款（同步）→ 成功时订单变 refunded | P0 |
| T-OM-066 | 正常流程：审核驳回 | 管理员有权限 | PUT /api/v1/orders/refunds/{id}/reject, remark | 退款记录置失败，订单恢复已支付/已完成，记录审核信息 | P0 |
| T-OM-067 | 验证：审核通过后权益回收 | 审核通过 | 检查会员 | 套餐到期时间调整为已使用天数后截止，重新计算会员等级 | P0 |
| T-OM-068 | 验证：成长值扣减 | 退款成功 | 检查成长值流水 | 生成 refund_deduct 类型流水（按已使用天数折算扣减成长值） | P0 |
| T-OM-069 | 验证：优惠券退回 | 订单使用了优惠券 | 退款成功 | sys_user_coupon.status 恢复为"未使用"(1)，used_qty-1 | P1 |
| T-OM-070 | 验证：退款结果通知 | 审核完成 | 检查通知 | 发送退款结果站内信通知用户 | P1 |
| T-OM-071 | 边界：渠道退款失败 | 渠道退款接口失败 | 审核通过 | 退款记录 status=refund_failed + error_message，订单恢复已支付/已完成，30 分钟后自动重试 | P0 |
| T-OM-072 | 异常：无权限审核 | 无 order:refund:approve 权限 | 审核操作 | 返回403 | P0 |
| T-OM-073 | 验证：余额订单审核通过直接成功 | 原支付方式=balance | 审核通过 | 不调渠道，直接标记退款成功 | P1 |

### 4.9 退款渠道处理 (F-OM-008)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-OM-074 | 正常流程：微信原路退回 | 原支付方式=wechat | 审核通过 | 调用微信退款接口，资金原路退回 | P0 |
| T-OM-075 | 正常流程：支付宝原路退回 | 原支付方式=alipay | 审核通过 | 调用支付宝退款接口 | P0 |
| T-OM-076 | 现状（技术债）：余额订单退款 | 原支付方式=balance | 审核通过 | 现状不调渠道直接标记退款成功（无余额退回）；改造目标：退回余额账户 | P1 |
| T-OM-077 | 改造目标：组合支付退款 | 原支付方式=combined | 审核通过 | 余额部分退回余额账户 + 第三方部分调渠道退款（按 §4.1.5 拆分） | P2 |
| T-OM-078 | 验证：channel_refund_no 记录 | 退款成功 | 检查退款记录 | 记录渠道退款流水号 | P1 |
| T-OM-079 | 验证：退款失败自动重试 | 退款失败（status=3） | 定时任务扫描 | 每 30 分钟重试，retry_count 递增，达 3 次标记最终失败 | P1 |

### 4.10 对账机制 (F-OM-016)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-OM-080 | 正常流程：对账无差异 | 前一日支付流水与渠道账单一致 | 每日 02:00 定时任务 | 对账通过，无差异记录 | P1 |
| T-OM-081 | 对账差异：金额不符(amount_mismatch) | 系统有、渠道有但金额不一致 | 对账任务逐笔比对 | 写入 sys_reconciliation_diff(diff_type=amount_mismatch, system_amount/channel_amount)，告警通知运营 | P0 |
| T-OM-082 | 对账差异：系统多单(system_only) | 系统有支付流水、渠道账单无 | 对账任务 | 写入差异记录(diff_type=system_only)，告警通知运营 | P0 |
| T-OM-083 | 对账差异：渠道多单(channel_only) | 渠道账单有、系统无支付流水 | 对账任务 | 写入差异记录(diff_type=channel_only)，告警通知运营 | P0 |

### 4.11 自动续费 (F-OM-005, F-PM-007)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-OM-084 | 正常流程：开启自动续费 | 用户已登录，套餐存在 | PUT /api/v1/orders/auto-renew/config, enabled=true | 配置写入 sys_auto_renew（status=1，**next_renew_time 未设置，首次开启不触发扣款**） | P0 |
| T-OM-085 | 正常流程：关闭自动续费 | 已开启自动续费 | PUT enabled=false | status 变为关闭（close_reason 由连续失败场景写入） | P0 |
| T-OM-086 | 验证：续费扣款时机 | 配置 next_renew_time <= NOW | 定时任务扫描 | 到期时执行扣款（非"到期前24小时"） | P0 |
| T-OM-087 | 验证：扣款失败重试 | 渠道扣款失败 | 重试机制 | 最多3次，间隔2小时（next_renew_time = NOW+2h） | P0 |
| T-OM-088 | 验证：3次失败后取消自动续费 | 连续失败3次 | 检查 sys_auto_renew | status 变为关闭，close_reason="连续失败超过限制" | P0 |
| T-OM-089 | 验证：续费订单叠加有效期 | 套餐未到期续费 | 续费成功 | package_expire_time 在原到期时间基础上叠加 | P0 |
| T-OM-090 | 验证：续费订单is_auto_renew标识 | 自动续费生成订单 | 检查订单 | is_auto_renew=1 | P1 |
| T-OM-091 | 边界：用户主动关闭后不再扣款 | status=0 | 定时任务扫描 | 跳过此用户 | P1 |
| T-OM-092 | 验证：自动续费95折优惠 | 开启自动续费 | 续费扣款 | 实付金额 = sale_price × 0.95（discount = 差价） | P1 |
| T-OM-093 | 验证：续费订单状态 | 自动续费生成订单 | 检查订单 | status=paid（已支付）、paidAmount=payable、无优惠券 | P1 |

### 4.12 定时任务（归档与过期）

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-OM-094 | 验证：订单到期归档 | 订单状态=paid 且套餐到期 | 每1小时定时任务扫描 | 订单状态 paid→completed | P1 |
| T-OM-095 | 验证：优惠券过期处理 | 用户券未使用且已过期 | 每1小时定时任务扫描 | sys_user_coupon 置为已过期 | P1 |

### 4.13 后台订单管理 (F-OM-009, F-OM-010)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-OM-096 | 正常流程：分页查询订单列表 | 存在订单数据 | GET /api/v1/orders/page | 返回分页数据，响应&lt;500ms | P0 |
| T-OM-097 | 正常流程：按订单号精确查询 | 订单存在 | orderNo=DH... | 返回匹配订单 | P0 |
| T-OM-098 | 正常流程：按用户信息模糊查询 | - | keywords="testuser" | 返回匹配的订单 | P0 |
| T-OM-099 | 正常流程：按状态筛选 | - | status=paid | 返回已支付订单 | P0 |
| T-OM-100 | 正常流程：按支付方式筛选 | - | payMethod=wechat | 返回微信支付订单 | P1 |
| T-OM-101 | 正常流程：按金额区间筛选 | - | amountMin=1000, amountMax=5000 | 返回金额区间内订单 | P1 |
| T-OM-102 | 正常流程：按支付时间范围筛选 | - | paidTimeStart, paidTimeEnd | 返回时间范围内订单 | P1 |
| T-OM-103 | 正常流程：订单详情完整数据 | 订单存在完整数据 | GET /api/v1/orders/{orderNo} | 返回订单信息 + 支付流水列表 + 退款记录（无权益/操作日志页签） | P0 |

### 4.14 订单统计 (F-OM-012)

| 用例ID | 场景 | 前置条件 | 输入 | 期望输出 | 优先级 |
|--------|------|---------|------|---------|--------|
| T-OM-104 | 正常流程：查询订单统计 | 存在订单数据 | GET /api/v1/orders/stats | 返回总额/状态分布/支付方式/套餐分布/每日趋势 | P0 |
| T-OM-105 | 验证：收入统计 | 多笔订单 | 查询统计 | 总订单数、总收入（已支付+已完成实付合计）正确；**无环比/同比** | P0 |
| T-OM-106 | 验证：套餐分布 | - | 查询统计 | 各套餐销量和收入 | P1 |
| T-OM-107 | 验证：支付方式占比 | - | 查询统计 | wechat/alipay/balance/combined 各订单数 | P1 |
| T-OM-108 | 验证：退款统计 | - | 查询统计 | 总退款金额、退款率 = 总退款/总收入；**无退款原因分布** | P1 |
| T-OM-109 | 验证：转化率统计 | - | 查询统计 | 无转化率维度（当前统计不含转化率） | P2 |
| T-OM-110 | 验证：每日趋势 | - | 查询统计 | 按日订单数/收入（近 30 天） | P1 |

## 5. 关键边界与异常场景

| 场景 | 测试点 | 期望行为 |
|------|--------|---------|
| **订单数量边界** | 订单达到万级 | 列表查询响应时间仍&lt;500ms |
| **支付回调并发** | 同一订单多次回调 | Redis 锁保护，仅一次执行业务 |
| **优惠券并发使用** | 多订单同时使用同一券 | 唯一约束或乐观锁阻止 |
| **自动续费并发** | 同一用户并发扣款 | 逐条处理，异常隔离 |
| **状态机非法流转** | cancelled → paid 等非法流转 | 校验失败，拒绝流转 |
| **时间边界** | 套餐未到期购买新套餐 | 有效期在原到期时间上叠加 |

## 6. 性能与并发测试

### 6.1 性能指标

> 性能目标参见 [需求规格.md](./需求规格.md) 第 1.3 节

| 指标 | 目标值 | 测试方法 |
|------|--------|---------|
| **订单创建响应时间** | &lt;200ms | 单次下单操作 |
| **支付回调处理时间** | &lt;3s | 从收到回调到权益生效 |
| **订单列表查询响应时间** | &lt;500ms | 万级订单分页查询 |
| **支付超时扫描** | 每5分钟，单次&lt;30s | 定时任务 |
| **自动续费扫描** | 每小时，单次&lt;60s | 定时任务 |

### 6.2 并发场景

| 场景 | 并发数 | 验证点 |
|------|--------|--------|
| **并发支付回调** | 同一订单10次回调 | 幂等，仅一次执行业务 |
| **并发下单同一套餐** | 50用户同时下单 | 订单创建成功，无重复 |
| **并发优惠券使用** | 10订单用同一券 | 仅1个成功 |
| **自动续费并发** | 100用户同时到期 | 逐条处理，无重复扣款 |

## 7. 安全性测试

| 测试项 | 验证方式 | 预期结果 |
|--------|---------|---------|
| **支付签名验证** | 伪造支付回调 | 签名校验失败，拒绝处理 |
| **金额校验** | 篡改回调金额 | 与订单金额不一致时记录告警 |
| **幂等处理** | 重复回调 | 幂等键唯一约束，仅一次处理 |
| **防重复下单** | 同一用户同一套餐5秒内重复 | 拒绝重复下单 |
| **敏感信息脱敏** | 支付流水号展示 | 脱敏显示 |
| **权限控制** | 无权限用户审核退款 | 返回403 |
| **数据隔离** | 用户查看他人订单 | 返回 A0530 |
| **SQL注入** | 订单号参数注入 | 参数化查询转义 |
| **传输安全** | 抓包分析请求 | HTTPS加密传输 |
