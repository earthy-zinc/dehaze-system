# 2026-09-15

## 环境修复：VS Code Java 插件要求 JDK 21+
- 系统原只有 JDK 17（`/usr/local/java/jdk17`，符号链接 → `jdk-17.0.9+9`），新版 Java Language Server 自身运行要求 Java 21+
- 安装 Temurin JDK 21.0.12 LTS 到 `/usr/local/java/jdk21`（Adoptium API 下载解压，未用 yum）
- 已在 `~/.vscode-server/data/Machine/settings.json` 配置：`java.jdt.ls.java.home=/usr/local/java/jdk21`（插件运行）；`java.configuration.runtimes` 中 JavaSE-17=default（dehaze-java 项目仍按 17 编译）+ JavaSE-21
- JDK 25 首次解压因磁盘满失败（残骸 212M 在 /data/root/Trash/files/jdk-25.0.4.1+1，无用）；空间恢复后已重装成功：Temurin 25.0.4.1 LTS → `/usr/local/java/jdk25`，已加入 VS Code `java.configuration.runtimes`（JavaSE-25）

## 系统盘空间治理（100G 满盘 → 25G 可用）
- 系统盘 /dev/vda1 100G 满导致所有写入故障；非系统盘 /dev/vdb 394G 挂 /data（工作区、maven 仓库 /data/cache 都在）
- **用户明确偏好：迁移而非删除**（"删了后还得重新下载"）——统一采用 mv 到 /data + 原位符号链接方案，零配置改动
- 已迁移到 `/data/root/`（原位 symlink）：`.gradle` 3.6G、`go` 2.6G（GOPATH=/root/go/1.24.4，go 工具链本体在 /root/.goenv 未动）、`.jdks` 1.2G、`.cache/` 下 modelscope/pnpm/ms-playwright/huggingface、整个 `.local/share/Trash`
- 唯一真删：`/var/log/messages` 7.9G + cron 506M（truncate 保留 inode）+ journal vacuum 到 200M
- go-build/torch/electron/typescript/gopls/goimports 缓存曾被 rm 送进回收站，已从 Trash 恢复到 /data/root/.cache/ 并 symlink，无需重新下载
- **重要坑**：本环境 safe-delete 钩子会把 rm 拦截转成 trash（移入 ~/.local/share/Trash），不真正释放空间；且对 Trash 目录本身操作会 fail-closed。rm 后要检查 Trash 是否囤积
- **JetBrains 26G 已迁移**：确认 `/codev/ide/IU/ideaIU-2025.1/remote-dev-server` 是云平台开机自启的 IDEA 网页 IDE 后端（父进程 systemd，非用户手动启动，闲置 23 天），经用户确认后 kill（206725/836214），迁移 `~/.cache/JetBrains` 22G + `~/.local/share/JetBrains` 4G 到 /data + symlink。若平台重启它，符号链接照常工作
- **最终结果：系统盘 51G 已用 / 50G 可用（51%）**，全天从"仅剩 143M"治理至此；符号链接共 16 处（/root 下 .gradle/.go/.jdks、.cache 下 12 项、.local/share 下 JetBrains/Trash）
- 不动清单（运行中服务）：.vscode-server 6G、.gf 4.1G、.codebuddy-server-cn 4.1G、.gongfeng-copilot 1.9G、CodeBuddyExtension 2.5G
- 遗留：/var/log/messages 曾涨到 7.9G，无 logrotate 限制的话还会再涨，建议后续加 logrotate 或定期检查
