# 2026-08-27

## 修复 dehaze-python 在新 mac 环境（darwin arm64）上的 11 个测试失败
- **沙箱（5 个）**：`app/infrastructure/sandbox/code_sandbox.py` 的 `_build_launcher` 用 `resource.setrlimit` 限制内存/进程/CPU，macOS 对 `RLIMIT_AS` 抛 `ValueError: current limit exceeds maximum limit`，导致沙箱子进程一启动就崩（exitCode 1）。修复：子进程内逐个 `setrlimit` 包 try/except（ValueError/OSError）防御，不支持的平台跳过该限制（保留超时终止+临时目录弱隔离兜底）。Linux 行为不变。
- **PDF 解析测试（3 个）**：`tests/service/test_kb_parser.py` 的 `_CJK_FONT` 硬编码 Linux 字体路径 `/usr/share/fonts/google-noto-cjk/NotoSansCJKsc-Regular.otf`，macOS 不存在。改为 pymupdf 内置 CJK 字体 `fontname="china-s"`（简体宋体，跨平台，已验证可嵌入并正确抽取中文），删除 `_CJK_FONT`。
- **MCP 健康探针测试（3 个）**：`tests/service/test_ai_mcp_server_service.py` 探针走真实 DNS 解析 `example.com`，本机 example.com 被 DNS 污染解析到 `::1`/`127.0.0.1`，`is_safe_url` 正确判为内网拒绝 → 探针不发 HTTP → respx 断言"route not called"。修复：测试用 monkeypatch 把服务里的 `is_safe_url` 桩为返回 True，只验证 HTTP 探针逻辑（SSRF 另有专项测试）。
- 环境提示：本机（EARTHYWU-MC0，macOS）`example.com` 解析到回环地址，属 DNS 污染，非代码问题。

## 修复本地 LLM 子进程启动失败（运行时报错，非测试）
- 现象：文档处理时 `embedding_client.embed_texts` → `ensure_running` 报 `本地 LLM 子进程启动失败（退出码 1）`。
- 根因：`local_llm_manager._start_and_wait` 用 `os.path.abspath(os.path.join(os.path.dirname(__file__), "..","..",".."))` 计算 `cwd`，从 `app/infrastructure/llm/local/` 只上溯 3 层落到 `dehaze-python/app`，导致 `python -m app.infrastructure.llm.local.local_llm_server` 找不到 `app` 包 → ModuleNotFoundError → 退出码 1。
- 修复：`import app as app_pkg`，`cwd` 改为 `os.path.dirname(os.path.dirname(os.path.abspath(app_pkg.__file__)))`（= dehaze-python 根，app 包上级），以包位置为锚点避免相对层数漂移。已验证 `ensure_running()` 就绪返回 8992 并可正常 shutdown。llama-cpp-python 0.3.35 本身可导入，模型文件也正常。

## 消除脆弱路径解析（标记文件锚点 + 配置中心化，2026-08-27）
- 起因：`local_llm_manager._start_and_wait` 用 `os.path.join(dirname(__file__),"..","..","..")` 数层数定位根目录算错（落到 app/），导致本地 LLM 子进程启动崩（ModuleNotFoundError 退出码 1，stderr 被 DEVNULL 吞）。
- 审计结论：除该 bug 外无实际算错的路径解析；但 Go testutil 的 `repoRoot()`/`goRepoRoot()`（`runtime.Caller` + 数 `..`）与 Python config.py/_bootstrap 属同类脆弱写法（当前正确）。
- 按"标记文件锚点 + 配置中心化"修复：
  - **Go**：`internal/testutil/config.go` 的 `goRepoRoot()` 改为以 `go.mod` 为标记向上查找（不依赖层数）；`mysql.go` 的 `repoRoot()`（dehaze-system）= `filepath.Dir(goRepoRoot())`，删除 mysql.go 的 runtime import。
  - **Python**：`app/config.py` 新增 `_find_python_project_root`（以 `pyproject.toml` 为标记向上查找，比 `.env` 可靠——`.env` 精简部署可能缺失）+ 常量 `PYTHON_PROJECT_ROOT`（dehaze-python）与 `PROJECT_ROOT`（dehaze-system）；`env_file`、`MODEL_CACHE_DIR` 改引用；`app/_bootstrap.py` 改引用 `PYTHON_PROJECT_ROOT`；`tests/conftest.py` 的 SQL 目录改引用 `PROJECT_ROOT`。
- 验证：Go build/vet 通过；Python 根路径解析正确、无循环导入（`app/__init__` 只 import _bootstrap，config 不反依赖）、a2a 单元测试通过。
- 最佳实践定论（本项目约定）：相邻资源用 `__file__` 相对定位；找项目根用标记文件（Go: go.mod / Python: pyproject.toml）向上查找，不做跨端通用 path 库。Java 走 classpath 天然规避。

## 改进 SQL 审计日志可读性（2026-08-27）
- 问题：开发环境控制台为文本格式（`LOG_FORMAT_JSON=False` 默认），SQL 审计日志的消息只有字面 "SQL"，语句/耗时/行数全在 `extra` 字段里被普通文本 formatter 丢弃，控制台看不出任何信息。
- 修复：`app/database.py` 的 `_after_cursor_execute` 把语句/耗时/行数拼进消息本身（`SQL [1.23ms, 2 rows] SELECT ...`；rowcount<0 的 DDL 不显示行数；慢查询 `SLOW_SQL [.., threshold Nms]`），`extra` 结构化字段保留供 JSON 文件日志聚合。生产 `LOG_FORMAT_JSON=True` 行为不变。
- 验证：文本格式下消息完整，lint 通过；无测试依赖旧消息格式。

## 关键结论（沿用 08-24 讨论）
- 三端测试库并发问题：用户回滚了 conftest 的 `MYSQL_TEST_DATABASE` 隔离修改，接受"现状靠串行跑测试纪律撑住"，暂不做每端分库名；已在 08-24 手动 DROP 残留库 dehaze_test_a..e/_dbg。

## dehaze-sdk-js 集成测试：资源自动保障 + 失败分类（2026-08-27）
- 现象：brief.json 22 失败 + 63 跳过。根因最大头是 `dehaze-sdk-js/test/resources/` 目录**整体不存在**，`gen_test_resources.py` 未运行，导致 item-file/model/integration/recommendation 等套件 beforeAll `fs.readFileSync` 读图 ENOENT → 直接失败 + "套件钩子失败"级联跳过。
- 修复：新增 `dehaze-sdk-js/test/config/ensure-test-resources.ts`，在 `vitest.globalSetup.ts` 的 `setup()` 首行调用。清单与 `scripts/gen_test_resources.py` 的 IMAGES/PNG_IMAGES 严格对齐（test/hazy 仅 41/42/43，无 44/45！我最初误加导致会重复触发），缺失任一则 execFileSync 调 `dehaze-python/.venv/bin/python scripts/gen_test_resources.py` 幂等全量生成。已手动运行一次成功生成全部资源。
- 环境事实：`dehaze-python/.venv/bin/python` 含 Pillow 12.0.0 + numpy 2.3.4，可直接跑资源脚本；脚本还会写项目根 `datasets/`（NH-HAZE-2023，nginx 容器挂载按 URL 访问，非 beforeAll 检测范围）。
- 其余失败分类（非资源，未改）：ai-conversation SSE 超时（依赖真实 LLM qwen3-0.6b）；dept 多 hook 超时（POST /api/v1/depts 每请求约 52s，后端慢）；knowledge-base import-url 抓 baidu.com 被反爬 A0500；voice WS ASR ErrorEvent（依赖真实 ASR）。skip/skipIf 标记为显式设计，合理。

## /health/ready 扩展检查全量基础设施（2026-08-27，用户确认后实施）
- 背景：三端 `/ready` 原只查 DB/Redis/RabbitMQ。用户要求按"启用才检查"探测各自依赖的全部基础设施。dehaze-doc 06-部署架构明确基础设施：MySQL/Redis/MongoDB/MinIO/RabbitMQ/ES/XXL-Job。
- 三端依赖差异（关键）：**ES 仅 Python 用**（知识库向量检索），Go/Java 不用 ES；三端都用 MongoDB(login/audit/ai_call 日志) 与 MinIO(默认 file storage)。
- 改动：
  - **Python** `app/router/health.py`：新增 `_es_ready`(es_client ping)/`_mongo_ready`(motor ping)/`_minio_ready`(同步SDK走 storage_executor 线程池) + `_probe(name, checker)`（asyncio.wait_for 5s 兜底）。ready 加 elasticsearch/mongodb（必选）+ minio（仅 FILE_STORAGE_TYPE=="minio"）。
  - **Go** `internal/app/app.go`：Application 加 `defaultStorage storage.StorageService` 字段（Init 里赋值）；`readinessHandler` 加 mongodb(mongo.GetMongoClient().Ping 5s 超时) + minio（仅 cfg.File.Type==minio，类型断言 `*storage.MinioStorageService` 调新增的 `Ping`）。`pkg/storage/minio.go` 新增 `Ping(ctx)`（BucketExists）。
  - **Java** `controller/HealthController.java`：注入 `MongoTemplate`（executeCommand ping）+ `@Autowired(required=false) MinioFileService`（仅 file.minio.endpoint 配置时装配）；`MinioFileService` 加 `ping()`（bucketExists）。
  - 文档：`02-系统架构/01-总体架构设计.md` §8.4 表格更新 ready 描述。
- 验证：Go build / Java mvn compile 通过；Python lint 通过；ES/Mongo/MinIO 客户端实际连通验证通过（ES ping 180ms / Mongo 380ms / MinIO 171ms）。**新代码需重启后端生效，未私自重启**。
- 附带修复：`app/infrastructure/mq/handlers.py` `handle_export_task` 使用 `task_consumer` 但缺导入（50 次 NameError），补与 `handle_dlq_message` 一致的函数内延迟导入。

## RabbitMQ 连接诊断（2026-08-27）
- `earthyzinc.devcloud.woa.com:5672` 现在完全正常：DNS 解析 4ms、AMQP 认证连接(root/Dehaze2026) 219ms、远程端口 OPEN；后端 01:54 已自动重连成功。
- 之前测试期间 120+ 次重连失败为**临时 DNS/网络故障**，非配置问题（aio_pika 重连机制最终恢复）。本地各端口 CLOSED 因无映射，正常。
- 排查工具提示：`dehaze-python/logs` 被 gitignore，**search_content 工具搜不到该目录**，需用命令行 grep/awk 或直接 read_file；`run.py ps` 在 macOS 报错（用了 Linux 的 `ss` 命令），用 `lsof -iTCP:<port> -sTCP:LISTEN` 替代。

## dept 测试超时根因（未完全定论，2026-08-27）
- POST /api/v1/depts 耗时 51-65s，但紧邻 SQL 仅 ~100ms，**慢在 SQL 之后的 `_clear_cache`**（`scan_iter("dept:tree*")`+`delete`+`publish`）。慢请求严格每 ~53s 一个，疑似 Redis 被周期性阻塞或测试期间 keyspace 膨胀导致 scan 全库慢。当前 Redis dbsize=2124、scan 全库 0.7s 正常，`dept:*`=0 个。
- `DeptCacheKeys.all_patterns()` 返回 `["dept:tree*","dept:options*"]`，但代码**无任何地方写 dept:tree** key（纯历史遗留），每次 dept 写操作都 scan 全库匹配一个不存在的 pattern，属性能隐患，待定论后优化。

## Python /ready 补充本地 LLM + ASR/TTS（2026-08-27，用户"按最佳实践解决"）
- 澄清部署形态：**本地 LLM 是唯一独立常驻子进程**（127.0.0.1:8992，OpenAI 兼容，自带 /health）；**ASR(FunASR)/TTS(Piper) 是进程内引擎**（模型懒加载，非子进程）；沙箱为临时子进程。
- 最佳实践结论（三端统一原则）：
  - **liveness `/health` 不变**（子进程/引擎挂掉不影响主进程存活，避免误重启容器）。
  - **本地 LLM 进 ready（核心阻断）**：`LOCAL_LLM_ENABLED`（默认 True）开关，纯云端 LLM 部署可跳过；探测先只读 `httpx :8992/health`（要求 loaded=true），未就绪则 `asyncio.to_thread(ensure_running)` 拉起再探。
  - **ASR/TTS 只读附带状态不阻断**：`voice_asr`/`voice_tts` 用 engine_status()（懒加载下 offline），完整状态仍走 `/api/v1/voice/service/status`。
- 改动：`app/config.py` 加 `LOCAL_LLM_ENABLED`；`app/router/health.py` 加 `_llm_http_healthy`/`_local_llm_ready`/`_voice_engine_status`，ready 加 local_llm（阻断）+ voice_asr/voice_tts（附带）。文档 §8.4 同步。
- 验证：lint 通过；本地 LLM 当前未运行（8992 无响应）、ASR/TTS offline（懒加载未触发），与设计预期一致。未重启后端生效。

## 语音引擎注册表改造（设计文档已完成，代码未改，2026-08-27）
- **背景**：多客户私有化部署配置高低差异，存在拿不到高性能服务器风险 → 需纯云端 ASR/TTS 选项。方案已确认，进入文档阶段（文档完成，Python 代码改造待后续）。
- **架构**：对齐 AI 模型管理（sys_ai_provider/sys_ai_provider_key/sys_ai_model）→ 新增 3 表 `sys_voice_provider`（引擎，engine_type=asr/tts、is_default、api_base_url、auth_type）/`sys_voice_provider_key`（AES 密文+哈希，多 Key 轮换）/`sys_voice_model`（ASR 模型/TTS 音色）。配置存数据库，本地 FunASR/Piper 与云端（阿里云/腾讯云/讯飞 ASR、Azure/阿里云 TTS）共存，**应用侧透明，不区分本地/云端**。
- **关键决策（用户拍板）**：asr/tts 各一个默认引擎（`is_default`，默认 local；纯云端部署 default 指向云端引擎）；local 作为内置 provider 进注册表；**纯云端部署 default 仍是 local 时 throw error（A0500）而非降级**；需要管理页；Java/Go 之后再议。
- **Provider 抽象**：`ASRProvider`/`TTSProvider` 接口 + LocalAsr/LocalTts（封装现有 funasr_engine/piper_tts_engine）+ CloudAsr/CloudTts；`VoiceEngineRegistry` 按 engine_type 选 is_default 引擎 + 校验冲突。
- **文档已更新**：`03-数据库设计.md` §4.10（3 表）；`语音交互/后端实现.md`（§1.1/§2.1/§2.4 注册表/§3.6/§4.5 云端/§8/§9）；`API接口.md` §2.5 管理端接口 + 权限 `voice:engine:manage`；`需求规格.md` §3.5 F-VS-005；`前端实现.md` VoiceEnginePanel。**代码改造（Python）未开始，待用户确认。**

## 语音引擎最终方案定稿补充（2026-08-27，设计先行，用户审核中）
- **3 表共用承载 asr/tts，`engine_type` 字段区分，不按能力拆表**（asr/tts 在 provider 层完全同构：地址/认证/Key/健康；差异只在 model 层）。同一云厂商按能力注册两条 provider 记录。
- **本地引擎模型注册表化**：FunASR/Piper 由写死常量（`_MODEL_IDS`/`_VOICE_MODEL_FILES`）改为从 `sys_voice_model` 解析（params 携带 funasr model_id/Piper onnx 路径+下载URL），可注册/切换多个本地 ASR 模型与 TTS 音色。已落文档（后端实现 §3.1/§4.1、数据库设计 §4.10、需求规格 §3.5.2）。
- **基础设施复用**：语音 provider 的 Key 管理（AES+哈希、多 Key 轮换、失败冷却 5/15/30min、限额）完全复用 `sys_ai_provider_key` 机制（仅表不同）；健康熔断复用 `provider_health_service`。已强化到文档 §2.4/§4.10。
- **架构边界（用户确认）**：语音模型域独立（sys_voice_model），不并入 AI 大模型 sys_ai_model（计费单位 token vs 秒/字符、能力字段差异大，全统一属过度设计）；仅复用 provider/Key/健康基础设施层。
- 数据库设计命名统一用 `model_id`（修正了 model_code 混用）。
- **下一步（待用户审核文档后）**：写 `sys_voice_*` 三表 SQL → 改造 Python（注册表/Provider/VoiceEngineRegistry/管理接口）→ 迁移开发库 + 测试库 config/sql。

## 语音引擎 SQL 已完成并建表成功（2026-08-27）
- 写了 3 张表 schema 到 `config/sql/schema/`（对齐 sys_ai_provider/key/model DDL 风格）：`sys_voice_provider`（engine_type 区分 asr/tts、is_default 每能力唯一、uk_provider_code、idx_engine_default/status）、`sys_voice_provider_key`（复用 sys_ai_provider_key 机制）、`sys_voice_model`（model_id+provider_id 联合唯一、engine_type+model_type、params）。
- 已用 pymysql 连远程 MySQL（root）执行到开发库 `dehaze`，三表建表成功（语法验证通过）。
- **种子策略（对齐 model_seeder）**：AI provider/model 是**运行时播种**（`model_seeder.ensure_local_models`）而非 SQL data 种子；model_seeder 注释明确旧架构"TTS/ASR 不进入模型注册表"（本次改造正是打破它）。因此语音 local 引擎将由**运行时 `voice_engine_seeder`** 幂等播种（local asr/tts provider + is_default + 默认模型 sensevoice/paraformer/huayan），不写 SQL data 种子。
- **下一步**：改造 Python（voice_engine_seeder 播种 local + Provider 抽象 ASR/TTSProvider + Local/Cloud 实现 + VoiceEngineRegistry + 管理端接口）。

## 语音引擎注册表 Python 改造完成（2026-08-27，team 团队协作）
- **DDL 修正**：`sys_voice_provider` 唯一索引从 `uk_provider_code`(单列) 改为 `uk_provider_engine(provider_code, engine_type)` 联合唯一（同一厂商按能力注册 asr/tts 两条，local asr/tts 各一条）。已同步 schema SQL、开发库（ALTER）、数据库设计/后端实现文档。
- **新增（团队成员产出，已整合编译验证通过）**：
  - 实体：`app/models/entity/sys_voice_provider.py`/`sys_voice_provider_key.py`(无软删)/`sys_voice_model.py`
  - 仓库：`app/repository/voice_provider_repository.py`（含 `get_by_provider_and_engine(db,code,engine_type,include_deleted=False)` 联合查重、`get_default`、`list_enabled`、`paginate_providers`）/`voice_provider_key_repository.py`/`voice_model_repository.py`
  - 播种器：`app/infrastructure/voice/seeder.py` `ensure_local_engines`（幂等播种 local asr/tts + sensevoice/paraformer/huayan），接入 `app/lifecycle.py`
  - Provider：`app/infrastructure/voice/provider/`（base.py ASRProvider/TTSProvider/ASRStreamSession 接口 + local_asr/local_tts 封装现有 funasr_client/piper_tts_engine + cloud_asr/cloud_tts 占位 + registry.py VoiceEngineRegistry）
  - 管理接口：`app/router/voice_admin.py`（13 端点，prefix=/api/v1/voice，权限 voice:engine:manage）+ `app/service/voice/voice_admin_service.py` + `app/models/schema/voice_admin.py`，已注册到 router/__init__.py
- **D 接入（lead 完成）**：`asr_service.py`（改经 `voice_engine_registry.get_asr_provider()` 调用 provider.recognize_stream/recognize_offline/register_hotwords，会话 model 字段置空）、`tts_service.py`（改经 `get_tts_provider()` 调用 provider.synthesize）。
- **接口协调**：TTSProvider.synthesize 补 `format_`/`sample_rate` 参数（保留 tts_service 多格式 API）；LocalAsrProvider.recognize_stream() 无 model 参数（模型从 sys_voice_model 解析）；create_provider 查重改用 get_by_provider_and_engine。
- **验证**：lint 0 错误；app.main 完整加载（26 个 voice 路由）；seeder 播种成功（local asr id=3/tts id=4, default=1）；registry 解析 → LocalAsrProvider/LocalTtsProvider；engine_status offline（懒加载未触发）。
- **未做**：完整语音功能回归测试（pytest/dehaze-test）、云端厂商协议适配、Java/Go 对齐（之后再说）。

## 本地引擎模型彻底注册表化 + 云端框架（2026-08-27，用户指定两点）
- **本地引擎模型彻底注册表化**：`funasr_engine._MODEL_IDS` 写死映射改为可变注册表 `_model_ids` + `configure_models(models)`（由 LocalAsrProvider 从 sys_voice_model 解析 {逻辑名: ModelScope ID} 注入）；`piper_tts_engine._VOICE_MODEL_FILES/_SIZES/_DOWNLOAD_URLS` 改为 `_VoiceConfig(onnx,size,url_templates)` + `_voice_configs` + `configure_voices(dict)`（LocalTtsProvider 从 sys_voice_model.params 注入）。engine_status 基于注入注册表与已加载模型判定。seeder 的 huayan params 补充 onnx/size/urls，且对已存在模型幂等补齐 params（种子参数演进）。验证：funasr 注入 {sensevoice→iic/SenseVoiceSmall, paraformer→...}、piper 注入 {huayan→{onnx,size,urls:2}} 均成功。
- **云端厂商适配（通用框架完成，厂商协议待 API 规格）**：新增 `app/infrastructure/voice/provider/cloud_base.py` `CloudBase`（API Key 选取 `_resolve_key` 走 sys_voice_provider_key、认证头 `_auth_headers` 按 auth_type bearer/x-api-key/custom、HTTP `_post_json/_post_audio` httpx async+超时+错误转换）。`CloudAsrProvider`/`CloudTtsProvider` 改为继承 `ASRProvider/TTSProvider, CloudBase`，engine_status 返回引擎信息，具体厂商协议（流式 WebSocket/SK 原生签名/离线端点/音色映射）保留方法边界 NotImplementedError，**需厂商 API 规格后才能实现**（6 厂商 API 差异大且无密钥无法测试，凭记忆硬写会引入不可验证错误）。验证：lint 0、CloudBase 认证头 bearer/custom 构建正确、app.main 加载 OK。
- **下一步（待用户决策）**：① 提供云端厂商 API 规格（或选首批厂商如阿里云 DashScope）→ 实现厂商协议；② 语音功能回归测试（pytest/dehaze-test）；③ 测试库重建验证 schema（sys_voice_* 已入 config/sql/schema/）；④ Java/Go 对齐。

## 语音回归测试全部通过（2026-08-27）
- **dehaze-python 单测 53 项通过**：asr_service(10)+tts_service(30)+service_status(4)+hotword(9)。适配：asr_service/tts_service 按 05-python-test-rules 规范补 `engine_registry` 构造注入（默认 voice_engine_registry 单例），测试改为注入 fake Provider（FakeAsrProvider/FakeTtsProvider + FakeEngineRegistry）；piper_tts_engine 测试文件顶部 `configure_voices` 注入内置默认音色（huayan onnx/size/urls）。
- **dehaze-sdk-js voice 集成测试 35 项通过**：`pnpm test:python test/modules/voice/voice.test.ts`（BACKEND_URL=8991 连 Python 新代码，64s）。覆盖 ASR 流式会话/结果、TTS 多格式/缓存、音色列表、热词（用户/全局）、WebSocket 流式 ASR（真实 FunASR）、离线格式校验、服务状态。
- **端到端验证**：重启 Python(8991)+Java(8989) 后端加载新代码后，`/ready` 返回 db/redis/rabbitmq/elasticsearch/mongodb/minio/local_llm 全 UP + voice_asr/voice_tts offline（只读）。注意：voice.test.ts 默认连 8989(Java)，但语音引擎改造在 Python，须用 `pnpm test:python`（连 8991）才测到新代码。误启动的 Java 后端已停止。

## 新代码必要单测补充（2026-08-27，用户要求聚焦边界不做过拟合）
- **VoiceEngineRegistry**（`tests/service/test_voice_engine_registry.py`，5 项，全部通过）：给 registry 加构造注入 `repository`/`session_factory`/`engine_available`（默认模块单例，符合 05-python-test-rules），覆盖：①纯云端部署 default=local 且本地引擎不可用 → 抛 A0500（BUSINESS_ERROR，不降级，设计核心最易忽略）；②未配置默认引擎 → 抛业务异常；③local→LocalAsrProvider/LocalTtsProvider、cloud→CloudAsrProvider/CloudTtsProvider 实例化；④Provider 按 engine_type 缓存（二次解析不再查 repository）。
- **VoiceAdminService**（`tests/service/test_voice_admin_service.py`，3 项 requires_db，全部通过）：覆盖 ①is_default 每能力维度唯一（新建同 engine_type default 引擎清除旧 default）；②provider_code 删除后不可复用（软删记录仍占用联合唯一键 → DATA_EXISTS）；③删除引擎存在启用模型引用 → 拒绝（DATA_BIND_EXISTS）。
- 未做无用/1+1=2 测试。lint 0 错误。
- **registry 构造注入改造（用户规范：禁止 fallback 兜底）**：`VoiceEngineRegistry.__init__` 改为必填注入 `repository`/`session_factory`/`engine_available`，移除 `self._x or 模块单例` 的多路径兜底（易排查困难）；模块单例 `voice_engine_registry` 在模块末尾**显式注入真实依赖**（voice_provider_repository/get_db_session/_local_engine_available），测试独立构造桩。验证：lint 0、app.main 加载 OK、registry+admin 8 项测试全过。

## 语音引擎注册表基础层（Python 代码已实现，2026-08-27，分派 voice-entity-seeder）
- 实体：`app/models/entity/sys_voice_provider.py`(SysVoiceProvider, 软删)/`sys_voice_provider_key.py`(SysVoiceProviderKey, 无软删)/`sys_voice_model.py`(SysVoiceModel, 软删)，均已注册 entity/__init__.py
- 仓库：`app/repository/voice_provider_repository.py`(get_by_provider_code/list_enabled/get_default/paginate_providers)/`voice_provider_key_repository.py`(get_by_hash/list_enabled_by_provider)/`voice_model_repository.py`(get_by_model_and_provider include_deleted/list_enabled)
- 播种器：`app/infrastructure/voice/seeder.py` `ensure_local_engines(db)` 幂等播种 local asr(本地FunASR)+tts(本地Piper) provider + sensevoice/paraformer/huayan 模型；`app/lifecycle.py` 在 ensure_local_models 后同模式调用（try/except 仅告警）
- **DDL 已修正（team-lead 确认）**：`sys_voice_provider` 唯一索引由 `uk_provider_code`(provider_code 单列) 改为 `uk_provider_engine(provider_code, engine_type)` 联合唯一（同一厂商按能力注册多条，开发库 ALTER 已迁移，schema SQL 与文档同步）。据此在仓库新增 `get_by_provider_and_engine(db, provider_code, engine_type)`，`get_by_provider_code` 改 `scalars().first()`（单列非唯一避免 MultipleResultsFound）；seeder 改调联合唯一方法查重。

## 语音引擎管理端接口（Python 已实现，2026-08-27，voice-admin-api）
- 3 个新文件 + 1 处路由注册：
  - `app/models/schema/voice_admin.py`：Provider/Key/Model 的 Create/Update/Result 全套 schema（OrmResult snake→camel）
  - `app/service/voice/voice_admin_service.py`：`voice_admin_service` 单例，含 provider 查重(include_deleted 软删不可复用)/is_default 每 engine_type 唯一(设 default 清其他)/Key AES+hash 存储物理删除/Model model_id+provider_id 联合唯一软删复用保护
  - `app/router/voice_admin.py`：13 个端点，`router=APIRouter(prefix="/api/v1/voice")`，权限统一 `@require_permission("voice:engine:manage")`
  - `app/router/__init__.py`：voice 后注册 voice_admin_router
- 端点清单：GET/POST /providers、GET /providers/enabled、PUT/DELETE /providers/{id}、POST /providers/{id}/test-connection、GET/POST /providers/{id}/keys、PUT/DELETE /providers/{id}/keys/{keyId}、POST /models、PUT/DELETE /models/{modelId}
- 决策：Model 用主键 `id` 作路径参数（model_id 可跨 provider 重复，主键无歧义）；Key 走物理删除(对齐 ai_provider_key，实体无软删)；test-connection local 返回"本地引擎"/云端返回"待实现"占位不阻断。
- 验证：`app.main` 完整加载 OK，voice_admin router 13 routes 全部注册；lint 0 错误。已用 `.venv` python 跑 import 校验。**尚未与 Provider/Registry/Service(voice-provider-registry) 集成**。
- **待 team-lead 整合注意**：`SysVoiceProvider` DDL 唯一索引 uk_provider_code 单列全局唯一与 local asr/tts 双播种冲突（见上 voice-entity-seeder 阻断），我的 create_provider 用 get_by_provider_code 查重，若改联合唯一需同步调整。

## 语音 Provider 抽象 + VoiceEngineRegistry（已实现，2026-08-27，分派 voice-provider-registry）
- 目录：`app/infrastructure/voice/provider/`，文件 `base.py / local_asr.py / local_tts.py / cloud_asr.py / cloud_tts.py / __init__.py / registry.py`。
- `base.py`：`ASRProvider`（engine_type="asr"；recognize_stream→ASRStreamSession / recognize_offline / engine_status / register_hotwords）+ `ASRStreamSession`（send_audio/send_eos/recv_messages 协议，对齐 funasr_client 流式会话）+ `TTSProvider`（engine_type="tts"；synthesize(text,voice_id,speed)→bytes / engine_status）。
- `local_asr.py` `LocalAsrProvider(provider)`：懒加载并缓存 {model_type: 逻辑模型名}（sensevoice/paraformer），模型从 `voice_model_repository.list_enabled(db,"asr")` 解析，流式/离线透传 `funasr_client`，engine_status 透传 `funasr_engine.engine_status()`。
- `local_tts.py` `LocalTtsProvider(provider)`：默认音色从 `voice_model_repository.list_enabled(db,"tts")` 的 model_type=="voice" 解析（缺省回退 `VOICE_TTS_VOICE_ID`），synthesize 默认 mp3/24000Hz 走 `piper_tts_engine.run_in_executor`。
- `cloud_asr/cloud_tts.py`：占位（NotImplementedError，engine_status 返回 offline+remark 注明待实现）。
- `registry.py` `VoiceEngineRegistry`：单例 `voice_engine_registry`。`get_asr_provider()/get_tts_provider()` → `_resolve(engine_type)` 用 `get_db_session` 查 `voice_provider_repository.get_default(db,engine_type)`，Provider 实例按 engine_type 缓存于内存 dict；provider_code=='local' 则 Local*、否则 Cloud*；default 为 local 但本地依赖（funasr/piper）未安装→抛 BusinessException(ResultCode.BUSINESS_ERROR=A0500) 不降级；无默认引擎同样 A0500。
- 设计注意：Provider 构造函数只接收 provider 实体（非 db），模型解析方法内自行 `async with get_db_session()`，保证 Provider 可安全缓存（db 会话不跨请求）。仓库 `voice_provider_repository / voice_model_repository` 为延迟 import（另一成员已实现，未落盘时该模块仍可 import，运行才失败，整合后生效）。

