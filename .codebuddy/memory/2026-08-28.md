# 2026-08-28

## AI对话模块文档拆分（方案B：目录级重组）
- 用户确认后执行：`核心模块/AI对话/` 35 个平铺文档重组为功能域子目录，**不拆成多个模块**（各功能域共享同一推理运行时，非独立业务边界）
- 新结构：根目录保留 4 个总览/公共文档（`需求规格.md`、`API接口.md`、`测试用例.md`、`后端实现-架构与公共.md`）+ 9 个子目录：`会话与消息/`、`多步推理/`、`上下文管理/`、`智能调度/`、`能力扩展/`、`消息反馈/`、`智能体管理/`、`定时与兼容/`（含两篇后端：`后端实现-定时调度.md`、`后端实现-第三方兼容.md`，故保留后缀命名）、`页面设计/`（6 个前端实现文档）；其余 7 域内文档统一命名为 `需求规格.md`/`后端实现.md`/`测试用例.md`
- 全部用 `git mv` 保留历史；三轮脚本修正跨文档相对路径（共 28 文件）：①map 重写 ②按旧文件名 basename 匹配重写（分篇互链 `./需求规格-X.md`）③子目录内指向未移动文件/外层目录的深度修正（`./API接口.md`→`../`、`../../基础模块/`→`../../../` 等）
- 经验：按目录 mv 后需三轮链接修复——内链（指向被移动文件）、basename 匹配（防止移入子目录后 `./x.md` 解析错误）、深度修正（指向未移动文件和外层目录，按"从原根解析正确则重算"规则）；校验脚本需 unquote 处理 `%20`
- 顺带修复历史断链：`MCP能力网关/` 旧目录名引用（实际目录为 `核心模块/MCP/`，改了 AI对话-能力扩展 和 05-改造计划/远期改造计划总览 两处）
- 复验结果：AI对话相关断链 0；剩余 14 处为与本次无关的历史断链（05-改造计划/06-部署架构/01-产品设计/收藏管理/推荐管理/前端 README 绝对路径等，未处理）
- VitePress config.ts 无硬编码 AI对话 路径，无需改

## AI对话文档清理"正确的废话"（用户提出文档质量标准）
- 用户标准：**"不做什么"式表述是正确的废话**（不做的范围是无限的，说不完），除非该"不做"附带真实的决策理由/行为规则/替代方案指针，否则删除
- 已修 4 处：架构与公共 §7 L426；前端实现-公共 L9（删"不在本模块文档定义"）；会话与消息/后端 L245（删"不在要求范围"）；需求规格 L342（删"本文档不重复定义"）
- 追加（用户二次纠偏）：L426 改成正面表述"多模态上限由会员权益/配额体系承载"仍不对——**不是本模块职责的东西连提都不该提**（类比"系统管理由基础模块承载"同样是废话），已整行删除；跨域指针只在信息源唯一处留（需求规格配额节已指向 AI计费管理 §2.8）
- 文档写作准则沉淀：只写本模块拥有的东西及其设计依据；"不做X/由Y承载"式表述仅当 (a)X 常被误认为属于本模块 且 (b)有决策理由或行为规则，才允许出现
- 第二轮清理（同日）：删 API接口.md §2.4"迁移墓碑"节（接口已抽离至AI模型管理，归属信息在需求规格§1.6已有正式记载，墓碑属过程记录；删后编号 2.3→2.6 跳号系历史遗留，重编号会波及 7 处跨文档 §2.x 引用，暂不动）；删 上下文管理/后端实现 §8.1 流程图中 VIP 等级日限额枚举（5/10/20/50），改为运行时真实来源 `sys_member_benefit.multimodal_limit`——数值是计费/会员域配置，本模块只消费字段，复述数值制造双源同步隐患
- 保留判例（边界类内容有价值）：需求规格 §1.6 模块边界表（每行同时写"归属方+本模块侧职责"，是职责划分决策的正式记载处）；兼容 API 暴露/不暴露面表（API 契约定义）；"配额不足暂停执行提示升级"（本模块自身行为）；跨域指针只在信息源唯一处留一处
- 第三轮（用户令"修改所有提一嘴垃圾+解决遗留"）：①API接口.md 章节重编号 2.4-2.13 连续化（原 2.4 墓碑/2.5 空缺已清），7 处跨文档 §2.x 引用同步（SKILL管理→2.12、MCP管理→2.11、智能体管理前端→2.5-2.7、Agent评测×3→2.6、第三方兼容→2.7/2.8；语音交互文档的 §2.4/2.5 指向其自身 API接口.md 不受影响），全库验证无旧编号残留；②清过程记录式表述：Agent评测/测试用例"由原智能体管理评测用例迁移扩展而来"、智能体管理/后端"推理参数从环境变量迁移到数据库"、需求规格 §6"积分计量配额扣减由计费模块统一管理"（§1.6 已有归属记载）
- 第四轮（用户指出 AI知识库前端文档重复指针）：**"公共层指针只留两处"定式**——概述 1 次（含 scope 分工说明）+ 正文仅在"读者真正需要跳转且有 § 精确定位"处留 1 次；组件表/Store 表内已标"（公共）"的行不再重复指路；兄弟文档互指删除（公共文档 L7 已统一链向各端，端间互指是循环导航）。清理：AI知识库 用户端/管理端各删"公共组件职责Store数据流见公共文档"注+Store表内链接+兄弟互指；AI对话 页面设计同款（管理端-会话管理 L39 注/L50 表内/L5 兄弟互指，用户端 L5 兄弟互指/L62 表内）。保留：AI对话用户端 L51（消息渲染组件清单+ModelSelector 消费 models/enabled，表内无对应行，有独有信息）、各篇 §4/§4.2/§5 精确跳转
- 第五轮（用户问 MCP/API接口 L5 是否垃圾 + 全库补漏）：MCP L5 判定正当——同名异物消歧（网关 vs MCP Server 管理），符合豁免条件 (a)极易混淆（历史上真发生过目录名引错）(b)带边界信息，且信息源唯一在需求规格 §1.0，API接口仅一句话内联+单指针。全库补漏（03-模块设计全覆盖）：修 5 处——收藏管理/需求规格 L9 迁移墓碑改当前态（"三后端统一使用本模块收藏 API，Java 权威"）、套餐管理/需求规格 L3 与 算法选择/前端实现 L5 与 去雾处理/后端实现 L202 删"本文档不重复(定义)"尾巴、数据集管理/后端实现 删重复任务管理指针（360/380 二留一）；其余 40+ 处"由X承载/本模块不做"核对后均正当（带理由/行为约束/正典指针），典型正当判例：认证vs用户管理职责边界、订单vs积分账户分离、AI知识库"不监听算法变更"（push 集成方向决策）、算法选择"不感知上游是否为 LLM"（解耦设计）
- **工具教训**：同文件多个 replace_in_file 并行调用会写锁竞争且 last-writer-wins 丢更新（第 1 个编辑被第 2 个覆盖，均报成功）——同文件编辑必须逐个串行，跨文件才可并行
- 保留的"不做"类（有信息量）：兼容 API 不做独立推理链路（Adapter 决策）、deepagents 不提供须自建（框架边界）、用户消息不提供删除（产品规则+替代方案）、审计视角只读（安全边界）、不自建 checkpoint 表（架构取舍带理由）、不提供运行时配置（配置分层原则）

## AI对话新增两大功能域：可观测性 + Agent 评测（需求分析阶段，2026-08-28）
- **用户明确**：两者是不同方面，必须拆成两个独立子模块（职责单向依赖——评测消费可观测的轨迹数据）
- **调研结论（联网业界最佳实践）**：可观测性遵循 OTel GenAI 语义约定/三支柱（Trace/Metrics/Logs）、防 Agent 循环"蜻蜓翅膀"Span 树；评测采用三层体系（任务层回归/轨迹级过程评估/生产持续评测），确定性比对优先于 LLM 判断，评测集含 Bad Case 回流、结果可复现
- **当前阶段交付**（用户要求：需求分析阶段只做需求规格，不含技术选型/实现）：
  - `可观测性/需求规格.md`（F-M08-013，章节 2.12）：对话过程透明、上下文可观测、能力调用观测、资源消耗透视、性能异常监测、告警、审计留痕
  - `Agent评测/需求规格.md`（F-M08-014，章节 2.13）：评测集管理、评测执行、评分体系（结果/过程/效率/约束/安全）、评测报告、发布门禁、生产持续评估、反馈闭环
  - 主文档 `需求规格.md` §2 索引表新增两行；文档内"评价体系"概念用产品语言（明确比对/标准评估），未提 OTel/DeepEval/存储等技术选型
- 待用户审核后进入下一阶段（技术选型、后端/测试/前端文档、页面设计同步——既有会话管理页"链路追踪"、智能体管理页"评测与调试"浅层描述需收敛为引用）

## 两模块技术选型 + 后端/测试/前端实现文档 + 迁移收敛（2026-08-28 完成）
- **技术选型（写入各域 后端实现.md §1）**：
  - 可观测性：**自建**（不引入 Langfuse/Phoenix，复用 trace_id 日志体系 + Prometheus/Grafana/Alertmanager + MySQL 存储）；全量记录；**双粒度观测**（消息级 sys_ai_trace + 每次 LLM 调用 span 级 sys_ai_llm_call）；上下文生成时快照
  - Agent 评测：**自研 EvalService/EvalRunner**（不引入 DeepEval）；判分模型用本地 LLM（qwen3-0.6b，温度0）；XXL-Job 异步批跑；结果复用 sys_ai_agent_eval_run
- **关键设计（用户审查后补全）**：sys_ai_trace 含 first_token_ms/cached_tokens/llm_call_count；新增 **sys_ai_llm_call** 表（trace_id+seq 唯一、step_position 关联 thought、input_snapshot 按角色统计、output_snapshot 截断500字符、cached_tokens）；指标补 ai_token_cache_hit_ratio；上下文快照含"用户信息"构成项；工具区分定义 vs 结果
- **迁移收敛**：智能体管理（需求/后端/测试/前端）的评测内容→Agent评测，会话管理页链路追踪→可观测性，原位置全部改引用；评测接口归属 F-M08-014；主文档 §3 新增 3.3.5 可观测中心页/3.3.6 评测中心页；API 新增 §2.15 可观测性接口
- **新增文件**：config/sql/schema/sys_ai_trace.sql、sys_ai_llm_call.sql（对应数据库设计 §4.7 已同步）；两域各 4 文档（技术选型并入后端实现/后端实现/测试用例/前端实现）
- **遗留待办**：config/prometheus 告警规则与三端 /metrics 埋点、评测/可观测 SDK 测试落地属编码阶段；全库断链 0（AI对话相关），仅剩 14 处历史断链

## Agent 评测缺陷修复（2026-08-28，用户审查后确认修复）
- **审查发现 2 缺陷 + 7 不足**，已按建议补全四文档：
  - D1 移除"结果可复现 ≥98%"（与多步推理非确定性矛盾）→ **pass@k 体系**（门禁关键用例 pass@1，稳定性质控 pass@8）；results JSON 记 attempts/passed
  - D2/D3 判分治理：judge 上线前**一致性验证**（人工标注集一致率 ≥90% 才启用）、**偏见缓解**（顺序打乱/多模型交叉）、**定期校准**（人工复核 1% + 漂移超阈值暂停门禁）；judge 可选外部强模型缓解"小模型难评强模型"
  - 门禁**三档量化**：绝对分阈值 + 相对退化阈值（与上次回归对比下降 >5% 阻断）+ 高风险硬阻断
  - 新增能力：开放式任务评测（Task Success，支撑线上抽样）、失败快停、增量评测、断点续跑、评测 Token 预算上限（200000）、评测集过时清理/覆盖度审计、人工复核通道
  - 新增配置项：regression_threshold/pass_k/judge_consistency_threshold/judge_review_ratio/budget_tokens/fail_fast
- 测试用例新增 T-EVAL-017/025/045/046/047/064/065/066/067；单元测试补 test_eval_judge.py
- 无需改表结构（pass@k/进度存 results JSON）；AI对话断链 0

## 评测指标细化（2026-08-28，用户指定指标清单）
- 用户指定评测指标集：任务完成率、工具调用准确率、步骤正确性、幻觉、token 消耗、耗时、latency、死循环、主观评分
- 落地为**四组指标**（保持四维 score_summary 框架，results 存指标明细，表结构不变）：
  - 结果质量：任务完成率（封闭式预期比对/开放式推断目标）+ **幻觉检测**（新增：引用支撑校验+事实核查+判分模型）+ 主观评分
  - 过程合规：工具调用准确率（正确调用/总调用）+ 步骤正确性 + **死循环检测**（新增：步数超阈值或同工具名+同参数摘要重复 N 次）
  - 安全边界：泄露/注入/禁止行为
  - 效率：token 消耗/总耗时/latency/步数（不直接判死，超预算阈值判定）
- 判定组（结果/过程/安全）任一低分或高风险失败即样本失败；效率组作报告与成本观测
- 测试用例新增 T-EVAL-006/007/008/018/019；test_eval_runner.py 覆盖补幻觉/死循环/准确率；AI对话断链 0

## 跨功能域健壮性补全（2026-08-28，用户点名的缺失项）
- **审查结论**：用户点名的 9 类问题中 5 类已覆盖（无限循环/越权工具/数据泄露/恶意代码/上下文压缩/成本控制均在各分篇已有），4 类缺失已补齐：
  1. **目标偏移检测**（多步推理 §2.2.6.1 + 后端 §6.3.1 + T-RS-072~074）：目标基线 + 每 5 步校验（任务偏离/约束违背/范围放大）+ interrupt 三选（继续/修正/终止）+ 回退；与动态重规划区分（路径调整 vs 目标校验）
  2. **多 Agent 协作治理**（智能体管理 §2.11.4.1 + 后端 §5.3.1 + T-MF-153~156）：委派去重（指纹查重复用产物）、结果精简（产物引用防上下文浪费）、冲突消解（优先级仲裁）、职责边界、冗余巡检（消费可观测性）
  3. **注入内容安全校验**（上下文管理 §2.3.6 + 后端 §7.4.1 + T-CT-130~132）：记忆/RAG/工具结果三通道注入检测，命中只拦片段不中断推理
  4. **工具返回校验**（能力扩展 §2.6.10 + 后端 §2.3.1 + T-MF-095~097）：schema 格式校验/完整性/注入拦截/标准化错误回传，与连续失败保护衔接
- AI对话断链 0

## 上下文压缩重构（2026-08-28，用户质疑+联网调研后）
- 用户质疑 5 点：70% 前提不满足怎么办/压缩前撑爆/重要度判断/压缩程度/靠 LLM 自主判断是否合理
- **联网调研**（知乎 Working Memory 管理、Lost in the Middle、budgeted summarization 等）：Salience Scoring 决定价值、Eviction Policy 决定淘汰、Token Budget 划边界、Pinned Context 保护关键信息、软硬阈值、输出空间预留（输入+输出=窗口）
- 落地重构（需求 §2.3.4 + 后端 §4.1 + T-CT-013~017）：
  1. **校验对象改为组装后实际上下文 token**（系统提示+记忆+摘要+原文+工具+当前输入），非消息累计（原缺陷：实际输入可能已超限）
  2. **两层阈值**：软 70% 预算化摘要；硬 90% 强制驱逐安全阀；**输出预留**：输入 ≤ max_context − max_tokens 防长回复截断
  3. **显著性分级**（规则前置）：豁免 Pinned（约束/偏好/决策/数值/任务状态/增量指令）+可压缩+可驱逐；双通道防丢（关键信息先转记忆/任务状态）
  4. **预算化压缩**：summary ≤ max_context×15%，超预算分块再压
  5. **压缩不可逆原则**：优先丢可推断信息，不丢不可再生信息；覆盖度检查补录记忆
- AI对话断链 0

## 提示词优化闭环修正（2026-08-28，用户质疑后）
- 用户质疑：LLM 自动优化提示词不可靠 + 恶意反馈投毒风险（如对"拒绝生成黄内容/政治敏感"点踩会诱导削弱安全护栏）
- 修正为"**建议 + 人工审批**"流程（上下文管理 §2.3.3/后端 §3.3.4 + 消息反馈 §2.9.7/后端 §2.7）：
  1. LLM 仅基于**非安全类**负反馈（too_long/irrelevant 等）生成优化**建议**（差异说明），不直接修改
  2. 管理员人工审批后才应用到提示词模板（新会话生效，经评测门禁）
  3. **安全红线**：harmful 等安全类反馈不参与自动优化，直接安全告警人工介入（防恶意点踩诱导削弱安全护栏）
- 原则固化：安全/合规约束绝不交由用户反馈自动改写；AI对话断链 0

## 上下文管理域审查补全（2026-08-28，用户系统性提问后）
- 用户提问：上下文遗漏情况/过度思考/组装撑爆/出错/提示词缺陷/运维管理 + 智能体配置化 + 会话状态结束
- 审查结论：智能体系统提示词/工具配置化已完备（§2.11.2 三级优先级），会话状态机已完备；补 7 项：
  - **G1 组装异常兜底**（上下文管理 §2.3.3 + 后端 §7.4.2 + T-CT-018）：任一组装环节失败降级"系统提示+最近 N 轮原文"，不阻塞推理
  - **G2 摘要异步化**（§2.3.4 + 后端 §4.1 + T-CT-019）：摘要后台异步执行，当前轮先用强制驱逐兜底，不拖慢首 Token
  - **G3 思考长度预算+过度思考检测**（多步推理 §2.2.6.2 + 后端 §6.2.1 + T-RS-075/076）：max_thinking_tokens 分模型、思考占比>80% 截断、thinking 计入输出预留、独立统计 thinking_tokens
  - **G4 提示词优化仅新会话生效**（§2.3.3 + 后端 §3.3.4）：优化落点从运行时会话 system_prompt 改为模板，避免中途变行为
  - **G5 提示词模板库**（§2.3.3 + 后端 §3.3.5 + 智能体管理系统提示词行）：版本化/回滚/评测门禁/灰度，运维可管
  - **G7 会话无结束状态澄清**（会话与消息 §2.1.4）：AI 对话持续容器语义，归档即事实结束
  - 智能体配置化/会话状态机确认已覆盖无需补
- AI对话断链 0

- 审查结论：会话与消息覆盖度最高（生命周期/消息树/幂等/并发/断线重连/审计均闭环），补 5 项后**用户否决 G1 消息撤回**并移除（理由：AI 对话中助手已回复问题再撤回会破坏回复对应关系，非即时通讯业务，且与编辑重发诉求重叠）：
  - **G1 消息撤回（已移除）**：需求/后端/测试/API 四处全删，后端 §5.5 恢复为原中断恢复编号；多模态输入"附件可单独撤回"指发送前移除附件，保留
  - **G2 附件限制与安全**（需求 §2.1.7 + 后端 §8.3 + T-CV-124~126）：单附件 ≤20MB、单消息 ≤10 个、magic 字节校验 + 恶意文件扫描
  - **G3 消息分页**（需求 §2.1.5 + 后端 §5.3 + API `before` 游标 + T-CV-127~128）：倒序增量加载，与上下文组装上限（20 轮/200 跳）独立
  - **G4 输入草稿**（需求 §2.1.6 + 前端实现-用户端.md）：localStorage 按会话维度、发送清空、不跨设备同步
  - **G5 搜索命中定位**（需求 §2.1.4 + 后端 §3.3 + API `matched_message_id` + T-CV-129）：ES 命中消息 ID 随会话列表返回，配合分页跳转
- AI对话断链 0

## AI对话文档治理（2026-08-28，用户点名"重复/冗余/过度引用"）
- 用户批评：公共文档兜圈子引用（§1.2 与正文重复、"83%-99%"出现3次）、空壳转发节（4.6/4.7 整节只有"详见"）、改一处引用要改十几处
- **《后端实现-架构与公共.md》修复**（12 项）：①§2.2 职责表并入 §2.1 注释后删除 ②§3 删外部模块表行+逐字重复导语（provider/model/billing 改归属说明） ③LlmClient 删 type 取值表+FC 解析代码块 ④复杂度评估小节（规则示例）删，改一句+引用多步推理 §3.1 ⑤§4.3 删"理论依据"叙述 ⑥§4.5→**§4.4 重编号** ⑦删空壳节 4.6/4.7 ⑧ai:model:list TTL 30分钟→**1小时**（代码 CACHE_TTL_HOUR 为准） ⑨删代码不存在的 `ai:model:{modelId}` 缓存行 ⑩§5.3 删 checkpoint 重复行 ⑪§6.9 删明文保护重复行 ⑫删"业界佐证"宣传句
- **功能域文档同步**：react_loop/execute_tools 过时残留 4 文档 13 处→deepagents 内部循环（多步推理 §3.2 图/§4.1 图/§5.1/§6.3/658 行、能力扩展 2 处、智能调度 2 处）；checkpoint 描述重复精简（多步推理 §2.2、智能体管理 §3.5）；能力扩展 §4.5 引用同步为 §4.4；状态标记清理（第三方兼容删 §4"落地状态"整章、定时调度/需求规格/Agent评测前后端/智能体管理测试用例删"已落地/原为/迁移至"）
- **设计-代码不一致发现（待用户决策）**：`MySQLCheckpointSaver` **三端代码均未实现**（dehaze-python 仅 RedisSaver 自定义实现），但公共文档 §4.3、多步推理 §2.2、智能体管理 §3.5、数据库设计 §4.7 均声称存在 Redis+MySQL 双层——若持久化层仍规划中需明确，已废弃需删；本次未动该设计描述
- 复核：react_loop 残留仅剩"取舍对比/替代说明"（正确），lint 全绿；会话与消息 §4.4/4.5 SSE 时序契约（含 410/status 流转）与公共 §4.1 能力清单层次不同，保留未删

## 配置项治理（2026-08-28，用户点名"过度配置化"）
- **判定标准固化**（已写入 MEMORY.md）：仅"管理员/运营有明确修改动机"的参数可配置（sys_dict 或应用配置/环境变量），纯技术参数（缓存TTL/分页/批次/轮询/锁定/心跳/cron/线程池）一律代码常量；个性化由实体级配置覆盖，不设第三层全局字典默认
- **AI对话**：架构与公共 §7 26 项精简为 2 项应用配置（AI_MESSAGE_MAX_LENGTH/AI_MEMORY_MAX_COUNT，实为 config.py 环境变量）+ 常量表；**`ai_reasoning_defaults` 字典整体删除**（9 项推理默认值改为 `agent_config_resolver.REASONING_DEFAULTS` 代码常量），`ai_guardrail_defaults` 保留（安全合规开关有运维场景）；缓存键改名 `ai:config:guardrail_defaults`；同步 python（resolver/dict_service/lifecycle/deep_agent_builder/config 删 AI_REASONING_TOOL_TIMEOUT/RETRY_MAX 死字段）+ SQL 种子 + seed contract/resolver/builder 测试；deep_agent_builder `_make_ctx` 改为严格 `config["tool_timeout"]`（发布时 resolved_config 保证完整，4 个测试夹具补 `**REASONING_DEFAULTS` 修复）
- **三端"常量→配置"升级**（收藏容量 + 成长值规则字典化）：新增字典类型 `favorite_capacity`（default/vip1/vip2/svip=200/500/1000/3000，原三端实现仅两档，升级为四档 level_0..3 键映射）与 `member_growth_rules`（sign_in_value=3/sign_in_streak_bonus=20/rating_growth_value=5/rating_growth_daily_limit=5）；SQL 种子 id=6/7 类型 + id=27-34 项；python（dict_service 通用化 `ensure_system_dict_defaults` + `get_dict_int` 缓存读取/字典增删改即时失效；favorite/growth/feedback 消费方删旧常量）、java（SysDictService.getIntValue + Favorite/Member/Rating 三服务）、go（dict.GetIntValue helper + 三服务）同步；缺键 warn+回退设计默认值，不保留旧常量转发。AI_CONSUME_*（AI 对话激励）保持常量
- **全模块审计**：约 26 个模块"配置项"章节改造——技术参数降常量（部门/字典/角色/任务/数据集/效果对比/算法选择/去雾/智能调度 batch 等）、已实现 env 标注"应用配置"（订单续费/语音 VOICE_*/计费 AI_BILLING_*/KB KB_*）、图像输入配额改标注由会员权益体系（monthlyDehazeQuota/history_retention 保留条数 100/500/2000/10000）承载
- **遗留**：开发库 dehaze 需增量执行 sys_dict_type/sys_dict 新增种子（或依赖启动幂等补齐）；KB 后端实现-架构与公共.md 中途出现内容还原迹象（疑似外部编辑），已基于最新状态重写 §6（应用配置+库级可覆盖列）

## 核心模块其余 7 模块文档治理（2026-08-28，用户要求扩展到去雾处理/算法管理/算法选择/图像输入/效果对比/数据集管理/MCP）
- 模式：3 个 code-explorer 并行只读审查（产出 ~130 条清单）→ 3 个 code agent 并行按统一规则修复（A 状态标记/B 冗余引用/C 文档内重复/D 跨文档重复按归属收敛）→ 主 agent 核实矛盾项以代码为准修正
- **修复量约 100 处**：状态标记清零（"已落地/待补齐/本次改造/已迁移至/技术债/前端未接入"等）、冗余引用与锚点错误修正、§5 边界汇总表改用例编号引用、组件职责表收敛 §2.2、nginx-dataset/事件机制等三写收敛、算法选择测试用例章节重排（原 §1→§3 跳号）
- **矛盾项以代码为准的修正**（8 项全核实）：
  1. 去雾需求规格"前端模拟进度"是**过时描述**——三端代码（Java DefaultProgressCallback/Go task_service/Python task_state）均已实现 WebSocket 真实 5 阶段推送，02-架构 08 文档佐证，已改真实推送口径
  2. 图像输入 API 接口 `image:upload` 是**错的**——三端 dataset-items/upload、item-files 均为 `sys:dataset:edit`，前端写对了 API 写错了
  3. 算法管理需求规格预测缓存"LRU"错——代码 `PREDICTION_CACHE_TTL=24h`（评估缓存才是 LRU）
  4. 算法选择后端 `algo:recommend:{userId}` 缓存行是**迁移残留**（代码无此 key，推荐归推荐管理模块）——已删
  5. T-AS-060 期望 algorithmScore/predictedCostMs 与代码不符——recommend 实际返回 algorithmId/Name/matchScore/reason/estimatedTime(null)，已修用例与自动化状态说明
  6. T-AM-061 monitor/stats 是独立按天聚合报表（get_monitor_stats_report），非"复用监控接口"——已修
  7. 算法管理测试 §6.1 P99<10s vs 后端 §11 200ms 是口径差异（异步任务完整推理 vs 接口提交），加口径注释
  8. 去雾测试"不包含收藏"vs §4.6 收藏用例：L50 改为"用例见 §4.6"消歧义
- 复验：核心模块状态标记扫描清零；引用密度降至合理水平（每文件 1-12 处必要深链）

## MCP 双通道概念治理（2026-08-28，用户质疑"MCP=网关实现"误读后）
- **用户质疑**：架构与公共 L21"通过 MCP 网关动态发现/新增 API 自动同步"让人分不清通用 MCP 协议与本项目自建网关。核实结论：代码实际存在两条 MCP 通道，文档区分了但概念级表述易误读
- **两条通道正式术语**（单一信息源落在 `核心模块/MCP/需求规格.md` §1.0 概念界定）：
  - **内部 MCP 能力网关**（dehaze-mcp-gateway，自建 MCP Server）：系统能力出口，读取后端 OpenAPI（启动时一次性加载，**无定时同步**，新增 API 需重启网关生效），3 个元 tool（lookup_tool/lookup_tool_param_schema/execute_tool）；dehaze-python 经 `McpGatewayClient` 作 MCP 客户端连接
  - **MCP Server 管理**（外部能力进口，AI 对话能力扩展域）：sys_ai_mcp_server/namespace/tool/call 四表 + AiMcpServerService/McpManageService，任意符合 MCP 协议的第三方服务注册接入（stdio/streamable-http/sse、凭据 AES、健康探测、tools/list 拉取）；`McpToolFetcher` 拉取清单
- **修正点**：MCP/需求规格 §1.0 新增概念界定节 + §1.1"不是什么"改为"不是 API 网关"（消除与模块名自相矛盾）+ §1.2 零配置→零代码接入；MCP/后端实现、API接口 概述加定位句；架构与公共 L21/L31/L97（依赖模块加 McpToolFetcher 行）；能力扩展后端"定时同步"错误修正（网关无定时刷新）+ 自动发现澄清为重启生效；术语表 06 落三行；产品概述 MCP 管理行对齐（删"手动刷新发现"）；09-AI能力链路统一称"内部 MCP 能力网关"+加概念界定引用；04-API规范 L104 修正 `/api/v1/mcp` 为独立服务 `127.0.0.1:8082/mcp`
- **发现的设计-代码不一致（待用户决策）**：能力扩展 §5 与需求规格 §2.6.13 声称"外部 Server 工具与内部网关工具统一纳入命名空间预筛选与工具装载"，但代码 `DehazeToolsBuilder` 仅装载内部网关元工具、`mcp_namespace_prefilter.py` 无外部 server 逻辑——外部工具运行时装载未实现（仅管理域 CRUD/拉取/审计已有）

## 基础模块 19 模块文档清理（2026-08-28，用户批评"重复设计/冗余/过度引用"）
- **用户诉求**：基础模块大量重复设计、冗余、能两句说清的绕弯子；大量"详见/参见"违反非必要不引用，改一处要改十几处引用。要求用 free code agent 逐个模块处理，每模块一个 agent，≥10 个
- **执行**：19 个模块全部覆盖（每个模块 1 个 free code agent，任务书含 03-doc-rules.md 要点：非必要不引用/同质化冗余过滤/文档类型边界/状态标记禁令）。结果：100 文件 +2768/-6914（净删约 4100 行）；链接全量校验 200 个相对链接有效（`%20` 为 vitepress 合法 URL 编码）；lint 0 错误；一级标题与章节编号保持不动（避免破坏外部链接）
- **典型处理**：需求规格删"确认提示文案"重复两遍/技术实现（BCrypt、Redis Key、LIKE/IN/BETWEEN、错误码、接口路径）改为产品表述；后端实现删与需求规格逐条重复的章节、Bug 修复/演进记录（"旧 XX 已删除""原流程已废弃"）；测试用例改引用功能点编号；通用规范引用（"详见 02-UI-UX 设计规范"）全删；锦上添花引用（"参见 需求规格.md"）删除
- **free code agent 稳定性教训**：并发 9 个时 4 个失败（not found），且部分 agent 只探索不落盘即退出（AI模型管理/反馈评价/推荐管理各重试 2-3 次才成功）；解决：单独派发、任务书强调"必须直接进入编辑阶段并落盘"、给足 max_turns 150
- **遗留（跨模块过度引用，未越界修改，需后续处理）**：任务管理/后端实现.md 被 11 处引用（导入导出框架同质化，建议独立成文统一收敛）；AI对话 多处重复描述认证/知识库/AI模型管理事实；02-系统架构/01-总体架构设计.md、04-API规范.md 反向引用模块文档；05-改造计划/远期改造计划总览.md 以"新增"口吻重列已落地能力；AI模型管理/前端实现-模型管理.md 曾错误指向 AI对话 目录（AI知识库 agent 修复了其自身指向错误，AI模型管理侧需核对）
- **各模块疑点（供用户决策）**：密码强度口径漂移（文档 8-20 vs Java 6-20 vs Python 8）；菜单类型枚举（Java=1菜单/2目录/3外链/4按钮 vs 文档 1目录/2菜单）；错误码双轨（API 定义 B03xx vs Java 实际 A0500）；权限标识缺口（前端用 sys:menu:list/sys:dept:list/sys:role:list 未登记 API 契约）；T-DPT-013 自相矛盾用例；章节编号跳号系历史遗留未重排
