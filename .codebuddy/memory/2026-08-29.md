# 2026-08-29

## front-voice：语音引擎管理前端对接（F-VS-005）
- SDK：dehaze-sdk-js/src/api/voice/{index.ts,model.ts} 补齐 providers CRUD/enabled/test-connection/keys CRUD/models CRUD 方法与 VoiceProvider*/VoiceProviderKey*/VoiceModel* 类型（camelCase 对齐后端 voice_admin schema）；index.ts 顶层导出已通过 `export * from "./src/api/voice/model"` 自动覆盖无需改动
- 前端：新建 dehaze-front-vue/src/views/ai-voice/components/VoiceEnginePanel.vue（引擎表格+CRUD 弹窗+密钥抽屉+模型/音色抽屉，数据组件内直调 SDK）；ai-voice/list/index.vue 三 tab 按权限 v-if 显示（voice:hotword:edit / voice:service:monitor / voice:engine:manage），移除"后端规划中"占位
- 权限种子：config/sql/data/sys_menu.sql 增 id=160 '引擎管理' voice:engine:manage（parent 141）；sys_role_menu.sql 同步给角色 2 绑定 (2,160)；mock/menu.mock.ts 三处（routes/menus/options）补语音菜单结构
- mock/voice.mock.ts 补引擎注册表 mock（local asr/tts is_default=1 种子 + aliyun），含 enabled 路由置于 :id 前、删除引擎时启用模型引用校验、设默认清同类型其他默认
- 发现并已报告 team-lead 的文档-实现矛盾：API接口.md §2.5 有 GET /voice/models 但 dehaze-python voice_admin.py 未实现该列表端点（SDK 按文档契约实现了 listVoiceModels，mock 支持）
- 死代码 VoicePlayback.vue/TtsClient.vue 已确认无外部引用，删除需审批超时未执行，待人工 rm
- 自检：SDK tsc/eslint 无新增错误（仅历史 recommendation 测试 1 错）；front-vue eslint voice 相关文件 0 错；vue-tsc voice 相关 0 错（Dataset/originUrl/usedQuota 等均为存量错误）

## obs-py-a-core：AI 可观测性 A 类后端改造（审计完整还原 agent 交互）
- A1 子 Agent 归属：sys_ai_agent_thought 实体补 agent_code/is_subagent；实测 langgraph v2 事件 ns 结构——**主图 ns=()（空元组），子图 ns 非空、段格式"父图节点名:task_id"（嵌套多段）**，判定"ns 非空即子 Agent"、agent_code 取末段 split(":")[0]（任务书"主图 ns=[图名]"描述与实测不符，已按实测实现）；AgentThoughtResult 透出 agent_code/is_subagent（snake_case→camelCase 自动）
- A2：sys_ai_trace 新增 error_detail json（context_snapshot 后）；settle/finalize_unsettled 加 error_detail 参数；run/_run_direct/resume except 处传 {message[:500], stack[:4000]}；**中断收尾不传 error_detail（无异常对象，error_type 已记中断类型）**
- A3：TraceCollector.record_event(**kwargs)（未结算才写 context_events）；护栏 _log_hit + pii_mask、record_plan（plan_summary json 截断 2000）、resume 决策事件 {event:"resume", interrupt_type, decision, from_trace_id} 埋点；resume 重构——start 采集器提前到 interrupt 检查后（confirm 处理异常也能 finalize_unsettled），agent_code/model 在会话加载后回填 collector 属性，from_trace_id 经 ai_trace_repository.get_latest_by_message_and_status(status=3)
- A4：TraceDetailResult 新增 billing/artifacts（TraceBillingItem/TraceArtifactItem）；billing 优先 request_id=trace_id（ai_billing_repository 新增 list_by_request_id），无命中回退 message_id；artifacts 按 message_id
- 测试：test_trace_collector 3 新增、converter 子 Agent 归属 3 新增、observability billing/artifacts 2 新增；**教训：无 db fixture 的用例不得触发 settle()（get_db_session 走真实引擎直接 commit 泄漏测试库，污染后续 requires_db 用例），改手动置 _settled=True**；全量 pytest 1787/0/0
- 开发库迁移 SQL（待执行）：`ALTER TABLE sys_ai_trace ADD COLUMN error_detail json NULL COMMENT '异常详情(消息+堆栈截断,失败/中断时填充)' AFTER context_snapshot;`（sys_ai_agent_thought 两字段 schema SQL 原本就有）
- 文档待同步（obs-doc-full 已通知）：可观测性 后端实现.md/API接口.md/数据库设计 sys_ai_trace.error_detail、详情 billing/artifacts、thought agent_code 归属、resume 采集链路

## obs-doc-b：AI 可观测性 B 类改造文档同步（2026-08-29）
- 五份文档已按 team-lead 契约更新：可观测性/后端实现.md（§1.3 两行补 B 类说明、§2.1.1 trace_type 行、§2.1.2 attempts 行、补充说明"调用粒度"改为 attempts 还原物理尝试、§2.3 采集要点补物理尝试明细行、新增 §2.3.1 旁路调用采集小节、§2.6 补旁路 trace 可见性段）、前端实现.md（§3.1 过程链下钻 + §3.2 TraceDetailDrawer 补 attempts 回放与 traceType 标注）、测试用例.md（测试目标补一项；T-OBS-094~098：attempts 采集/熔断跳过/旁路独立 trace/旁路异常不污染主链路+ContextVar 恢复/旁路详情可查）、API接口.md §2.13（traces 补 traceType、详情补 attempts 字段结构）、02-系统架构/03-数据库设计.md（sys_ai_trace/sys_ai_llm_call 概述行补 trace_type/attempts）
- 落笔时 dehaze-python 代码尚未含 bypass_span/trace_type/attempts/observe_attempt（B 类后端由 obs-py-b 并行进行中），文档按契约落笔，待代码落地后如有出入需回调

## obs-vue-b：AI 可观测中心 B 类审计展示前端补全（dehaze-front-vue，2026-08-29）
- format.ts 新增 ATTEMPT_STATUS_META（1 成功/2 失败/3 跳过）、TRACE_TYPE_META（summary/memory_extraction/suggestion/step_summary 中文标签）+ traceTypeMeta() 回退函数（conversation 或缺省返回 undefined 不渲染，未知类型显示原值）
- TraceSearchTable：Trace ID 列旁按 traceType 显示类型标签（插槽 row 无类型，访问不触发 TS 报错）
- TraceDetailDrawer：状态标签旁 traceType 类型标签；LLM 调用回放 timeline-item 内 attempts 折叠区（物理调用尝试 N 次：provider_id/key_id/model/状态标签/error_code/latency_ms，复用 .content-collapse/.msg-list/el-tag 样式）
- 依赖协调：obs-sdk-b 未及时落地时，已在 dehaze-sdk-js/src/api/ai-observability/model.ts 的 AiObservabilityTraceItem 补 `traceType?: string`（attempts 由 obs-sdk-b 已加好），已通知 obs-sdk-b 避免重复定义
- 验证：pnpm build（SDK）成功；vue-tsc ai-observability 零错误；read_lints 0 诊断。旁路 trace（messageId=null、无 thoughts/billing/artifacts）依赖现有空态与 `?.` 防护，无新改动

## 语音交互引擎注册表重构（voice-refactor 团队收尾，2026-08-29）
- 背景：语音交互模块文档重构为引擎注册表架构（F-VS-005），代码侧对齐完成（dehaze-python + dehaze-front-vue）
- dehaze-python（python-voice + 主 agent 收尾）：
  1. VoiceEngineRegistry 默认引擎解析改 Redis 缓存 `voice:engine:{asr|tts}`（JSON provider_id/provider_code，TTL 60s），命中复用内存 Provider 实例；Redis 异常静默降级直查库；VoiceAdminService 变更 is_default/status/删除时 invalidate_default_cache 即时生效（对齐后端实现 §2.4）
  2. voice_service_status 按注册表默认引擎上报：local→本地引擎状态（不变）；cloud→health_check_enabled=0 报 online、否则 offline（含 `voice:provider:{id}:circuit_open` 熔断分支占位，厂商协议未接入恒 offline）
  3. test_connection 采方案B：cloud 返回"云端厂商协议未接入，跳过连通性测试" connected=None（不做无意义 TCP 探测）
  4. 主 agent 补 `GET /api/v1/voice/models`（repository.list_by_engine_type + service.list_models + router；SDK 契约=数组+engine_type 筛选），文档-实现矛盾解决
  - 测试：`pytest tests -k voice` = 76 passed
- dehaze-front-vue（front-voice + 主 agent 收尾）：
  1. SDK VoiceAPI 补 15 方法（providers/enabled/CRUD/test-connection/keys CRUD/models CRUD）+ VoiceProvider*/Key*/Model* 类型（camelCase 对齐后端 schema）
  2. 新建 VoiceEnginePanel.vue（引擎表格/CRUD 弹窗/密钥抽屉/模型音色抽屉）；ai-voice/list 三 tab 按权限 v-if（hotword/service/engine）
  3. sys_menu.sql 增 id=160 `voice:engine:manage`（parent 141）+ sys_role_menu (2,160)；mock/menu.mock.ts + voice.mock.ts 同步
  4. 死代码清理：删 VoicePlayback.vue/TtsClient.vue（零引用确认）；语音交互/前端实现.md §2 组件树改 voiceStore.playSpeech（播报无独立组件）
- 边界：云端厂商协议适配仍占位（CloudAsr/CloudTts NotImplementedError，厂商未定）；Java/Go 语音走 Python 接口（设计 §8）未改
- 待用户决策：①语音回复自动播报开关接线（ttsPreference.enabled 仅存 localStorage 未驱动 AI 对话自动朗读，跨 chat store 联动）②云端"健康"语义与熔断分支当前不可观测（协议未接入）

## 语音回复自动播报开关接线（用户确认纳入下一步，2026-08-29）
- 用户端 chat store（dehaze-front-vue/src/store/modules/chat.ts）实现：
  1. 新增 `toSpeechText(markdown)`：markdown→朗读纯文本（代码块/行内码/图片/链接/标题/加粗/斜体/列表/表格符号剥离 + 空白压缩），手动朗读与自动播报共用
  2. 新增 `autoSpeak(message)`：`scope==="self"`（管理端审计不播报）且 `ttsPreference.enabled===true` 时，对 `toSpeechText(content)` 非空则 `voiceStore.playSpeech(text).catch(静默降级)`（对齐 §3.2.4）
  3. 触发锚点：buildHandlers.onEnd 中 `msg.status===2`（正常完成，排除 canceled/error/content_filter/中断挂起）；regenerate/edit/resume 完成同样生效
  4. `speakMessage`（手动朗读）改用 `toSpeechText`，与自动播报行为一致
- 文档同步：语音交互/前端实现.md §5.3 数据流加"自动播报"行、§6 交互决策加"播报开关驱动自动朗读"、§6 首行过时 VoicePlayback 表述改 voiceStore.playSpeech
- 验证：vue-tsc chat.ts/voice.ts 0 错；文档无 VoicePlayback/TtsClient 残留
- 产品行为：开关开启后每次助手回复完成自动朗读；用户可手动停止（playbackState 单实例）或关闭开关

## AI 可观测性审计完整还原改造（obs-audit-full 团队两波完成，2026-08-29）
- 起因演进：①用户发现审计详情看不到系统提示/用户输入/中间步骤/工具返回（仅聚合统计）→ ②要求覆盖"整个会话方方面面"（摘要/长期记忆/工具/MCP/每轮输入原文）→ ③要求还原"agent 交互所有看得见看不见的东西"
- **第一波（输入侧采集增强）**：trace_collector 新增常量 MESSAGE_CONTENT_MAX_CHARS=2000 / MEMORY_CONTENT_MAX_CHARS=1000 / TOOL_DESC_MAX_CHARS=500；record_context 的 summary 补 content（摘要原文限 5000）、memory 补 items（记忆原文清单）；_build_input_snapshot 的 messages 补 items（每轮发给模型的完整消息原文）、tools（工具定义清单）；SDK/Vue/文档同步
- **A 类（agent 交互还原）**：thought 子 Agent 归属（实体补 agent_code/is_subagent + sse_event_converter 解析 langgraph v2 事件 ns：**ns 非空即子 Agent，agent_code 取末段 split(":")[0]**，实测主图 ns=()）；sys_ai_trace 新增 error_detail json（message[:500]/stack[:4000]）；TraceCollector.record_event 扩展 context_events（guardrail/plan/resume 事件，resume 独立采集器+from_trace_id 关联中断链）；详情关联 billing（request_id=trace_id 优先、message_id 回退）+ artifacts
- 验证：pytest 全量 1787/0/0；SDK build+61/61；Vue vue-tsc ai-observability 零错误；跨端契约一致（快照 JSON 键 snake_case、顶层 camelCase 由 OrmResult 转换）
- **开发库迁移 SQL（待用户执行）**：`ALTER TABLE sys_ai_trace ADD COLUMN error_detail json NULL COMMENT '异常详情(消息+堆栈截断,失败/中断时填充)' AFTER context_snapshot;`
- **遗留（B 类，需用户决策）**：①供应商物理路由/重试明细（sys_ai_llm_call 无 provider_id/key_id，物理重试仅 Redis/日志）②旁路 LLM 调用（摘要/记忆提取/标题/建议被排除在 trace 外，消耗 token 无明细）③存量 trace 无法回溯新采集（护栏/计划/中断决策/输入原文仅对未来生效）
- 经验沉淀：①**OrmResult 只转顶层字段，JSON 值原样透传 snake_case**——快照内部键前端/SDK 必须用 snake_case（曾误用 systemContent 取不到值）②团队并行时 python 共享文件（trace_collector.py）会被多成员同时编辑覆盖，需主 agent 整合时逐文件核对 ③langgraph 事件 ns 结构与文档描述不符时以实测为准

## 开发库迁移脚本 + 500 修复（2026-08-29）
- 现象：前端访问 `/api/v1/ai/observability/traces` 返回 500 C0300（数据库操作失败）——**开发库 dehaze 未执行可观测性改造新增列的迁移**，代码查询引用不存在的列
- 新增幂等迁移脚本 `dehaze-test/scripts/migrate_observability.py`（复用 utils.mysql，SHOW COLUMNS 检查缺列才 ALTER，可重复执行）：
  - `sys_ai_trace.error_detail`（A 类异常堆栈）、`sys_ai_trace.trace_type`（B 类过程链类型，default 'conversation'）、`sys_ai_llm_call.attempts`（B 类物理调用尝试明细）
  - `sys_ai_agent_thought.agent_code/is_subagent` 开发库原本就有（schema 早有），跳过
- 迁移后验证：`GET /traces` 00000 total=29；详情接口 billing/llmCalls 正常返回，存量 traceType=null（前端 `?.` 防护）
- 教训：**后端改 SQL schema + 实体后，开发库必须同步迁移，否则线上 500**；pytest 测试库由 conftest 自动重建不受影响。B 类前端/SDK/文档已完成，后端 obs-py-b 落地中

## B 类改造完成（obs-b-class 团队，2026-08-29）——可观测性审计盲区全部补齐
- **B1 供应商物理路由/重试明细**：sys_ai_llm_call 新增 `attempts` JSON；LlmCallRecord.observe_attempt 逐 Key 采集（status 1成功/2失败/3熔断跳过）；llm_client 把 call 透传 _call_route/_stream_with_key_retry，成功/失败/no_key/circuit_open 处记录 provider_id/key_id/model/error_code/latency_ms
- **B2 旁路 LLM 调用独立过程链**：sys_ai_trace 新增 `trace_type`（conversation/summary/memory_extraction/suggestion/step_summary）；trace_collector 新增 `bypass_span` asynccontextmanager（进入保存 prev_token 覆盖、异常 finalize_unsettled 后 re-raise 保持原兜底、退出 reset 恢复外层采集器）；四处接入——summary_service.maybe_compress（message_id=None）、memory_extraction（新增 conversation_id 参数，agent_hooks 传）、suggestion_service（新增 conversation_id/message_id/user_id）、step_summarizer（新增 conversation_id/message_id）
- 主 agent 整合补：**TraceItem 加 trace_type 字段透出 DTO**（obs-py-b 遗留项，SDK/Vue 依赖 traceType）
- 验证：pytest 71 passed（B 类核心）；SDK build + 61/61；Vue vue-tsc ai-observability 零错误；接口 traceType=conversation 正常透出
- 关键坑：**非 db fixture 单测接入 bypass_span 后 settle 走真实 get_db_session 直接 commit 泄漏测试库**，污染 requires_db 统计用例（需挂 db fixture 走同源事务回滚）；团队并行编辑热点文件（trace_collector.py）有丢失竞态，需整合时逐文件核对
- 至此可观测性审计能力完整：输入侧（系统提示/摘要/记忆/每轮输入/工具定义）+ 推理过程（步骤/子Agent/护栏/计划/中断决策）+ 输出（消息/工具调用）+ 路由重试明细 + 旁路调用 + 计费/产物 + 异常堆栈，全部可追溯


## 本地模型上下文窗口调整 + 上下文管理审查（2026-08-29）
### 模型真实能力核实
- 用户质疑数据库 max_context_tokens 是否编造：实读 GGUF 元数据 `qwen3.context_length = 40960`（模型真实 40K）；数据库 8192 是对齐运行时 `LOCAL_LLM_CTX_SIZE=8192` 的人为保守配置（CPU 推理 + KV cache 内存）
- **已按用户选择 B 调整到 16384**：`config.py` LOCAL_LLM_CTX_SIZE=16384、`config/sql/data/sys_ai_model.sql` qwen3-0.6b max_context_tokens=16384、运行时 DB UPDATE、重启 local_llm_server 验证
- token 截断预算自动适配：16384-2048-200 ≈ 14136（`_run_direct` 从模型配置动态计算）

### 上下文管理审查发现的漏洞（部分已修/标记）
1. ✅ **已修（我引入）**：`_run_direct` 传完整历史后无 token 截断保护 → 新增预算截断（优先裁剪最早非 system 对话消息，保留 system_prompt/摘要/记忆与最近对话）
2. ⚠️ **待修**：`summary_service.maybe_compress` 的 `list_for_summary` 按会话+水位（不区分分支）→ 多分支会话摘要混入其他分支 → 切回分支上下文污染
3. ⚠️ **待修**：build_context `_load_recent_messages` 过滤 `role in (user, assistant)` → **tool 消息不进上下文**；且图路径 thread_id=`{conv_id}:{msg_id}` 每轮变化 → 工具调用历史跨轮不回放（resume 中断场景除外）
4. ⚠️ **待优化**：`maybe_compress` 与 run 的 build_context **双调用**（记忆 touch 副作用翻倍，与注释"仅执行一次"矛盾）；每次推理前 LLM 摘要阻塞主链路
5. ⚠️ **边界**：`current_branch_message_id` 为 None 时 get_chain 空上下文；resume 依赖 Redis checkpoint

## obs-sdk-b：SDK AI 可观测性 B 类类型补全（2026-08-29）
- dehaze-sdk-js/src/api/ai-observability/model.ts：`AiObservabilityLlmCall` 新增 `attempts`（物理调用尝试明细，内联匿名数组，键 snake_case 原样透传：provider_id/key_id/model/status/error_code/latency_ms）；`AiObservabilityTraceItem` 新增 `traceType?: string`（conversation/summary/memory_extraction/suggestion/step_summary，Detail 继承自动获得）
- 验证：pnpm build 全绿 + test:unit 61/61

## obs-py-b：AI 可观测性 B 类后端改造落地（B1 attempts + B2 bypass_span，2026-08-29）
- B1 供应商物理尝试明细：sys_ai_llm_call 新增 `attempts` json（schema/entity）；LlmCallRecord 加 `_attempts` + `observe_attempt(provider_id/key_id/model/status/error_code/latency_ms)`（status 1成功/2失败/3熔断跳过）+ finish 落库（空为 None）；llm_client `_stream_with_key_retry`/`stream_chat` 加 `call` 参数（可选，None 防护）——成功/失败逐 Key 采集、no_key 前 status=2 error_code="no_key"、熔断跳过 status=3 error_code="circuit_open"
- B2 旁路独立过程链：sys_ai_trace 新增 `trace_type` varchar(32) default 'conversation'（schema/entity）；TraceCollector 加 trace_type（settle 写入）；trace_collector 新增模块级 `bypass_span` asynccontextmanager——set(None) 保存 prev_token + start + 退出 reset(token) 恢复外层采集器；异常 finalize_unsettled 后 re-raise（不改变旁路兜底行为）；`start()`/`TraceCollector.__init__` 的 message_id 类型放宽为 `int | None`
- 四处接入：summary_service.maybe_compress（主压缩+再压缩一个 span，message_id=None）、memory_extraction.extract_memories（**新增 conversation_id 关键字参数**，agent_hooks 传 state.conversation_id）、suggestion_service._generate_questions（**新增 conversation_id/message_id/user_id 关键字参数**，包在 try 内 wait_for 处，超时/失败正确落失败态）、step_summarizer._generate_summaries（**新增 conversation_id/message_id 关键字参数**，summarize_steps/schedule_step_summaries 链路加 conv_id，reasoning_service:383 同步）；suggestion/step_summary 旁路 trace 关联 message_id（文档 §2.3.1 归属描述已按实现修正，原写"全部 null"）
- **测试关键教训：非 db fixture 单测接入 bypass_span 后 settle 走真实 get_db_session 直接 commit 泄漏测试库**（污染 requires_db 统计用例，如 test_summary_empty）——此类用例必须挂 `db` fixture 使落库走同源事务回滚（test_context_refactor 两例、test_step_summarizer 三例已加）；同文件批量编辑存在并发丢失竞态（本轮 3 次复现，编辑后必须 grep 核对）
- 测试：test_trace_collector 5 新增（attempts 落库/无 attempts 为 None/bypass 独立 trace/外层采集器恢复/异常 finalize+ContextVar 清理/主链路默认 conversation）、test_ai_observability_service 1 新增（旁路 trace message_id=None 详情可查）；验证 86+134 全绿；开发库迁移已由 `dehaze-test/scripts/migrate_observability.py` 幂等脚本覆盖（trace_type/attempts 均在列）

## llm_client 签名半成品修复 + 上下文管理高危漏洞修复（2026-08-29）
### llm_client `_stream_with_key_retry` 签名修复
- 现象：用户发消息报 `LlmClient._stream_with_key_retry() takes 11 positional arguments but 12 were given`（A0600）
- 根因：其他成员（obs-infra）改造 llm_client 时调用方传了可观测性采集器 `call`（begin_llm_call 产物）且函数体用了它，但**签名漏加 call 参数**
- 修复：签名在 on_route_result 后补 `call=None`；llm_client 7 测试 + 全量 1796 passed 恢复
- 经验：**改动签名与调用方/函数体不一致的半成品是运行时炸弹**，全量回归能抓到（此前 7 个 llm_client 测试因并发冲突被误判）

### 上下文管理高危漏洞修复（上轮审查项的落实）
1. **摘要压缩不区分分支**：`summary_service._load_messages_to_summarize` 从 `list_for_summary`（会话+时间序）改为 `get_chain_by_id`（沿 current_branch_message_id 分支链）→ 多分支会话摘要不再混入其他分支；测试 mock 同步更新（2 处）
2. **工具调用历史跨轮不回放**：`context_manager._load_recent_messages` 把 role=tool 消息折叠为 `[工具调用结果] 文本` 追加到前一条 assistant 消息（单条截断 1000 字符）→ 后续轮次模型能看到历史工具结果，且不破坏 user/assistant 交替结构；新增测试用例
- 全量回归 1796 passed / 0 failed

## 项目 SKILL 理解修正为业界 Agent Skills 规范（2026-08-29，重要）
### 问题
- 用户指出：项目把 SKILL 理解成"Markdown 指令 + 可选脚本"是错的；业界 Agent Skills 规范（Anthropic/agentskills）中 SKILL 是**目录 + SKILL.md（YAML frontmatter + 指令正文）**，可选 reference/ script/ assets/，**用 zip 压缩包上传**
- 联网核实规范：SKILL.md frontmatter 必填 name（≤64 字符、小写+数字+连字符、与目录名一致）/description（1-1024）；可选 license/compatibility/metadata/allowed-tools；渐进披露三级（元数据→指令→资源）

### 交付（文档 + 后端实现）
- **文档**：需求规格 §2.6.11/§2.6.11.1（SKILL 格式规范）+ 后端实现 §4（zip 解析流程、数据模型、三级披露）
- **表结构**：`sys_ai_skill` 加 4 列（license/compatibility/metadata/allowed_tools）；新建 `sys_ai_skill_file`（skill_id/path/content + create_by/update_by）——注意实体继承 BaseModel 含 create_by/update_by，DDL 必须带这两列否则 INSERT 报 Unknown column
- **服务**：`SkillManageService.create_skill_from_zip`（zipfile 安全解压防路径穿越、frontmatter YAML 解析、name 命名/目录一致性/description 校验、危险操作拦截、容量上限 2MB）
- **SkillManager**：三级渐进披露，新增 `load_skill_file(name, path)`；refresh_index 文件索引失败降级不影响元数据
- **路由**：`POST /api/v1/ai/skills/upload`（multipart zip）
- **测试**：test_ai_skill_service.py 新增 6 个 zip 用例（成功+5 校验失败）；全量 1802 passed
- **教训**：SQLAlchemy Declarative 的 `metadata` 是保留属性名，实体字段必须避开（用 skill_metadata 映射列名 metadata）；zip 上传必须先落 DDL 再重启后端（后端 reload 启动时会查新列，库未迁移会启动失败）

### 前端联动（待前端团队）
- SDK `AiSkillAPI` 加 `uploadSkill(file)`（multipart）；管理端 Skill 页改为压缩包上传 + 详情展示 frontmatter/files；SKILL 详情页展示 reference/script 资源

## SKILL 存储方案 A 实施（对象存储 MinIO + DB 清单，对齐业界 Agent Skills 目录语义）（2026-08-29）
- 背景：用户否决"文件内容塞 DB text 列"方案，选择方案 A（业界最佳实践：SKILL 是完整目录，文件存对象存储，DB 只做元数据索引）
- **最终架构**：
  - `sys_ai_skill`：元数据 + frontmatter 字段（license/compatibility/metadata/allowed_tools）+ SKILL.md 正文（instruction 列，LLM 直接加载）
  - SKILL 目录其余文件（reference/script/assets/README）内容传 **MinIO**（对象 key=`skills/{name}/{path}`，bucket=settings.MINIO_BUCKET），DB 只存**清单**（`sys_ai_skill_file`: path/file_size/file_type）
  - `SkillManager`：缓存清单（path/size/type）不缓存全文；`load_skill_file(name,path)` async 从 MinIO 按需读；`list_skill_files` 返回清单
  - 删除/更新清理 MinIO 对象（list_objects prefix + delete）
  - 路由 `GET /api/v1/ai/skills/{id}/file?path=` 下载（须命中清单防任意对象读取）
- 存储抽象：`app.service.storage.factory.get_storage_service()`（sync 方法，service 用 asyncio.to_thread 包裹）；单测 monkeypatch `app.service.ai_skill_service.get_storage_service`（**必须模块级 import**，方法内局部 import 无法 mock）
- 端到端验证：上传→清单（size/type）→下载（MinIO）→非法路径 400→删除清理 ✓；全量 1802 passed
- 教训：DDL 的 sys_ai_skill_file 必须含 create_by/update_by（实体继承 BaseModel，INSERT 会带这两列）；SQLAlchemy Declarative 的 metadata 是保留字（字段用 skill_metadata 映射列名）
- 前端联动（待前端）：SDK `AiSkillAPI.uploadSkill(file)` + `getSkillFile(id, path)`；管理端 Skill 页改压缩包上传 + 详情展示文件清单（path/size/type）+ 文件预览下载

## 前端适配 SKILL 方案 A（SDK + 管理端页面 + 测试）（2026-08-29）
### dehaze-sdk-js
- `model.ts`：`SkillVO` 加 frontmatter 字段（license/compatibility/metadata/allowedTools）+ `files: SkillFileVO[]`（path/fileSize/fileType）；新增 `SkillFileVO`
- `ai-skill/index.ts`：新增 `uploadSkill(file, filename?)`（multipart POST /skills/upload）与 `getSkillFile(id, path)`（blob）
  - **坑**：SDK service 默认 `Content-Type: application/json`，axios Node adapter 检测 FormData 时保留该 header → 后端解析不到 file（400 field required）。修复：`headers: { "Content-Type": undefined } as unknown as AxiosHeaders`（类型断言，运行时 axios 自动 multipart+boundary）
- 测试：factories 手写 STORE zip 构造（buildSkillZip，含 CRC32）；新增 T-MF-092/093/094（上传解析 frontmatter+清单、读取资源文件、缺 SKILL.md 校验失败）。12 passed
- SDK 需 `npx tsup` 重建 dist（前端 workspace 链接用 dist）

### dehaze-front-vue 管理端
- `adminSkill.ts`：加 `uploadSkill(file)`/`fetchSkillDetail(id)`/`getSkillFile(id,path)`
- `SkillFormDialog.vue` 重构双模式：**新建 = zip 拖拽上传**（选文件→上传→后端解析校验）；**编辑 = 文本表单 + frontmatter 只读（license/compatibility/allowedTools）+ 资源文件清单（path/size/type + 预览下载按钮，blob URL）**
- vite build 通过（53s）

### 前端后续注意
- SDK 改动后必须 `npx tsup` 重建 dist，否则前端拿到旧类型（前端通过 pnpm workspace 链接 SDK 的 dist）

## MCP/Function Calling 规范审查与修复（2026-08-29 下午）
### 审查结论：Function Calling 符合度高（OpenAI/Anthropic 协议转换、错误恢复、护栏均规范）；MCP 管理闭环完备但**运行时接入缺失**（凭据存而不用、外部 Server 工具未注入 LLM、审计不落库、SSRF 不一致）
### 修复清单（全部完成并回归）
1. **凭据注入**：`mcp_connection.py`（新）`build_mcp_auth_headers`（AES 解密 api_key/extra.access_token → Bearer）；`mcp_tool_fetcher` 拉工具带鉴权头（streamablehttp_client/sse_client 支持 headers 参数）
2. **外部 Server 工具运行时装载**：`mcp_external_tool_loader.py`（新）按 agent.mcp_namespaces 反向关联启用 server → 装载为 StructuredTool（命名 `<ns>_<tool>`，JSON Schema→pydantic args_schema，执行走 call_remote_tool，审计落库）；注入 `deep_agent_builder.build_from_snapshot`（extra_tools 参数，失败降级空）
   - **坑**：langchain `StructuredTool.from_function` 对闭包 async 函数误判为 sync（ainvoke 返回未 await coroutine）→ 显式传 `coroutine=_call`
3. **审计打通**：外部工具执行与管理员试调用均调 `mcp_manage_service.record_call` 落库 sys_ai_mcp_call
4. **SSRF 一致**：`check_endpoint_safe`（市场预设白名单 PRESET_ENDPOINTS 放行本地内网 http Server，其余 is_safe_url https+禁内网）；拉取/装载/健康探测统一走 `apply_ssrf_guard`
5. **健康探测**：streamable-http 改 POST initialize（JSON-RPC 2.0），sse/其他回退 GET
6. **工具试调用**：后端 `POST /servers/{id}/tools/test`（McpToolTestForm/Result）+ SDK `AiMCPAPI.testTool` + 前端 ToolTable 加"试运行"按钮对话框（Vue 模板不支持 `as` 类型断言、不支持嵌套反引号模板字符串，均已规避）
7. **Function Calling**：`dehaze_chat_model` tool_call_chunks index 从 None 改真实顺序号；MCP 元工具结果截断 500→4000
8. **常量抽取**：`mcp_presets.py`（新）承载 MARKET_PRESETS，SSRF 白名单与市场共用
### 回归：MCP 相关 72 + service 全量 1341 + llm/sse 54 全过；SDK tsup 构建成功；前端 vite build 成功（55s）
### 遗留
- mcp 版本分裂：dehaze-mcp-gateway 用 mcp SDK 2.0（`mcp.server.mcpserver` 模块），主后端 1.x——各自工作正常未统一
- OAuth2 完整授权流未实现（支持手动配置 access_token 走 Bearer，够用）
- 前端 vite build 清空 dist 被 IDE safe-delete shim 拦截（SAFE_DELETE_BULK_CONFIRM_REQUIRED）→ 用 `--emptyOutDir false` 验证；router/schema 存量 E501 未动（非本次问题）
