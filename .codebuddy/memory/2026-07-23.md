# 2026-07-23

## scripts/run.py 大幅精简
- 从 353 行精简到 103 行（减少 71%）
- 删除所有冗余工具函数：`port_open`、`port_pids`、`find_pids_on_port`、`wait_pid_exit`、`pid_file`/`pid_save`/`pid_del` 等
- 删除 `kill <port>` 命令及 netstat 解析逻辑
- 删除端口探测、SIGTERM 优雅停止/强制停止三段式回退
- 改用开发模式启动命令：`go run ./cmd/main.go` / `uvicorn --reload --host 0.0.0.0` / `mvn spring-boot:run -DskipTests`
- 用 `tuple[Path, list[str], int]` 替代 `TypedDict`，3 字段足够
- 仅保留 `_alive()` 一个工具函数，PID 文件操作直接内联
- Pylance 零报错

## Java /auth/me + /users/me 重复 API 修复
- 编译错误根因：`UserConverter` 引用 `perms` 但 `UserInfoVO` 字段名是 `permissions`
- `UserInfoVO.permissions` 重命名为 `perms`（与 Go/Python/Harmony SDK/Android SDK 一致）
- 删除 `AuthService.getAuthInfo()`（返回无类型 `Map<String, Object>`）
- `AuthController.me()` 改为返回 `Result<UserInfoVO>`，直接调用 `userService.getCurrentUserInfo()`
- 删除 `SysUserController` 的 `/users/me` 端点（与 `/auth/me` 重复）
- 三端统一：Go/Python/Java 的 `/auth/me` 均返回 `perms` 字段
- 全局更新 SDK + 前端引用：JS SDK model/test、Vue store/mock/test、React store/hooks/mock/page、Android SDK URL、Harmony SDK URL

## 三端 /users/me -> /auth/me 统一迁移
- **Go**: 删除 `router/user.go` 的 `/users/me` 路由和 `sys_user.go` 的 `GetCurrentUserInfo` handler（`/auth/me` 已存在于 `router/auth.go`）
- **Python**: `auth.py` 新增 `/auth/me` 端点（返回 `CurrentUserVO`），删除 `user.py` 的 `/users/me`
- **Flutter**: `user_model.dart` 的 `permissions` -> `perms`，`user_model.g.dart` 同步，删除 `api_constants.dart` 的 `usersMe` 常量
- **React Native**: `profile/index.tsx` 的 `userInfo?.permissions` -> `userInfo?.perms`
- **UniApp**: `store/auth.ts` computed `permissions` -> `perms`，`user-center/index.vue` 模板引用同步
- **Taro**: `stores/global.tsx` 全量替换 `permissions` -> `perms`，`storage.ts` 方法名 `setPermissions/getPermissions` -> `setPerms/getPerms`，`CacheEnum.ts` `PERMISSIONS` -> `PERMS`，`hooks/usePermission.ts` + `pages/profile/index.tsx` 同步
- **文档**: 用户管理 API 接口.md 删除 `/users/me` 行，API 规范.md 两处 `users/me` -> `auth/me`，认证管理需求规格/前端实现/测试用例 中 `permissions` -> `perms`

## JWT claims 与 captcha 存储三端统一
- Python JWT 重构（`app/utils/jwt.py` + `app/dependencies/auth.py` + `app/service/auth_service.py`）：
  - 删除 `username`/`nickname`/`permissions`/`type` 额外字段，新增 `deptId`/`dataScope`（取用户所有角色 data_scope 最小值，复用 `role_repository.get_maximum_data_scope`）
  - **权限合并进 `authorities`**（与 Go 一致：`["ROLE_"+r for r in roles]` + perms），`UserContext.permissions` 从 authorities 拆分（非 ROLE_ 前缀项），保证 `require_permission` 鉴权仍可用
  - `UserContext` 去掉 `nickname`，新增 `dept_id`/`data_scope`
- captcha 存储三端统一为 Redis db0 纯文本：
  - Python `config.py` `DevelopmentSettings.REDIS_DB` 3 → 0
  - Go `config.yaml` `cache.redis.db` 3 → 0
  - Java `AuthServiceImpl` 改用 `StringRedisTemplate`（去掉 Jackson 引号序列化）
- 联动更新：`scripts/debug_helper.py`（captcha_db 全 0、strip_quotes 全 False）、`dehaze-sdk-js/test/config/backend.ts`（captchaRedisDB 全 0，删 captchaJacksonQuoted 字段）、`dehaze-sdk-js/test/utils/auth.ts`（删 Jackson 去引号逻辑）、`dehaze-sdk-js/vitest.setup.ts` 注释、Python 测试 `tests/utils.py`/`tests/conftest.py` 的测试 token 生成、dehaze-doc UserContext 结构说明
- Go `config.test.yaml`（db4）未改：属测试隔离环境，非运行后端

## 删除未使用的 Python SECRET_KEY 配置项
- `SECRET_KEY` 此前仅在 `config.py` 的两处启动校验（dev 非空、prod 长度≥32）中被引用，业务代码从未消费（JWT 用 `JWT_SECRET_KEY`，FastAPI 无 session 签名），属历史遗留占位配置
- 清理点：`app/config.py`（删字段定义 + dev/prod 两处校验）、`tests/test_config.py`（删 `test_secret_key_set`）、`tests/conftest.py`（删 `os.environ["SECRET_KEY"]` 设置）、根目录 `.env`（删 `SECRET_KEY` 行及注释）、`dehaze-doc/05-Python算法服务架构文档.md`（配置清单/`.env` 示例/安全校验三处描述）
- 编译校验通过，Python 代码已无 `SECRET_KEY` 残留引用（仅保留无关的 `MINIO_SECRET_KEY`）
