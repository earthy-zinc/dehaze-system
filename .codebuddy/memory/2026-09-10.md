# 2026-09-10

## front-vue-dedup：Label/TagType 冗余全局治理完成（dehaze-front-vue，2026-09-10）
- 基建：`src/enums/TagType.ts` 全项目唯一定义 `type TagType = "primary"|"success"|"warning"|"danger"|"info"`；就近共享常量 4 个：`views/order/constants.ts`（orderStatus/payMethod/refundStatus/refundReason 四组，order 4 页共用）、`views/feedback/constants.ts`（type/status/replyType/module 四组，feedback 3 页共用）、`views/algorithm/constants.ts`（algorithmStatusMap，list/detail 共用）、`views/package/constants.ts`（periodOptions，list/shop 共用）。
- 模式：单源数组 `{label,value,tag}` 驱动 options/label/tagType；页面函数内联 `.find((o)=>o.value===v)?.label ?? v` 派生（刻意不建 findOption 辅助函数，避免跨模块重复）；`EnumOption` 接口 tag 必填（纯下拉选项用内联 `{label,value}` 类型），tagType 函数用 `find(...)!.tag` 保持原行为。改动 29 文件（24 页面/组件 + store/adminProvider + 5 新建/别名文件）。
- 豁免：模板内单值三元 `:type="status===1?'success':'info'"` 17 处；el-progress 的 `""|"success"|"exception"|"warning"`；二值状态成对三元（ai-conversations ConversationAuditTable、ai-voice engineTagType）；已是单源的 Record meta（ai-billing AnomalyMonitor 等）仅换 TagType 别名。
- EvalPanel.runTag/PublishDialog.gateTag 与 eval-meta.ts RUN_STATUS_META 三份拷贝收敛为跨模块复用 `@/views/ai-eval-center/eval-meta`（评测域内聚，可接受）。
- 行为零变更：所有 label 文案/tag 颜色/options 顺序/筛选值保持原样（含 feedback-squad 修正的 priority 1普通/2紧急/3高优 info/warning/danger）。验证：vue-tsc 0 错 + `npx vite build --outDir /tmp/... --emptyOutDir` 构建成功（1m3s，用临时 outDir 绕 dist shim）。发现原映射疑似 bug：无（feedback priority 由 feedback-squad 先行修正）。

## feedback-squad：4 项决策执行（2026-09-10，用户拍板后）
- ①平均响应/关闭时间单位统一**小时**：Java FeedbackStatsVO 两字段 Long→Double（注释改小时）+ calcFeedbackStats 除以 3_600_000 保留2位；Go 核实本就返回小时（.Hours()）不动；front-vue stats 页"分钟"标签→"小时"；文档（需求规格 §4.4.2）同步口径。②Go ReplyFeedback 补未分配自动分配（assignee_id+assigned_time，事务内 update map 扩展）。③replyType 口径：三端保持可选 + 值域校验（python Literal[...]|None / Java @Pattern / Go map 校验，均 A0400），需求规格 §4.2.4 必填→可选。④A0503 三端口径核实一致（python OPERATION_NOT_ALLOW=A0503 / Java 同 / Go common.OPERATION_NOT_ALLOW），无需改。
- 验证：pytest 32/0/0；Java mvn compile 过；Go go build+test(feedback) 过；SDK feedback 83/0/0（+非法 replyType A0400 用例，pydantic 校验失败经全局 handler 映射 A0400 已由用例证实）；front-vue vue-tsc 仅 1 错为 front-vue-dedup 进行中的 TaskListDrawer.vue TagType 抽取（与 feedback 无关）。

## feedback-squad：反馈评价模块全量审查（python 后端 + front-vue + 测试，2026-09-10）
- 契约事实：错误码权威为 A0540-A0546（后端实现.md 曾误写 A0560-A0567 已修）；rating 越权（create 非本人/by-prediction 非本人）三端统一 A0503（不暴露存在性），feedback 详情/补充越权 A0543；supplement 用户补充 reply_type 固定 "info"（python 曾用 "supplement"，已对齐 Java/Go）；反馈 priority 枚举 1=普通/2=紧急/3=高优（T-FE-062），front-vue 曾误用 1低/2中/3高/4紧急。
- python 修复：①reply_feedback 未分配时自动分配给回复管理员+assigned_time（对齐 Java，Go 也缺此逻辑待修）；②supplement/reply/assign/close 后失效 feedback:stats 缓存（原来只有 create 失效，方法签名加 redis 参数）；③list_paged_ratings 补 tags 逐个 LIKE 筛选 + router 暴露 tags 参数（Java/Go 契约本有）；④update_rating isAnonymous 未传保留原值（原会重置 0）；⑤get_feedback_stats topKeywords [:20]→[:10]（对齐 Top 10）。
- front-vue 修复：①list/index.vue 时间范围传 "YYYY-MM-DD"（后端 strptime "%Y-%m-%d %H:%M:%S" 解析失败 500）→ 补 " 00:00:00"/" 23:59:59"（rating 页本来正确）；②优先级枚举修正；③rating 页已隐藏评价的"显示"按钮删除（后端无恢复接口，原点击仍调 hide 幂等无效）；④my 页新建反馈补截图上传（MultiUpload limit=5，对齐前端实现.md）；⑤rating 页补 tags 筛选控件 + keywords placeholder 修正（后端只匹配用户名/昵称）。
- 测试：pytest tests/service/test_feedback_service.py 11→17（+update 保留匿名×2、supplement reply_type=info+状态回退、reply 自动分配×2、close 失效 stats 缓存）；SDK feedback.test.ts 追加 5 用例（tags 筛选、对抗性脏语料往返一致 emoji/零宽/CRLF/全半角、supplement replyType=info 断言、回复自动分配 assigneeId、topKeywords≤10）→ 82/0/0；pytest 全模块 32/0/0。front-vue vue-tsc 0 错 + vite build 成功（注意：pnpm build 会因 IDE safe-delete shim 拦 dist 清理而失败，用 `npx vite build --outDir <临时目录>` 绕过验证）。
- 【需决策·未改】①平均响应时间单位三端不一致：python=小时、Java=毫秒、Go 未知，前端 stats 页标"分钟"——需统一（建议小时）并同步三端；②Go ReplyFeedback 缺自动分配（与 Java/文档不符）；③需求规格 §4.2.4 称回复类型"必填"但三端 FeedbackReplyForm.replyType 均可选无枚举校验（文档与实现矛盾）；④需求规格 §2.1.5/§3.1.4 提到"我的反馈页含新建对话框"，评价提交入口 RatingCard（components/RatingCard）已对齐需求（标签动态切换/5MB/3张/匿名），无需改。
- 值得记住：feedback 模块缓存失效规范——反馈创建/回复/分配/关闭/补充均需删 feedback:stats（Java/Go 每操作都失效，python 原缺失）；评价创建/修改删 rating:stats:global + rating:stats:algorithm:{id}；hide/reply/updateTags 不删统计缓存。sys_rating 无 replier 字段，replyRating 只写 admin_reply/reply_time。

## billing-frontend：AI计费管理模块前端审查修复（dehaze-front-vue + dehaze-sdk-js）- 契约事实（后端为权威）：sys_ai_billing `model`=实际使用模型（降级场景为降级模型）、`actual_model`=用户原选模型（仅降级时非空，NULL=未降级）；异常记录 sys_ai_billing_anomaly 字段为 detail(str)/status(int 0待处理/1已处理/2已忽略)/trigger_at，异常类型四类 single_high/burst/consecutive_quota_fail/empty_high_output（SDK 旧 AnomalyRecordVO 的 anomalous/manual/auto_compensated + username/costCredits/credits/reason/status:string 全是臆造）。
- 修复：①SDK model.ts 纠正 model/actualModel 语义注释 + AnomalyRecordVO/AnomalyRecordQuery 对齐后端（pnpm build 已重建）；②front-vue 计费明细模型列改"用户所选(actualModel??model) + 降级附注实际 model"，RecordStatusTags 悬浮改"已降级为 model"；③adminBilling.resolvePeriodRange 的 toISOString() UTC 时区 bug（月初会偏移一天）改本地格式化；④【架构】billingDataStore 全局单例改按 scope 键控多实例（`useBillingDataStore(scope)` 内部 defineStore(key, setup)()，key=self/user-{id}）——管理端下钻面板(selectedUserId)与积分调整面板(form.userId)同页嵌多个公共组件，单例 scope 互相覆盖会拉错用户数据；删 resetScope 死代码；auditRefund/submitCreditAdjust 按目标 userId 取对应实例刷新；⑤AnomalyMonitor 重写对齐四类异常+detail+int状态+triggerAt；⑥CreditAdjustPanel/CostUnitPriceManage 的 `ref(ElForm)` 改 `ref<FormInstance>()`，删空 handleUserChange。
- 验证：SDK build 过；front-vue vue-tsc 0 错；单元测试 4 个失败文件（Waterfall/Pagination/Hamburger/settings）为存量、与 billing 无引用关系；order/detail 5 个 TS 错为存量（order 模块）。
- 【需决策·未改】①`GET /cost-stats` 文档称"按模型/供应商/时间"但后端只收 startTime/endTime（无分组），管理端"供应商维度下钻"实际显示全局汇总；SDK CostStatQuery 的 modelId/providerId 后端不支持。②SDK ModelCostForm.providerId 可选但后端创建必填；updateCost 用 Partial<ModelCostForm> 但后端 UpdateRequest 只收 currency/effective/status。③API接口.md 异常类型描述若按 anomalous/manual/auto_compensated 理解需以后端四类规则为准核对。④sys_menu 134/135 目录/菜单节点 perm 为 NULL（按钮级四权限已种子），菜单可见性依赖角色授权配置。

## billing-backend：AI计费管理模块后端质量审查与修复（dehaze-python）
- 修复 4 项：①退款重复回补漏洞——audit_refund approved 时补校验原计费记录存在+该 billing_id 无已通过退款（A0681），apply_refund 补校验 amount ≤ record.credits（A0400），ai_refund_repository 新增 has_approved_by_billing_id；②预估 token 估算中文低估——estimate_service 新增 _approx_tokens（非 ASCII 1字≈1token，ASCII 4字≈1token，替换 len//4），len//4 对中文低估近 5 倍致配额防滥用形同虚设；③billing_service 模块 docstring 删除虚构的 record_tool_llm/record_kb_inject（实际不存在）；④router._parse_datetime 非法时间格式改抛 A0400（原静默吞掉当 None 过滤）。tests/service/test_billing_module.py 同步（估算期望值改 _approx 口径+退款 4 新用例）。bill/refund 相关 134/0/0。
- 追加（billing-test 暴露缺陷）：①POST /adjust 幽灵 userId 充值——router adjust_credits 前置校验 user_repository.get_by_id，不存在抛 A0401（新用例断言 recharge 未被调用）；②根因级修复 balance_service._increase_cas/_deduct_cas 前置读取 None（用户余额账户不存在）由静默 return 改抛 RuntimeError，防 Redis/MySQL 余额永久背离全链路（充值/退款回补/VIP 赠送/欠费扣减）。bill/refund 相关 166/0/0。
- 【需决策·未改】①tool_llm/kb_inject 计费入口未实现（文档 §4.3/§9 称 BillingService.record(tool_llm)，代码无任何调用方，sys_ai_billing.tool_credits 无写入方）；②asr/tts 将秒数/字符数复用 input_tokens 字段落库，污染 summary/统计的 token 与缓存命中率口径；③配额 fail-open：无会员记录/权益停用 → 限额 (0,0)=无限额，与需求 §2.8.2 普通用户日 10000 冲突（测试已锁定该行为，改动属设计决策）；④reconcile/import 仅为按行计数骨架；download_bill 返回 JSON 非 PDF/Excel（代码内已注明简化）。
- 值得记住：settle 差额退补顺序正确（预扣→按实际退补）；billing 无越权问题（_resolve_query_user+服务层 user_id 归属校验）；balance/quota 预扣-实扣-回补链路审查无并发漏洞。

## order-fix：订单管理模块 6 项决策执行落地（python+Java+Go+SDK+front-vue+React+doc）
- ①组合支付 channel 契约：python PayRequest 增 channel（组合支付必填 wechat/alipay），payment_service.combined 分支按 channel 调 unified_order + 预写流水渠道即真实渠道（消除"预写 combined 回调再修正"旧语义）；SDK PayRequest 补 channel+balanceAmount；front-vue 订单详情支付弹窗增组合支付选项+第三方渠道单选。pytest 组合支付用例改写为 3 个（缺渠道/非法渠道/正常下单含流水渠道断言）。
- ②React 退款表单修复：order/my/index.tsx 原 reason 中文自由文本（必 422）→ reasonType 四枚举下拉 + customReason 选填，对齐 front-vue。
- ③Java/Go 契约对齐：Java PayRequest/OrderCreateForm 补 channel/balanceAmount（Long），Go bo.PayRequest/OrderCreateForm 补 Channel/BalanceAmount（*int64）；两端 combined 业务链路仍缺失（无余额账户子系统，收到 combined 按"不支持的支付渠道"拒绝），已在后端实现.md §2.6/§6 如实登记，属遗留项。
- ④等级口径文档统一：会员管理/后端实现.md §4.2 + 订单管理/后端实现.md §4.3 各加"成长值累积必触发升级检查（不限来源）+ 购买类操作等级只升不降（max）"口径说明，消除两条路径"不一致"误解（对应 order-test 遗留设计不一致项的决策结论）。
- ⑤订单文档修订：API接口.md 补登 /orders/recharge；后端实现.md 增 §2.6 PayRequest DTO；前端实现.md PaymentDialog 简化（去倒计时）、paymentStore 倒计时标注规划项未实现、keepAlive 描述对齐实际（my 页 keepAlive+onActivated 刷新，detail 页未启用）、支付方式切换行对齐组合支付渠道选择语义。
- ⑥SDK cleanup 软删兜底：test/utils/cleanup.ts 新增 deletePackageOrOffline（物理删除失败→updateStatus(id,0) 下架），order.test.ts/order-edge.test.ts/package.test.ts 三处 registerIds 接入，消除已支付订单 A0522 导致的测试套餐累积。
- 验证：mvn compile 过；go build+test(order) 过；SDK build 过；front-vue vue-tsc 0 错；React tsc 仅存量错误（Dataset/favorite 等，与本次无关）；pytest 全量 2214/0/0（基线 2212+2）；SDK order 82/0/0、package 93/0/0（服务由 lead 重启后连跑）。
- 遗留：Java/Go combined 完整链路（余额冻结/扣减/拆分退款）待单独任务；React 端 PaymentDialog 未加组合支付选项（React 侧任务仅要求修退款表单，契约类型已支持）。

## order-test：订单管理模块测试审查与强化（team module-review，2026-09-10）
- pytest 新增 tests/service/test_order_state_machine.py 36 用例（状态机非法流转参数化 A0531/A0532/A0533/A0400、越权 cancel/pay/refund/detail A0530、支付金额快照一致性（建单后改价支付仍按 order.payable_amount）、回调幂等一次履约/取消单回调拒/分布式锁持有幂等返回 True、退款折算边界（vip 用完=0/credit 全消耗=0/period_days 缺失=0/不变量 0≤amount≤paid 参数化）、主观 reasonType 拒绝、脏语料 customReason（合法码点 emoji+零宽+CRLF+全半角）原样拼接保存、approve/reject 重复审核 A0531、A0537、list_my 隔离与状态筛选、paid→completed 归档跳过 credit、expire_orders 释放券）。订单域 pytest 全量 102/0/0。
- SDK 新增 test/modules/order/order-edge.test.ts 24 用例（状态机严格 A0533/A0531、越权 cancel/pay/refund、A053B 天价套餐余额不足、金额快照一致性（admin 改价后 paidAmount==原 payable）、会员卡履约（level_0→level_1+growth 增）、积分卡履约（completed+creditAmount+等级不变+无到期时间）、非法 reasonType A0400、脏语料 cancel reason 原样、LIKE 通配符/非法 status 不 500、分页边界 A0400（pageSize=101/pageNum=0）、性能烟测（listMy<500ms/getPage/getStats<2s）、packageType 筛选不变量、refund reasonType 契约）。订单 SDK 82 用例：80 过 / 2 失败（均为暴露性用例）/ 0 skip。
- SDK 契约补齐：src/api/order/model.ts OrderQuery 补 packageType（后端已支持）、RefundRecordVO 补 reasonType、RefundQuery 补 reasonType 筛选；dist 已重建。
- 【暴露缺陷 1】积分卡下单 500：开发库 sys_order 与 config/sql/schema 漂移——package_level（lead 已修）与 period_days（仍 NOT NULL 待修）应为 NULL（积分卡订单写 NULL），Credit INSERT IntegrityError 1048。
- 【暴露缺陷 2】POST /orders/balance-refund 500：balance_refund_repository.get_pending_by_user_id 用 scalar_one_or_none()，开发库多条历史 pending 记录（旧版无防重复+测试未清理）→ Multiple rows 500；修复需 .first() 防御 + 清理 USER(id=5) 历史 pending 记录（否则防重复检查永久拦截）。
- 【次要观察】order_service.get_detail 的 Redis 缓存键仅 order_no 但缓存首访者视角的 _admin 标记，admin 复查会命中用户视角缓存（数据内容一致，仅标记失真）——已转 order-backend。
- 测试技巧：集成测试构造"余额必不足"用天价套餐（salePrice 999999900 分）比猜测账号余额可控；pytest get_detail 缓存以 order_no 为键，同号多视角断言需换订单号隔离。
- 收尾（缺陷闭环后）：两缺陷修复复跑 82/82 全绿（连跑两轮验证可重跑）。积分卡履约用例断言修正：member_service 对非 vip 订单仅累积成长值但会调用全局 _check_and_adjust_level（成长值升级不限来源），故积分卡支付后 levelCode 可能升级（如 level_1→level_3），真实契约是 levelSource/expireTime 不变+成长值 1:1+等级只升不降。存量 order.test.ts balanceRefund 用例改造：防重复检查（每人一条 pending）生效后无法清理（无余额退款查询/审核 id 回查接口，apply 只返回 refundNo），改为"首笔成功或 A0503+第二笔必 A0503"自洽断言。遗留设计不一致待决策：买 level_1 会员卡的成长值高用户保持 level_1（on_order_paid 取 max(现等级,卡等级)），同一用户买积分卡却触发成长值重算升 level_3——两条购买路径等级口径不一致。

## package-fix2（追加）：Java PackageForm 补积分卡字段 + Go 促销接口补齐
- Java：PackageForm 补 packageType(@NotBlank+@Pattern vip|credit)/creditAmount，levelCode/period/periodDays 去掉注解级必填（改服务层按类型校验）；PackageServiceImpl 校验改 validatePackageForm(form, packageType) 对齐 python（vip 缺等级/周期/天数、credit 缺 creditAmount 分型报错），新增 applyTypedFields 按类型差异化写入（另一类型字段置 NULL），update 以库中 packageType 为准锁定，getForm 补回显。
- Go 促销补齐（对齐 python 6 端点）：新增 bo/promotion.go、query/promotion.go、repository/pkgsale/promotion_repository.go（IPromotionRepository 进 interfaces.go；RebindPackages 事务内删+插关联）、service/pkgsale/promotion_service.go（变更后失效 package:detail:{pid} 缓存）、api/sys_promotion.go、router/promotion.go（/packages/promotions 独立注册组）；vo PromotionVO 补 createTime(omitempty)；app.go 显式 wiring 4 处。风格要点：事务在 service 层 s.db.Transaction 内 NewRepository(tx)；时间解析 timeFormat="2006-01-02 15:04:05"；update 不碰 status（对齐 python）；bindPackages 折扣方式从 activityRules 推导（无规则回退 type==full_reduction→full_reduction 否则 percent）。
- 验证：mvn compile 过；go build/vet/test 全过。
- Go DI 教训：app.go 无 wire/fx，纯显式构造，新增服务需同步 4 处（repo 构造/service 构造/api 构造/路由注册），漏一处仅 build 期报错（declared and not used/undefined）。

## package-fix2：套餐管理模块 5 项决策执行落地（python+Java+SDK+front-vue+doc）
- ①权益覆盖展示侧统一 max 语义+扩字段：python package_service `_get_effective_benefits` 重构（BENEFIT_QUOTA_FIELDS 10 个配额类字段含 8 任务配额+aiCreditsDaily/Monthly 取 max，BENEFIT_FIELDS 共 18 项）；Java 补 SysMemberBenefit 实体 10 字段 + BenefitOverrides 10 字段 + buildBenefits max 合并（benefits Map 改 Long），PackageDetailVO.benefits 同步；test_package_service 用例改 max 双向断言。②batch_distribute 文档降级：后端实现.md §5.7 改"同步执行（当前量级），异步化列为规划"。③Java 促销模块补齐 6 新文件（PromotionController/Service/ServiceImpl + PromotionForm/PromotionPackageForm/PromotionPageQuery），PromotionVO 补 createTime，逻辑对齐 python（bindPackages 从 activityRules 推 discount_type/value；Java 无商品缓存故不失效缓存）；PackagePageVO/SysPackage 补 packageType/creditAmount/creditUnitPrice。④促销分页统一 pageNum/pageSize：python router/service 改参 + SDK PromotionQuery 恢复 extends PageQuery + factories pageQuery + front-vue promotion/index.vue 三处绑定。⑤禁用券：Java receive/batchDistribute 改 BUSINESS_ERROR"优惠券已禁用"（禁用≠不存在）；Java calculatePrice 补新用户专享校验（有付费订单拒购）。doc：后端实现.md §5.5 消息对齐/§5.6 错误语义/§3.3 合并语义、API接口.md A0500 补两错误。
- 验证：pytest 全量 2175/0/0；mvn compile 过；SDK package 93/93 全绿（lead 重启服务后复跑）；SDK build DTS 过。
- 【遗留缺口需 lead 决策】dehaze-go 促销接口同样全缺（仅 internal/model/sys_promotion.go，无 router/service）——文档声称三端一致，Go 需单独任务补齐；Java PackageForm 仍无 packageType/creditAmount 字段（save/update 不写这两列，Java 后台无法创建积分卡），属促销之外的既有缺口。

## package-fix：package.test.ts 复跑 9 失败+5 skip 连锁收尾（全绿 93/0/0）
- 根因与修复：①测试缺 `PromotionAPI`/`PromotionForm` 导入 + `createdPromotionIds` 未声明（5 失败+5 skip 连锁根因）；②促销分页契约对齐后端：python promotions/page 实际参数 `page/size`（packages/page、coupons/page 为 pageNum/pageSize，后端自身不一致已列决策项）——SDK `PromotionQuery` 改 page/size 独立接口（不再 extends PageQuery），factories createPromotionQuery 同步，front-vue promotion/index.vue queryParams+pagination 组件绑定+resetQuery 同步；③积分卡价格断言：payable=salePrice(990) 非 originalPrice(1990)（促销价高于原价校验意味着 originalPrice≥salePrice，payable 恒以 salePrice 为基）；④totalQty=-1 两用例改 100000（实现 ge=0），doc 需求规格.md §2.3.2 已改"≥0，不设-1不限量语义"；⑤logout/forceLogin 补导入（auth.ts 本就有）；⑥促销 update 忽略 status 字段（后端故意，status 走 /status 端点且联动清缓存）——测试改用 updateStatus 断言；⑦emoji 名称用例必须 uniqueName（uk 含软删行，固定名跨次运行 A0501 占用）；⑧促销联动积分卡 salePrice=4000>默认 originalPrice=1990 触发 A0500，补 originalPrice=yuan(50)。
- 遗留：开发库残留软删/活体 "🎮游戏礼包ＦＵＬＬ版" 套餐（可忽略）；促销分页 page/size vs 全局 pageNum/pageSize 不一致待用户决策。
- 排查技巧：套件级 beforeAll 失败只进 brief 的 collectErrors（无请求明细），需按用例推读代码定位（本次是 A0500 业务错映射 HTTP 400）。

## front-vue-tsc-fix：dehaze-front-vue 构建类型错误清零（26 处）
- SDK 类型演进后前端滞后：`Dataset`→`DatasetVO`（DataList×2、DatasetImageSelect、mock/data）；`ImageUrlVO` 无 `originUrl`（删回退直用 url）；`SkillVO` 已无 `scriptContent`/`templateId`（mock 删两处，注意 TS2353 每个字面量只报第一个多余属性，templateId 是连带修复）；`RefundApplyForm` 改 `reasonType`(必选)+`customReason`(选填补充)——order/my 退款表单重做为四选项枚举下拉；`RefundRecordVO.usedQuota`→`usedDays`/`usedCredits` 按值条件渲染两行（order/detail+list）
- 其余：`LoginData` 必填 captchaKey/captchaCode（user.spec 三处 mock 补齐）；el-rate modelValue 不收 null（`?? undefined`）；el-table slot row 为 DefaultRow 需 `as McpToolVO`；markdown-it-texmath 无类型声明 → 在 src/typings/shims-vue.d.ts 补 `declare module`（PluginSimple）
- 同步文档：订单管理/前端实现.md §6「退款流程引导」改为 reasonType 必填 + customReason 选填
- vue-tsc --noEmit 0 错误（IDE ts-plugin 对 sdk-js 的报错可能缓存陈旧，以 CLI 为准）

## package-backend：套餐管理模块审查与修复（dehaze-python）
- 修复 4 处：①package_service.create 校验顺序（类型/参数校验先于名称查重，对齐文档 §5.1）；②get_page 补 levelName 填充（原 benefit_map 死代码恒空，对齐 Java toPageVO：无 benefit 回退 levelCode）；③coupon receive 补 fixed 券过期校验（对齐 Java + 文档 §5.6）；④折扣券面值 >100 拒绝（文档 §2.3.2）。测试 73 全过（补 5 新用例 + 修存量 flaky：test_receive_until_stock_empty 因 Core UPDATE(synchronize_session=False) 不同步 identity map，改列级 select 直读库存；注意该测试环境 expire_all 会触发 MissingGreenlet，勿用）。
- 【跨模块严重缺陷待 member-fix】benefit_overrides key 失配：套餐侧写 camelCase（monthlyDehazeQuota，与 Java 契约/DB 数据一致），但 quota_service._effective_task_quota 读 snake_case（monthly_dehaze_quota）→ 会员卡覆盖项在 python 会员履约/配额合并中静默失效。修复应在会员侧读 camelCase。
- 【需决策】①权益覆盖合并语义：文档（套餐 §2.1.2/会员 §3.3）说配额覆盖取 max，python/java 展示侧均直接覆盖且仅 9 字段（缺 6 类任务配额+AI 限额），商城展示与履约（max）不一致；②batch_distribute all 场景文档说异步任务 ID，python 同步单事务；③促销管理接口 python 有 /promotions/* 而 Java 无 Controller；④Java PackagePageVO 缺 packageType/creditAmount/creditUnitPrice；⑤禁用券领取 python BUSINESS_ERROR vs Java COUPON_NOT_FOUND；⑥Java calculatePrice 无新用户专享校验（python 符合文档）。
- 【教训】共享仓库严禁 git stash/pop——与队友 stash/commit 交错会卷走全仓库未提交修改（本次我的 4 文件+队友 front-vue/sdk-js 修改被卷入 stash，靠 git stash show 定位 + git checkout 'stash@{n}' -- <files> 精准恢复）。基线对照用 git show HEAD:path 临时覆盖验证后立即恢复。

## dict-frontend（字典管理前端审查与闭环，dehaze-front-vue + dehaze-sdk-js）
- 第一轮修复：SDK api/dict 对齐后端 VO（PageVO 必填化、删 deleteDictByIds 空 ID 兜底）；front-vue 新增 store/modules/dict.ts（dictStore 缓存选项 Promise + invalidate + preload，对齐前端实现.md §3）；Dictionary/index.vue 走缓存；字典类型页修编辑态 code 未只读、批量删除缺 v-hasPerm、删除文案与逻辑删除矛盾；dict-item.vue 增删改后 invalidate(typeCode)。
- 第二轮（后端就绪后闭环）：①SDK model 补回 DictQuery/DictTypeQuery.status（后端已支持筛选）、DictTypePageVO.isPreset: boolean；已 pnpm build 重建 dist。②预置类型 UI 保护：行删除按钮 v-if="!row.isPreset" + 选择列 :selectable="(row) => !row.isPreset"（防批量删除勾中预置行）。③单条类型删除确认框展示关联数量（复用 getDictPage?typeCode&pageSize=1 的 total；批量删除保持通用提示）。④两页搜索表单补状态筛选（clearable + width 120px，对齐 notify/template 惯例）；顺带修类型页搜索框 prop="name"→"keywords"（原 resetFields 清不掉关键字）。vue-tsc 字典相关 0 错误。
- 注意：IDE ts-plugin 对 dehaze-sdk-js 类型报错可能为缓存陈旧，以 vue-tsc CLI 结果为准。

## dict-test：字典管理模块测试强化（team module-review）
- pytest 新增 51 用例全绿：tests/service/test_dict_service.py（35）+ tests/router/test_dict_router.py（16）
- SDK dict.test.ts 追加 24 用例（77→82 全过，0 skip）：权限（data 三接口 A0301 严格）、长度边界（name/code/remark/value 50/51 等）、对抗语料（全角/emoji/零宽/CRLF/XSS/LIKE 通配符）、defaulted 语义、options 排序+label/value 不变量、软删唯一语义、isPreset、性能烟测（<2s CI 容差）
- SDK 契约同步：src/api/dict/model.ts DictTypeForm 补 isPreset?: boolean
- dict-backend 并发修复后的新契约（测试已按其固化）：逻辑删除（软删行占用 uk_type_name/uk_code → 重建同 identity 返回 A0501，value 可复用）、name 唯一性服务层校验（A0501"名称已存在"）、缓存失效 defer_after_commit（测试用 run_after_commit_callbacks 模拟提交）、DictTypeFormVO 增 isPreset、分页接口增 status 筛选、DictTypeForm.name 实际 max 50（文档称 64，矛盾已上报）、sort ge=1（0 拒绝）
- SDK 测试技巧：删除后同 body 重建会触发 A0002 防重复提交（429），body 加 remark 差异绕过；开发库预置类型定位不可用"create_time 最早"法（历史残留干扰），用 keywords="gender" 精确定位

## package-fe3：shop 购买流程补齐 + React package 页类型适配
- front-vue shop（views/package/shop/index.vue）：ElMessageBox 确认升级为购买弹窗——价格明细（原价/促销优惠/券抵扣/应付，calculatePrice 实时算）+ 我的未使用券选择（listMy(1) 后按 applicableScope 前端预过滤：空=全部适用，含 pkg.id 或 packageType）+ 确认携带 couponId 建单跳订单详情。SDK 契约完整支持（calculatePrice(packageId,userCouponId)/OrderCreateForm.couponId/CouponAPI.listMy），无契约缺口。
- React package 页适配积分卡可选类型（pages/package/{shop,list}）：shop 卡片按 packageType 分支渲染（积分卡显示 creditAmount/creditUnitPrice，levelCode/period/levelName 全空值防护），comparison 过滤 vip+levelCode 防 undefined 键；list 等级/周期列 null 显示"—"；PackageFormDialog 补 packageType Radio（编辑禁用）+ 条件化字段（credit→creditAmount 必填；vip→等级/周期/权益区）+ 提交按类型裁剪字段（对齐 Vue 表单）。
- 验证：vue-tsc 全项目 0 错误；React tsc package 页 0 错误（项目其他模块 Dataset/order/favorite 等既有错误不在本任务范围）。
- 遗留发现（React，未改，需决策）：①PackageFormDialog 金额无 元→分 转换（Vue 有 Math.round(*100)，React 直传表单值，若后端契约单位为分则为既有 bug）；②React 表单保留 status Radio（文档规定编辑弹窗隐藏状态字段，上下架走表格 switch）；③React shop 购买仍为简单 confirm 无券选择（本任务范围仅 Vue shop）；④PromotionQuery 分页参数最终演进为 extends PageQuery（pageNum/pageSize），promotion 页 pagination 绑定遗漏的 2 处引用已由我补齐。
- 教训：SDK dist 类型滞后会导致前端 tsc 误报——改 SDK model.ts 后必须 pnpm build 重建 dist 再跑前端类型检查。

### 追加 3 项 React 对齐（同日第二轮）
- PackageFormDialog：金额提交 Math.round(元*100)、编辑回填 /100（对齐 Vue）；移除 status Radio（DEFAULT_FORM/setFieldsValue/表单项全删，payload 本就未含 status）
- React shop：购买对齐 Vue 弹窗（Modal 价格明细+券选择，recalcPrice(pkgId,couponId) 参数化避免 ref，Modal footer 数组写法），yuan() 提为模块级
- 同款 100 倍金额显示 bug 波及面（React 直 toFixed(2) 未 /100）：shop 卡片（salePrice/originalPrice/dailyPrice/creditUnitPrice）、list 列（原价/售价/日均）、SalesStatsDrawer（totalRevenue 统计卡+两个图表 revenue）——全部已修为分转元
- 验证：tsc package 页 0 错误、read_lints 0。
- 【需决策待 lead 汇总】① Pub/Sub dict:invalidate 三端均未实现（T-DM-065 P0）；② 文档逻辑删除口径已由 dict-backend 对齐实现，文档 §3.1/§9.2 现为一致；③ name/code 长度文档 64/32 vs 实现 50/50 矛盾
- 执行互斥流程生效：跑测试前 send_message 报备 main，与 dict-backend 协调测试窗口（dehaze_test 重建有 GET_LOCK 保护但合跑仍会假失败，需错峰）

## package-frontend（套餐管理前端审查与修复，dehaze-front-vue + dehaze-sdk-js）
- 金额单位（分）全面修复：后端套餐/优惠券/统计金额均为分，front-vue 三页原样直显（×100 错误）且表单把元当分提交；已建"接口分/表单元"转换（list handleSubmit Math.round(*100)、coupon 满减/无门槛面值+门槛、统计图表 /100）。折扣券 faceValue 语义=减免百分比（后端 base*(100-face)/100，face=20 即 8 折），页面已注明。
- admin 套餐表单补齐积分卡支持：packageType radio（编辑态锁定）+ creditAmount + 类型条件显隐 vip 字段/权益覆盖区；补校验（name 2-32、原价/售价>0、salePrice≤originalPrice、periodDays≤365）；删除表单内状态开关（文档：新建默认下架、状态只走上下架）；列表加商品类型列/等级|可得积分列/日均价|积分单价列/查询类型筛选。
- shop 页：会员卡/积分卡 Tab（F-PM-005）、积分卡卡片（可得积分大字+每积分单价+立即购买）、权益对比仅会员卡+当前等级列高亮（原 isHighlight 恒 false）、购买确认文案不再谎称"余额支付"（订单详情页选支付方式）。
- SDK：PackageForm/PackagePageVO/PackageDetailVO 的 levelCode/levelName/period/periodDays 改可选（积分卡为 null）；PackageAPI.listOnSale(packageType?) 补过滤参数；dist 已重建。
- 新建 views/package/promotion/index.vue（菜单 id=121 已指向 component 但页面此前缺失会 404）：CRUD+上下架+关联套餐弹窗+进行中活动改规则二次确认，activityRules 契约 {discount_type, discount_value, tiers:[{threshold,faceValue}]}（金额分）。
- 【需决策/协调】①python 促销分页参数 page/size 与全局 pageNum/pageSize 及 SDK 不一致（促销页分页失效，需 package-backend 改）；②促销 API 仅 python 有，Java(权威)/Go 缺失；③文档优惠券 totalQty "-1 不限量" vs python ge=0 矛盾；④front-react package/* 消费同 SDK 类型，可空化后可能需适配（member-frontend）；⑤shop 购买未走价格预览/优惠券选择（文档数据流如此），保留现状待确认。
- 教训：front-vue 的 shop/list 文件在编辑中途被外部还原过一次（部分保留部分丢失，疑并行 teammate 冲突）——package 模块文件改动后务必 vue-tsc 复查+重读关键区域确认最终状态。事后确认系 package-backend 误用 git stash 卷走改动；SDK 两文件已按 lead 指引 `git checkout 'stash@{0}' -- <file>` 恢复并重建 dist，front-vue list/shop 工作区版本完好（关键改动逐项核对+vue-tsc 0 错误）。共享工作区纪律：禁用 git stash/reset 等危险操作。


## 字典管理模块（6/28，2026-09-10 完成，commit 1374d32e7，19 文件 +1357/-138）
- 3 成员。严重修复：①删除物理 DELETE 改逻辑删除（违反文档）②同类型 name 重复 uk_type_name 冲突 500→A0501 含软删 ③类型编码查重不含软删→重建 500 ④缓存失效改 defer_after_commit+CacheService 双清
- 预置字典三层保护（后端 A0503+VO isPreset+前端隐藏删除/批量不可勾选）；status 筛选补齐；dictStore 前端缓存补齐（文档设计未实现）
- 最终态：pytest 全量 2108/0/0、SDK 六模块 336/336
- 待用户决策 3 项：①缓存键 Python dict:options:{typeCode} vs 文档 dict:data:{typeCode}（三端共享 Redis 需统一，Java/Go 联动）②文档声称 dict:invalidate Pub/Sub 三端同步（T-DM-065 P0）三端均未实现——改文档承认各端自管 or 补实现（架构级）③字段长度文档 64/32 vs 实现 50（以实现改文档 or 收紧 Schema）
- 建议：SDK 预置定位法改按种子编码（create_time 最早法不可靠，历史测试残留）
- 教训：pytest 重建 dehaze_test 与其他成员测试需错峰（本轮 dict-backend/test 合跑假失败一次）
- 进度 6/28（权限体系五模块+字典），下一模块候选：会员/套餐/订单/AI计费

## member-frontend：会员管理模块前端审查与修复（dehaze-front-vue + dehaze-sdk-js）
- SDK：model.ts BenefitVO 补后端 _benefit_to_vo 实际返回的 6 类任务月度配额（derain/desnow/lowlight/super_resolution/denoise/inpaint），已 pnpm build 重建 dist。
- list/index.vue：①等级调整弹窗 expireTime value-format 改 "YYYY-MM-DD HH:mm:ss"（后端 _parse_dt 严格 %Y-%m-%d %H:%M:%S，纯日期会 500）；②权益配置抽屉补 AI日/月限额、多模态日限额、VIP月赠积分 4 可编辑列 + saveBenefit 携带（后端 BenefitForm 支持且文档注释强调）；③详情弹窗移除"成长值流水"页签（原调用 /members/growth-logs 当前用户接口，展示的是管理员自己的流水——错误数据；后端无 admin 成长值流水接口）与空占位"操作日志"页签及相关代码/样式；④levelOptions 标签对齐种子（普通用户/VIP1/VIP2/SVIP）。
- growth-logs/index.vue：补 ai_consume 选项；CSS tag-dehaze→tag-process（原 process 类型无样式）+ tag-ai_consume。

## member-fe2：会员管理决策执行（memberStore + 详情 4 页签，2026-09-10）
- memberStore 落地（文档 §3 设计→实现）：dehaze-front-vue/src/store/modules/member.ts（对齐 dictStore 模式：pending Promise 合并并发；loadProfile 缓存复用 / refreshProfile 强刷 / invalidate 失效），注册进 store/index.ts。消费点：package/shop 改用 store（不再自行 getProfile）；member/center profile 走 store（onActivated refreshProfile+benefitSummary 局部）；member/center 签到成功后 refreshProfile；order/detail 支付成功（data.paid 与 handlePaySuccess 两处）invalidate。
- 详情弹窗 4 页签恢复（懒加载 tab-change 时按 loaded 标记请求，open/close 均 resetDetailTabs）：契约以 member-fix 并发落地的 SDK 为准——getAdminGrowthLogs(userId, GrowthLogQuery)→GrowthLogVO[]、getConsumptionRecords(userId, MyOrderQuery)→MyOrderVO[]（复用订单模型）、getBenefitUsage(userId)→BenefitSummaryVO（复用）、getOperationLogs(userId, PageQuery)→MemberAuditLogVO[]（Mongo 审计，action=level_change/growth_change/status_change，beforeValue/afterValue JSON 展示）。中途我曾自拟 MemberConsumptionRecordVO/MemberOperationType 契约，与 member-fix 撞车后被其覆盖，已按其版本对齐页面（教训：并行改同一 SDK 文件需先 send_message 协调）。
- 越权核查：MemberAPI.getDetail 全局唯一调用点是管理端 member/list（管理员页面），用户端仅 /profile、/benefit-summary、/trial-status、/growth-logs、/sign-in* 本人接口——无越权调用点，无需修复。
- 验证：SDK pnpm build 重建 dist + test:unit 61/61；front-vue vue-tsc 本任务文件 0 新增错误（order/detail usedQuota、package/shop PackageOnSaleVO、dataset Dataset 等为存量/他人并发错误）。
- 文档：前端实现.md §3 修正为与实现一致（memberStore 去 signInCalendar、补 load/refresh/invalidate 与失效时机）。API接口.md 4 个 admin 接口尚未补文档定义（member-fix 职责，未代写）。

## member-test：会员管理模块测试审查与强化（team module-review，2026-09-10 完成）
- 盘点：pytest 存量 5 文件（member/member_expiry/growth/benefit/quota service，51 用例）+ SDK member.test.ts（46 用例）；member-router 无 pytest（service 层已覆盖核心分支，router 仅权限装饰器薄层）
- pytest 新增 tests/service/test_member_regression.py 18 用例：get_or_init_member 三段式回归（活跃会员不清零/重复幂等/软删复活保 total_consumption）、成长值阈值边界参数化（4999→5000 升 level_2、19999→20000 恰达 SVIP≥20000 升 level_3、差1不升）、签到断签重置 T-MM-072、同日唯一索引 uk_user_sign_date 兜底（IntegrityError）、字典缺键回退默认值、脏语料 reason（合法 emoji/零宽/CRLF/全半角/150字）+ operator_id 审计、adjust_growth changeValue=0 拒绝、LIKE 通配符转义不变量（total < 全量）、配额 used≤quota 不变量
- SDK 新增 test/modules/member/member-edge.test.ts 10 用例：越权（普通用户改他人 growth/level→A0301）、参数边界（changeValue=0/month=13/pageSize=101→A0400）、脏语料 reason 流水原样+operatorId、LIKE 注入关键字、并发签到恰一次成功（自适应当日已签状态，全 A0512）、性能烟测（列表<500ms、benefit-summary<200ms）
- member.test.ts 修正：删除过时"契约先行"注释（benefit-summary/trial-status 已实现）强化断言（7类明细最低剩余口径、月限≥日限、used≤quota）；trial-status 断言改派生不变量而非固定值（开发库 USER 已有激活体验券+付费订单，硬编码 newUserExclusiveAvailable=true 会假失败）
- member-backend 配合修复：①MemberGrowthAdjustForm/MemberLevelAdjustForm.reason 加 max_length=256（原超长→MySQL DataError 500，我写的 SDK 暴露用例转绿）②update_benefit 后新增该等级会员配额快照分批刷新（§5.6.2 立即生效）
- 测试用例口径教训：①孤立代理码点 "\ud83d\ude00" 在 Python 是非法 Unicode 标量，DB 驱动 UnicodeEncodeError，对抗语料必须用合法码点 "\U0001F600"；②配额快照 monthly_*_quota 仅随等级联动路径刷新，测试直改 level_code 绕过联动时需手动同步快照；③SVIP 阈值 20000 属 level_3（闭区间下限），不要期望 level_2
- 最终态：pytest 6 文件 69/0/0、SDK member 目录 57/0/0（0 skip）；执行前与 member-backend 错峰互认（先跑者完成后交接通知）
- center/index.vue：handleUpgrade 由"即将开放"提示改为跳转 /package/shop（页面已存在）；移除 onMounted（keepAlive 下 onActivated 首次也触发，避免双请求）。
- 【需决策】①文档"详情弹窗 5 页签"（成长值流水/消费记录/权益使用/操作日志）后端均无对应接口，前端仅保留基本信息页签——需后端补接口或改文档；②文档 §3 memberStore（跨页共享会员档案，套餐商城购买按钮依赖当前等级）未实现，shop 页自行 getProfile——补 store 或改文档承认页面局部加载；④GET /members/{userId} 登录态即可查任意用户详情的越权风险（lead 已标注为安全问题呈报）。
- 决策③联动闭环（member-backend 修复 BenefitForm 后）：SDK BenefitForm 补 6 类任务配额字段（dist 已重建）；list/index.vue 权益抽屉补月去雨/去雪/低光/超分/去噪/修复 6 编辑列 + saveBenefit 携带。vue-tsc member 0 错误。

## dict-fix：字典管理决策执行（2026-09-10 完成）
- ①缓存键三端统一：核实发现三端实际均用 `dict:options:`（非任务书假设的仅 Python 偏差），按既定决策统一改代码到文档口径 `dict:data:{typeCode}`（Python dict_service/router-cache/测试、Java SysDictServiceImpl、Go sys_dict.go），旧键零残留
- ②失效广播核实：Python CacheService.delete/delete_pattern 已发布 cache:invalidation 广播（覆盖）；Java dict 走 RedisTemplate 直连无 L1（天然一致，零代码）；Go pubsub 基建在但无人发布/订阅——已补：multilevel 增 WithInvalidationPublisher，Delete 后发布，CacheManager 订阅 "key" 消息清本实例 L1。文档 §5.2/需求规格 §3.10.2/T-DM-065 已改"各端通用广播维护同端多实例 L1 一致性，跨后端不互听"
- ③字段长度对齐 DB（sys_dict_type/sys_dict 均 50/50/255）：Java DictTypeForm name 64→50、code 32→50；Go DictFormBO TypeCode 32→50；Python DictTypeForm code 32→50；文档长度表同步
- ④SDK dict.test.ts：预置类型删除用例改 keywords="gender" 种子编码定位；类型编码边界 32/33→50/51
- 验证：pytest 全量 2108/0/0、mvn compile 过、go build + go test（internal+pkg）全绿
- 遗留观察（已闭环）：文档 §5.1 的 `dict:type:{code}` 缓存行经 lead 批准按"以实现为准"删除（后端实现.md §5.1/§9 + 需求规格.md §3.10.1/§3.10.2 共 4 处），全仓零残留；Java dict 若未来引入 L1 需接入 CacheInvalidationRelay 同款机制
- 注意：本轮 test_cache_router.py 第 88 行编辑曾被外部进程回退（旧键名复活导致一次假失败），重改后恢复——多 agent 并行编辑同一文件需警惕 stale buffer 回写


## member-fix：会员管理决策执行（2026-09-10 完成）
- ①会员卡只升不降：on_order_paid 等级取 max(现等级,卡等级)（benefit growth_min 比较），配额按卡权益/覆盖项与现等级权益较高值刷新；level_source=purchase 先置再算 resolve_card_overrides（修复首购覆盖项不生效）；level_code 仍身兼两职——成长值高等级用户买低级卡，卡覆盖项后续消费路径（get_benefit_summary/reset 按 level_code 查 package）不生效，数据模型级限制仍在
- ②到期回落本已实现（expiry_service 按成长值重算），补回归用例；adjust_level 显式覆盖语义已在文档注明（需求规格 §4.3.1 + 后端实现 §4.9）
- ③管理端 4 接口落地（均挂 member:list）：GET /{userId}/growth-logs（复用 growth_service）、/{userId}/consumption-records（复用 order_service.list_my，MyOrderVO）、/{userId}/benefit-usage（复用 get_benefit_summary）、/{userId}/operation-logs（新增 MongoAuditLogRepository.list_by_target + member_service.list_member_audit_logs，UTC 转本地）；路径与 member-frontend 预置 SDK 方法对齐（消费记录用 consumption-records 非 orders）
- ④详情越权收紧：GET /{userId} 仅本人或持 member:list，否则 A0301（BusinessException）；SDK model.ts 删除文档先行未实现的 MemberConsumptionRecordVO/MemberOperationLogVO/MemberOperationType（零引用），新增 MemberAuditLogVO（id 为 string、action=level_change/growth_change/status_change）
- mongo_audit_log_repository 改为 lazy `from app.dependencies import mongo` 模块引用（对齐 mongo_ai_call_log_repository 模式，conftest mongo_db 单点 patch 可覆盖）
- 测试：pytest 定向 75/75、全量 2128/0/0（基线 2122+新增6）、SDK member 64/0/0；SysMemberSignIn 唯一约束核实已在 1343d319f 落地无需改动
- 通知 member-frontend：详情弹窗 5 页签后端已就绪（4 新接口契约见后端实现.md §4.10），前端可恢复成长值流水/消费记录/权益使用/操作日志页签
- 缓存键三端统一 dict:data:{typeCode}（核实发现三端实际均用 dict:options:，非仅 Python——按决策改代码对齐文档口径，旧键零残留）
- 广播口径：各端通用缓存失效广播维护同端多实例 L1 一致性、跨后端不互听（生产部署单一后端）；Python 已覆盖/Java 无 L1 共享 Redis 天然一致/Go 补齐 multilevel 发布+订阅（cache:invalidation）
- 字段长度以 DB 为准：name/code=50、remark=255 三端 Schema 对齐；dict:type:{code} 未实现缓存描述 4 处删除
- 最终态：pytest 2108/0/0、SDK 六模块 336/336
- 字典模块累计 2 commit（1374d32e7+7d1c25ce1）。进度 6/28 完成；下一模块：会员管理（7/28）
- 教训：多 agent 并行编辑同一文件有 stale buffer 回退风险（dict-fix 遇一次）；任务书假设需成员核实纠正（三端键名一致而非仅 Python 偏差）

## member-backend：会员模块后端审查与修复（2026-09-10）
- 修复：quota_service Redis/DB 一致性（扣减 Lua 带 expected 校正漂移、落库失败 INCR 回补、restore 存在性守卫防无 TTL 持久键）；adjust_level 校验目标等级存在+expireTime 容错（A0400）；sign_in 并发双签由唯一索引兜底转 A0512；BenefitForm 补 6 个缺失月度配额字段；reason 加 max_length=256；update_status 操作人统一参数传递；_invalidate_member_cache 仅在等级联动时清 benefit:all
- 修复：会员卡覆盖（benefit_overrides）合并逻辑收敛为 quota_service.resolve_card_overrides（消除 member_service/quota_service 两份重复）；on_order_paid 履约按覆盖值与等级权益较高值刷新配额快照（原仅按 benefit）；update_benefit 后分批(500)刷新该等级活跃会员配额快照（§5.6.2 立即生效）
- 文档：后端实现.md §4.6 同步落库+回补、§6 quota 缓存 TTL 改"至次月 1 日 0 点"、§3.3 权益修改刷新快照
- 遗留（member-test 用例 bug 已回报）：①regression upper_boundary 期望 20000→level_2 与需求 §2.1 SVIP≥20000 矛盾（应为 level_3）；②dirty reason 用孤立代理对 \ud83d\ude00 非法 Unicode 致 utf-8 编码失败（应改 \U0001F680）；③quota_invariant setup 直改 level_code 未同步配额快照（真实路径均会刷新）
- 待决策：on_order_paid 低等级会员卡购买会把成长值高等级会员降为卡等级，与需求 §3.1.1"会员卡期间享有较高者"矛盾，但数据模型无独立卡等级字段，未擅改


## 会员管理模块（7/28，2026-09-10 完成，commit 1343d319f，17 文件 +852/-193）
- 3 成员。P1 修复：①配额 restore incr 无 TTL 持久键（永久误拦）②Redis 扣减落库失败无回补（Lua expected 校正+INCR 回补）③会员卡覆盖值未并入履约/权益快照（按较高值刷新）④BenefitForm 缺 6 类配额字段
- P0 前端：详情弹窗成长值流水冒充目标会员（后端无 admin 流水接口、前端误用登录态接口）→移除页签；权益抽屉 8 类配额编辑前后端闭环
- 最终态：pytest 全量 2122/0/0、SDK 七模块 393/393
- 待用户决策 5 项：①【重点·数据模型级】on_order_paid 低等级会员卡购买会把成长值高等级会员降级，违反需求 §3.1.1"较高者"语义——level_code 身兼卡/成长两职，需数据模型口径（如独立 card_level 字段或履约时 max 计算）②admin 侧成长值流水等 4 接口文档设计了后端全无（详情弹窗 5 页签设计）③memberStore 跨页共享会员档案未实现（shop 页自行 getProfile）④GET /members/{userId} 与 /benefits 登录态即可读任意会员详情（含 totalConsumption/frozenReason）——文档如此设计，是否收紧 ⑤SysMemberSignIn ORM 未声明唯一约束（行为正确，模型与 schema 不一致）
- 观察：member-router 无 pytest（薄层，SDK 覆盖）；进度 7/28；下一模块候选：套餐/订单/AI计费


## 套餐管理模块（8/28，2026-09-10 完成，commit 137b1b78f，40 文件 +2763/-442）
- 3 成员+package-fix 收尾+lead 代修。严重修复：①金额单位分/元全面错误（展示+表单双向）②admin 无法创建积分卡③促销管理页缺失但菜单已挂载（404）→新建④【跨模块】benefit_overrides key 失配（套餐写 camelCase/会员履约读 snake_case→覆盖项静默失效）——quota_service+member_service 三处统一 camelCase（lead 代修，package-backend 中断未完成）
- 促销分页实际 page/size 与全局 pageNum/pageSize 不一致（按实现封装，待决策统一）；totalQty -1 不限量语义按实现修订文档
- 最终态：pytest 全量 2175/0/0、SDK package 93/93
- 【重要教训】package-backend 误用 git stash 与队友交错卷走工作区（事故已闭环：SDK 2 文件恢复+front-vue 核查完好）；共享工作区严禁 git stash/reset——已全员广播；stash@{1} 为历史旧 stash（61 文件 AI 测试，勿动）
- 待用户决策 7 项：①覆盖合并语义（文档 max vs 套餐展示侧直接覆盖+仅 9 字段，扩字段需 Java 同步）②batch_distribute all 场景同步单事务 vs 文档异步任务 ③Java 促销接口全缺+PackagePageVO 缺 3 字段（三端对齐大项）④促销分页参数统一 ⑤禁用券错误码 Java COUPON_NOT_FOUND vs python BUSINESS_ERROR ⑥shop 购买流程缺价格预览/优惠券选择 ⑦React 端 package 类型适配
- 进度 8/28；下一模块：订单管理（9/28）


## 套餐模块决策执行（package-fix2/fe3，2026-09-10，commit cd8685d94，41 文件 +1952/-281）
- 7 项决策全部落地：权益覆盖展示侧 max+18 字段（python+Java）、Java 促销 6 端点补齐、Go 促销 6 端点补齐（三端一致）、Java PackageForm 补积分卡类型字段（原无法创建）、促销分页统一 pageNum/pageSize（三处联动）、Java 禁用券码+新用户专享校验对齐、batch_distribute 文档降级同步
- shop 购买流程补价格预览+优惠券选择（Vue+React 双端）；React 修复金额双 bug（显示/提交 100 倍）
- 最终态：pytest 2175/0/0、SDK package 93/93、go 全绿、mvn compile 通过
- 套餐模块累计 2 commit（137b1b78f+cd8685d94）。进度 8/28 完成；下一模块：订单管理（9/28）

## ts-plugin 幽灵报错的判定方法（2026-09-10，order/detail「PayRequest 无 channel」案例）
- 现象：IDE 报 `order/detail/index.vue:497 类型"{ payMethod: PayMethod; }"上不存在属性"channel"`，但 `npx vue-tsc --noEmit` 全项目 0 错误。
- 判定手法（一步确认）：用 LSP hover 该变量，得到 `{ payMethod: PayMethod }`；再 `goToDefinition` PayRequest → 指向 `dehaze-sdk-js/dist/index.d.ts:5326`，而该行当前实际是 `BalanceRefundAuditForm.channel`（PayRequest 实际在 5359）——行号偏移即证明 ts-plugin 内存里持有的是旧版 dist 内容（dist 已 rebuild 但插件未重读，dist 属 gitignore 目录不被 watcher 关注）。`touch dist/index.d.ts` 无法触发重读。
- 结论与处置：源码/SDK dist 均正确（src model.ts:79-85 与 dist 5359 均含 `channel?: "wechat"|"alipay"`，dist/index.js 21:08 晚于 src 19:31 即最新构建），代码无需改动，**重启 TS 服务器**（命令面板 "TypeScript: Restart TS Server" / 重载窗口）即消失。
- 排查顺序固化：IDE 报 sdk-js 类型错 → ①`vue-tsc --noEmit` 复现？②不复现则 hover+goToDefinition 看行号是否偏移 → 直接判为插件缓存，不要改代码。注意 IDE 报错还可能是并发 teammate 正在编辑的中间态（本次 vue-tsc 首跑曾报 algorithm/list 的 `algorithmStatusMap` 不存在，1 分钟后同一文件被队友改完即消失）。

## order-frontend：订单管理前端审查与修复（dehaze-front-vue + dehaze-sdk-js）
- 金额分/元同款 bug 全量修复（对齐 package 模块 yuan() 模式）：my/detail/list/refund 四页所有金额直显 toFixed(2) 未 /100（列表、详情明细、支付流水、退款金额、统计卡 totalRevenue/totalRefund、ECharts 两图 revenue、支付弹窗）——后端 sys_order 全链路单位为分。
- 严重功能修复：①refund 页权限标识 order:refund:audit→order:refund:approve（原按钮对所有人隐藏）；②detail 套餐"到期"误用 expireTime（支付超时时间+30min）→packageExpireTime；③amountMin/amountMax 表单元→提交分（Math.round*100，原直接把元当分过滤恒空）；④usedQuota 幽灵列（后端无此字段）→usedDays/usedCredits 条件渲染。
- 补齐文档设计缺口：detail 支付弹窗轮询（3s getDetail，paid/completed 即停+memberStore.invalidate，弹窗关闭/卸载清理）；list 商品类型筛选 packageType；refund 原因类型筛选 reasonType；my 页移除语义错误的原价删除线（用户端 MyOrderVO 无原价字段，paidAmount 冒充原价）；credit 单 packageLevel 空 tag 加 v-if。
- SDK（model.ts/index.ts，dist 已重建，vue-tsc 全项目 0 错误）：新增 RefundReasonType/BalanceAccountVO 类型；RefundRecordVO+reasonType、OrderDetailVO+balanceAmount、OrderQuery+packageType、RefundQuery+reasonType、OrderStatsVO+packageTypeDistribution/refundReasonDistribution（对齐 python 实际返回）；OrderAPI 补 getBalance()（GET /orders/balance，文档有 SDK 缺）。
- 【需决策上报】①API接口.md 缺 POST /orders/recharge（后端+SDK 均已实现，文档漏）；②我的订单页文档设计含 PaymentDialog（二维码/倒计时），实现简化为跳详情页支付；③支付倒计时（expireTime 基础）文档设计但未实现，本轮只补轮询未加倒计时。

## order-backend：订单管理模块后端审查与修复（dehaze-python，2026-09-10）
- 严重修复 5 项：①pay 余额/组合支付加订单级锁 payment:lock:{orderNo}+锁内 _ensure_payable 复检+complete_payment 幂等守卫（status!=1 跳过）——原并发双击余额支付可重复冻结+重复履约；②_restore_status_for_order 反转 bug（未到期回 completed/已到期回 paid，与文档相反），修正为未到期→paid(2)、已到期或 credit→completed(3)；③complete_payment 回写流水 channel 为实际支付渠道（组合支付预写 combined、回调修正为 wechat/alipay），修复组合支付退款 _channel_refund 拿到 "combined" 必抛"不支持的支付渠道"；④自动续费订单支付成功回写续费配置（auto_renew_repository.mark_renewed：next_renew_time=会员叠加后 expire_time、fail_count=0）——原 wechat/alipay 半自动续费用户付款后下轮扫描仍重复建单；auto_renew_service balance 成功分支去掉自己的非叠加到期时间回写（防提前重复续费）；⑤组合支付取消/超时关单遗漏（仅 wechat/alipay 关单）→ _close_channel_order 统一从支付流水解析实际渠道关单。
- 次要修复：退款率改订单数口径（退款成功订单数/支付成功订单数，需求 §6.4.2，原为金额比）；RefundApplyForm 删文档外遗留 reason 字段（customReason 拼接契约保留）；apply_balance_refund 补 amount>余额拒绝+待审核申请唯一性（新 repo 方法 get_pending_by_user_id）；auto_renew 折扣 int(round()) 防浮点截断；get_detail 缓存 VO 移除冗余 _admin 视角标记（userId 归属校验已足够，命中条件 user_id is None or cached.userId==user_id）；审计 after_value 无效三元简化；详情响应不外露内部标记。
- 补文档缺口行为：complete_payment 补支付成功站内信通知（后端实现 §4.3 步骤 8 原未实现）。
- 测试：订单域 9 文件 92/0/0（test_order_service/state_machine/payment/refund/auto_renew/recharge/reconciliation/permission_fix）；改动 3 个测试文件（order_service mock 补 list_by_order_id、state_machine 去 _admin 断言、auto_renew 新增 test_mark_renewed_updates_config+balance 续费改断言 complete_payment 负责回写）。
- 【需决策】①组合支付第三方渠道无法确定：PayRequest 仅 payMethod/balanceAmount，unified_order("combined") 实际抛"不支持的支付渠道"——组合支付下单链路本身不通（退款已修但下单仍不通）；SDK PayRequest 无 balanceAmount、Java 无 balanceAmount 实现，三端契约缺失，建议 PayRequest 增 channel 字段（wechat/alipay）；②dehaze-front-react 退款申请只传自由文本 reason 未传 reasonType（必选），按契约后端会 422，前端需修；③dehaze-front-vue 已按 reasonType+customReason 对齐（front-vue-tsc-fix 已做）。
- 协作：test_order_state_machine 缓存 _admin 观察项由 order-test 提出、我按"标记冗余直接删除"闭环（未采用分视角键方案，VO 内容两视角一致无需分键）。
- 缺陷 2 闭环（order-test SDK 暴露）：get_pending_by_user_id 加 order_by(id.desc()).limit(1) 防御历史多条 pending（开发库 user_id=5 有 9 条残留 → Multiple rows 500）；开发库 sys_balance_refund 该 9 条已幂等置 status=3+error_message（pymysql 直连 .env，UPDATE 非删，无脚本残留）；已通知 order-test 复跑。


## 订单管理模块（9/28，2026-09-10 完成，commit 5ae7e14a6，20 文件 +1385/-166）
- 3 成员。严重修复：①并发重复支付（余额路径无锁无复检双击双履约）→订单级锁+复检+幂等守卫 ②自动续费配置不更新致重复扣款→回写真实到期时间 ③退款状态恢复条件写反 ④组合支付退款必失败（channel=combined）→回写实际渠道 ⑤开发库 sys_order.package_level/period_days NOT NULL 漂移致积分卡下单 500（config/sql 正确，开发库 ALTER 修正）⑥余额退款 scalar_one_or_none 多行 500→limit(1)+历史数据清理
- front-vue P0：金额 ×100 四页+图表+筛选双向修复、退款按钮权限标识错误全隐藏、到期时间字段误用
- 最终态：pytest 全量 2212/0/0、SDK order+package+member 239/239
- 待用户决策：①组合支付契约缺口（PayRequest 无 channel 字段，三端 SDK/前端/文档联动）②React 退款表单缺 reasonType 必传 ③Java/Go 订单端 balanceAmount/combined 语义未核实 ④【设计不一致】等级口径：会员卡 max(现等级,卡等级)不按成长值 vs 积分卡触发成长值重算直升——a)会员卡也按成长值比 b)积分卡不触发重算（严格 T-OM-022）⑤文档 4 小项（recharge 接口漏写/PaymentDialog 简化/倒计时未实现/keepAlive 不符）⑥测试套餐清理策略（已支付订单 A0522 阻塞删除，建议 cleanup 软删兜底）
- 进度 9/28；下一模块：AI计费管理（10/28）


## 订单模块决策执行（order-fix，2026-09-10，commit cfb10f756）
- 6 项落地：PayRequest 补 channel+balanceAmount（组合支付契约闭环，python 流水直写真实渠道）；React 退款 reasonType 修复；Java/Go 契约对齐（combined 完整链路缺失登记，依赖余额账户子系统）；等级口径文档统一（成长值累积必触发升级+购买只升不降）；文档 4 小项；cleanup 软删兜底
- 最终态：pytest 2214/0/0、SDK order 82+package 93

## billing-test：AI 计费管理模块测试审查与强化（team module-review，2026-09-10）
- pytest 新增 tests/service/test_billing_edge.py 19 用例（并发不超扣 quota 10×3000 恰 1 成功/1200 并发 1 积分恰扣满 1000、月超限 Lua 整体回滚日侧、余额并发防负余额恰 3 成功、坏缓存 "100.00" ResponseError 自愈、限额恰好/差1边界、check_budget 滚动预算 3 用例（原零覆盖）、审核驳回不回补、脏语料 refund reason 原样、跨月账单隔离）+ router 扩展 12 用例（adjust amount=0/越权/脏语料 operator_id、stats/anomalies/costs/cost-stats/reconcile 权限矩阵、bills 月份校验、balance 性能烟测）。计费域 120/120 全绿。
- SDK 新增 test/modules/billing/billing-edge.test.ts 19 用例（分页边界 pageNum=0/pageSize=101、summary year A0400、2099-13 A0400/2026-1 strptime 接受空账期 A0401、records 非法日期严格 A0400（新行为）、adjust 脏语料 reason 回查、amount 1.5 A0400、成本四接口 A0301、性能烟测）；存量 billing.test.ts 退款重复用例改选 credits>0 记录且 amount=可退上限（适配退款上界校验）。billing 目录 69 过/1 暴露性失败/0 skip。
- 【暴露缺陷】POST /ai-billing/adjust 不存在 userId 无存在性校验：balance_service._increase_cas 读到 None 静默 return（MySQL 零写入）但 Redis INCRBY 累加+写幽灵 sys_ai_credit_log（实测 1→2→3→4 累加），危害所有 increase 调用方（充值/退款回补/VIP 赠送）。**已闭环（lead 派修复）：adjust 前置用户存在性校验 A0401 + _increase_cas/_deduct_cas None 抛 RuntimeError 全链路加固，SDK billing 70/70 全绿、幽灵行 0 条**。
- 需决策：①API接口.md §4 A0403 全模块未使用（越权实际统一 A0301）②downloadBill SDK 注释 Blob vs 实际 JSON 信封 ③"2026-1" 非零填充月份 strptime 接受（无害）。
- 技巧：fakeredis+lupa 支持 Lua eval 可写并发原子性用例；strptime("%Y-%m") 接受非零填充月份；db_query.py 需用 dehaze-python/.venv/bin/python 跑（系统 python3 无 pymysql）；SDK 暴露性用例写脏数据后记得清理开发库。
- 订单模块累计 2 commit（5ae7e14a6+cfb10f756）。进度 9/28 完成
- 全局遗留专项（收尾批次处理）：Java/Go combined 完整链路、React PaymentDialog 组合支付、时间格式三端、React onBizError、createTime SDK 类型、swag 生成失败、算法段位冲突等
- 任务列表重规划（用户确认）：剩余=基础模块 10（AI计费→反馈评价→收藏→推荐→消息通知→文件管理→任务管理→语音交互→AI模型→AI知识库）+核心 8（去雾/图像输入/效果对比/数据集/算法管理/算法选择/MCP/AI对话）+收尾专项


## AI计费管理模块（10/28，2026-09-10 完成，commit fc013ab06，26 文件 +940/-142）
- 3 成员。高危修复：①退款无限重复回补（同记录循环申请通过）→A0681+上界 ②幽灵用户充值（MySQL 零写 Redis 累加永久背离）→存在性校验+CAS None 抛异常全链路 ③中文 token 预估低估 5 倍→保守估算 ④SDK AnomalyRecordVO 臆造类型重写 ⑤billingDataStore 单例 scope 覆盖→键控多实例 ⑥model/actualModel 语义颠倒
- 最终态：pytest 全量 2249/0/0、SDK billing 70/70
- 待用户决策（去重 8 项）：①tool_llm/kb_inject 计费入口文档声称代码无（sys_ai_billing.tool_credits 无写入方）②asr/tts 秒数/字符数存 input_tokens 污染 token 统计 ③配额 fail-open（无权益=(0,0)无限额）vs 需求日限 10000——fail-closed 拍板 ④cost-stats 文档称按模型/供应商分组后端仅时间范围 ⑤SDK ModelCostForm Create/Update 类型应拆分（update 静默忽略多余字段）⑥sys_menu 134/135 perm=NULL 菜单级权限策略（全局）⑦anomalies 字段契约文档补记 ⑧A0403 文档错误码未用应改 A0301 + downloadBill 注释漂移 + 非零月份可接受（3 小项文档）
- 进度 10/28；下一模块：反馈评价（11/28）

## billing-fix：AI计费模块 8 项决策执行落地（python+SDK+front-vue+doc，2026-09-10）
- 8 项全部落地：①tool_llm/kb_inject 文档删超前设计（后端实现.md §4.3/§9、需求规格 §1.4/§2.4.5/§2.4.6/§2.10.2、测试用例 T-AB-017/018/019 改口径；tool_credits 字段保留）；②asr/tts 计量污染：summary 固定 bill_type=chat（repository 两聚合方法补过滤参数）、管理员统计缓存命中率改 chat 条件聚合（func.if_），文档标注"input_tokens 存秒数/字符数非 token"+独立计量字段列语音模块待办；③配额 fail-closed：get_limits 返回 tuple|None（None=无会员/权益未配置/停用），check_quota/pre_deduct None→False，5 个调用方同步（pre_charge 权益缺失提示"AI 权益未生效"、settle 异常检测 or (0,0)、ai_schedule_executor fail-closed 拒执行、hooks middleware/路由余额展示 or (0,0)）；level_0 种子核实=10000/100000 与需求 §2.8.2 一致未改；④cost-stats 补 groupBy=overall|model|provider+modelId/providerId 过滤（分组行仅 dimension+cost，订单实收无法归因），SDK getCostStats 重载（分组→CostGroupVO、整体→CostStatVO 保持必填，勿把整体口径字段可选化——AdminOverviewBoard 会连环报错），front-vue 供应商下钻接真实分组（表格改供应商ID+成本两列）；⑤SDK ModelCostForm 拆 ModelCostUpdateForm（四字段），updateCost 类型收敛，front-vue saveCostVersion 显式构造 update 载荷；⑥sys_menu 134/135 核实：全库 type=1/2 节点 perm 一律 NULL（权限码只挂按钮）+预置菜单禁改 perm+role 2 已授权 134-139 → 符合惯例零改动（lead 已接受关闭）；⑦API接口.md 补 §5 关键契约（/anomalies detail/status 三态/triggerAt+四类规则、/cost-stats 分组契约、summary 口径）；⑧A0403→A0301 修正+downloadBill 注释 JSON 化+非零月份行为注明
- 测试反转/新增：test_billing_module（fail-open 锁定反转+missing benefit 哨兵 _DEFAULT 修 _quota_svc benefit=None 默认歧义）、test_billing_summary asr/tts 排除、test_billing_cost 分组+非法 groupBy、test_ai_schedule_executor fail-closed、SDK billing groupBy=provider 正向+user A0400
- 最终态：pytest 全量 2258/0/0（基线 2249+9）、SDK billing 71/0/0、SDK build 过、front-vue billing 相关 vue-tsc 0 错
- 教训：SDK 静态方法重载更具体签名必须放前面（宽签名在前会先命中）；改 SDK 类型后需 pnpm build 重建 dist 再跑 front-vue vue-tsc（stale dist 误报）


## 计费模块决策执行（billing-fix，2026-09-10，commit b99d37f18）
- 8 项落地：fail-closed（权益缺失拒绝，get_limits None 语义；等级限额 0=管理员显式不限额）、cost-stats 分组（分组仅 dimension+cost 不虚构收入）、asr/tts 统计改 chat-only、tool_llm/kb_inject 文档删、SDK 类型拆分、菜单 perm 惯例核实（type=1/2 节点一律 NULL，权限码只挂按钮——134/135 符合惯例零改动）
- level_0 种子 ai_credits 10000/100000 与需求一致
- 最终态：pytest 2258/0/0、SDK billing 71/71
- 计费模块累计 2 commit（fc013ab06+b99d37f18）。进度 10/28 完成；下一模块：反馈评价（11/28）
- 观察：ai_conversation_repository 会话级 token 聚合仍含 asr/tts（审计口径，语音模块批次处理）；Java/Go 无 AI 计费模块（python 权威现状）

## feedback 决策执行（4 项，2026-09-10 收尾轮）
- ①响应时间统一小时：Java FeedbackStatsVO Long→Double + 毫秒÷3,600,000 保留 2 位；Go 本就 .Hours() float64 无需改；front-vue stats 标签"分钟"→"小时"；需求规格 §4.4.2 补口径。②Go ReplyFeedback 补未分配自动分配（assignee_id+assigned_time）。③replyType 三端改可选 + 值域校验（合法值 info/resolved/unsupported/dev_transfer，非法 A0400；python Literal/Java @Pattern/Go 白名单），需求规格 §4.2.4 "必填"→"可选"。④A0503 三端口径核实一致（非本人统一 OPERATION_NOT_ALLOW），零改动。
- 验证：pytest feedback+ai_feedback 32/0/0、SDK feedback 83/0/0（新增非法枚举 A0400 用例）、Java mvn compile 过、Go build+test 过。
- main 收尾修复：TaskListDrawer.vue 补 TagType import（front-vue-dedup 遗漏，vue-tsc 曾报 TS2304）；vue-tsc 全量复验 0 错。反馈评价模块完成（11/28）。

## 进度与提交（2026-09-10）
- 本轮 3 commit：11caa0c85 feat(feedback) 审查修复+决策执行、9247275d5 refactor(front-vue) Label/TagType 全局治理、b99d37f18 feat(billing) 决策执行（billing-backend 自行提交：fail-closed、cost-stats groupBy、menu 134/135 核实 type=1/2 节点 perm NULL 为惯例零改动）
- 模块清单确认：基础 19 + 核心 8；已完成 11（认证/用户/角色/部门/菜单/字典/会员/套餐/订单/AI计费/反馈评价）；剩余：文件管理(F-FM-001)/消息通知(F-MN-001)/任务管理(F-TM-001)/收藏管理(F-FAV-001)/推荐管理(F-REC-001)/语音交互(F-VS-001)/AI模型管理/AI知识库(F-KB-001) + 核心模块 8 个（AI对话/MCP/去雾处理/图像输入/效果对比/数据集管理/算法管理/算法选择）
- 12/28 文件管理已启动 file-squad（单人全视角模式，重点：安全维度——上传校验/路径穿越/下载越权/Content-Type 伪造）

## file-squad：文件管理模块审查修复（12/28，commit ebbe314b7）
- 三端安全修复：Java 下载路径遍历校验+文件名 basename 化（../evil.txt 原样入库）、@TableLogic 逃逸（type 搜索泄露已删文件）；Go FindPage 补软删过滤+路径遍历校验、错误码统一 B0401/B0402/B0404；python 孤儿清理补 48h 阈值（原会误删上传事务窗口期文件）、sanitize_filename 长度校验、分页双列匹配、Content-Type 按扩展名。Java multipart 50→100MB。pytest 全量 2300/0/0（+36）、SDK file 37/0/0（+16）。
- 【需决策·未改】①签名 URL 下载鉴权（B0407）三端未实现，现状登录用户持 objectName 可下载任意文件无归属校验（IDOR）；②business_type 列/按类型限额/md5 秒传入参/分片上传断点续传均未实现；③"元数据独立"与 MD5 唯一单记录+复活实现矛盾（三端+SDK 全按单记录）；④MIME 内容嗅探未实现（SDK 已固化行为）；⑤file:meta:{fileId} 缓存未实现；⑥孤儿清理 xxl job 仅 python 有。file-squad 建议 4-6 按文档标注规划中处理。

## file-squad 决策执行（2026-09-11，commit 8644c7a01，文件模块收官 12/28）
- 越权修复：三端 list/download/delete/detail 补归属（普通用户 create_by=自己、ROOT/ADMIN 全量、越权 B0407），check 秒传豁免（md5 命中即同内容）；附带修复 Java download permitAll 匿名可下载、Go 上传不落 createBy（sys_file/sys_item_file/sys_dataset_item 调用方同步）、Go download 移入 protectedV1（local 后端直链现需登录态，仅开发环境）。
- 图片 magic bytes 轻量校验三端（B0403，jpg/png/gif/bmp/webp 验魔数，非图片不校验）。URL 直链现状登记：MinIO public read/nginx-static 无鉴权，依赖 object_name 含 MD5 不可枚举。
- 测试：pytest 文件 82/0/0、全量 2319/0/0；SDK file 45/0/0。剩余模块 15：消息通知/任务管理/收藏/推荐/语音交互/AI模型/AI知识库 + 核心 8 个。

## 文件管理模块修复（12/28，file-squad，2026-09-10）
- 三端代码修复：①文件名校验三端统一 python 口径（basename 化剥路径前缀 + 拒绝 `/:*?"<>|` 与 \x00-\x1f + name≤100/ext≤20，防 varchar(100) 入库 500）——go validateFileName 重写+api 层回写 Filename、java FileDTOFactory.populateFileDTO 补校验、python 补长度；②go file repo 四查询补 deleted=0（FindPage 修复已删文件泄露 + OR 逃逸、FindByObjectName/ByID/ByIDs）；③java 分页 .like().or() 逃逸 @TableLogic 修复（.and 包裹）——MP or 逃逸经典坑；④java multipart 50MB→100MB + MaxUploadSizeExceededException→B0402；⑤go 新增 B0401/B0402/B0404 码（此前 A0401/A0702/无校验，与 python/java B 系列对齐）；⑥下载 Content-Type 三端按扩展名推断（python/go 此前固定 octet-stream）；⑦java/go 下载补 `..` 路径遍历校验；⑧孤儿清理任务补 48h 保留阈值（此前 list_objects 无 mtime 直接删，会误删"存储成功但 DB 未写"窗口期文件）——StorageService.list_objects 契约改返回 (name, mtime)；⑨python delete 不存在改 B0401
- 测试：pytest 新增 36（tests/service/test_file_service.py 对抗文件名语料+真实 local 存储 upsert 复活/秒传/孤儿阈值，tests/router/test_file_router.py 路径遍历/Content-Type/超限）；SDK file.test.ts 21→37 用例（路径遍历剥离、控制字符 undici percent-encode 传输假象、超长、零宽/emoji/GBK 乱码、伪装扩展名、下载已删文件、详情已删 B0401、性能烟测）
- 最终态：pytest 全量 2300/0/0、SDK file 37/0/0、go build/java compile 过
- 文档：API接口.md（DELETE 改 query 参数、check 响应=文件信息或 null、下载 Content-Type 描述）、需求规格 3.4.3、测试用例 T-FM-032/033、后端实现 §4.5 文件名统一口径
- **待用户决策（文档超前于实现，未擅改）**：①签名 URL 下载鉴权（§4.3/B0407）三端均未实现，现登录即持 objectName 可下载他人文件；②business_type 全概念未落地（表无列/无分类限额/上传无 md5 秒传入参/分片上传断点续传未实现）；③需求"元数据独立"与实现"MD5 唯一单记录+复活"矛盾；④MIME 内容嗅探未实现（SDK 已固化当前行为）；⑤file:meta 缓存未实现；⑥孤儿清理 job 仅 python 有，java/go 无对应 xxl job
- 教训：pytest 事务模式下 identity map 持有旧状态（ODKU 复活后同对象 deleted 仍=1），断言用列查询；undici FormData 将文件名控制字符 percent-encode 后传输，SDK 层无法测真实控制字符文件名（pytest 层覆盖）

## 文件管理决策项执行（2026-09-10 第二轮，用户拍板 4 项）
- ①归属校验三端落地：普通用户仅可见/可操作 create_by=自己 的文件（admin 全量），越权 B0407（python/java/go ResultCode 均新增）。作用面 list/download/delete/detail；check 秒传预检豁免（md5 命中即返回）。python router 注入 UserContext + repo get_page owner_id 参数；java SecurityUtils.isAdmin + page wrapper eq(createBy)；go security.IsAdmin/RequireUserID + repo FindPage ownerID *int64（interfaces 同步）；go UploadFile 签名加 createBy（sys_file/sys_item_file/sys_dataset_item 三调用方同步，uploadFormFile 加 c 参数）；java/go download 收口登录（java 删 SecurityConfig download permitAll、go download 移入 protectedV1）
- ②magic bytes 三端：图片扩展名（jpg/jpeg/png/gif/bmp/webp）文件头魔数校验，不符 B0403；非图片不校验。python validate_image_magic_bytes；go service 内 ReadFull(12)+MultiReader 拼回；java FileDTOFactory 读 tempFile 头（java switch 箭头语法）
- ③MinIO/nginx-static URL 直链现状（python config 注释已明确 bucket public read）登记进文档 §3.2.4/§4.3/§8.1；local baseUrl 指向下载接口在登录收口后直链需登录态（登记，仅开发环境）
- ④文档：需求规格（§3.1.2 参数/§3.1.4 现行口径/§3.1.5 分片规划中/§3.2 下载/§3.3.3 删除语义/§3.4.1 check 豁免）、后端实现（§4.1/4.2/4.3/4.5/4.7/7/8/10 全面对齐现状）、API接口（B0403/B0407 错误码）、测试用例（T-FM-023/024/025 规划中、031 复活、045 单记录、新增 052 越权/053 伪装）
- 测试：pytest 文件相关 82 过（新增越权 7 + magic bytes 12）、SDK file 45/0/0（新增越权 6 + magic 3）、全量 pytest 2319/0/0、java/go 编译过。8991 uvicorn reload 自动加载新代码，SDK B0407/B0403 断言直接生效
- 教训：直接调 FastAPI router 函数的既有测试，新加依赖参数（user: UserContext = Depends(...)）默认值是 Depends 对象，所有调用方测试须显式传参
