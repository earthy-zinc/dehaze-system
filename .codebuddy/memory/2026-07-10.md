# 2026-07-10

## 统一后端配置修改

批量修改了 Go/Python/Java 三个后端项目的数据库、Redis、MongoDB、MinIO 等连接配置：

### 修改内容
- **host**: `earthyzinc.devcloud.woa.com` → `localhost`（Docker 运行在本地）
- **密码**: 统一使用 `${DEHAZE_PASSWORD}` 环境变量引用，实际值 `12345678`
- **JWT 密钥**: 三端统一为 `SecretKey012345678901234567890123456789012345678901234567890123456789`，通过 `JWT_SECRET_KEY` 环境变量注入
- 每个子项目添加了对应的 `.env` 文件

### 验证结果
| 项目 | 端口 | 状态 | 关键日志 |
|------|------|------|----------|
| Go | 8990 | 启动成功 | MySQL主库已连接, Redis Pub/Sub订阅成功, RabbitMQ连接正常 |
| Python | 8991 | 配置加载成功 | DB/REDIS/RabbitMQ/MinIO URL 全部正确解析 |
| Java | 8989 | 启动成功 | Started in 9.338s, MongoDB/Redis bootstrapped, Tomcat on 8989 |

### 额外修复
- Python: 修复 `DatasetIdPath` 等死代码导入
- Java: 添加 `spring-dotenv` 依赖、修复 KafkaConfig `ProducerListener` 编译错、RabbitMQPublisher `DeliveryMode` API 变更、补充 captcha font 配置
- 启动了 rabbitmq 容器以支持 Go 项目

## 密码统一为 12345678 (2026-07-11 凌晨)

MinIO 要求密码 >= 8 位，`123456` 太短。将 `DEHAZE_PASSWORD` 统一改为 `12345678`。

### 操作步骤
1. 四个 .env 文件（根、go、python、java）全部改为 `DEHAZE_PASSWORD=12345678`
2. Java `application-dev.yml` MinIO secret-key 改回 `${DEHAZE_PASSWORD}`（统一变量引用）
3. 停止并删除所有旧容器
4. docker-compose.yml 中 mysql/redis/mongo/minio volumes 添加 `external: true`（复用旧数据）
5. `docker compose --env-file .env up -d` 重建容器
6. MySQL 旧数据密码为 `123456`，执行 `ALTER USER` 改为 `12345678`
7. Redis/MongoDB/MinIO/PostgreSQL 均直接用 `12345678` 验证通过

### 最终验证
| 项目 | 状态 | MySQL | Redis | MongoDB | MinIO | PostgreSQL |
|------|------|-------|-------|---------|-------|------------|
| Go | 启动成功 | 主库已连接 | Pub/Sub OK | — | — | — |
| Python | 启动成功 | 配置正确 | 配置正确 | — | 配置正确 | — |
| Java | 启动成功 | 连接池就绪 | Redisson OK | MongoClient OK | — | — |

### 重要提醒
- docker-compose.yml 中基础服务 volumes 已设为 `external: true`，确保不会因重建丢失数据
- 密码统一为 `12345678`，通过 `DEHAZE_PASSWORD` 环境变量管理
- Python 使用 `uv run` 启动，Java 用 Maven spring-boot:run 启动

## API 一致性修复 (2026-07-11)

通过对比 API 设计规范，修复了 Go/Python/Java 三后端的 API 不一致。

### 响应格式统一
| 修复项 | Go | Python | 说明 |
|--------|----|--------|------|
| Response 添加 traceId/timestamp/errors | 已修复 | 已修复 | Java 参考实现含这些字段 |
| NoAuth 返回 200 而非 401 | 已修复 | — | 与其他响应一致 |

### 错误码纠正
- Python `ResultCode` 中 A0230/A0231 原定义为"验证码错误/过期"，规范定义为"token无效/禁止"，已修复
- 删除 `CAPTCHA_ERROR`/`CAPTCHA_EXPIRED`，改用 `VERIFY_CODE_ERROR`/`VERIFY_CODE_TIMEOUT`
- auth.py 中 `JSONResponse` 直接构造改为使用正确的 ResultCode

### 分页参数统一
- task.py: `page`/`size` → `pageNum`/`pageSize`（匹配 Java 和规范）
- dataset_item.py: pageSize 上限从 200 改回 100

### 数据完整性
- MySQL `dehaze` 数据库 18 张表全部完好
- 239,974 条数据项、539,982 条文件记录无损

### Go 代码重构
- `pkg/app/app.go` → `internal/app/app.go`（应用启动代码属于 internal）
- 删除 `service/filter/` 空目录
- `internal/model/` 结构清晰（sys_*.go DB模型 + bo/dto/vo/query/read/enum子包），保持不变

### Python HTTP 方法修正
- `role.py`: `/status` PUT → PATCH, `/menus` PUT → PATCH（部分更新语义）
- `user.py`: `/password` PUT → PATCH（密码修改）
- `exceptions.py`: `RequestValidationError` 响应体增加 `errors` 详情列表

### URL 复数统一（暂缓）
- `/dept`→`/depts`, `/algorithm`→`/algorithms` 暂缓（需前端/SDK 同步配合）
