# 2026-09-11

## uk 软删治理（方案A：deleted=行id + 唯一键含 deleted）— Java 端落地（uk-java）
- **delval 实测结论**：MP 3.5.5 源码验证（TableInfo.formatLogicDeleteSql 将 delval 原样拼接进 SQL，非参数绑定）+ 单测 TableLogicDelvalTest 3/3：`@TableLogic(value="0", delval="id")` 生成 `UPDATE t SET deleted=id WHERE id=#{id} AND deleted=0`；**前提 deleted 字段必须非 CharSequence**（String 会生成 `deleted='id'`），故 33 个实体 deleted Integer→Long（bigint 对齐）
- 33 实体 @TableLogic 统一改 `@TableLogic(value = "0", delval = "id")`；yml 三处（dev/prod/test）global-config `logic-delete-value: 1 → id`（MP 3.5.5 starter 中 GlobalConfig 完全取自 yml properties，bean 不被消费）；entity.java.vm 模板同步
- 删除 MybatisConfig.globalConfig() 死代码 bean（3.5.5 autoconfigure 不消费 GlobalConfig bean，MetaObjectHandler 走 @Component 自动装配）
- 判重改活跃行（原生 SQL 判重全删）：role name/code（wrapper 嵌套 or）、dict_type code、dict value、package name、algorithm version、dept 同级名称；SysMemberBenefitMapper/SysMessageTemplateMapper 的 countBy*All 系列本就无调用方，连 XML 一起清空
- **白名单保留**：sys_user.username 三处调用（admin saveUser/updateUser + **AuthServiceImpl register 补齐**——旧唯一键靠 DB uk 拦截、新唯一键必须应用层拦截，注册已注入 SysUserMapper 查全表）；Java 无 sys_ai_provider 代码，第二白名单不适用
- FavoriteServiceImpl.deleteByIds 手写 `.set(getDeleted, 1)` → `.setSql("deleted = id")`；4 个 upsert mapper（member/favorite/autoRenew/memberQuota）删掉死的 `deleted = 0` 复活写入并修正注释（新唯一键下冲突只可能命中活跃行）
- 清理 job 无 deleted=1 读点（均走 wrapper 物理删）；无现有单测断言"删后重建被拒"
- 验证：mvn -o compile/test-compile 通过；surefire 296 跑 293 过，3 错为 FileDTOFactoryTest 图片魔数校验存量失败（与本次无关，相关文件零改动）；新增 src/test/java/com/pei/dehaze/model/entity/TableLogicDelvalTest.java 回归测试
- 遗留：①6 个空 XML（SysDict/SysDictType/SysPackage/SysAlgorithmVersion/SysMemberBenefit/SysMessageTemplate Mapper.xml）删除需审批，暂存为空 mapper 存根；②.tmp-mpsrc 临时目录删除需审批；③dehaze-doc 多处文档仍写旧语义（deleted=1/逻辑删除 1:已删除），待 uk-governance 统一更新

## uk 软删治理（方案A）— Go 端落地（uk-go）
- 迁移 SQL 基准：`config/sql/migration/2026-09-11_uk_deleted_aware.sql`（37 表，deleted 改 bigint，唯一键追加 deleted）
- dehaze-go 改动（13 处软删写点）：`Update("deleted", 1)` / map `"deleted": 1` → `gorm.Expr("id")`，涉及 announcement/message/dataset/favorite/dept/role(×2)/user/file/coupon/package/promotion/user_coupon 各 repository
- 判重口径：9 个 `Exists*` 方法移除 `Unscoped()` 改为只查活跃行（role name/code、user mobile/email、algorithm version、message_template code、dict value、dict_type code、member_benefit level_code、package name）；`user ExistsByUsername` 保留 Unscoped 白名单（软删用户名永久不可复用，用户决策），已加注释。Go 端无 sys_ai_provider 代码，第二白名单不适用
- 模型：34 个 model 文件 Deleted 字段 int8→int64、type tinyint→bigint、注释统一为「(0:未删除;>0:已删除,值为删除时的行id)」；gofmt 已跑
- 读点：`message_service.go` `msg.Deleted == 1` → `!= 0`；pkg/job 清理任务无 deleted=1 读点（物理删按 expires_at/create_time）
- `handlers_user_test.go` newUser 参数 int8→int64 适配
- 验证：go build / go vet 全绿；go test ./... 全量通过

## main 协调记录（uk 治理三端）- uk-go 已提交 0956df053（74 文件）并关闭；uk-java 已提交 341acf671（69 文件）并关闭——@TableLogic delval="id" 源码级验证+单测锁定，register 路径 username 白名单缺口补齐（旧 DB uk 隐性拦截随新键消失），surefire 293/296（3 个 FileDTOFactoryTest 魔数存量失败为文件轮遗留待查）
- uk-governance（schema 37 表 + python 9 写点 + 白名单 2 处 + 迁移 + 文档统一旧语义 + pytest 全量收口）进行中
- 今日累计 38 commit

## uk-governance：方案A全量落地完成（用户确认 + 白名单分层）

**全量 pytest 2575 passed / 0 failed**。未 commit。

### 落地内容
1. **schema 61 表**：deleted tinyint→bigint（COMMENT: 0未删除;>0已删除,值为删除时的行id），37 表 41 个唯一键追加 deleted 列（索引名不变）。白名单 sys_user.username / sys_ai_provider.provider_code 保留"不可复用"（应用层查全表判重），其余 35 表放开复用。
2. **迁移 SQL 定稿** `config/sql/migration/2026-09-11_uk_deleted_aware.sql` 三段式：①61 表修数 SET deleted=id WHERE deleted=1（防新唯一键同键冲突，必先执行）→②61 表 MODIFY bigint→③41 条索引重建。一次性执行不可重跑。
3. **python**：base.py soft_delete_by_ids 写行 id；9 处手写 .values(deleted=1)→Model.id；7 处读点 deleted==1→!=0（conversation/memory 回收站清理、auto_renew/order、mcp_manage 复活判断）；判重兜底移除 12 处（role/dict/mcp_server/voice_provider/voice_model/skill/package/ai_model，改为活跃行查重）；复活类 include_deleted 保留（favorite/feedback/member/eval/endpoint/mcp_manage_preset）；model_cost/price 版本号全历史递增保留。**踩坑记录**：同一文件并行 replace_in_file 会相互覆盖（role_repository 漏改一处），同文件多处编辑必须串行；查软删行的列查询也需 include_deleted=True（with_loader_criteria 对列级 select 同样生效）。
4. **测试**：role 删后重建同名/同码 3 用例、dict 2、ai_model 2、package 2、favorite 2（重收藏改插新行语义）、file 2、voice_admin 1 反转；eval/endpoint 复活 4 用例。test_send_expires_at_by_type 为时间边界既有 flaky（全量中已通过）。
5. **文档**：03-数据库设计 §5.3 字段规范（bigint/行id）+ §5.3.1 定稿（统一口径+白名单表）+ §5.3.2 演进论证存档；32 个模块文档 + Go 架构文档旧语义（deleted=1/类别①②/不可复用/历史记录占用）全部改写。
6. **协同**：uk-java 33 实体 @TableLogic(delval="id") 已完成；uk-go 进行中。favorite/file 的 ON DUPLICATE upsert 复活分支在新键位下自然退化为普通插入（保留无害）。

## uk 治理全量落地收官（决策⑥闭环，2026-09-11）
- 三端提交：Go 0956df053（74 文件）/ Java 341acf671（69 文件）/ schema+python+文档 51730f2b9（146 文件）+ 迁移修复 00f0f7b27 + role SDK 语义翻转 ff0470ac4
- **开发库迁移执行记录（main）**：①迁移 SQL 有生成 bug——40 处唯一索引 deleted 列重复（`name, deleted, deleted`→1060），已修正；②执行顺序必须是 改列→数据修复→重建索引（先修数据会因 tinyint 溢出失败 id>127）；③朴素分号切分会破坏 COMMENT 内分号，且会漏 sys_ai_agent 语句（原因未深究，已手动补齐 MODIFY/UPDATE/INDEX）；④sys_ai_credit_log 开发库缺 deleted 列已按 schema 补列；⑤终态：61 表 bigint、293 行补行 id、40 唯一键含 deleted、软删后重建 PASS（事务验证）；⑥"残留 deleted=1" 检查口径注意：id=1 的软删行 deleted=id=1 属正常
- SDK 端到端抽验：role 旧语义用例（断言软删后不可重建）按新语义翻转为"可重建+活跃重复仍拒 A0501"，59/59；8991 已重启
- pytest 全量 2575/0/0。**决策执行轮 7/7 全部闭环**。今日累计 43 commit，进度 20/28，核心模块剩 7（MCP/去雾处理/图像输入/效果对比/数据集管理/算法管理/算法选择）
- 待查登记：FileDTOFactoryTest 图片魔数 3 个存量失败（Java 侧，文件轮遗留）
