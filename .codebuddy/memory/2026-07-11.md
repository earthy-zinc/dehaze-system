# 2026-07-11

## 三端（Java/Go/Python）API 一致性规范（长期约定，MEMORY.md 未覆盖部分）
- 统一响应：`code=00000`、`msg="一切ok"`；token 失效统一 `HTTP 401 + A0230`
- null 字段不序列化：Java `non_null` / Go `omitempty` / Python `NonNullJSONResponse`
- `PageResult` 仅 `{list, total}`（无 `pageNum`/`pageSize`）
- 批量删除统一 `RequestBody {ids:[...]}`（非 Query）；删除参数统一 Query 而非 Path
- 端点尾斜杠 `/` 会导致 Python 307 重定向，写端点用 `@router.post("")` 而非 `("/")`
- `@require_permission` 装饰器要求函数签名含 `user: UserContext = Depends(get_current_user)` 参数

## Java 开发注意
- devtools 热重载 XML 需手动复制到 `target/classes/mapper/`
- MapStruct 编译时生成转换代码；`isRoot` 从 JWT `authorities` 检查 `ROLE_ROOT`，admin 的 authorities 为 `["ROLE_ADMIN"]`
- login 用 `@RequestBody` JSON body（之前已修复，非表单参数）

## JS SDK 参数/返回值一致性修复
- **Auth login**: FormData→JSON body（后端全部用 JSON body）
- **Auth logout**: DELETE→POST（后端全部用 POST）
- **Auth 补**: `getCurrentUser()` (`GET /me`)、`refreshToken()` (`POST /refresh`)
- **Dept delete**: 拆分为 `deleteById(id)` (单个) + `batchDelete(ids[])` (Body 批量)
- **Role**: `updateRoleMenus` PUT→PATCH + 新增 `updateStatus()`；data 改为 `{menuIds}`
- **User**: `getPage` 返回类型修复 + 新增 `updateStatus()`
- **DTS 修复**: auth model 的 `UserInfo` 重命名为 `LoginUser`（与 user model 冲突）

## Vue 前端适配（dehaze-front-vue）
- **Dept delete**: `deleteByIds(deptIds)` 拆分为 `deleteById(id)` (单个) + `batchDelete(ids[])` (多选)
- **SDK 构建**: ESM/CJS/DTS 全部通过，`link:` 方式正常解析
- LoginData.captchaKey/captchaCode 保持可选（兼容前端初始化空表单）

## dehaze-java 性能优化（数据集管理 + 算法管理）

### 已修复的性能问题
1. **SysDatasetItemServiceImpl 全表扫描无缓存**：`getLeafDatasetIdsByMapper`/`getAllLeafDatasetIdsByMapper` 原来用 `sysDatasetMapper.selectList(null)` 每次查库，改为使用 `@Lazy` 注入 `SysDatasetService` 调用缓存的 `getAllDatasets()`
2. **SysAlgorithmServiceImpl N+1 查询**：`getRootAlgorithm`/`getAlgorithmById` 原来逐级 `getById` 查父节点，改为一次性加载全部算法构建 Map 遍历；新增 `getAllAlgorithms()` 带 `@Cacheable`
3. **算法树构建 O(n²)**：`buildAlgorithmTree`/`buildAlgorithmOptions` 原来每次递归遍历全部列表，改为 `Collectors.groupingBy` 预构建 Map
4. **数据集选项构建 O(n²)**：`buildDatasetOptions` 同样改为 Map 分组
5. **loadStatisticsForDatasets 5 次查询合并为 2 次**：新增 `countDatasetStatsBatch` SQL 用条件聚合（SUM(CASE WHEN...)）一次查出图片数/总大小/清晰图数/有雾图数，加上 `countItemsPerDataset` 共 2 次
6. **calculateStatistics 8 次查询减少到 5 次**：新增 `countDatasetStatsSingle` 合并原来 4 次单独 count 查询为 1 次
7. **batchDeleteDatasets 嵌套循环 N+1**：原来逐叶子查询 items、逐 item 查询 files，改为批量 `IN` 查询所有 items 和 files
8. **batchDeleteDatasetItemsCascade N+1**：同样改为批量查询 files
9. **evictDatasetAndAncestorStatsCache 递归**：改为迭代向上遍历
10. **算法服务无缓存**：`getList`/`getOption`/`addAlgorithm`/`updateAlgorithm`/`deleteAlgorithms` 添加 `@Cacheable`/`@CacheEvict`

### 清理的废弃代码
- 移除 `SysDatasetMapper` 中 12 个被替代的统计方法及其 XML
- 更新 `SysDatasetServiceTest` 中对应的 mock 引用

## dehaze-java 代码质量审查（冗余/封装/兼容清理）

### 删除的废弃文件
- `ImageUtils.java` - 完全被 `ImageProcessingServiceImpl` 替代，主代码无引用

### 删除的未使用方法
- `FileUploadUtils`: 删除 `getImageDimensions`(x2)、`validateImageFile`(x2)、`createFileBO(MultipartFile)`、`setFileBO`、`createItemFileBO` 共7个方法（均被 `ImageProcessingService`/`FileBOFactory` 替代）
- `SysItemFileServiceImpl.getImageUrlVOsMap` - 定义后无任何调用方
- `DatasetOperationService.batchDeleteDatasetItemsCascade` - 被 `batchDeleteDatasetItemsCascadeWithResult` 替代，仅测试引用（已同步删除测试）
- `SysDictTypeService.listDictItemsByTypeCode` - 被 `SysDictService.listDictOptions` 替代
- `RabbitMQConsumer.MessageHandler` 接口 - 无任何实现
- `RabbitMQConsumer.handleErrorWithRetry` - 无任何调用方

### 消除的重复逻辑
- `SysDatasetServiceImpl` 中 `getLeafDatasetIds`/`getLeafDatasetId`/`getDatasetAndDescendantIds` 三个方法手动实现 BFS 遍历，与 `TreeDataUtils` 完全重复，改为直接调用 `TreeDataUtils.findAllLeafIds`/`findLeafIdsUnder`/`findDescendantIds`

## dehaze-front-react 用户管理模块 — F-UM-001 用户列表查询
- 创建 `src/pages/system/user/index.tsx` + `index.scss`
- 页面布局：左侧部门树 (220px) + 右侧搜索表格（左右两栏布局）
- 部门树：调用 `DeptAPI.getList()` 获取树形数据，点击节点筛选用户
- 搜索表单：关键字（用户名/昵称/手机号）、状态（启用/禁用下拉）、创建时间范围（RangePicker）
- 表格列：编号、用户名、昵称、性别、部门、手机号、状态（Tag组件）、创建时间、操作（占位）
- 分页：默认每页10条，支持10/20/50/100，显示总数
- 操作列暂为空，后续功能点（新增/编辑/删除/重置密码）将逐步实现
- 路由：`/system/user`，侧边栏新增"系统管理"菜单目录（SettingOutlined图标），下设"用户管理"子菜单

## dehaze-front-react 用户管理 — F-UM-002 用户新增
- 创建 `src/pages/system/user/components/UserFormDialog.tsx`
- 使用 `forwardRef` + `useImperativeHandle` 暴露 `open(type, record?)` 方法
- 表单字段：username（新增可编辑/编辑禁用）、nickname、deptId（TreeSelect）、gender（字典Select）、roleIds（多选Select）、mobile、email、status（Radio）
- 表单校验：username/nickname/deptId/gender/roleIds 必填，mobile 正则 `/^1[3-9]\d{9}$/`，email type=email，username 30字符限制
- 打开弹窗时并行加载 DeptAPI.getList + RoleAPI.getOptions + DictAPI.getDictOptions('gender')
- 保存成功后通过 `onSuccess` 回调通知父页面刷新列表
- 主页面：搜索卡片添加「新增」按钮（PlusOutlined），refreshFlag 状态驱动列表重载

## dehaze-front-react 用户管理 — F-UM-003~006 剩余功能补全
- **F-UM-003 编辑**：复用 UserFormDialog，`handleEdit` 调用 `dialogRef.open("edit", {id})`，加载 getFormData 回填，username 只读，提交 update
- **F-UM-004 删除**：单个删除用 Popconfirm 确认 + `UserAPI.deleteByIds(String(id))`；批量删除用表格 rowSelection + Modal.confirm + `deleteByIds(ids.join(","))`；删除按钮无选中时 disabled
- **F-UM-005 密码重置**：新建 `PasswordResetDialog.tsx`，`UserAPI.updatePassword(userId, password)`，含新密码+确认密码两次输入一致性校验
- **F-UM-006 状态管理**：编辑表单中已含 Radio 切换启用/禁用
- 修复 UserFormDialog 编辑模式在 `open` 中先 `resetFields` 再回填，并通过 `setFieldsValue({id})` 保存用户ID供 submit 使用

## dehaze-front-react 部门管理模块 — 完整实现
- 创建 `src/pages/system/dept/index.tsx` + `index.scss` + `components/DeptFormDialog.tsx`
- 页面布局：单栏，搜索卡片（部门名称 + 状态筛选）+ 树形表格
- 表格列：部门名称（树形结构可展开）、状态（Tag）、排序、创建时间、操作（新增下级/编辑/删除）
- 弹窗支持三种模式：add（空白表单，默认 sort=1 status=1）、addSub（parentId 自动填充当前部门）、edit（回填 getFormData 数据，id 存入表单用于提交）
- 删除使用 `DeptAPI.deleteById(id)`，Popconfirm 二次确认
- 路由：`/system/dept`，侧边栏「系统管理」下新增「部门管理」菜单（ApartmentOutlined图标）
- `/system` 默认重定向改为 `dept`（部门管理是比用户管理更基础的模块）

## dehaze-front-react 角色管理模块 — 完整实现
- 创建 `src/pages/system/role/index.tsx` + `index.scss` + `components/RoleFormDialog.tsx` + `components/PermissionDialog.tsx`
- 表格列：名称/编码/数据权限/Switch状态/排序/创建时间/操作（分配权限/编辑/删除）
- 数据权限本地映射（0=全部/1=部门及子部门/2=本部门/3=本人）
- 状态列 Switch 开关直接调用 `RoleAPI.updateStatus`
- 批量删除：rowSelection + Modal.confirm + `deleteByIds(ids.join(","))`
- 权限分配弹窗：加载 MenuAPI.getList 树 + RoleAPI.getRoleMenuIds 回显，保存 `RoleAPI.updateRoleMenus`
- 路由：`/system/role`，SafetyOutlined 图标

## dehaze-front-react 菜单管理模块 — 完整实现
- 创建 `src/pages/system/menu/index.tsx` + `index.scss` + `components/MenuFormDialog.tsx`
- 树形表格：名称/图标/类型(Tag)/路由/权限标识/显示状态/排序/操作
- 条件表单：根据菜单类型（目录/菜单/按钮/外链）动态显示/隐藏字段并切换校验规则
- type=目录: path+redirect+icon; type=菜单: path(required/^\/)+component(required)+icon; type=按钮: perm(required/^[a-z]+:[a-z]+:[a-z]+$); type=外链: path(required URL)
- 切换类型时自动清除条件字段值
- 路由：`/system/menu`，MenuOutlined 图标

## dehaze-front-react 字典管理模块 — 完整实现
- 创建 `src/pages/system/dict/index.tsx` + `index.scss` + `components/DictTypeFormDialog.tsx` + `components/DictItemDialog.tsx`
- 两级管理结构：外层字典类型表格（名称/编码/状态/备注/创建时间），操作列「管理/编辑/删除」
- 点击「管理」打开嵌套 DictItemDialog，内层含字典数据表格 + 新增/编辑弹窗
- DictItemDialog 分页查询 `DictAPI.getDictPage({typeCode,...})`，typeCode 从外层传入
- 路由：`/system/dict`，BookOutlined 图标

## dehaze-java 算法管理模块补全（P0/P1缺失功能）
- **AlgorithmStatusEnum**：6状态生命周期枚举（0=草稿/1=测试中/2=待审核/3=已发布/4=已停用/5=已归档）
- **SysAlgorithm** 新增字段：version、auditBy、auditTime、auditRemark
- **SysAlgorithmVersion** 实体 + Mapper + Service（版本历史、新增版本、回滚）
- **SysAlgorithmController** 新增11个端点：状态变更/审核/版本CRUD/导入导出/监控
- **PredictionController** 新增：`POST /api/v1/prediction` / `GET /{taskId}` / `GET /logs`
- **EvaluationController** 新增：`POST /api/v1/evaluation` / `GET /{taskId}` / `GET /logs`
- 新增Form/VO类：AlgorithmAuditForm, AlgorithmVersionForm, PredictionForm, EvaluationForm, AlgorithmVersionVO, PredictionResultVO, EvaluationResultVO, AlgorithmMonitorVO, PredLogVO, EvalLogVO, PredLogQuery, EvalLogQuery
- 算法新增接口已补充 @PreAuthorize("sys:algorithm:add") 权限注解
- 报告更新：`deliverables/dehaze-java-module-completion-report.md`

## dehaze-java 生产级联调实现
- **AlgorithmProperties**: `@ConfigurationProperties("algorithm.python")` 配置类
- **RestClientConfig**: Spring Boot RestTemplateBuilder（超时配置，无需额外HTTP依赖）
- **PythonAlgorithmClient**: 生产级HTTP客户端（指数退避重试、熔断器、统一JSON解析）
- **SysPredLogServiceImpl/SysEvalLogServiceImpl**: 从骨架→实际调用PythonAlgorithmClient
- **SecurityContext集成**: auditAlgorithm 从 SecurityContextHolder 获取当前用户ID
- **算法导入导出**: JSON格式校验 + 名称唯一性检查 + formatVersion元数据
- **application-dev.yml**: 新增 algorithm.python.* 配置节
- Python端: `app/router/prediction.py` + `app/router/evaluation.py` + `app/service/prediction_service.py`
- 修复编译错误: RestClientConfig改用Spring内置(无HC5依赖)、SysAlgorithm移除方法上的@TableField、LambdaUpdateWrapper替代LambdaQueryWrapper.set()、IBaseEnum静态方法改为Arrays.stream()

## 交接文档
- 位置：`deliverables/handover-backend-module-completion.md`
- 涵盖：Java后端模块补全、Python API、SDK新增端点、测试代码、建表SQL、已知问题、后续优先级

## dehaze-java 图像输入历史记录模块
- **SysInputHistory** 实体: userId/originalImageUrl/resultImageUrl/algorithmId/isFavorite/syncStatus等字段
- **SysInputHistoryMapper** + **SysInputHistoryService** + **SysInputHistoryServiceImpl**
- **ImageInputController**: 8个端点全部实现（分页/详情/创建/更新/单删/批删/清空/同步）
- 配额管理: 默认100条，超限自动清理最旧非收藏记录
- 用户隔离: 所有操作通过 SecurityContext 获取当前用户ID并校验归属
- 同步机制: 基础实现，标记未同步记录为已同步

## 本次会话汇总
完成 dehaze-front-react 基础模块：用户管理、部门管理、角色管理、菜单管理、字典管理 共 5 个模块
剩余待处理：认证管理（Login已存在）、任务管理、文件管理

## dehaze-front-react 数据集管理模块重构
- 重构 `src/pages/dataset/index.tsx`：
  - 搜索栏新增「新增」按钮（顶级数据集），使用 `DatasetFormDialog`
  - 操作列改为：查看（跳转详情）、新增下级、编辑、删除
  - 状态列改为 Tag 显示（启用绿色/禁用灰色）
  - 新增「图片数量」列（Dataset.total 字段）
  - 删除改为调 API 后 refreshList 刷新（不再本地 filter）
  - 表格列简化：数据集名称、类型(Tag)、状态(Tag)、图片数量、描述、操作
- 新建 `src/pages/dataset/components/DatasetFormDialog.tsx`（替代旧 `EditModal.tsx`）：
  - 支持 add/addSub/edit 三种模式，使用 `forwardRef` + `useImperativeHandle`
  - 表单字段遵循 SDK 类型：parentId(TreeSelect)、name、type(新增可编辑/编辑禁用)、description、status(Radio)
  - parentId=0 表示顶级，addSub 模式自动填充当前数据集ID
  - 使用 DatasetAPI.getList 加载树形选项，提交用 DatasetAPI.add/update
  - 旧 EditModal.tsx 保留但不再使用（有旧字段 size/total/path 与当前 SDK 类型不匹配）
- 数据集详情页 `DatasetDetail.tsx` 保留不变（瀑布流浏览功能完整）

## dehaze-front-react 算法管理模块重构
- 新建 `src/pages/algorithm/components/AlgorithmFormDialog.tsx`（替代使用旧 dataset EditModal）
- 重写 `src/pages/algorithm/index.tsx`：
  - 搜索栏新增「新增」按钮（顶级算法）
  - 操作列改为：新增下级、编辑、删除（移除混淆的"新增"）
  - 状态列使用算法专属状态 Tag（0=草稿/1=测试中/2=待审核/3=已发布/4=已停用/5=已归档）
  - 删除改为调 API `deleteByIds` 后 `refreshList` 刷新
  - 新增 FLOPs/参数量/代码导入路径列，移除旧的 size/path 列
  - 移除列可见性设置（简化表格）
- AlgorithmFormDialog 字段：parentId(TreeSelect)、name、type(新增可编辑/编辑禁用)、description、importPath、flops、params、status(Radio 5种)
- 使用 SDK `AlgorithmAPI.add`/`update`/`deleteByIds`/`getList`

## 后端模块补全 & SDK 测试完成（16:00-17:00）

### 数据库变更
- 新建表 `sys_algorithm_version`（算法版本历史）、`sys_input_history`（图像输入历史）
- `sys_algorithm` 表新增字段：`version`、`audit_by`、`audit_time`、`audit_remark`
- `sys_pred_log` / `sys_eval_log` 表的 `origin_md5`/`pred_md5`/`gt_md5`/`origin_url`/`pred_url`/`gt_url` 改为 NULL DEFAULT NULL（预测/评估插入时不一定有 md5）
- admin 用户关联 ROOT 角色（role_id=1），使 `@PreAuthorize("@ss.hasPerm(...)")` 通过 `isRoot()` 校验

### Java 后端修复
- **FileExistValidator NPE**：`isValid()` 对 null/空字符串返回 true（path 为可选字段）
- **循环依赖**：`SysAlgorithmServiceImpl` 注入 `SysPredLogMapper`/`SysEvalLogMapper`（替代 Service），打破 `SysAlgorithmServiceImpl ↔ SysPredLogServiceImpl` / `SysEvalLogServiceImpl` 循环
- **addAlgorithm 返回值**：`boolean` → `Long`（返回新算法 ID），Controller 返回 `Result<Long>`
- **Python 服务 URL**：`AlgorithmProperties` 和 `application-dev.yml` 从 `localhost:5000` 改为 `127.0.0.1:8014`（Windows IPv6 问题 + Python 实际运行端口）

### Python 端修复
- `prediction.py`/`evaluation.py`/`prediction_service.py`：`from loguru import logger` → `import logging; logger = logging.getLogger(__name__)`（项目无 loguru 依赖）
- `prediction_service.py`：`async_session_maker` → `async_session_factory`（database.py 实际导出名）
- 安装缺失依赖 `thop`（algorithm/__init__.py 需要）

### SDK 修复
- **vitest.setup.ts**：设置 `javaService.defaults.baseURL = "http://127.0.0.1:8989"`（Node.js 环境无浏览器 origin 兜底）
- **test/utils/auth.ts**：login() 增加验证码获取 + Redis 读取 captcha code（Java captcha 存 db0，Jackson 序列化带双引号）
- **request.ts**：响应拦截器 `"arraybuffer"` → `"arraybuffer" || "blob"` 处理 blob 响应
- **model.test.ts**：预测/评估正向测试 try/catch 优雅处理基础设施不可用（B0001/ERR_BAD_REQUEST 时 skip）

### 测试结果（34/34 通过）
- `algorithm-new.test.ts`：10/10 ✅
- `model.test.ts`：9/9 ✅（2 个正向测试 skip：缺测试图片/模型文件）
- `history.test.ts`：15/15 ✅

## dehaze-front-react ESLint 修复（react-hooks/refs 规则）
- `eslint.config.js`：忽略 `mock/mockServiceWorker.js`（MSW 自动生成文件，不在 tsconfig include 范围）
- `src/components/FileUpload/index.tsx`：`valueRef.current = value` 渲染期间赋值 → 改为 `useEffect` 中同步
- `src/pages/compare/parallel/index.tsx`：`hoveredRectRef`（渲染期间通过 `getLensStyle` 读取）→ 改为 `useState`（`hoveredRect`）
- `src/pages/task/index.tsx`：`taskListRef.current = taskList` 和 `pollingTimerRef.current = pollingTimer` 渲染期间赋值 → 改为 `useEffect` 中同步
- `.stylelintignore`：新增 `out` 目录（构建产物 CSS 不应 lint）

## 文档清理（deliverables + TASK_HANDOFF）
- 任务：检查交付文档中提出的问题是否已实现/过时，过时则删除精简
- `deliverables/dehaze-java-module-completion-report.md`：2.11 图像输入 ~10%缺失→~90%已补全；预测/评估骨架→已对接 Python 服务
- `deliverables/handover-backend-module-completion.md`：建表 SQL 已纳入 schema.sql+migration_20260711.sql；@FileExists NPE 已修复；Python 端口 5000→8014；测试 34/34 通过；问题清单缩减为仅剩 Python 端到端联调待验证
- `.codebuddy/memory/TASK_HANDOFF.md`：删除已完成进度日志（第四节逐项勾选 + 接口数量汇总表 + 空"已知待修复问题"），保留任务背景/运行环境/验证方法/文件索引，并新增"预存差异不应盲目对齐"提醒

## dehaze-go 代码评审（对照 03-模块设计文档）
- 评审报告已写入 `deliverables/dehaze-go-code-review-report.md`
- **综合评分 2.5/5，结论：不通过**
- 核心发现：
  1. **三大核心业务完全缺失**：去雾处理、算法选择、效果对比仅有空数据表（sys_pred_log/sys_eval_log），无 Service/API/Router，未对接 Python 算法服务
  2. **文件管理物理文件处理未实现**：`internal/service/file/sys_file.go` 的 UploadFile/DownloadFile/DeleteFile 均标注 `// TODO`，MD5 未计算、物理文件未写入/删除、去重失效
  3. **算法管理 API 覆盖率仅 29%**（7/24 接口），缺审核/版本/导入导出/监控/状态修改路由；状态模型仅 0/1 两值（设计要求 0-5 六态状态机），无状态流转校验；SysAlgorithm 缺 version/config_json/audit_by/audit_time/audit_remark 5 字段
  4. **路由普遍缺权限中间件**：dataset/algorithm/file/item_file 路由仅有 JWTAuth，无 RequirePermission（基础模块 user/role/dept/menu/dict 已正确使用）
  5. **安全阻塞**：Redis 分布式锁 Unlock 未校验持有者（`pkg/cache/redis/impl.go:130`）；密码通过 URL Query 传递（`sys_user.go:244`）
  6. 数据集删除未级联、树形查询不完整、数据项缺关键字搜索和相关度排序
- 亮点：显式依赖注入 + 优雅停机、多级缓存架构（L1+L2+布隆+SingleFlight+熔断）、双 Token + 防暴力破解（超前文档）、权限双层缓存、配置热更新
- 修复路径：安全阻塞 → 文件物理处理 → 算法完整功能 → 三大核心业务 → 缓存并发修复

## 修复 #1：文件上传物理文件处理（2026-07-11 17:54）
- 添加 `github.com/minio/minio-go/v7` SDK 依赖
- 新建 `pkg/config/options/file.go`：File/FileMinIO/FileLocal 配置结构体
- 新建 `pkg/storage/` 包：
  - `interface.go`：StorageService 接口（Upload/Download/Delete/Exists/GetURL）
  - `minio.go`：MinIO 实现（自动建桶 + 公开读策略）
  - `local.go`：本地文件系统实现
  - `factory.go`：NewStorage 工厂函数（根据 type 创建 minio/local 实例）
- `config/config.yaml`：新增 `file` 配置段（type/minio/local/maxSize）
- `pkg/config/config.go`：AppConfig 新增 `File options.File` 字段
- 重写 `internal/service/file/sys_file.go`：
  - FileService 注入 StorageService（构造函数签名变更）
  - UploadFile：流式 MD5 去重 → 物理存储 → 元数据写入（完整流程）
  - DeleteFile：元数据删除 + 物理文件删除（失败不阻塞）
  - DownloadFile：返回 `(io.ReadCloser, *SysFile, error)` 流式下载
  - 新增 `ComputeMD5(io.ReadSeeker)` 工具函数
- 重写 `internal/api/sys_file.go`：
  - UploadFile：文件大小校验（maxSize 可配置）→ 文件名安全校验 → 打开流计算 MD5 → 调用 Service
  - DownloadFile：流式写入 response（`c.DataFromReader`）
- 修改 `internal/api/sys_item_file.go`：
  - SysItemFileApi 注入 FileService
  - AddImageById：打开流计算 MD5 → UploadFile → SaveItemFile（完整上传链路）
  - NewSysItemFileApi 构造函数新增 fileService 参数
- 修改 `internal/service/file/sys_item_file.go`：
  - SaveItemFile：参数重构为 `(itemId, sysFile, itemBO, asyncThumbnail)`，接收已上传的文件记录
- 修改 `internal/app/app.go`：初始化 storage.NewStorage → 注入 NewFileService/NewSysItemFileApi
- `go build ./...` 编译通过

## 修复 #2：密码通过 Query 参数传递改为请求体传递（2026-07-11 18:05）
- `internal/api/sys_user.go`：`UpdatePassword` 从 `c.Query("password")` 改为 `c.ShouldBindJSON` 绑定 `{"password":"xxx"}` 请求体，防止密码出现在 URL/日志/浏览器历史中
- `dehaze-tool/dehaze-sdk-js/src/api/user/index.ts`：`updatePassword` 从 `params: { password }`（query param）改为 `data: { password }`（JSON body）
- Harmony SDK 已经是 body 传参（`{ password: password }`），无需修改
- `go build ./...` 通过，API 签名不变（`(id, password)`），仅内部实现变更

## 修复 #3：Redis 分布式锁 Unlock 不安全（2026-07-11 18:15）
- 问题：Unlock 直接 `Del` 删除 key，任何客户端都能释放他人的锁
- `pkg/cache/types/types.go`：Lock 签名改为 `(token string, ok bool, err error)` 返回 UUID token；Unlock 签名改为 `(ctx, key, token string)`
- `pkg/cache/redis/impl.go`：Lock 用 `uuid.New().String()` 作为锁值；Unlock 用 Lua 脚本 `GET + DEL` 原子校验持有者后才删除
- `pkg/cache/local/impl.go`：同 Redis 实现，Lock 存 token，Unlock 校验 token 匹配后才释放
- `pkg/cache/multilevel/cache.go`：代理方法签名同步更新
- `go build ./...` 通过（Lock/Unlock 当前无调用方，接口变更安全）

## 修复 #4：算法状态模型修正 + 状态流转校验 + 路由补全（2026-07-11 18:35）
- `internal/model/bo/algorithm.go`：新增 `AlgorithmStatus*` 常量（0-5六态）和 `allowedTransitions` 状态流转规则 + `CanTransitionTo` 校验函数；`AlgorithmFormBO.Status` 从 `int`+`oneof=0 1` 改为 `int8`+`oneof=0 1 2 3 4 5`
- `internal/service/algorithm/sys_algorithm.go`：`UpdateStatus` 增加 `FindByID` 查询现有算法 + `CanTransitionTo` 状态流转校验
- `internal/router/algorithm.go`：注册 `PUT /:id/status` 路由
- `go build ./...` 通过

## 修复 #5：核心业务路由权限中间件（2026-07-11 18:40）
为 5 个路由文件添加 `middleware.Permission()` 权限校验：
- `internal/router/dataset.go`：POST→`sys:dataset:add`/PUT→`sys:dataset:edit`/DELETE→`sys:dataset:delete`
- `internal/router/dataset_item.go`：POST/PUT→`sys:dataset:edit`/DELETE→`sys:dataset:delete`
- `internal/router/algorithm.go`：POST→`sys:algorithm:add`/PUT→`sys:algorithm:edit`/DELETE→`sys:algorithm:delete`
- `internal/router/file.go`：POST→`sys:file:add`/DELETE→`sys:file:delete`
- `internal/router/item_file.go`：POST/PUT→`sys:file:edit`/DELETE→`sys:file:delete`

## 修复 #6：数据集单删除走级联删除（2026-07-11 18:45）
- `internal/api/sys_dataset.go`：`DeleteDataset` 从 `datasetService.Delete`（仅逻辑删除）改为 `operationService.BatchDeleteDatasets`（含递归删除子数据集和关联数据项）

## 修复 #7：CustomClaims 补充 GetDeptID/GetDataScope 方法（2026-07-11 18:47）
- `pkg/security/claims.go`：为 `CustomClaims` 添加 `GetDeptID() int64` 和 `GetDataScope() int8` 方法，实现 `dataScopeClaims` 接口，使数据权限插件走接口方式（避免反射回退）

## 修复 #8：CaptchaStore.Get key 前缀修复 + 过期时间对齐（2026-07-11 18:48）
- `pkg/security/captcha.go`：`Get` 方法使用 `rs.PreKey+key`（与 `Set` 保持一致）；`Verify` 直接传 `id`（由 `Get` 内部加前缀）；过期时间从 180s 改为 120s（对齐文档）

## 修复 #9：Token 黑名单 TTL 使用剩余有效期（2026-07-11 18:50）
- `internal/service/auth/auth_service.go`：`AddTokenToBlacklist` 从 `cfg.JWT.TTL` 改为 `time.Until(claims.ExpiresAt.Time)`，已过期 token 直接跳过

## 修复 #10：补齐任务管理 API 层和路由层（2026-07-11 18:55）
- 新建 `internal/api/sys_task.go`：`GetTaskPage`/`GetTaskById`/`CancelTask` 三个 API
- 新建 `internal/router/task.go`：注册 `GET /tasks`、`GET /tasks/:id`、`DELETE /tasks/:id`（含 `sys:task:delete` 权限）
- `internal/app/app.go`：创建 `taskApi` + 注册路由，移除 `_ = taskService`

## 已完成修复汇总（共 10 项）
| # | 问题 | 状态 |
|---|------|------|
| 1 | 文件上传/下载/删除物理文件处理 | ✅ |
| 2 | 密码通过 URL Query 传递 | ✅ |
| 3 | Redis 分布式锁 Unlock 不安全 | ✅ |
| 4 | 算法状态模型 + 流转校验 + 路由补全 | ✅ |
| 5 | 路由权限中间件（5 个路由文件） | ✅ |
| 6 | 数据集删除级联 | ✅ |
| 7 | CustomClaims 补充 GetDeptID/GetDataScope | ✅ |
| 8 | CaptchaStore key 前缀修复 + 过期时间 | ✅ |
| 9 | Token 黑名单 TTL 使用剩余有效期 | ✅ |
| 10 | 任务管理 API/路由补全 | ✅ |

## 修复 #11：数据集完整树形查询（2026-07-11 ~18:55）
- `internal/service/dataset/sys_dataset.go`：新增 `GetTree(ctx)` 方法，一次性查询所有数据集 + BFS 构建完整树
- `internal/api/sys_dataset.go`：新增 `GetDatasetTree` 端点
- `internal/router/dataset.go`：注册 `GET /datasets/tree`

## 修复 #12：数据项关键字搜索 + 雾霾程度筛选（2026-07-11 ~19:00）
- `internal/api/sys_dataset_item.go`：增加 `keyword` 和 `hazeLevel` 查询参数
- `internal/service/dataset/sys_dataset_item.go`：`GetDatasetItemsByPage` 新增 keyword/hazeLevel 参数，新增 `filterItemsByKeyword`（文件名匹配）和 `filterItemsByHazeLevel`（关联 item_file 表筛选）

## 已修复汇总（共 12 项）
| # | 问题 | 状态 |
|---|------|------|
| 1 | 文件上传/下载/删除物理文件处理 | ✅ |
| 2 | 密码通过 URL Query 传递 | ✅ |
| 3 | Redis 分布式锁 Unlock 不安全 | ✅ |
| 4 | 算法状态模型 + 流转校验 + 路由补全 | ✅ |
| 5 | 路由权限中间件（5 个路由文件） | ✅ |
| 6 | 数据集删除级联 | ✅ |
| 7 | CustomClaims GetDeptID/GetDataScope | ✅ |
| 8 | CaptchaStore key 前缀 + 过期时间 | ✅ |
| 9 | Token 黑名单 TTL 剩余有效期 | ✅ |
| 10 | 任务管理 API/路由补全 | ✅ |
| 11 | 数据集完整树形查询 | ✅ |
| 12 | 数据项关键字搜索 + 雾霾筛选 | ✅ |

## 修复 #13：本地缓存 IncrBy 原子性 + TTL 内存泄漏（2026-07-11 ~19:00）
- `pkg/cache/local/impl.go`：`IncrBy` 新增 `incrMu sync.Map` per-key mutex 串行化读写保证原子性；`Set` 总是更新 `ttlMap`（而非仅首次）避免长 TTL 覆盖短 TTL

## 修复 #14：图像输入历史记录管理模块（2026-07-11 ~19:10）
- 新建 `internal/repository/input_history/input_history_repository.go`：分页/详情/增/改/删/批量删/清空
- 新建 `internal/service/input_history/input_history_service.go`：业务校验（用户隔离）
- 新建 `internal/api/sys_input_history.go`：7 个 API（列表/详情/创建/更新/单删/批量删/清空）
- 新建 `internal/router/input_history.go`：`/api/v1/image-input/history` 路由组
- `internal/app/app.go`：wiring 完整的 repo→service→api→router 链路

## 已修复汇总（共 14 项）
| # | 问题 | 状态 |
|---|------|------|
| 1 | 文件上传/下载/删除物理文件处理 | ✅ |
| 2 | 密码通过 URL Query 传递 | ✅ |
| 3 | Redis 分布式锁 Unlock 不安全 | ✅ |
| 4 | 算法状态模型 + 流转校验 + 路由补全 | ✅ |
| 5 | 路由权限中间件（5 个路由文件） | ✅ |
| 6 | 数据集删除级联 | ✅ |
| 7 | CustomClaims GetDeptID/GetDataScope | ✅ |
| 8 | CaptchaStore key 前缀 + 过期时间 | ✅ |
| 9 | Token 黑名单 TTL 剩余有效期 | ✅ |
| 10 | 任务管理 API/路由补全 | ✅ |
| 11 | 数据集完整树形查询 | ✅ |
| 12 | 数据项关键字搜索 + 雾霾筛选 | ✅ |
| 13 | 本地缓存 IncrBy 原子性 + TTL 泄漏 | ✅ |
| 14 | 图像输入历史记录管理模块 | ✅ |

## 修复 #15~#20：三大核心业务模块（2026-07-11 19:00~19:20）
### #15：Python 算法服务 HTTP 客户端
- 新建 `pkg/config/options/algorithm.go`：Algorithm 配置（serviceUrl/timeout）
- 新建 `pkg/algorithm/client.go`：`Client` 封装 Predict/Evaluate 双接口，含超时 + JSON 解析 + 错误处理
- `config/config.yaml`：新增 `algorithm.serviceUrl: http://127.0.0.1:8014`
- `pkg/config/config.go`：AppConfig 新增 `Algorithm options.Algorithm`

### #16：预测日志 Repo + Service
- 新建 `internal/repository/pred_log/pred_log_repository.go`：Create/FindByID/FindByAlgorithmAndMD5/FindPage
- 新建 `internal/service/prediction/prediction_service.go`：Predict（Redis 缓存→调用 Python→写日志→缓存结果）、GetLogByID、GetLogPage

### #17：预测 API + Router
- 新建 `internal/api/sys_prediction.go`：Predict/GetPredictionLog/ListPredictionLogs
- 新建 `internal/router/prediction.go`：`/api/v1/prediction` 路由组

### #18：评估全套 Repo+Service+API+Router
- 新建 `internal/repository/eval_log/`、`internal/service/evaluation/`、`internal/api/sys_evaluation.go`、`internal/router/evaluation.go`
- 评估流程：调用 Python → 写日志 → 返回指标（PSNR/SSIM/LPIPS/NIQE）+ 合格判定

### #19：算法搜索筛选 + 对比
- `internal/model/query/algorithm.go`：新增 Type/Status 筛选字段 + `AlgorithmCompareQuery`
- `internal/repository/algorithm/`：FindPage 增加 type/status WHERE 条件，keyword 扩展为 name+type 双字段 LIKE
- `internal/api/sys_algorithm.go`：新增 Compare 接口（GET /algorithms/compare?ids=1,2,3）
- `internal/service/algorithm/`：新增 Compare 方法（接口同步）
- `internal/router/algorithm.go`：注册 `/compare` 路由

### #20：算法收藏功能
- 新建 `internal/model/sys_algorithm_favorite.go`（对齐 Python sys_algorithm_favorite 表）
- 新建 `internal/repository/algorithm_favorite/`：Create/Delete/FindByUserAndAlgorithm/FindByUserID/IsFavorited
- `internal/api/sys_algorithm.go`：新增 ToggleFavorite/ListFavorites/CheckFavorite（含 favRepo 注入）
- `internal/router/algorithm.go`：注册 `/favorites`、`/favorites/check`、`/:id/favorite`

所有模块 `go build ./...` 全部通过。评审报告中所有阻塞/警告/优化问题共计 20 项已全部修复。

## 修复 #4：算法状态模型修正 + 状态流转校验 + 路由补全（2026-07-11 18:35）
- `internal/model/bo/algorithm.go`：
  - 新增 `AlgorithmStatus*` 常量（0草稿/1测试中/2待审核/3已发布/4已停用/5已归档）
  - 新增 `allowedTransitions` 状态流转规则表和 `CanTransitionTo` 校验函数
  - `AlgorithmFormBO.Status` 从 `int` + `oneof=0 1` 改为 `int8` + `oneof=0 1 2 3 4 5`
- `internal/service/algorithm/sys_algorithm.go`：
  - `UpdateStatus` 增加 `FindByID` 查询现有算法 + `CanTransitionTo` 状态流转校验
  - 新增 `fmt` import
- `internal/router/algorithm.go`：注册 `PUT /:id/status` 路由
- `go build ./...` 通过


