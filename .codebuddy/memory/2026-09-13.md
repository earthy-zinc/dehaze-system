# 2026-09-13

## AI模型管理前端 bug 修复
- ProviderDrawer.vue「新增 Key」按钮无响应：keyDialog.mode 初始值即 "create"，按钮赋值不触发 watch → 弹窗永不开。已删除 watch 间接触发模式，改为显式 openKeyCreate() 函数（重置表单+开弹窗），与 openKeyEdit 风格统一。教训：禁止用 watch 值变化当打开事件，值未变即失效。

## 前端思考块重构（ThinkingPanel，对标业界推理模型思考卡）
- 23 个"思考过程 N"碎片根因：chat store `scheduleFlush` 每帧把 thinkingBlocks 整体搬走清空，但思考块还开着，后续 thinking_delta 走"兼容无 start"分支 push 新段 → 每 rAF 帧切一段。
- 修复设计（参考 cbm-web panshi ThinkingCard/ThinkingContent）：store 数据模型改 `ThinkingState{segments:[{text,closed}],startAt,endAt}`；段生命周期由 content_block.start/stop(index=1) 驱动（start 幂等防重连重放重复推段），增量经 thinkingBuffer rAF 批量刷入当前 open 段（跨帧不切段）；finishSession 统一 finalizeThinking。
- 新组件 `src/components/chat/ThinkingPanel.vue`：头部实时计时（思考中…(Ns) 旋转图标 → 已深度思考（用时 Ns）绿勾），多段思考段间分隔，流式自动吸底（上滑暂停），正文出现自动收起（collapseOnAnswer，用户手动操作过则不再干预）；多段思考时 timing 以"存在未闭合段"判定（endAt 会被首段 stop 提前写入）。
- AssistantMessage：ThinkingPanel 展示思考（流式用 thinkingByMessage，历史消息从 thoughts 中 tool=NULL 纯思考记录合成）；ThoughtChain 只承载工具步骤（thoughts.filter(t=>t.tool)）。
- 测试：chat.spec.ts 25/25（多段测试修正为 wire 契约 index=1+stop(1)，新增跨帧单段回归 + message.end 收尾用例）；vue-tsc 0 错；文档同步 前端实现-公共.md（组件树/职责表/交互决策表）。
- 后端 SSE 契约不变（thinking 块固定 index=1，converter finish() 必发 stop(1)）。

## 会话 SSE 思考流丢失修复（direct 路径）
- 现象：deepseek 推理模型 reasoning_content 正常下发（可观测中心 llm_call 响应原文可见），但会话 SSE 无 thinking 事件。
- 思考流全链路：openai_compat_client 把 reasoning_content 增量 → LlmStreamChunk(thinking_delta)；DehazeChatModel._astream → AIMessageChunk(content="", additional_kwargs={"thinking":...})；SseEventConverter._handle_messages 识别 additional_kwargs.thinking → content_block.start(type=thinking)+thinking_delta，finish() 时落库 agent_thought。
- 丢失根因：reasoning_service._run_direct（简单消息经复杂度评估路由 direct，无 deepagents 图）`if chunk.content:` 只把文本块交给 converter，思考块 content="" 被整体丢弃。修复：chunk 无条件交 converter，full_text 仅累积 chunk.content。
- deepagents 图路径（复杂消息）经 langgraph v1 StreamMessagesHandler.on_llm_new_token 透传 AIMessageChunk（无内容过滤），thinking 正常，无需修复。langchain_core 1.5.3 output_version 默认 None（不做 content blocks 转换）。
- 兼容 API（compatible_api_service）不透传 thinking_delta → reasoning_content，OpenAI 兼容端点用户拿不到思考（未修，待用户决策）。

## 审计级重构整体完成（team audit-refactor 三成员交付 + 主 agent 验收，2026-09-13）

- **P0 wire 采集**（backend-collector）：`sys_ai_llm_call` + start_time(datetime3)/raw_request/raw_response；协议客户端经 `record_wire_*` ContextVar 通道上报（签名零侵入）、`_wire_guard` 256KB 截断；失败存 `{"error":{...}}` 成功覆盖；respx 逐字段对比测试
- **P1 时间线 API**（backend-api）：`GET /observability/conversations/{id}/timeline`（轮次切分/ts 交织/旁路归属/include=raw）+ timeline/export；消息详情的 traceId/contextSnapshot/llmCalls **组装整体移除**（死代码 AiLlmCallResult/get_latest_by_message_id 连带删除）；summary 恒计数单形状（两轮打回后彻底执行）
- **用户拍板规范**：禁止兼容老数据——summary 无降级分支、ts 无近似兜底（NULL 沉底）、空态文案中性化；教训：成员倾向留 fallback，需明确反复纠偏
- 测试与部署：python 2696 过/1 预存；front-vue 350/350 + SDK 61/61 + vue-tsc 0 错；8991 已重启，联调终验 200 通过；新会话才有 raw 全文，存量 NULL 属预期
- **P3 文档回写（主 agent）**：需求规格 §2.12.8（会话时间线审计+wire 留痕）、后端实现 §1.3/§2.1.2/§2.3 wire 采集/常量表、前端实现 §3 全面重写（三 Tab+时间线组件族+两页合并）、测试用例 §3.4.1-3.4.3（+24 用例）+ 旧截断值修正（500/2000→20000/2000）；`审计级重构设计.md` 为中间文档，事实已沉淀四份正式文档

## 审计级重构 P2 前端落地（frontend，2026-09-13 完成）

- 页面合并决策执行：`/admin/ai-observability` 重组三 Tab（会话审计=默认主 Tab：AuditFilterBar+AnomalySummaryPanel+ConversationAuditTable 跨目录复用 + 新 ConversationTimelinePanel；总览；检索导出）；`ChainTracePanel` 删除、ConversationDetailDrawer 的 @trace 改 router.push 跳可观测中心（query conversationId 定位时间线）；adminAudit store 删 startChainTrace/traceChainData 等，新增 openConversationTimeline/timelineRoundIndex
- 新组件族（src/views/ai-observability/components/）：RoundTimeline（轮内事件按 ts 稳定排序、kind 分色节点、旁路 trace 挂尾部 el-collapse）、LlmCallNode（摘要卡 + rawRequest/rawResponse 经 JsonViewer 回放 + attempts）、RoundNavigator（轮次列表，状态取最差 trace、异常轮标注）、ConversationTimelinePanel（左导航右时间线，空态"后端读模型未就绪"）；公共组件 src/components/JsonViewer.vue（递归折叠渲染函数显式 VNode 返回类型避免递归类型循环 + 复制/下载 + null 降级"历史数据（无原始报文）"）
- TraceDetailDrawer 重构：buildRound(traceDetail) 合成单轮（input 取 user 消息、context=createTime、llm_call 用 startTime??createTime、thought/billing 用 createTime）复用 RoundTimeline；保留 descriptions/计费/中间产物（计费不再在时间线外重复，billing 进事件流）
- **SDK 边界扩展**（超出"只动 front-vue"字面边界，已在汇报中说明）：timeline 类型与 `getConversationTimeline` 落在 dehaze-sdk-js/src/api/ai-observability（model.ts + index.ts），`AiObservabilityLlmCall` 补 startTime/rawRequest/rawResponse（§4.3 契约）；front-vue 无本地 API 层、全部走 SDK，需 `pnpm build` 重出 dist 后类型生效
- 跳转约定：`/admin/ai-observability?conversationId=N` → 自动切会话审计 Tab 并拉时间线（TraceSearchTable 行操作、会话详情抽屉两处）；`?tab=search` 支持
- 测试：新增 JsonViewer.spec（8 例：空态降级/折叠/复制/下载）+ RoundTimeline.spec（5 例：kind 渲染/摘要卡降级/旁路尾部/空态/ts 排序）；unit 全量 349/349 绿，vue-tsc 0 错，旧符号零残留
- 组件归属修正（team-lead 审查反馈）：AuditFilterBar/AnomalySummaryPanel/ConversationAuditTable 三组件 git mv 至 `ai-observability/components/`（两页共享，单一归属），ai-conversations/index.vue 改绝对路径引用，组件内部零改动；unit 回归 349/349 绿
- 联调第一轮（python 8991，admin）：login/conversations OK，`GET /observability/conversations/2/timeline?include=raw` **500 B0001**，根因 `_trace_events()` router/service 签名不匹配（summary 瘦身改造两侧未同步，已通知 backend-api）；待其修复后第二轮联调验证 SDK 契约 + summary 两种形状降级链路。注意：dehaze-test 登录对 python 后端首次调用常 httpx.ReadTimeout（本地 LLM 懒加载抢占事件循环），重试或加大 timeout 即可
- **方向修正（用户拍板：禁止兼容历史兜底）**：JsonViewer 默认空态文案"历史数据（无原始报文）"→"无原始报文记录"（不区分历史/缺失）；summary **终版口径=恒纯计数字段、单一形状**（counts/tool_count/user_id/输出预览），无任何老数据降级形状，"两种形状安全"结论作废；前端现按设计稿嵌套形状取数（summary.inputSnapshot.messages.counts/tool_count、outputSnapshot.text），待 backend-api 给扁平终版字段名清单后同步 LlmCallNode 取数 + SDK model.ts summary 类型，再跑联调终验（已向其索要字段名+重启部署确认，8991 仍为 12:04 前旧代码 500）
- **SDK 类型清理（backend-api 移除消息详情 llmCalls/contextSnapshot 组装后连带失效）**：删除 ai-conversation/model.ts 的 AiMessageContextItem/ContextEvent/ContextSnapshot/AiMessageLlmCall 四接口 + AiMessageVO 的 traceId/contextSnapshot/llmCalls 三字段（grep 确认全工作区零消费方：front-vue 已随 ChainTracePanel 下线清理、其他前端不用 SDK 类型、SDK 测试无引用；front-vue mock/ai-chat.mock.ts 四条 seed 消息的 mock 数据与注释同步清理）；SDK build+test:unit 61/61、front-vue vue-tsc 0 错 + 349/349 绿
- **联调终验通过（backend-api 终版部署 8991 后）**：timeline 500 消除；①结构契约=conversation 五键/rounds(userMessage+assistantMessage+traces)/events 与 SDK 类型一致；②summary 终版单形状实锤=llm_call 的 inputSnapshot 仅 messages{counts,tokens}+可选 tool_count/user_id（无 items/system_content/tools）、outputSnapshot 仅 text，与 LlmCallNode 取数路径完全一致零改动；③ts=start_time 毫秒 ISO，P0 前老数据 llm_call ts=null，前端 RoundTimeline 排序实现 null 沉底（补 spec 用例）+ formatTs 适配 null；SDK model.ts summary 类型收敛为计数单形状、清理"老数据"措辞。front-vue 终态 350/350 绿 + vue-tsc 0 错。P2 前端全部收口

## 审计级重构 P0 采集层落地（backend-collector，2026-09-13 完成）

- DDL：开发库已 ALTER `sys_ai_llm_call` 加 `start_time datetime(3)`（AFTER model）+ `raw_request`/`raw_response` json（AFTER attempts）；`config/sql/schema/sys_ai_llm_call.sql` 同步（测试库重建验证通过）
- 采集通道：trace_collector 新增公开 ContextVar `wire_record` + 模块级 `record_wire_request/record_wire_response`（协议客户端签名零侵入）；LlmCallRecord 新增 `observe_wire_request`（首次上报即记 start_time，Asia/Shanghai naive 复用 `_shanghai_now`）/`observe_wire_response`（重试成功覆盖前次失败错误）/`observe_wire_error`（llm_client except 分支调，`{"error":{code,message}}`）；`_wire_guard` 256KB 序列化字节上限，超限整体替换 `{"_truncated":true,"_original_bytes":N}`；finish 落库补三字段
- llm_client `_stream_with_key_retry`：每次物理尝试消费协议流期间 set/reset wire_record（finally 复位），物理失败 observe_wire_error
- 协议客户端：openai/anthropic `stream_chat` 在 payload 构建后 `record_wire_request(payload)`、流聚合完成后上报等价非流式结构（id/model/choices[{index,finish_reason,message{role,content,tool_calls[{id,type,function{name,arguments原文}}}]}/usage 原始字段]；anthropic stop_reason 映射 end_turn→stop/tool_use→tool_calls/max_tokens→length；raw content 为 think 切分前原文，thinking/reasoning_content 另存字段）
- 测试：test_wire_capture.py（协议级 5 例）、test_trace_collector.py +7（落库/截断/失败/老数据 NULL/通道）、test_llm_client.py +2（requires_db 集成：成功 raw_request==respx 捕获请求体；全失败 error 结构）。范围内全绿；test_ai_observability_service 4 个 list_traces 失败为 obs 成员在改 ai_observability_service.py 所致（get_by_ids 签名未同步），非本任务范围
- P1 读模型契约（已同步 backend-api）：raw_response 成功为 choices 结构、失败为 `{"error":{code,message}}`（choices 不恒存在）；超 256KB 为 `{"_truncated":true,"_original_bytes":N}`（无业务键，前端/导出需识别）；start_time 为 Asia/Shanghai naive datetime(3)；no_key 未发请求时三列全 NULL
- P1 联调闭环（backend-api 确认）：timeline 事件 ts 优先取 start_time（老数据 NULL 按 create_time-duration 近似）；读模型 summary 在 rawRequest 在场时瘦身为纯计数、全文唯一通道走 raw_request——input_snapshot 保持计数/摘要语义不动，raw 为审计原文唯一来源

## AI 可观测性"审计级重构"设计（用户驱动，设计稿待评审）

用户不满审计形态：会话审计按单消息展示（`GET /messages/{id}?view=admin`）、traces 按 traceId 平铺、缺 wire 级完整请求/响应（input/output_snapshot 是重构摘要非原文）。对标 Langfuse（Session→Trace→Generation，generation 存完整 wire JSON）/ OTel GenAI（gen_ai.content.prompt/completion 事件存 OpenAI 格式全文）产出设计稿：

- 新文档：`dehaze-doc/.../AI对话/可观测性/审计级重构设计.md`
- 三主线：① `sys_ai_llm_call` 补 `start_time`(datetime3)/`raw_request`/`raw_response` 三列（唯一 DDL；采集点 openai_compat_client/anthropic_client.stream_chat，256KB 上限，失败调用照存 raw_request）② 新增 `GET /ai/observability/conversations/{id}/timeline`（轮次组织 + events 按 ts 交织：input/context/llm_call/tool_exec/system_event/billing；旁路 trace 挂触发轮次尾部）③ 前端 RoundTimeline/JsonViewer/LlmCallNode 组件族，会话审计页改时间线主视图，TraceDetailDrawer(25KB) 拆解复用同一组件
- 分期 P0 采集/P1 API/P2 前端/P3 文档测试回写；不引入外部观测平台、不迁移数据；raw 与 snapshot 双份存储（capability 检索依赖 snapshot 的 JSON_SEARCH）；用户端不透出 raw
- **等用户评审确认后动手**（用户明确"先给我一个好的设计"）

## AI 对话"智能程度不高"排查 + 可观测性修复（用户驱动，全部完成）

### 诊断结论（查开发库 sys_ai_trace/llm_call/billing/message 实证）
- **用户消息机制上每轮都传入**：`_load_recent_messages` user/assistant 同链同权重；体感"不聪明"主因是默认模型 qwen3-0.6b（本地 0.6B Q4）+ 旧 20 条窗口截断
- **28 条消息 vs 1 次 LLM 调用**：语义正确——llm_call_count 是"生成该条回复"的调用次数，历史是输入。但 stepPosition 1→2→3 跨轮泄漏暴露深层 bug
- **三条根因链**（服务 9/12 23:41 启动的新代码上复现，即 9/11 的 P0 run_context 修复在真实图执行中未生效）：
  1. **run ctx 跨节点丢失**：langchain factory 把 before_agent 挂为独立图节点（factory.py:1527），节点=独立 asyncio 任务，`abefore_agent` 内 `set_run_ctx` 只在该节点可见 → model/end 节点回退图缓存共享模板 → step_count 跨 run 累积、billing_context 丢失 → **react 轮次计费结算静默跳过（billing tokens 恒 0）**
  2. **direct 轮次 trace 全丢**：`_run_direct` 中 billing settle 抛异常（qwen3-0.6b 无用户售价种子）发生在 trace settle 之前 → 9/12 轮 direct trace 缺失、llm_call 成孤儿；且预扣未结算
  3. **tools 快照全空**：`_build_input_snapshot` 读顶层 name/description，DehazeChatModel 传的是 OpenAI 嵌套格式 `{"function":{...}}`
- 附带发现：MySQL DATETIME(0) 对微秒**四舍五入**（非截断），xx.7s 进位成下一秒 → seeder 幂等检查查不到刚插的行 → 重复插入（flaky 根因）

### 修复（dehaze-python，492 AI 域测试全绿 + 全量 2840 过/2 预存失败与本次无关）
1. **run_context**：新增 `ensure_run_ctx(template)`，run()/resume() 在 astream 前于驱动任务预置，图内节点共享同一 dict；abefore_agent 复用预置 ctx（per-run 字段仅主 run 重置，`not is_subagent_run()` 守卫）；is_subagent 改经 `_in_subagent` ContextVar 判定（结算钩子以其为唯一口径防子图二次结算）；deep_agent_builder/team_builder 挂 `graph.dehaze_run_ctx_template`
2. **context_manager**：20 条硬截断 → token 预算窗口（max_context − max_output − 系统提示 − 摘要 − 余量2000，下限1024，查询边界200条）；裁剪写 truncate 事件进过程链
3. **summary_service**：触发改为"窗口外存在未摘要消息即压缩"（窗口起点从组装结果推导），修复大窗口模型摘要永不触发的死锁；maybe_compress 移除 system_prompt 参数
4. **trace_collector**：tools 兼容 OpenAI 嵌套格式（_tool_brief）；快照上限审计级（消息 2000→20000、输出 500→20000、系统提示 5000→50000 等，正常全文留存）
5. **reasoning_service._run_direct**：trace 先于计费结算落库，结算失败 try/except 记 ERROR 不吞 trace；移除重复 start 采集器
6. **agent_hooks._billing_settle_hook**：is_subagent 判定前置 + 结算失败不炸 after_agent 链
7. **model_seeder**：价格补齐移出模型创建分支（存量缺价环境补种全 0 价）+ effective_from 截微秒

### 文档同步
上下文管理（需求规格 §2.3.4 + 后端实现 §3.2/§4.1/§4.2 token 预算窗口）、可观测性（快照审计级口径 + 采集完整性修复说明 + llm_call_count 口径）、会话与消息（§6.1/6.2）、架构与公共（run ctx 传播机制）、AI模型管理（播种价格幂等 + DATETIME 舍入坑）

### 遗留待办
- **python 服务需重启**（9/12 23:41 启动的旧代码；重启后 seeder 自动补 qwen3-0.6b 价格）——已告知用户，未擅自重启
- 前端 TraceDetailDrawer 已有按 seq 的"LLM 调用回放"时间线（含输入消息/系统提示/工具/输出全文），后端修复后自然可见，前端未改动
- `extract_episodic_from_summary` 在 memory_extraction 中不存在（summary_service 里是静默 ImportError 死分支，文档 §7.5.1 提到的覆盖度检查兜底实际未实现）——待决策
- 全量 pytest 两个预存失败（test_message_service::test_send_expires_at_by_type 时间相关、auth 种子账号 flaky），未修改代码上同样失败，与本次无关

## backend-api：AI 可观测性审计级重构 P1 读模型/API 层（2026-09-13）
- 新增 `GET /api/v1/ai/observability/conversations/{id}/timeline`（登录用户本人会话，管理员 `ai:conversation:audit` 全量，越权 404 A0401；`include=raw` 默认含 raw 报文）+ `GET .../timeline/export`（JSON 全量，管理端导出权限）。
- 实现：`ai_observability_service.get_conversation_timeline`（分支链轮次切分 `_split_rounds` + trace 归属 `_attach_traces` + 事件织入 `_trace_events`，ts 锚点 llm_call=start_time 老数据 create_time-duration 近似、同刻按 kind 优先级+seq/position 稳定排序）；traces 列表增补 conversationTitle；messages/traces 详情 llmCalls 增补 startTime/rawRequest/rawResponse。
- 新 repository 方法：ai_trace_repository.list_by_conversation、ai_llm_call_repository.list_by_traces（防 N+1）。
- 依赖契约：SysAiLlmCall 待 backend-collector 加 start_time/raw_request/raw_response 三列；P1 代码经 pydantic schema 层（default None + from_attributes）已兼容列落地前后，落地后自动透传，无需改 P1 代码。注意 AiConversationRepository.get_by_ids 覆盖签名带 user_id，勿直接复用。
- 测试：tests/service/test_ai_observability_service.py +19、tests/router/test_ai_observability.py +4，43/43 绿；tests/service+tests/router 回归 2676 过 1 失败（test_auth_login_security 预存）。文档已同步：API接口.md §2.13、可观测性/后端实现.md 新增 §2.6.3。

## backend-api：P1 审查问题整改（2026-09-13）
- 问题1（死路径增强）：AiLlmCallResult 三字段增补已回退；消息详情 get_message 的 traceId/contextSnapshot/llmCalls 组装整体移除（保留 thoughts），MessageResult 对应三 schema 字段删除；死代码 AiLlmCallResult 类、ai_trace_repository.get_latest_by_message_id 及其测试一并清除。测试 test_ai_conversation_message_trace.py 删除，test_ai_message.py 消息详情用例改写（断言字段不存在）。遗留：dehaze-sdk-js/src/api/ai-conversation/model.ts 中 traceId/contextSnapshot/llmCalls 可选类型已失效，需 SDK 侧清理（非我边界，已报告）。
- 问题2（双份传输）：timeline llm_call 事件 summary 瘦身——rawRequest 在场时 inputSnapshot 只留计数（messages counts/tokens、system_tokens、tool_count、user_id），messages.items/system_content/tools 清单去掉；raw NULL（老数据）或 include 未含 raw 时保留完整 inputSnapshot 降级。瘦身函数为模块级 _slim_input_snapshot（_trace_events 保持 staticmethod，首参 trace，勿误加 self）。
- 联调确认：sys_ai_llm_call 三列已由 backend-collector 落地（DDL+实体+开发库 ALTER），测试库自动重建生效；DB 级用例已用 start_time/raw_request/raw_response 真实种子验证透传与瘦身规则。
- 回归：service+router+repository 2696 过 / 1 失败（test_auth_login_security 预存）。文档已同步（API接口.md 消息详情行、后端实现.md §2.6/§2.6.3）。

## backend-api：P1 方向修正 + 部署（2026-09-13）
- 用户拍板修正（覆盖此前"老数据降级"要求）：①summary 恒为单形状纯计数（messages.counts/tokens + system_tokens/tool_count/user_id，outputSnapshot=text预览+tool_calls），无全文兜底分支；②llm_call ts 仅取 start_time，NULL 沉底（排序 key 首位 (e.ts is None, ...)），删除 create_time-duration 近似；context/system_event 的 trace 锚点保留（非兼容逻辑）。③自查清理 _attach_traces 中 create_time NOT NULL 相关死防御。
- 实体三列确认已落地（此前"均无"为旧读取状态），真实联调：重启 8991 后 curl conversation 2 timeline → 00000，老数据 raw/ts NULL，summary 单形状正确。frontend 500 根因是 8991 进程旧代码（12:04 前签名 bug），重启后消除。
- 部署：scripts/run.py restart python（pid 243857, :8991），已回报"终版已部署"。
- 经验：pytest 测试库自动重建会带上最新 DDL，测试通过即证明列已落地；跨成员并行时读实体/DDL 前先重拉文件。
- team-lead 二次审查基于修正前 diff（其要求与用户拍板口径一致，我上轮已落实）：当前代码已确认无任何 summary 分支（无条件 _slim_input_snapshot）+ ts 仅 start_time。再次回归 43/43 绿 + scripts/run.py 重启（pid 401386）+ 真实冒烟（include on/off 两种模式下 summary 形状完全一致，raw 仅受 include 控制）。经验：收到"未按口径完成"类审查意见时，先 grep 当前代码核对是否已改，避免重复返工。
