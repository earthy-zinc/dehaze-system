# 2026-09-16

## feedback 模块修复完成（java-fixer 第二波第 3 批，commit 4371636）
- **结果**：feedback.test.ts **83/83 全绿 0 skip**（原 18 失败），mvn 296/296。java 侧累计 6 个 commit 未 push
- **修复**：①FeedbackCreateForm 补 feedbackType 枚举/title 5-50/content 10-1000 校验（python schema 对齐）；②RatingCreateForm.comment @Size(500)（原 501 字符 B0001 数据截断）；③RatingPageQuery 时间参数 @DateTimeFormat("yyyy-MM-dd HH:mm:ss")（原 LocalDateTime ISO 绑定失败）；④getRatingByPrediction 过滤 isHidden；⑤图片 URL 校验改多存储后端白名单（注入 List<FileService> 收集 baseUrl，对齐 python _get_allowed_image_url_prefixes）替代单一 file.baseUrl；⑥yml 修正 file.nginx-static.baseUrl 端口 8081→${NGINX_STATIC_PORT}
- **关键认知**：测试 ensurePredictionLog 有自愈回退（预测失败→直插 DB completed 记录），python 端 prediction 对 127.0.0.1 内网图同样拒绝（SSRF _assert_public_host），java→python 算法链路的内网拒绝**不是缺陷**（测试回退路径覆盖）；真正拦截点是 rating/feedback 的 imageUrls 白名单
- **同款缓存排查清单（未改，报 lead）**：RedisTemplate<String,Object> 注入点=TaskCleanupJob/TaskExecutorImpl/TaskServiceImpl/DefaultProgressCallback/HealthController/ExportDlxConsumer，均为 task:* 前缀 java 内部自写自读（对称序列化器），无跨端互操作，暂无 dict:data: 同款风险；python 侧 get_json 读 task key 场景未发现

## favorite 模块修复完成（java-fixer 第二波第 2 批，commit f516b01）
- **结果**：favorite.test.ts **34/1**（原 20 失败）、favorite-integration.test.ts **3/3**（原 3 失败）；mvn 296/296
- **核心根因**：SysDictServiceImpl.listDictOptions 用 RedisTemplate<String,Object>（GenericJackson2JsonRedisSerializer + NON_FINAL default typing）读写 `dict:data:*`——自写自读格式不一致（写 WRAPPER_ARRAY 元素、读 deserialize(Object.class) 期望类型 id）→ POST /favorites 容量校验读缓存时 500；且 python/go 写的裸 JSON 它也读不了。修复：改 StringRedisTemplate 存裸 JSON [{value,label}]，三端互认 + 解析失败删 key 回源自愈；同步更新 SysDictServiceImplTest 构造签名
- **重要发现（不修代码，汇报为准）**："取消后重新收藏应复活原行" 用例在 **python 基准端实测同样失败**（8991 跑 favorite.test.ts 34/1，实测 164→165 新行不复活）——含 deleted 唯一键下软删行不占键位，python upsert 的 on_duplicate_key_update(deleted=0) 复活分支是死代码，python soft_delete favorite 写 deleted=1 也非行 id。java 行为已与 python 实际一致。**该用例属"测试断言与基准实现冲突"，需 lead 决策改测试或改设计**
- **教训**：①三端共享 Redis 的缓存 key 格式必须统一裸 JSON，java 带类型信息的 RedisTemplate<String,Object> 只能 java 内部用；②复现不了的"复活"语义先在基准端实测再动手

## menu 模块修复完成（java-fixer 第二波任务第 1 批，单个 commit fix(menu)）
- **结果**：menu.test.ts **53/53 全绿 0 skip**（原 32 失败+10 skip），mvn 296/296；java 侧累计 4 个 commit 未 push
- **核心根因（一个 NPE 级联 30+ 失败）**：MyBatis-Plus 条件式 `.eq(condition, col, val)` 的 val **总是被求值**——`GET /menus` 不带 type 时 `IBaseEnum.getEnumByValue(null)` 抛 NPE → B0001，级联 createMenuAndGetId 与 3 个 beforeAll 全挂。修复：getEnumByValue null 安全，枚举转换前置由结果决定条件
- 其余真缺陷：saveMenu 整套业务校验缺失（同级重名/perm 唯一/父级类型/循环引用/5 级上限/条件必填；含软删查重用 SysMenuMapper 原生 SQL 绕 @TableLogic）；PATCH visible 误用 @RequestParam（python 是 body MenuVisibleBody）；options 未过滤 BUTTON；deleteMenu 重复 ID 误报 A0401、非数字 ID 500；MenuForm 缺 @NotBlank/@Min(sort)，name Pattern 的 `.` 不匹配 CRLF 误拒脏语料（加 (?s)）
- **教训**：①java 全局 IllegalArgumentException handler 用单参 `Result.failed(msg)` → B0001 非 A0400，"A0400 期望"须显式 BusinessException(PARAM_ERROR)；②java tree_path 格式 "0,1,2"（root 为 "0"），python 为 ",1,2," 含自身 ID，层级算法不同（java=split 段数）；③带 import 的编辑后必须完整编译再重启（曾漏 MenuVisibleBody import 致服务起不来）；④routes/options 有 1h Redis 缓存（menu:routes/menu:options），修复后测试前需 DEL

## go-fixer 完成 user+auth 模块对齐修复（3 commits，未 push）
- **验证结果**：go test 全绿（14 个测试包）；SDK 冒烟 user.test.ts **61/61**、auth.test.ts **22/22**（原 22 失败全修复）；已 `scripts/run.py restart go` 生效
- commit b298260a `fix(common)`：权限中间件无权限/校验失败统一 A0301（对齐 python 403 语义，修正原 A0300）；新增 A0505 ROOT_USER_PROTECTED
- commit 7940e10f `feat(auth)`：GET /auth/login-logs（mongo 分页+筛选，isAdmin=ROLE_ROOT/ADMIN 判定，普通用户 user_ids 限定本人）+ GET/DELETE /auth/sessions（scan session:* 排除 session:user:*，kick 保护 ROLE_ROOT 会话 A0503、不存在 A0401）；验证码区分 A0213（Key 不存在/已消费）与 A0214（比对错，EqualFold 大小写不敏感）；SessionData 补 deviceType/loginIp/loginTime/lastAccessTime，LoginRequest 加 deviceType；LoginLog bson 字段改名对齐 python LoginLogDocument（user_id/create_time/device_type，**共享同一 mongo 集合，原 camelCase 会污染 python 读取**），RecordLogin 末参 location→deviceType；新增 internal/service/session 包（KickByUserIDs 供 user 服务禁用/删除/重置密码联动，规避 auth↔user 循环依赖）；mock_IAuthService 为 mockery testify 风格（**不能用 mockgen 重新生成**，会改写为 uber 风格破坏全部测试）
- commit d445f915 `fix(user)`：update 用户名只读 A0503；updateStatus 存在性 A0401+超管 A0505+自禁 A0503；delete 自删 A0503+超管 A0505；updatePassword 复杂度(8-20 字母数字 A0400，先校验复杂度再查存在性)+A0401；Login 用户不存在→A0210（防枚举）；getPageParams 严格校验（pageNum≥1、1≤pageSize≤100 → A0400，**11 个调用点全部改为返回 error**）；user FindPage 补 startTime/endTime（endTime 按天含当日 <end+1d）
- **存量测试修正**：auth_service_register_test.go TestRegister_CaptchaWrong 原断言"缺验证码→A0214"，按 python 口径改断言 A0213
- **附加审计结论**：①go role:perms 缓存全走 ICache 抽象无裸 redis 绕过；②go RoleRepository.HasUsersInBatch 已排除软删用户（与 python 一致），且 python 删除用户也不清理 sys_user_role（仅软删 sys_user），go 无需改；③越权错误码经 middleware A0301 全局对齐
- **数据修复**：开发库 root(id=1)/admin(id=2) 曾被旧 go 二进制真删（deleted=1/2、root status=0），已 UPDATE 恢复；这是 go 旧缺陷污染共享开发库的直接证据，java 侧 admin deleted=2 污染同源

## java-fixer 完成 user+auth 模块对齐修复（2 commits，未 push）
- **补充 commit fe32674**（team-lead 指示）：禁用/删除/重置密码后 kickUserSessions 联动——SysUserServiceImpl 内私有方法，单次 scan session:* 按 userId 批量匹配删除 + 清理 session:user:{username} 索引，ROOT 会话保护；updateStatus 仅禁用(status==0)时踢、启用不踢（python 一致）。设计取向：注入 StringRedisTemplate 直接实现而非 @Lazy 注入 AuthService（避免 SysUserServiceImpl↔AuthServiceImpl 循环依赖）；未复制 python 的 role:perms:{code} 失效动作（java 无 L1 且角色未变，无行为意义）。同步修正文档"会话清理按索引逐删"的失实描述为"扫描 session:* 匹配 userId"。mvn test 296/296 ✓，已重启 java 生效，未跑 vitest（lead 要求收口统一跑）
- **验证结果**：mvn test 296/296 全绿；SDK 冒烟 user.test.ts **61/61**（原 17 失败全修复）、auth.test.ts **22/22**（原 7 失败全修复）
- commit d9c8652 `fix(user)`：UserForm 补邮箱格式/deptId必填/username≤64 校验；updateUser 存在性(A0401)+用户名只读(A0503)；deleteUsers 自删(A0503)+超管保护(A0505)+user:auth 缓存失效；updatePassword 复杂度(8-20字母数字, A0400)+存在性(A0401)+缓存失效；updateStatus 收敛服务层(超管禁用 A0505/自禁 A0503/存在性)；分页 @Valid 生效(pageNum≥1, pageSize≤100)；新增 ResultCode.A0505 ROOT_USER_PROTECTED；删除无引用的 isCurrentUser
- commit 二 `fix(auth)`：SysUserDetailsService 把用户缺失 BusinessException 转 UsernameNotFoundException→登录 A0210；AuthServiceImpl 泛型 catch 还原被包装业务码；session JSON 补 deviceType/loginIp/loginTime/lastAccessTime；新增 GET /auth/login-logs（Mongo 分页，管理员全量/普通用户仅本人，LoginLogQuery+LoginLogVO，LoginLog 实体加 deviceType，recordLogin 末参 location→deviceType）+ GET/DELETE /auth/sessions（scan session:* 排除 session:user:*，kick 保护 ROLE_ROOT 会话 A0503、不存在 A0401）；LoginForm 加 deviceType @Pattern
- **附加审计结论**：①java role:perms 缓存无 L1 进程内缓存，全走 StringRedisTemplate（无 python 同款缺陷）；②sys_user_role 不清理与 python 一致，SysUserRoleMapper.countUsersForRole 已排除软删用户
- **遗留（范围外可选对齐）**：java 未实现 python 的禁用/删除/重置密码后 kick_user_sessions 联动（SDK 用例未覆盖）；register 会话 JSON 未补 loginIp/loginTime
- **重要教训**：admin(id=2) 在开发库中曾被软删(deleted=2)且残留——导致本次冒烟登录钩子全 400；已 UPDATE sys_user SET deleted=0 恢复。排查登录类全挂先查 sys_user.deleted 与 login:fail:* 锁

## align-java-go 团队（用户指令：修 java/go 对齐 python）
- 团队名 align-java-go，两成员并行：java-fixer（dehaze-java）、go-fixer（dehaze-go），均为 code agent + bypassPermissions
- 基线画像（今日全量 SDK 实测）：**java 87 失败**（70=未实现模块 No static resource，范围外；17=user 行为缺陷+auth login-logs/sessions 缺接口）；**go 22 失败**（14=user 缺陷含超管删除保护/删除自己/分页边界/弱密码/越权语义 + auth 3 类）
- 任务书要点：python 为唯一事实源；SDK 测试为验收；附加审计项（role:perms 缓存 L1 绕过、sys_user_role 清理、AuthServiceImpl:144 泛型 catch 掩盖真实错误）；整模块未实现的不做；每修复独立 commit 不 push
- 协作纪律：SDK 测试严禁两端同时跑（共享开发库+Redis）；服务重启走 scripts/run.py 且只许动自己负责的后端；冒烟前 ps 检查无 vitest 进程
- 验证由 main 串行收口（test:java 全量 → test:go 全量 + 单测基线 java 296/296）

## 第一波结果（2026-09-16 完成）
- **java-fixer 3 commits**（d9c8652 user 校验/保护、fix(auth) 登录错误分类+login-logs/sessions 补齐、fe32674 踢会话联动）：user/auth 单文件全绿，mvn test 296/296
- **go-fixer 3 commits**（b298260a 权限中间件 A0301、7940e10f login-logs/sessions/验证码区分、d445f915 user 全套保护）：user/auth 单文件全绿
- 两端开发库 root/admin 被旧二进制真删的污染（root deleted=1、admin deleted=2）均已恢复

## 第二波（用户指令 2026-09-16 下午）
- **用户要求**：①修复已实现接口的全部存量缺陷（未实现接口忽略）；②**一个接口一个接口修、一次不给成员派太多任务**（go-fixer 曾触发 500 次请求上限暂停）；③go 的 A0301 跨文件污染要修；④全部完成后 java 执行 Spring Boot 3.3.11→3.5 升级（4.x 暂不做，生态未跟上）
- 全量真实验证画像（admin 恢复后，第一波 1430 大规模跳过掩盖的）：**java 185 个存量缺陷**（menu 32/favorite 20/feedback 18/order 17+14/package 12/dict 11/role 9/member-edge 8/file 7 等）；**go 456 失败中 73 个 A0301 跨文件污染**（user/role/menu admin 权限中途丢失，单文件冒烟全绿→典型状态污染，嫌疑：go 新加踢会话逻辑/权限中间件/scan 误删）
- 排查方法论已传达成员：用 test/logs/<backend>/detail.log（NDJSON）按时间序定位首个失败 traceId 及前序请求，不要猜
- 节奏：java-fixer 逐模块推进（menu→favorite→feedback→order→...），go-fixer 先修 A0301 污染再重跑全量拿干净缺陷清单再逐模块
- **教训**：分析 brief.json 失败分布时文件名带行号（menu.test.ts:43），用 endswith('menu.test.ts') 匹配不到会误报 0 失败，必须先 split(':')

## 第二波进度（滚动更新）
### java（java-fixer，7+ commit）
- 已完成：user/auth(第一波) + menu 7 缺陷（IBaseEnum NPE 级联 25 失败、@RequestParam→@RequestBody 绑定错等，53/53）+ favorite（dict:data: 缓存 GenericJackson2Json 序列化跨端不兼容根修为裸 JSON 三端互认；"取消后重新收藏"断言按方案 a 修测试——用户决策，uk 治理 A 下复活是死代码）+ feedback 6 缺陷（校验缺失/时间格式/@DateTimeFormat/隐藏评价泄漏/图片 URL 多存储白名单+yml 端口 NGINX_STATIC_PORT 修正，83/83）
- 进行中：order（17+14）
- 队列：package(12)/dict(11)/role(9)/member-edge(8)/file(7)/algorithm(6)/member(6)/小模块
- java 序列化缓存排查结论：6 处 RedisTemplate<String,Object> 手写缓存均 task:* 前缀 java 自写自读对称，无跨端风险；dict:data: 是唯一三端共享已根治

### go（go-fixer，7 commit）
- **A0301 根因已修并全量验证通过**：commit 2308a58b6——登录权限聚合被截断（全量运行中改角色/菜单数据触发重新聚合暴露，单文件不触发），修复后 73 个级联污染归零
- 全量干净基线：1019 过/278 失败（剔除 404 未实现后真实缺陷约 100 个）：package 17/order 7/menu 9/dict 9/dataset 12/member 9/dept 6/feedback 6/role 5/file 4 + 权限缺失类（接口存在但无管理员校验应 A0301）约 7 个
- 已固化：PUT 参数校验先于权限校验（recommendation/dept/package 三路由）、recommendation 表单校验、member 调整原因长度
- 进行中：package → order → menu/dict 逐模块推进

### 运维要点
- 成员 500 请求上限暂停后：用 Task 同名+同 team_name 重生，会话历史自动载回（已验证两次）
- go-fixer 曾遇 mock_IAuthService.go 为 mockery 风格不能跑 mockgen（会改写成 uber 风格破坏测试），须手工同步

## dehaze-java 迁移 JDK 25（用户确认执行）
- **pom 变更**：`maven.compiler.source/target` → `maven.compiler.release=17`（字节码目标不变，防 API 漂移）；lombok 1.18.32→1.18.48、mapstruct 1.5.5.Final→1.6.3（均 Maven Central 最新）；新增覆盖 Spring Boot 3.3.11 父级管理版本：mockito 5.23.0、byte-buddy 1.18.14（**JDK 25 上跑测试的必要条件**，旧版 Mockito/ByteBuddy 会 138/296 全挂）
- **验证结论**：JDK 25 编译通过（仅 lombok 内部 Unsafe 弃用警告，无害）；mvn test 296/296 全绿；服务在 JDK 25 运行正常；SDK auth 模块冒烟 15 过/7 挂（全是存量差距：5 个 No static resource + 2 个以 python 为基准的行为差异）
- **文档同步**：`04-项目实现/后端/01-Java架构文档.md` 首段 + README.md 技术栈行，已注明"字节码目标 Java 17，工具链 17~25"
- 构建命令：`JAVA_HOME=/usr/local/java/jdk25 mvn test`；服务：`JAVA_HOME=/usr/local/java/jdk25 python3 scripts/run.py restart java`

## 全量 SDK 跑出的两个 java 存量缺陷（已修复数据，未改代码）
1. **超管删除保护缺失**：user.test.ts "删除超级管理员" 在 java 端返回 00000 真删 → admin 被软删除（deleted=2），后续所有登录挂 A0500。已恢复 `UPDATE sys_user SET deleted=0 WHERE username='admin'`。python 端有保护，java 端缺
2. **A0500 误导性报错**：`AuthServiceImpl:144` 泛型 catch 把 `loadUserByUsername` 抛的非凭证异常（如查到已删用户）包装成"认证服务暂时不可用"，排查时应先查 DB 用户行状态

## 用户明确偏好（重要）
- **不要多次无意义重试**测试/命令
- java/go 后端很多 API 未实现，dehaze-python API 最全；SDK 测试套件以 python 为基准，java/go 上失败属预期存量状态，无需逐一排查
- admin 真实密码与 .env 的 ADMIN_PASSWORD 可能不一致（种子 bcrypt 固化），SDK 测试用的是 test/config/constant.ts 的默认值 `Dehaze2026`

## order 模块修复完成（java-fixer 第二波第 4 批，3 commits）
- **结果**：order.test.ts + order-edge.test.ts **82/82 全绿 0 skip**（原 17+14 失败），favorite.test.ts 35/35 全绿，mvn 296/296。java 侧累计 11 commits 未 push
- **commits**：`0e9bbc509`（先固化的 order 部分改动）、`f4b0ef2b5`（余额域补齐：4 实体+4 mapper+BalanceService+4 端点+A053B 余额校验+详情补 packageType/creditAmount）、`2b0333684`（SDK 余额自愈 ensureBalance）、`f2459eed8`（favorite 断言改新行）
- **根因分类**：①java 余额域整体缺失（recharge/balance-refund/audit/balance 端点 404、余额支付无校验不扣减）；②SDK 测试依赖开发库 user 预置余额且余额支付真实扣减 → 多轮运行耗尽后 A053B 级联失败（python 端同样会挂，属测试基建缺口，已加 ensureBalance 直写自愈）；③favorite"复活"断言基于软删治理前旧行为（uk 含 deleted 后取消重藏=新行）
- **教训**：①java 加余额校验前 sys_balance 空表 → 修复导致之前"假绿"的余额支付用例真实暴露，修复须与测试数据自愈同步做；②复验时失败数从 9 涨到 15 不是回归，是第一轮跑测试（无扣减）掩盖的余额耗尽；③dehaze-test/utils/api.request 请求体校验失败（400）会 raise_for_status，探查原始响应需用 httpx 直连

## go-fixer：A0301 全量验证 + package 模块修复（第三波）
- 进度固化 3 commits（314066561 middleware PUT 参数校验先于权限、10d6279a5 recommendation 必填、65140c9db member reason 256）+ 32b775fb0 fix(package)
- **A0301 全量验证**：test:go 全量 1019/278/275，2308a58b6 跨文件级联污染消除（73→仅剩孤立 3 处语义误用）。干净缺陷清单已报 lead
- **package 模块 17→1**（92/1/0）：唯一剩余 = package.test.ts:1089"删除后同名创建应失败"测试-基准冲突（python 端实测 create→delete→同名 recreate=00000 成功，uk_name 含 deleted 软删不占键位，与 favorite"复活"用例同性质，待 lead 决策）
- **gorm `default:1` 标签陷阱（系统性，java 需排查同款）**：Create 时零值字段被跳过 → 显式 status=0 落库变 1（禁用券/默认下架活动失效）；修复=移除模型 default 标签，DB 列默认值不受影响
- go SysPackage 模型曾整段缺失 package_type/credit_amount；sys_promotion 模型有 DB 不存在的 applicable_scope 死字段（促销创建 C0300 根因）
- 库存防超发：无条件 UPDATE → 条件原子递增（WHERE issued_qty+n<=total_qty 或 total_qty=-1）；领取错误码对齐 A0500/A0524/A0526/A0528
- 数据污染教训：旧并发缺陷每跑一轮全量在开发库遗留一张超发票券（共 8 张已 API 清理）；分页全量断言类用例会被历史污染打挂，先清数据再判缺陷
- 下一步：order 模块（7 失败：非法支付方式不报错、退款申请/驳回、退款不存在误 A0301 应 A0401、packageType/usedDays 契约字段、createRecharge 404 未实现、balanceRefund A0503 误报）

## package 模块修复完成（java-fixer 第二波第 5 批，commit 2dc0b9fec）
- **结果**：package.test.ts **93/93 全绿 0 skip**（原 17 失败），mvn 296/296。java 侧累计 13 commits 未 push
- **跨端协同实证**：go-fixer 的 32b775fb0（go 侧同款修复）提交于我跑 java 测试同一时刻，其 commit message 即共性根因清单——java 对照修了全部同款；期间 package.test.ts 被他改为新断言导致我首轮失败清单出现过旧用例名（行号与文件不符），甄别方法：错误栈行号 + responses 内容比对当前文件
- **修复**：①SysPromotion/PromotionForm 的 applicable_scope 超纲列（500 根因，表单字段保留落库忽略，对齐 python）；②CouponForm/CouponVO/UserCouponVO 的 applicableScope List<Long>→List<Object>（["credit"] 反序列化 400 根因）+ scope 混合匹配（int=套餐ID / str=packageType）；③体验券不参与计价 A0500 + 每人限领 1 次 A0500；④validType 枚举/折扣面值上限/适用商品合法性校验；⑤促销满减 tiers 阶梯；⑥详情补 packageType/creditAmount/creditUnitPrice，activePromotions 仅详情返回；⑦分页补 packageType 筛选
- **教训**：修复引入行为变化（余额扣减/校验收紧）前，先查测试数据前置依赖与自愈机制，否则会产出"越修越挂"的假回归

## go-fixer：order 模块修复 + 余额域补齐（第四波）
- 新 commits：3602b7118 test(package)（用户方案 a 先例：删除后同名创建改为可重建断言）、f39cef784 fix(order)
- **order + order-edge：82/82 全绿 0 skip（与 java 持平）**；package 92/1；go test ./... 全绿
- 关键根因：sys_refund_record go 模型残留旧设计列 used_quota（DB 无）→ 申请退款 INSERT 必炸 + A0301 级联；退款路由权限标识自造（refund:list/refund:audit）vs python（order:refund:list/order:refund:approve）→ admin 被误拒
- 补齐余额域：sys_balance/sys_balance_log/sys_balance_refund/sys_recharge 四表模型+仓储+服务；乐观锁 CAS（version）单行 UPDATE + 流水；余额支付真实扣减 A053B、重复支付 A0533、分页 1..100 A0400
- 支付履约对齐 python on_order_paid：成长值=实付 1:1（分→成长值）、积分卡支付即完成 status 3 不动等级、会员卡等级只升不降、level_source=purchase（go 单测期望同步改）
- 已知分歧：积分卡退款折算依赖 AI 积分余额，go billing 域未实现（18 个 404），暂 used_credits=0 全额退并注释标注
- 测试工程教训：order-edge 此前 beforeAll 因 credit 套餐创建失败整文件失败，真实期望在 package 模块修复后才暴露——模块间依赖修复会"解锁"新失败面，勿把上轮失败清单当完备
- java-fixer 已同步 go 侧跨端共性根因 7 条（gorm default:1 ↔ MP 零值跳过、applicableScope 混合类型、package_type 缺列、sys_promotion 无 applicable_scope、原子扣减、券错误码、满减 tiers）
- 待办：menu(9)/dict(9)/dataset(12)/dept(6)/feedback(6) 等剩余模块，听 lead 指示

## dict 模块修复完成（java-fixer 第二波第 6 批，2 commits）
- **结果**：dict.test.ts java **82/82 全绿**（原 11 失败），**python 端顺带 80→82 转绿**（原本失败的 2 个软删用例断言按 python 实际行为修正），mvn 296/296。java 侧累计 15 commits 未 push
- **commits**：`5ac4304ca` fix(dict)（code 只读 A0503、预置类型保护、下拉禁用类型过滤+缓存失效、name 查重 A0501、sort 默认 1、取值校验、XSS 允许多行）、`9561ac052` test(dict)（软删后重建断言改"可重建"）
- **关键判定**：1185/1213"软删占用唯一索引"断言与 python 实现矛盾——python SoftDeleteMixin 全局 do_orm_execute 事件对所有 ORM select 自动追加 deleted=0 过滤，唯一性查重天然仅活跃行，软删行不占用。跑 python 基准单文件确认（80/2，失败的正是这俩）后按用户方案 a 先例改断言
- **教训**：①共享测试文件被多端使用时，改断言前先跑 python 基准单文件确认真实行为（比读 python 源码推演更快更准）；②python 全局软删过滤机制（SoftDeleteMixin + do_orm_execute）意味着 python 侧所有"查重含不含软删"问题都以"仅活跃行"为准；③重建类用例注意 A0002 同 body 防重复提交（需 remark 等差异）与多次重建间的活跃行挤占（同 value 不能重建两次）

## go-fixer：dataset 模块修复（第五波）
- 317e6d28d fix(dataset)：dataset 三测试文件 **86/86 全绿 0 skip**（原 16 缺陷清零）；本会话累计 7 commits 未 push
- 关键根因（java 对照项已同步 java-fixer 与 lead）：
  1. 表单上限照抄 Java 旧 DTO（name max=255）而非列宽 64 → 超长走 DB 报 C0300；python max_length=64/500
  2. go 曾按 Java @PreventDuplicateSubmit 加 AntiRepeat，python 无 → 同 body 重名创建被防重拦截掩盖唯一性校验；已移除
  3. GET /datasets/evaluation-options 缺失被 /:id 吞掉 → 补（含清晰图 GT + 启用 + taskType 过滤）
  4. 批量删除结果对齐 python {total,succeeded,failed,results}：不存在 ID 计 failed(RESOURCE_NOT_FOUND)，succeeded 按请求 ID 计（级联不计）
  5. item-files 上传校验顺序：类型枚举→扩展名白名单→魔数一致性，全 A0400；go 原先非图片扩展名直接放行（存储型 XSS 防线缺口）+ 魔数校验在枚举前（B0403 而非 A0400）
- 测试工程：先广播再改测试文件（java-fixer 曾因 package.test.ts 版本错位排查浪费时间）；批次安排由 lead 协调避免同文件冲突
- 下一批：menu(9)→dept(6)→feedback(6)，dict 留 java 收口后

## role + member/member-edge 修复完成（java-fixer 第二波第 7 批，3 commits）
- **结果**：role/member/member-edge 三文件 **123/123 全绿**（原 25 失败），mvn 296/296。java 侧累计 18 commits 未 push
- **commits**：`fcdea84a6` fix(role)、`4f8dad24b`+`31987b943` fix(member)（后者是前者 add 笔误漏文件的补提交）
- **role**：PATCH status 补 sys:role:edit 权限（越权放行）、内置角色禁改状态 A0503、删除消息统一"不可删除"、RoleForm 校验（sort/status/dataScope/code XSS）、导入内置行优先拒绝 + dataScope 默认 2（原 5 越域）
- **member**：profile 补 levelSource、详情越权收紧（本人或 member:list）、分页 member:list + @Valid、到期筛选改 LocalDateTime（原 LocalDate 收 datetime 400）、changeValue=0 拒、reason ≤256、month 1-12 校验、补 6 端点（benefit-summary/trial-status/管理端 growth-logs/consumption-records/benefit-usage/operation-logs 走 Mongo 审计）
- **教训**：git add 多文件命令写错（dehazi-java typo）导致 commit 静默缺文件——commit 后必须 git show --stat 核对内容完整性；单测构造器注入参数变化需同步（MemberServiceImplTest 新增 4 个 @Mock）
- **trial-status 简化口径**：java 无 AI 积分流水域，aiTrialCreditsBalance 恒 0（测试仅断言 typeof 与派生不变量，等价成立）；若后续 java 落地 AI 计费流水需回填真实值

## go-fixer：menu/dict/dept/feedback 四模块清零（第六波）
- commits：658971a30 fix(menu)、ea8c4969a fix(dept)、e96590fee fix(feedback)、58683a223 fix(seed)；本会话累计 11 commits 未 push
- 复验：menu 53/53、dept 59/59、dict 全过、feedback 83/83，全部 0 skip
- **种子缺陷（跨端）**：sys_role_menu.sql 从未给 ADMIN 分配反馈菜单 88-96 → go/python admin 一律 A0301（python 基准实测同样失败 17 个）；java 因 admin 超管 bypass（isRoot 按用户）未暴露——跨端 isRoot 语义分歧待对齐。已修种子+dev DB
- **L1 滞留案例第二个**：feedback/rating 日限计数器走多级缓存 → L1 滞留导致外部清 Redis 无效；修复=GetL2Cache() 注入纯 redis（python 计数器直接存 redis 口径）。role:perms 是第一个案例
- menu 根因：PATCH visible 路由参数名错位（c.Param("menuId") vs :id）+ 误读 query；错误码口径（唯一 A0501/规则 A0503）；python 无路径唯一与组件必填校验
- dept：删除不级联（A0502）；唯一含软删；sort 正整数（指针+缺省 1）
- dict：编码唯一仅活跃行；typeCode 只读 A0503；预置保护 A0503；下拉 join 类型状态；isPreset
- feedback：feedbackType 枚举、隐藏评价过滤（T-FE-026）、评分分布补 1-5 档
- 排查方法论：SDK 测试 login 有会话缓存，admin 权限快照在 redis session:{uuid} 里可直接解码核对；DB 侧用 dehaze-test db_query/pymysql 直查 sys_role_menu
- 剩余：role(5)/member(9)/file(4)/favorite(1)/recommendation(1)/task(1)/image-input(2) + 权限缺失类 7 个
