# 2026-09-09

## menu-backend（菜单管理 dehaze-python 后端审查修复，2026-09-09）
- 修复 6 处（menu_repository/menu_service/router/menu.py/schema/menu.py/menu_import/menu_export）：①删除菜单原为 `delete(SysMenu)` 物理删除（Java @TableLogic 逻辑删，契约违反）→ 改 soft_delete_by_ids；②save_menu 上级变更不级联子孙 tree_path（dept 同款缺陷）→ 参照 update_subtree_tree_path 平移前缀（menu tree_path 格式 ",1,2,"，前缀自带尾逗号可直接 LIKE）；③routes 补 meta.roles（join role_menu+role 聚合，对齐 Java/后端实现.md）且 get_route_menus 含隐藏菜单（meta.hidden 标记，原 visible==1 过滤使 hidden 恒 False 死逻辑）；④新增菜单默认授权 ROOT+ADMIN → 仅 ROOT（文档+Java 权威）；⑤批量删除去重+软删跳过+不存在才 A0401、router ids 解析非法值 A0400、补 perm/path 查询参数（后端实现.md §3 明列、SDK 测试已用 perm）；⑥导入导出 type 中文标签映射颠倒（导出 1↔2、导入同样颠倒→导入行 type 反向落库）双双修正，导入改为委托 save_menu（原旁路 create_menu 无 tree_path/无校验/无 ROOT 授权，Java 即委托 SysMenuService.saveMenu）。
- 存在性校验 exists_by_name/exists_by_perm 显式 execution_options(include_deleted=True)（含软删唯一口径，与 role/dept 先例一致；count select 是否被全局软删过滤拦截在 SQLAlchemy 中行为不显式，显式声明消除歧义）。
- 测试基建：conftest mock_redis fixture 增加清空共享 L1（_get_shared_l1 进程级单例跨用例残留 vs 每用例全新 fakeredis——读命中 L1 不写 redis，按 redis 断言缓存写入的用例误判，menu 缓存用例曾因此假失败）。全量 pytest 2052 passed。
- 【需决策①文档/三端矛盾】routes 隐藏菜单处理：需求 3.8.2+测试用例 T-MM-059 说"隐藏不生成路由"，但需求 3.5.3"隐藏仅导航不显示"+后端实现.md meta.hidden+Java 实现均为"生成但标记"。已按 Java 权威（生成+标记）实现，T-MM-059/062 与 Java 矛盾需用户裁定（若裁定服务端过滤，menu:routes 全局缓存设计需改按角色键，三端联动）。
- 【需决策②】T-MM-062"仅返回用户有权限访问的路由"（服务端权限过滤）与 Java 全局缓存+meta.roles 前端过滤模式矛盾；menu-test 报的"routes 不按角色过滤"安全问题本质即此契约分歧，已通过 meta.roles 提供过滤依据，契约级变更需三端联动决策。
- 【三端不一致记录】Java MenuQuery 仅 keywords（缺 perm/path/type/visible，SDK perm 筛选用例在 Java 侧会假通过）；Java PATCH /menus/{menuId} 无 @PreAuthorize（python 有 sys:menu:edit）；Java listMenus 不做 type/visible 过滤。menu-frontend 汇报的 sys_menu.py 实体注释实际正确（1:菜单 2:目录），无需订正。

## menu-frontend（菜单管理前端/SDK 审查修复，2026-09-09）
- SDK：`MenuQuery.type` 收窄为 `number`（后端 GET /menus 查询参数仅收整数，原 `MenuTypeEnum|number` 联合是坏兼容）；`RouteVO.component` 收窄 `any→string`、`children` 改可选（后端叶子为 null）；`MenuAPI.update(id)` 参数 `string→number`；pnpm build 已重建 dist。
- front-vue 菜单页：显示状态切换从「PUT 全量 spread row（含 children）」改用契约 PATCH `MenuAPI.updateVisible`；补齐文档要求的 5 个搜索条件（keywords/perm/path/type/visible）；删除确认文案对齐文档（级联删除提示，去掉与逻辑删除矛盾的"不可恢复"）；默认展开第一级改为动态 expandedKeys（原硬编码 '1'）；handleQuery 改 finally 防 loading 卡死；删除死代码 selectedRowMenuId/onRowClick。
- **关键缺陷（已修）**：菜单变更后 `generateRoutes` 只更新 permission store 侧边栏数据，不重新 router.addRoute → 新增菜单路由不生效需手动刷新。plugins/permission.ts 导出 `reloadDynamicRoutes()`：移除旧动态路由（按顶层 name 追踪）+ 重新拉取注册 + 命名化 404 catch-all（hasRoute 防重复）。
- 类型收紧连锁修复（children 变可选导致 TS2345）：front-vue permission.ts 与 front-react permissionSlice.ts 递归改传原始 `route.children`（顺带修复原 bug：此前把已转换的 RouteRecordRaw 传回 RouteVO[] 递归）；front-react menu/index.tsx `update(String(id))` 改 number。
- 【需决策】①预置菜单保护（需求规格 3.4 预置不可删、前端禁用+后端兜底）：sys_menu 无预置标识字段、三端后端均未实现——需先定数据模型再加前后端；②表单条件显示与文档偏差：visible 文档"始终显示"但按钮类型隐藏（合理保留）、icon 文档"仅目录/菜单"但页面外链也显示（外链导航需要图标，合理保留）。
- 【汇报给后端】python `sys_menu.py` 实体注释 `type` 写成 "1:目录 2:菜单"，实际映射 MENU=1/CATALOG=2（schema.sql 与 service 均正确），注释需订正。

## dept-test（部门管理模块测试强化，2026-09-09）
- 新增 `dehaze-python/tests/service/test_dept_service.py`（22 用例 requires_db）+ `tests/router/test_dept_router.py`（14 用例 api）+ SDK `dept.test.ts` 追加 12 用例（脏语料往返保真须 unique 化名称——同级含软删唯一后固定名称跨运行必撞 A0501，角色模块同教训）。pytest 38 过+2 暴露失败；SDK 48 过+11 失败（8991 被 dept-backend 实时 reload：PUT 部门 500 NameError traceId bcc74d94417b79e12719d8d71edb989d + socket hang up，待部署稳定复跑）。
- **暴露缺陷①**：update_dept 移动非叶子部门只改自身 tree_path/parent_id，不级联子部门 tree_path → 孤儿子树（数据权限 children_ids/循环引用检测/层级计数失真）；Java SysDeptServiceImpl.updateDept 同缺（三端一致缺口）。
- **暴露缺陷②**：apply_data_scope 过滤后父节点不在结果集，_build_dept_tree/get_dept_options_tree 把"既非根又找不到父"的子树静默丢弃 → scope=1 部门管理员 options/list 返回空。修复方向：父不在结果集提升为根。
- dept-backend 重构已落地并用测试固化转绿：根部门保护（改上级/删除 A0503）、同级含软删唯一（A0501，check_name_exists 用 include_deleted=True）、混合 ID 批量删除整体拒绝 A0401、get_children_ids 前缀带分隔符（or_(tree_path==prefix, like(prefix+",%"))，修复兄弟子树误匹配）、删除显式 A0401、get_by_ids(for_update=True) 行锁防 TOCTOU。
- **需决策**：文档 API接口.md §4 根部门错误码 A0234 与三端实现（均 A0503）矛盾，建议按多数派修订文档；dept 名称唯一性已按"同级+含软删行"落地（对齐文档 T-DPT-009/035b，sys_dept 无唯一索引、纯应用层校验）。
- 层级计数口径：tree_path "0"=第1级、"0,1"=第2级（_assert_max_dept_depth 按 split 长度）；5 级链根(1)+4 个新建，第 5 个 = 第 6 级被拒——写层级测试时链长易错。
- 服务层直调 create_dept 不再拦 XSS（_validate_name_safety 已移除），防护收敛到 schema 层（router 测试覆盖 A0400）。
- **最终态（dept-backend 修复后复跑全绿）**：pytest dept 相关 52/52（service 26 + validation_fix 12 + router 14），SDK dept 59/59，0 skip 0 暴露失败。修复项：tree_path 级联（update_subtree_tree_path 同事务前缀替换）、scope=1 孤儿提升为根、NameError（update 导入被并发编辑覆盖）。
- SDK 部门用例三教训：①64 字符边界名称需毫秒尾缀 unique 化（固定中文名跨运行撞含软删唯一）；②后端 DeptForm.sort 口径为 ge=1（sort=0 报 A0400）；③同 body 快速重建必撞 A0002 防重复提交，测唯一性须换 body 字段（如 sort=101）——角色模块已验，部门模块复现。
  【订正】dept-backend 最终实现恢复了 _validate_name_safety（复用公共 validate_no_xss，ValueError→A0400），服务层/schema 双层防护保留；导入 handler 名称也补了 validate_no_xss + 层级深度校验（对齐后端实现 §8.2）。

## dept-backend（部门管理模块 dehaze-python 审查修复，2026-09-09）
- 全部修复落地且 pytest dept 定向 62/62 绿（含 dept-test 暴露用例）：dept_service 重构为显式错误码（A0401 不存在/A0501 同级同名含软删/A0502 子部门·用户/A0503 内置保护+循环引用/A0504 层级），裸 BusinessException(B0001) 清零；根部门改上级保护补齐（Java 同缺待对齐）；移动非叶子部门同事务级联平移子树 tree_path（repo.update_subtree_tree_path，func.concat+substring 前缀替换，Java 同缺待对齐）；get_children_ids 前缀带分隔符修复 "0,1%" 误匹配 "0,10" 兄弟子树（影响 user/order/export 数据权限）；get_dept_list 补传 children_ids（原 scope=1 直接空集）；树构建"父不在可见集提升为根"（service._build_dept_tree + repo.get_dept_options_tree 两处）；删除流程 get_by_ids(for_update=True) 行锁防 TOCTOU、不存在 ID 整体拒绝 A0401；router ids 非数字 A0400；DeptForm.sort 正整数默认 1（文档 §3.2.2）。
- 文档已按三端多数派修订（main 批准）：API接口.md §4 删 A0234（A0503 内置保护）、A0501 补"同级+含软删"；需求规格 §2.3 A0234→A0503；后端实现 §3.2 补移动级联/树构建容错行；测试用例 T-DPT-031/032 同步。
- SDK dept.test.ts 根保护/循环引用期望码补 A0503（保留 A0234 兼容列表）；tests/service/test_dept_validation_fix.py 更新深度用例改非根 id=7 并补根保护/不存在删除/名称重复用例。
- 遗留需 main 协调（Java/Go 待同步）：①Java updateDept 缺根部门改上级保护+缺子树级联；②Java 名称查重为全局+排除软删（文档/Go/Python=同级+含软删）；③文档 §5 dept:tree 缓存三端均未实现列表树缓存（仅 options 缓存）；④DeptTreeVO.treePath 文档有、三端 VO 均无（SDK model 已删该死字段）；⑤时间格式三端不一致（Java HH:mm/Go RFC3339/Python ISO，全局口径 HH:mm:ss）；⑥A0504 枚举名 DATA_BIND_EXISTS 语义与"层级超限"错位（全局枚举命名问题）。
- 【追加任务完成】Java 对齐 3 项：SysDeptMapper 新增 countByNameForLevel（@Select 原生 SQL 绕过 @TableLogic，同级+含软删+excludeId 排自身）与 updateSubtreeTreePath（@Update CONCAT+SUBSTRING 前缀平移）；SysDeptServiceImpl.updateDept 补 getById 存在性断言、根部门改上级保护（BusinessException OPERATION_NOT_ALLOW=A0503，必须置于循环引用检测之前——所有子树 tree_path 都含 ",1,"，后检会误判移动根部门为循环引用）、移动后级联平移（parentId 变化时才执行）；saveDept/updateDept 名称查重改同级含软删。mvn compile 通过（Java 无 dept 单测，覆盖缺口）。文档：DeptTreeVO 删 treePath 行；§5 缓存删 dept:tree 行改为规划项注（列表按数据权限过滤结果因人而异）；§9 删部门树缓存 TTL 常量行。

## dept-frontend（部门管理 front-vue + sdk-js 审查修复）
- **sdk-js**：src/api/dept/model.ts 删除 DeptVO.treePath（三端后端均不返回的死字段，无引用）；pnpm build 重建 dist 通过。
- **front-vue views/system/dept/index.vue**：修复单个删除传 number 致 SDK deleteByIds `ids.trim()` 崩溃（改 String(row.id)）；移除 el-table tree-props 非法 checkStrictly；补齐文档要求的：行内状态开关（update 全量表单、失败回滚）、根部门 id=1 删除禁用+编辑时上级选择器禁用、编辑时上级选项过滤自身及子树（防循环引用）、达 5 级禁用新增下级（findDeptDepth 递归算层级）、部门用户查看弹窗（UserAPI.getPage deptId）、formData 补 DeptForm 必填 name 初始值（修既有 TS 错误）；rules trigger/名称上限 64 对齐后端。vue-tsc dept 相关错误清零。
- **SDK dept 集成测试**（python 8991）：54/5——5 个失败均为后端问题非本次改动：①根部门保护后端返回 A0503+HTTP400（文档错误码表为 A0234，且业务错误走 HTTP 400 使 expectBizError 拿到 AxiosError）；②sort=0 后端拒绝（Python 代码 DeptForm ge=0，运行中服务疑似旧版或三端口径不一致）；③"不同父部门同名应拒绝"用例与文档"同一 parentId 下唯一"矛盾；④软删复用撞 A0002 防重复提交（已知）。
- **需决策/报备**：①文档"部门下拉选项可配置仅启用"，三端 options 接口均无该参数；②SDK 多模块 createTime/updateTime 类型为 Date，实际返回 "yyyy-MM-dd HH:mm:ss" 字符串（全局口径问题）；③Java DeptVO 时间格式 yyyy-MM-dd HH:mm（分钟级）与口径 HH:mm:ss 不一致；④API接口.md 无 dept 导入导出端点但前端挂了 ImportExportToolbar（后端有 handlers，文档缺记）。

## role-fix2（角色管理 5 项决策执行：①在线权限传播 ②role:options 缓存 ③afterCommit ④权限提升防护 ⑥RoleFormVO 对齐）
- **①在线用户权限传播**：assign_menus_to_role 后反查角色关联活跃用户（sys_user_role join sys_user deleted=0）逐个踢出会话；Python kick_user_sessions 重构为 SCAN `session:*` 按 userId 匹配（原依赖 session:user:{userId} 索引，但索引仅在 USE_MULTI_POINT 开启时维护——默认 False，原实现对禁用多点的环境是空转 bug），新增 kick_user_sessions_batch（批量单次扫描，ROOT 会话保护）；Java/Go 等价实现（Java keys+username 匹配/hutool JSONObject、Go rediscache.GetClient().Keys）。**角色删除路径未加 kick**（删除前置校验已保证无活跃用户，kick 为死代码）——已向 main 说明
- **②role:options 缓存（TTL 1h 代码常量）**：Python get_json_with_loader + CacheService.delete 失效（create/update/status/delete/import 全覆盖，import handler 经 defer 回调内自取 redis 客户端）；Java stringRedisTemplate JSON 缓存（常量收口 SystemConstants.ROLE_OPTIONS_CACHE_KEY），**顺带修正 Java options 原缺 status=1 过滤**（Python/Go 均有）；Go service 层 JSON 缓存，repo FindOptions 去掉 isRoot 参数改为缓存全量含 code、可见性过滤移到 service 层（避免首个调用者视角污染缓存，三端口径一致）
- **③缓存清理时机 afterCommit**：Python 新增 defer_after_commit/run_after_commit_callbacks（database.py），DBSessionMiddleware 统一在 commit 后执行；**会话 ES 同步的 es_sync_conv_ids 机制已并入该通用回调（单轨）**，DBSessionMiddleware 构造器不再注入 sync_conversation_to_es；get_db_session 也执行回调；Java assignMenusToRole/deleteRoles 用 TransactionSynchronizationManager.runAfterCommit（仿 TaskServiceImpl，带 isSynchronizationActive 兜底）；Go repo 事务在 repository 内提交，service 缓存操作本就在提交后，核查无缺陷未改
- **④权限提升防护（A0301"访问未授权"三端统一）**：非 ROOT 操作者的权限集合必须覆盖被分配菜单的全部按钮 perm，否则拒绝；ROOT 旁路（种子数据 ROOT 角色无菜单、快照无 perm，必须旁路）。Python service 接收 UserContext（user_service 有先例）；Java SecurityUtils.getPerms()（authorities 过滤 ROLE_ 前缀）；Go handler 从 claims.Authorities 提取后传参（service 不依赖 gin）
- **⑥RoleFormVO 三端对齐**：统一 id/name/code/sort/status/dataScope/dataScopeLabel/createTime/updateTime；Java RoleForm 加 3 字段+converter（entity2Form dataScope→dataScopeLabel、form2Entity 时间字段 ignore）；Python RoleFormVO+router 加 updateTime（dataScopeLabel 本就有）；Go read.RoleForm/bo.RoleFormBO 加字段（顺带修 FindPage 漏选 update_time）
- 测试：**pytest 全量 1970/0/0**（基线 1965+新增）；role 定向 67/67；新增用例：菜单变更踢会话（fakeredis 造 session+索引）、options 缓存命中/失效、越权分配被拒/持有放行/ROOT 旁路；测试要点：改密/禁用/删除踢会话的旧测试 session payload 原为 "{}"（无 userId），已补真实 payload；Java Role*Test 22/22（RoleImportHandlerTest 补 StringRedisTemplate @Mock）；Go mockery 重新生成 mock + go test 全绿
- 文档：角色管理/后端实现.md（§2.2 表单回显字段+三端一致性注、§3.2 权限分配逻辑加权限提升防护步骤、§4.2 缓存口径与在线权限传播触发点/方案）、认证管理/后端实现.md（§5.1/5.2 索引仅多点开启时维护、批量踢出 SCAN 机制）
- 遗留需 main 决策：①非 root options 可见性口径不一致（Python 隐藏 ROOT+ADMIN、Java/Go 仅隐藏 ROOT）②update_role_status（禁用角色）未踢在线用户（lead 任务书未含，文档 §4.2 提及"状态变更"联动）③SDK 测试待服务重启后由 lead 复跑
- **追加坡次（lead 批准）**：①禁用角色踢在线用户（启用不踢）三端落地（Python _defer_kick_role_users on status==0、Java updateRoleStatus runAfterCommit+kickRoleUserSessions、Go UpdateStatus kick）；②非 root 下拉可见性三端统一为隐藏 ROOT+ADMIN（Java SystemConstants.ADMIN_ROLE_CODE、Go 过滤加 ADMIN）。新测试：Python test_role_service 31/31（禁用踢/启用不踢、非 root 隐藏内置角色——注意种子数据 ADMIN 角色名为"系统管理员"）+ pytest 全量 1972/0/0；Java SysRoleServiceImplStatusKickTest 3/3（spy getById/updateById 模式）；Go internal/service/role 测试 3 用例（kick 依赖真实 redis client，单测中 GetClient 为 nil 自动短路，只能验证缓存刷新路径）。文档 §4.2 两处口径同步。

## role-fix（角色管理模块缺陷修复）
- 缺陷①修复：三处 role:perms:* 失效（role_service._clear_role_perms_cache / menu_service._clear_menu_cache / auth_service.kick_user_sessions）由裸 redis.delete 改走 CacheService(redis).delete（双清 L1+L2+Pub/Sub 广播）。全局排查确认无其他"CacheService 写入 + 裸 redis.delete 失效"绕过点（voice/package/promotion/dataset/prediction 等为纯 L2 读写一致；provider_health/dict/ai_model/agent 等本就走 CacheService.delete）。
- 缺陷②修复（单轨取舍）：user_repository.count_users_by_roles join SysUser 过滤 deleted=0，角色删除校验以"活跃用户"为准；delete_users 不清理 sys_user_role（软删语义下保留关联，用户恢复后角色不丢，避免双轨）。test_delete_role_with_only_deleted_users_should_succeed 转绿。
- 三端 VO 对齐（main 决策，对齐 Go 口径）：Java RolePageVO 补 dataScope/dataScopeLabel（RoleConverter 新增 entity2PageVO 元素级映射 + dataScopeLabel default 方法；注意 Page 级 @Mapping 无效会编译报错，元素映射必须单独声明）；Python RolePageVO 补 updateTime（router role.py page 响应同步）。
- 测试：role 定向 39/39 过，pytest 全量 1965/0/0；Java mvn compile 通过且生成代码确认应用 dataScopeLabel 转换。
- Java/Go 缓存核查（未修改）：Java role:perms 全程走 stringRedisTemplate（L2-only 读写一致，从未进 Caffeine L1，@Cacheable 的 user:auth 等经 MultiLevelCacheManager+CacheInvalidationRelay 无绕过）；Go role:perms 经 multilevel cacheManager 的 Set/Delete（L1+L2 双清）一致。两端口径均无 Python 侧该缺陷。
- 三端"软删用户阻塞角色删除"核查（lead 追加任务）：Java countUsersForRole（SysUserRoleMapper.xml）本就 join sys_user deleted=0，无缺陷未改；Go RoleRepository.HasUsersInBatch 原按 sys_user_role 裸计数（同 Python 缺陷），已修复 join sys_user 过滤 deleted=0（不清理 sys_user_role，与 Python 口径一致），go build + go test ./... 全绿。

## role-backend（角色管理模块 dehaze-python 审查修复）
- 修复：role_service.delete_roles 的 ids 解析健壮性（非数字/空值报 A0400，原 500）；update_role_status 变更后清除 role:perms 缓存（对齐文档 §3.2 与 Java）；create_role 去除不可达 dataScope 兜底默认值。
- 修复导入导出：role_import data_scope=5（非法值，有效域 0-3）→ 默认 2；导入拒绝内置角色编码（ROOT/ADMIN）；状态标签非法值由静默取默认改为整行报错。role_export 排除内置角色（文档 §3.8.2）；role_repository.get_list 新增 exclude_codes 参数；提取 _build_query_filters 消重。
- Go 三端对齐：roles/{roleId}/status 由 PUT 改 PATCH（java/python 均 PATCH、go menus 本就是 PATCH）；同步 swagger 注解 + 手工同步 docs.go/swagger.json/swagger.yaml（swag CLI 不可用）；go build 通过。
- 文档对齐（以 Java 权威 + 三端多数派）：API接口.md 接口清单 PUT→PATCH（status/menus）；错误码表删 A0233（三端不存在，实为 A0503 内置角色不可删除/不可禁用）、关联用户 A0502→A0500；后端实现.md §3.2/§6.2 同步。
- 测试：pytest -k role 11 passed；tests/service/import_export/ 全量 71 passed。
- 遗留（未擅改，已报备 main）：①在线用户权限传播（§4.2 会话失效联动）三端均未实现，角色变更后已登录用户权限快照直到重新登录才更新；②role:options 缓存（§4.2 TTL 1h）python 未实现；③缓存清理时机为事务提交前（文档要求 afterCommit），python/设计层差异；④权限提升风险：持有 sys:role:edit 者可给自己角色分配任意菜单（三端同现状，需设计决策）；⑤RolePageVO.updateTime 文档有、三端 page 响应未见（低优先）。

## role-test（角色管理模块测试强化）
- 新增 `dehaze-python/tests/service/test_role_service.py`（23 用例，requires_db）+ `tests/router/test_role_router.py`（13 用例，api 越权/参数校验）；增强 `dehaze-sdk-js/test/modules/role/role.test.ts`（追加约 21 用例：脏语料往返保真/参数边界/软删编码复用/越权 A0301/授权变更→重登录权限回收链路/角色导入 T-RM-049~051）。最终态：pytest 38 过+1 暴露失败；SDK 58 过+1 暴露失败，0 skip。
- **暴露缺陷①（安全）**：role:perms:* 缓存失效用裸 `redis.delete`（role_service.py:382 / auth_service.py:532 / menu_service.py:614），绕过 CacheService 未清 L1 进程内 TTLCache → 清空角色菜单+重新登录后旧权限仍生效（L2 null 但 L1 命中）。修复方向：统一改 `CacheService(redis).delete(key)`。已报 main 待派修复。
- **暴露缺陷②**：`user_service.delete_users` 软删用户不清理 sys_user_role，且 `count_users_by_roles` 不排除已删用户 → 已删用户永久阻塞其角色删除。修复方向：count join sys_user 过滤 deleted=0 或删用户时清理关联。
- 测试工程要点（角色模块）：sys_role 的 uk_name 唯一索引含软删行 → 角色名称事实上永久唯一，测试中名称/编码必须用唯一生成值（固定中文/固定脏名称二次运行必撞 A0501）；防重复提交 A0002 会对"同 body 快速重建"拦截，测唯一性需换 body；MenuAPI.getList 返回树形，找 perm 需递归或用 `getList({perm})` 过滤；缺陷②修复前 SDK 权限链路用例每轮泄漏 1 角色（afterAll 清不掉）。

## role-doc（角色管理模块文档收敛，仅改文档）
- `前端实现.md`：删除独立数据权限抽屉（RoleDataScopeDrawer/ScopeOptions/saveDataScope），dataScope 归入 RoleFormDrawer 表单字段，新增"数据权限配置入口"说明——Vue 实现（`dehaze-front-vue/src/views/system/role/index.vue`）确为表单内 el-select + 随表单提交，后端无独立 dataScope 端点。
- 表单校验按 Java `RoleForm` 权威修订（`需求规格.md` §3.2.2、`后端实现.md` §2.2）：name `@NotBlank @Size(max=64) @Pattern(^(?!.*javascript:)(?!.*<[a-zA-Z]).*$)`（原文档 2-30 错误）；code `@NotBlank @Size(max=32)` 无格式约束（原文档"小写字母+下划线 3-32"错误，内置编码 ROOT/ADMIN 为大写）；dataScope 新增时必填（Java service 报 A0400）。
- **待决策（已报 main，未改）**：`config/sql/schema/sys_role.sql` 的 `UNIQUE INDEX uk_name(name)` 不含 deleted → 角色名称含软删行全局唯一；但 `测试用例.md` T-RM-019 写"名称允许重复"。Java `countByNameOrCodeAll`/Python `check_name_exists` 均按含软删查重。改文档承认唯一 vs 改实现允许重复（涉及 DDL）二选一。
- **三端 VO 不一致（新发现，已报）**：RolePageVO 字段 Java=id/name/code/status/sort/createTime/updateTime（缺 dataScope/dataScopeLabel）；Go=含 dataScope+dataScopeLabel+updateTime；Python=含 dataScope+dataScopeLabel 但无 updateTime。前端列表列与需求规格 §3.1.3 依赖 dataScopeLabel → Java 存在缺口待 role-fix。**main 决策**：三端统一对齐 Go 口径（Java 补 2 字段、Python 补 updateTime），role-fix 修完后我再补 `后端实现.md` §2.3 的 dataScopeLabel 行。
- **预置角色口径订正（main 批准，已改 `需求规格.md`）**：以种子数据 `config/sql/data/sys_role.sql` 为准——编码统一大写 ROOT/ADMIN/GUEST/DEPT_ADMIN/USER（原文档写小写 root/admin/user/guest），删除不存在的 `vip` 行、改为 `DEPT_ADMIN` 部门管理员；GUEST 数据权限 2（仅本部门，原写 3）、USER 3（仅本人）、DEPT_ADMIN 1。同步改 §1.2 功能目标、§2.3 前言（ROOT/ADMIN 内置不可删除，与 `BUILTIN_ROLE_CODES` 一致）、§3.4.3/§3.5.3 `root`→`ROOT`、§7.1 会员模块依赖、§7.2 `guest`→`GUEST`。`测试用例.md` 的 T-RM-051「Excel 含 `ROOT` 编码」与 §7 安全性验证「删除/禁用 `ROOT` 角色」已同步改大写（只改大小写，A0233 等错误码未动）。
- **名称唯一性：用户决策采纳方案 A（2026-09-09 已执行）**——文档承认角色名称含软删行全局唯一（`uk_name(name)` 无 deleted 前缀）。改动：`测试用例.md` T-RM-019 期望「允许重复，修改成功」→「修改失败，提示"角色名称已被历史记录占用"」；`需求规格.md` §5.3 唯一性校验补"角色名称、角色编码（均含已删除记录，已软删不可复用）"、§3.2.2 name 校验规则补"系统唯一（含已删除记录）"。零 DDL、零代码改动。
- **RolePageVO 三端已对齐（role-fix 2026-09-09 完成，文档已同步）**：Java 补 dataScope/dataScopeLabel（RoleConverter 元素级映射 + default dataScopeLabel(Integer)，空值→""）、Python 补 updateTime；标签文案统一 0 全部数据/1 部门及子部门数据/2 本部门数据/3 本人数据。`后端实现.md` §2.3 已加 dataScopeLabel 行与三端一致性说明。遗留：Python `RoleFormVO`（GET /{id}/form）仍无 updateTime，文档未定义表单 VO，待 main 决定是否对齐。


## 角色管理模块（3/28，2026-09-09 完成，commit 04e0b28bb，29 文件 +995/-252）
- 5 成员（backend/frontend/test/fix/doc）。重大修复：①【安全】role:perms:* 失效绕过 CacheService 只清 L2 不清 L1（L1 TTLCache 进程内），清空角色菜单后重新登录仍持旧权限可越权——三处（role/menu/auth service）统一改 CacheService.delete 双清；全仓排查无其他绕过点；Java/Go 核查无此缺陷（Python 特有 L1+L2 架构）②软删用户永久阻塞角色删除（count 不过滤 deleted）Python/Go 修复，Java 本就正确；口径：只算活跃用户、不清理 sys_user_role 保留恢复语义 ③导入 data_scope=5 非法落库/空库可导入 ROOT 绕过超管保护 ④Go status PUT→PATCH 三端统一
- 三端 RolePageVO 对齐 Go 口径（Java 补 dataScope/dataScopeLabel+元素级 MapStruct 映射、Python 补 updateTime）；文档全面以实现/Java/种子数据为准订正
- 最终态：pytest 全量 1965/0/0、SDK role 59/59（权限回收链路用例验证转绿）、go test 全绿
- 【缓存架构事实】Python CacheService 为 L1（进程内 TTLCache）+L2（Redis）+Pub/Sub 广播双层——所有经 CacheService 写入的键失效必须走 CacheService.delete，禁止裸 redis.delete（role:perms:* 教训）
- 待用户决策 6 项：①在线用户权限传播（角色变更失效在线会话，三端未实现，全局架构）②role:options 缓存 TTL 1h Python 未实现（性能项）③缓存清理事务提交前 vs afterCommit 三端不一致口径 ④权限提升风险（sys:role:edit 可自授任意菜单，安全设计）⑤角色名称唯一性方案 A（文档承认 uk_name 含软删唯一，推荐）/B（改实现允许重复，DDL+三端+测试）⑥RoleFormVO 表单回显章节是否补+三端对齐（Python 含 dataScope/createTime 无 updateTime、Java 返回 RoleForm 无时间字段）
- role-doc 成员保留待命挂起①⑤⑥执行项


## 角色模块决策执行（role-fix2 + role-doc，2026-09-09，commit fcd305f2f，48 文件 +1313/-185）
- 6 项决策全部落地：①权限传播（菜单变更/禁用后提交回调反查活跃用户批量踢出，启用不踢）②提权防护（操作者权限需覆盖被分配权限标识，A0301，ROOT 旁路）③缓存 afterCommit（Python 通用 defer_after_commit/run_after_commit_callbacks，ES 同步并入单轨；Java TransactionSynchronizationManager）④role:options 三端缓存（全量含 code+读取侧过滤可见性，非 root 隐藏 ROOT+ADMIN 三端统一）⑤名称唯一性方案 A（T-RM-019 反转+§5.3）⑥RoleFormVO 三端对齐+文档章节
- 【重要发现】kick 原依赖 session:user:{id} 索引但索引仅多点登录开启时维护（Python/Java 默认 False）——原 kick 在禁用多点环境空转，已改 SCAN session:* 匹配 userId/username；用户模块禁用/删除/改密踢会话联动受益
- 最终态：pytest 1972/0/0、Java Role*Test 25/25、Go 13 包、SDK role+user+auth 142/142
- 角色模块累计 2 commit（04e0b28bb + fcd305f2f）。进度 3/28 完成（认证/用户/角色），下一模块：部门管理
- Go kick 单测短板：GetClient nil 自动短路，踢出路径靠 Python 单测+SDK 集成覆盖


## 部门管理模块（4/28，2026-09-09 完成，commit 9cf1e0a40，19 文件 +1177/-155）
- 3 成员（backend/frontend/test）。高危修复：①get_children_ids LIKE "0,1%" 前缀误匹配兄弟子树→scope=1 数据权限越权泄漏 ②移动非叶子部门 tree_path 孤儿化（Python+Java 同事务级联平移修复）③scope=1 树构建父节点丢失静默丢弃子树（部门管理员列表不可用）→孤儿提升为根
- 三端统一：名称唯一性=同级+含软删（Java 原全局+排除软删已改）；根部门改上级保护 A0503（Java 补齐）；dept 错误码裸 B0001 清零
- 【实现陷阱】根部门保护必须置于循环引用检测之前（所有子树 tree_path 含 ",1,"，后检会误判移动根为循环）
- front-vue：单个删除必崩（number 传 SDK string ids）修复+5 项文档功能补齐（状态开关/根保护 UI/防循环/层级限制/部门用户弹窗）
- 最终态：pytest 全量 2018/0/0、SDK dept+user+role+auth 201/201
- 待用户决策：①删除拦截码三端不一致（Java A0504 vs Python A0502）+A0504 枚举名 DATA_BIND_EXISTS 与层级超限语义错位 ②全局时间格式三端不一致（Java HH:mm/Go RFC3339/Python 空格式）——已第三次被提出，建议专项治理 ③dept 导入导出端点文档缺记 ④options "仅启用"参数文档有实现无 ⑤SDK 多模块 VO createTime Date 类型全局口径
- Java dept 零单测（靠编译验证）；进度 4/28（认证/用户/角色/部门），下一模块：菜单管理
