# 2026-09-12

## kb-billing：KB embedding/rerank 计费（实施完成，pytest 15 新增 + 回归 148/2618 全绿，未 commit）
- **拍板结论（main 确认）**：rerank 按 token；检索 query 向量化计费（bill_type=embedding）；local 完全跳过不落 0 记录；单价读 sys_ai_model_price（任务书 cost 是笔误）；未配置售价 warn 回退 0 免费。
- **新增** `app/service/kb/kb_billing_service.py`：ensure（欠费熔断→配额→余额 fail-closed，抛 QUOTA_INSUFFICIENT）+ charge_embedding/charge_rerank（实扣 quota/balance deduct、落 sys_ai_billing pre_deduct=0、consume 流水）；local 跳过但 ensure 照常；单价走 ai_model_price_service.calculate（configured=False→0+warn）。
- **接入点**：document_service Step 6.5（分块精确 token_count 求和 ensure → 向量索引成功后 charge，失败不扣，计费主体=owner）；search_service（user_id 贯穿至 _run_retrieval/_rerank_kb_results，查询向量化与 rerank 均 ensure→调用→charge，token 用 _approx_tokens）；search 加 bill 参数（test_set_service 传 False，内部评估不计费）；search_internal user_id=None 不计费；**QUOTA_INSUFFICIENT 在 _retrieve_with_timeout 透传拒绝而非降级空结果**（关键边界：except BusinessException 单列）。
- **schema/注释**：sys_ai_billing.sql bill_type 注释 + 表注释加 embedding/rerank（comment-only 无 migration）；ai_billing_repository CHAT_BILL_TYPES 注释补充 KB 口径。
- **测试**：tests/service/test_kb_billing.py 15 用例（stub 式：credits 换算/local 跳过/未配置回退 0/0 价不扣/ensure 四拒绝/local 仍校验权益 + 检索接入：query token 计量/search_internal 不计费/缓存命中不重复计费/rerank token=query+候选/配额拒绝透传）。test_kb_service.py 与 test_kb_search_service.py 加 autouse `_no_kb_billing` fixture（流水线/检索测试不测计费，monkeypatch 单例 ensure/charge）。
- **文档**：AI计费管理 后端实现 §3.1 计量注记 + 新增 §4.9；AI知识库 文档管理（流水线 Step 6 计费 + 失败表预校验拒绝行）与检索引擎（新增 §1.1 检索计费 + §4.1 rerank 计费 + §6 降级表透传行）。
- **插曲**：与 mcp-hardening 竞态——其给残留 `_unused` 函数补 import 时我方 document_service 的 kb_billing import 被覆盖丢失（NameError），最终态已清 `_unused`+unused import、恢复 import，py_compile 全过。
- **遗留**：①计费仅 python 端实现（任务范围），Java/Go KB 未计费需登记三端差异；②外部 embedding/rerank 模型需管理员配置 sys_ai_model_price 才计费（未配置=免费）；③后续测试教训：跨模块计费接入时，既有"行为测试"文件需 autouse 跳过计费，拒绝路径在计费专项文件覆盖。

## algo-squad 算法管理+算法选择审查修复（2026-09-12 完成，pytest 56/56+回归 103 过，SDK 待 8991 重启）
- **SDK 代跑 63/72 根修（2 类）**：①tree 返回空 []——根因是"空分类过滤"在无分类标识字段时无法区分"空分类"与"根下算法"，而开发库已发布算法全是 SDK 测试动态创建的 parentId=0 扁平数据 → 整树被滤空（Java 原实现同样有此缺陷，只是无人直连 Java 消费 tree）。三端（python/go/java）撤销空分类过滤改全量返回，文档 §3.1 标规划中（待 task_type/节点类型字段落地恢复）；②A0501 名称重复用例撞 A0002 防重复提交——差异化 body（同 name 不同 description）绕过防重后命中唯一性校验（role 测试同款做法）。python 回归 56/56，go build/java compile/sdk tsc 全过，待 main 重启复跑
- **python 修复**：①状态机根修——python 原"终态不可变+仅拦已发布跳转"与 Java/Go 白名单完全不同（已发布永远无法停用→删除保护死锁），重写为三端统一 ALLOWED_TRANSITIONS 白名单（repository.AlgorithmStatus）+ DELETABLE_STATUSES（草稿/已停用/已归档可删，原仅拦已发布，TESTING/PENDING 也能删）；②删除语义——硬删改软删（soft_delete_by_ids，deleted=行id 对齐 Java @TableLogic delval=id）；③版本管理对齐 Java——is_active 单活跃管理（旧实现自动归档口径删除）+ 防重复回滚 + 回滚切换 is_active 回写主表；④权限补齐——POST /{id}/version、/{id}/rollback 加 sys:algorithm:version（原任意登录用户可回滚版本=越权）；⑤死代码清理——JSON 单算法导出 /{id}/_export、/_import/validate、/_import 三接口+service 三方法删除（文档无此接口，通用 import_export 框架已覆盖 Excel/CSV，且 POST /algorithms/_import 被通用路由先注册拦截=原 JSON 导入永不可达）；⑥create 名称查重 A0501（三端补齐）；⑦算法选择域：tree isLeaf→leaf+空分类过滤+保留 parentId（原 pop 掉）、search 空关键词返回空（原返回全量）、detail 补 path/version/status/ratingCount、test 支持 fileId 二选一+透传 PredictionResultVO 契约（原 processTime 自造字段）、compare 从元数据对比改为执行预测（对齐 Java/文档 §3.3：2-3 个+异常隔离置空+{algorithmId,algorithmName,resultUrl,time}）+ CompareRequest 加 fileId；⑧schema name max 64（对齐 DB varchar(64)）、删 form version/status 死字段；SysAlgorithmVersion UniqueConstraint 补 deleted 对齐 uk_algo_version
- **Go 修复**：repo FindByID/FindAll/FindPage/FindOptions/FindVersions 加 deleted=0（原全量裸查，软删行泄漏到 options/树/详情）；Delete 硬删→软删 gorm.Expr("id")；compare VO 对齐 {algorithmId,algorithmName,resultUrl,time}+裸数组（原 12 字段+PageResult 包装）+min=2；GetTree 改 AlgorithmSelectNodeVO（leaf+空分类过滤）；Search 空关键词返回空；Create 名称查重（借 FindAll keywords 免改接口/mock）；bo 长度对齐 DB（name 64/type 100/desc 2048）
- **Java**：addAlgorithm 名称查重 A0501；AlgorithmForm name max 50→64。Java 编译过，go build/vet/test 全绿
- **SDK**：AlgorithmSelectNodeVO isLeaf→leaf；AlgorithmCompareVO 删 metrics 死字段（三端均未返回）；tree()/search() 删 taskType 死参数（三端均未实现，文档标规划中）；build 通过
- **测试**：pytest 新增 tests/service/test_algorithm_service.py（27）+ test_algorithm_select_service.py（29）=56 全绿（独立库 dehaze_test_algo），回归 test_algorithm_recommend 9+prediction 36+import_export 67 过；SDK algorithm.test.ts 重写状态机参数化（11 非法流转+4 无效值）+compare 对抗（缺图片/未发布/异常隔离）+版本 is_active/格式校验+名称重复/删后重建+越权（普通用户 addVersion/rollback A0301）+性能烟测 3s×2
- **文档**：两模块 后端实现.md 加"实现状态速览"表；状态机注释改三端统一；版本流程图改 is_active 切换+uk 含 deleted；算法选择 §3.1 task_type 分组/§3.2 taskType 联动标规划中（表字段未落地）；compare 契约字段重写；API接口.md 树节点 leaf 字段/compare 执行预测/A0400 缺图输入
- **跨域报备**：feedback_repository.py 加 count_by_algorithm（ratingCount 口径，已私信 feedback-squad）；登记跨域遗留：prediction_service.predict 下载图片失败在扣配额后无回滚（fetch_image 抛异常配额泄漏，test/compare/predict router 共用，归去雾域）；Go 缺 audit/version/rollback 接口、Java 缺 recommend 接口已登记规划中
- **SDK 测试待 main 统一重启 8991 后跑** `pnpm test:python test/modules/algorithm/algorithm.test.ts`（python 端契约变更必须重启生效；algorithm-new.test.ts 兼容无需改）

## mcp-hardening：MCP 网关安全三项加固（2026-09-12 全部完成，live 20/20）
- **B1 网关共享密钥**：server.py 自行加载根 .env（run.py 不注入网关环境变量），新增 `X-MCP-Key` 鉴权 ASGI 中间件（hmac.compare_digest 常量时间比较，缺失/错误 401，`{"code":"A0231"}` 信封）；启动改 `streamable_http_app()+uvicorn.run(中间件)`，MCP_GATEWAY_KEY 未配置拒绝启动（fail closed）。客户端 McpGatewayClient `_session` 无密钥 raise RuntimeError（各方法 catch 降级），`streamablehttp_client(url, headers={"X-MCP-Key":...})` 携带；settings 新增 MCP_GATEWAY_KEY（默认空串，不静默裸连）
- **B2 M2M key 迁 .env**：`M2M_API_KEY` 入根 .env/.env.example；server.py 的 DEHAZE_API_KEY 变量改名（零回退无默认值）；java application-dev.yml `api-key: ${M2M_API_KEY}`、go config.yaml+config.prod.yaml `apiKey: "${M2M_API_KEY}"` 硬编码全删。python 侧校验本就走 DB（sys_api_key）无硬编码
- **B3 写操作白名单**：_parse_spec 层非 GET 工具仅白名单内注册（LLM 源头不可见），默认 `_DEFAULT_WRITE_TOOLS={"post_api_v1_prediction"}`（去雾预测=文档定义唯一常规 LLM 写场景；批量等高频写走直连不经网关），`MCP_WRITE_TOOLS` env 逗号分隔扩充；DELETE 原方法过滤保留，二者独立管道
- **测试**：gateway 单测 34/34（新增白名单 3 + 鉴权 4 用例，apis fixture 临时扩白名单放行 put 以保参数映射用例）；python 客户端单测 9/9（新增 header 携带/缺密钥拒绝）；8082 重启后 live 20/20 全绿（401 鉴权 3 用例、真实后端 00000 信封、lookup 7-12ms/次、非白名单写工具 execute 返回"不存在"）；live_test 首轮 1 个 FAIL 是脚本沿用旧 PUT 工具验"缺必填参数"（该工具已被白名单移除=预期行为），已改用 post_api_v1_prediction 验证并新增"非白名单写工具不可见"用例
- **顺手修**：kb_billing_service.py（他人未跟踪新文件）缺 `from contextlib import asynccontextmanager` 导致 pytest 收集全挂，已补
- **文档**：MCP/后端实现.md（启动流程 uvicorn+鉴权、过滤管道加白名单、配置表 M2M_API_KEY/MCP_GATEWAY_KEY/MCP_WRITE_TOOLS、新增 §6.1 鉴权/§6.2 白名单/§6.3 密钥管理）、需求规格.md（§1.2 网关鉴权行、§2.3.2 白名单行）
- **待 main**：重启 8082 后跑 `dehaze-python/.venv/bin/python dehaze-mcp-gateway/tests/live_test.py`；.env 的 MCP_GATEWAY_KEY=461c2054...b414cf21eaf7

## mcp-squad MCP 能力网关审查修复（2026-09-12 完成，24/28）
- **P0 发现（通道从未工作）**：McpGatewayClient 三个方法直接 call_tool/list_tools，从未 initialize——mcp 1.24 的 ClientSession.__aenter__ 不自动初始化，网关对未协商会话的 tools/call 返回 400，调用挂起后被 anyio cancel scope 取消；lookup/execute 的 `except Exception` 接不住 CancelledError 直接炸进推理链，list_tools 因 `except BaseException` **静默降级空列表**（生产一直表现为"网关不可用"，prefilter 一直退化为仅标题搜索）。修复：`_McpSessionContext.__aenter__` 返回前 `await self._session.initialize()`（dehaze-python/app/infrastructure/clients/mcp_gateway_client.py）。**需重启 8991 生效**
- **server.py 修复×6**：①删除 x-backend-url/x-dehaze-api-key 请求头覆盖（任意客户端可伪造后端地址使网关把 M2M key 发往任意 URL=SSRF/凭证外泄，客户端从未使用）；②绑定 0.0.0.0→MCP_BIND_HOST 默认 127.0.0.1（网关持 M2M 凭证且自身无鉴权）；③路径参数整体 quote 编码（原 str 直拼可注入额外路径段）；④binary/file 过滤失效根修——原比对 _describe_schema 文本首词（恒为"string"），改 schema format=binary/type=file + requestBody 无 application/json 排除；⑤递归/循环 schema 深度上限（_resolve 解引用10次、_describe_schema/_schema_example 深度5）——菜单树自引用原会爆栈；⑥OpenAPI 启动加载重试3次+首次工具调用按需补载（run.py java→mcp 无健康等待冷启动必失败）+ arguments 非对象拒绝
- **测试**：gateway 单测 tests/test_server.py 27/27（`cd dehaze-mcp-gateway && uv run --with pytest pytest tests -q`，网关 venv mcp 2.0.0 的 `mcp.server.mcpserver.MCPServer` 与 dehaze-python 1.24 客户端 wire 兼容）；live tests/live_test.py 15/15（mcp1.24 client↔mcp2.0 server 握手 57ms、路径注入被 Tomcat 400 拒、真实后端 00000 信封透传、lookup 12.2ms/次）；客户端回归 tests/infrastructure/test_mcp_gateway_client.py 7/7（mock 模块级 streamablehttp_client/ClientSession 让真实 __aenter__ 执行）；客户端端到端 4/4
- **文档**：MCP 三文档同步（启动重试/懒补载、配置仅环境变量+MCP_BIND_HOST、路径编码、arguments 校验、认证头表述）；AI对话/后端实现-架构与公共.md"新增 API 自动同步"改"重启网关生效"
- **待决策**：①网关自身无鉴权（127.0.0.1 绑定缓解，跨机部署需共享密钥）；②M2M key 硬编码默认值仍在 server.py+java/go 配置（secrets env-only 全局整改）；③M2M key 认证映射 owner 用户权限，owner 为 admin 时 LLM 可达 admin API（DELETE 已过滤，POST/PUT 未过滤 admin 端点）
- **坑**：网关 venv 与 python venv 的 mcp 版本不同（2.0 vs 1.24），server.py 测试必须用网关 venv（`uv run --with pytest`），客户端测试用 python venv

## 三模块并行启动（21-23/28，用户批准图像处理链三连）
- image-input-squad（图像输入 21/28）：重点越权/伪装文件（复用文件模块魔数口径）/sys_pred_log 关联
- dehaze-squad（去雾处理 22/28，F-REC-002）：重点任务状态机/算法调用健壮性/计费联动（后扣费 deduct 语义）
- compare-squad（效果对比 23/28）：重点 PSNR/SSIM 指标实现正确性（公式一致/边界图像/确定性不变量）
- 三模块是图像处理链紧邻（输入→去雾→对比），sys_pred_log/图像存储/算法客户端可能共享——改公共文件前向 main 报备归属；测试窗口串行
- 昨日（2026-09-11）总账：43 commit，20/28，决策执行轮 7/7 闭环；FileDTOFactoryTest 魔数 3 存量失败待查；核心模块剩：本轮 3 个 + MCP/数据集管理/算法管理/算法选择

## dehaze-squad 去雾处理审查修复（完成，待统一重启后跑 SDK 测试）
- **python 修复**：①越权×3——GET /prediction/{taskId} 与 POST cancel 加 create_by 归属校验（A0401 防枚举）、GET /logs 按 create_by 过滤；②状态机 guard——pred_log_repository.update_result/update_status 带 `status=processing` 条件更新返回 bool，取消后推理完成不再复活终态，并发取消仅流转成功方回滚配额；③run_dehaze 包 asyncio.wait_for 30s 超时（INFERENCE_TIMEOUT_SECONDS 常量）超时→failed+回滚配额；④predict 先校验算法再扣配额（防算法不存在配额凭空丢失）；⑤批量 20 张硬编码删除，sys_member_benefit.batch_limit 唯一权威（否则拦截 SVIP 1000 张）；⑥image_source 任意本地文件读取漏洞删除（原 imageUrl 支持本地路径！）+ SSRF 防护（_assert_public_host：外部 URL DNS 解析逐 IP 拒私网/回环/链路本地/保留，系统存储 base_url 前缀分支不受影响，evaluation_service 复用同样受益）
- **Java/Go 同步**：Java getTaskStatus 归属校验（Result.failed(RESOURCE_NOT_FOUND)）+ getPredLogPage createBy 过滤；Go GetTaskStatus/GetLogPage 加 userID（repo.FindPage 加参数、mock 手工同步）、UpdateResult/UpdateStatus 加 processing guard、BatchPredict 删 20 硬编码。mvn 编译过、go build/vet/test 过
- **测试**：pytest 新增 tests/service/test_prediction_security.py（20）+ tests/router/test_prediction_router.py（5）+ 既有回归 = 30/30 通过。SDK model.test.ts 加「越权防护」组（vip1 读/取消 user 任务 A0401 + 日志隔离，用 forceLogin 处理单点互踢），待 main 统一重启 8991 后串行跑
- **main 转办完成**：presentation/dehaze/index.vue 处理成功回调回写 ImageInputHistoryAPI.create（原图/结果图 URL、算法名/参数、耗时 res.time 毫秒、status=1、inputSource 按 upload/camera/sample 跟踪，失败静默），vue-tsc 本页 0 错
- **文档**：去雾处理五文档同步（API 归属口径/A0401 触发场景、后端实现 §3.4 取消归属+guard、§7.3 数据隔离、§7.5 新增超时与 SSRF、常量表 30s、需求规格批量上限权威口径、测试用例 §7 安全性表落地映射、前端实现回写数据流）
- **待用户决策**：recommendedBy 三端接收但后端完全未消费未记录（sys_pred_log 无字段，加列涉及共享 schema 三端迁移）

## 图像处理链三连进展（2026-09-12）
- **去雾处理完成**（commit 25667e8a9，17 文件，pytest 30/30，dehaze-squad 已关闭）：P0 任意本地文件读取（imageUrl 本地路径分支）+ SSRF 解析后 IP 防护（fetch_image，evaluation_service 复用受益）；越权×3（任务详情/取消/日志，A0401 防枚举）；状态机竞态（processing 前置条件更新防 CANCELLED 被覆盖/双重回滚）；推理 30s 超时；配额先校验后扣；批量上限权威化（batch_limit，原 20 硬编码拦 SVIP 1000）；历史回写链路补通（presentation/dehaze 回调 ImageInputHistoryAPI.create，云端历史此前无生产者）；Java/Go 同步（无 cancel 接口差异登记）。SDK 越权组待统一重启由 main 代跑（model.test.ts，forceLogin 切号）。
- 【待决策】recommendedBy 三端接收未消费未记录（采纳率追踪，sys_pred_log 加列涉及共享 schema）

## image-input-squad 图像输入审查修复（2026-09-12 完成，SDK 测试待 8991 统一重启）
- **python 修复**：①历史记录配额补齐——Java 原有 100 条固定配额升级为按 `sys_member_benefit.history_retention` 等级口径（100/500/2000/10000），python/go 补齐同语义（缺失回退常量 100，retention=0 也回退），超限自动清理最旧一条（create_time,id 升序）；②InputHistoryForm 强校验——URL≤500/algorithmName≤100/processingTime≥0/status∈{1,2,3}/inputSource Literal 枚举/algorithmParams 必须合法 JSON（MySQL json 列前置拦截）；③批量删除裸 dict→BatchDeleteForm；④item-files 越权修复——POST/PUT/DELETE/batch 加 `@require_permission("sys:dataset:edit")`（原仅登录即可对任意数据项挂图/改图/删图；数据集管理轮复核此修复）
- **Go 修复**：BatchDelete 返回实际删除数（原返回 len(ids) 假计数）+空数组返回 0；GetByID 越权→RESOURCE_NOT_FOUND 不泄露存在性；Delete 他人记录静默幂等；Create 加同套校验+配额（构造器加 memberRepo/benefitRepo，app.go 已接线）；repo 加 CountByUserID/DeleteOldest；keyword LIKE escapeLike 转义。go build/vet 过
- **Java 修复**：batchDeleteHistory 返回实际删除数（原 remove 返回 boolean 误报 ids.size()）；getHistoryById 越权→RESOURCE_NOT_FOUND；deleteHistory 他人记录幂等；createHistory 配额改 history_retention 等级口径（注入 SysMemberMapper+MemberBenefitService）；HistoryForm 加 @Size/@Min/@Max/@Pattern 校验。mvn compile 过
- **SDK**：clearAll() 去掉无效 confirm 参数（三端均无此参数）；HistoryQuery 删 isFavorite/startTime/endTime（三端均未实现，收藏走 favorites 模块）
- **front-vue**：image-input/index.vue 上传+拍照补 MD5 秒传（computeFileMd5+uploadCheck，对齐需求 §2.1.7 与 SingleUpload 范式），vue-tsc 0 错
- **测试**：pytest 新增 32 用例全绿（router/test_input_history_router.py 15 + service/test_input_history_service.py 11 + router/test_item_file_permission_fix.py 6，独立库 PYTEST_MYSQL_DATABASE=dehaze_test_imageinput）；dataset/file 回归 58 过。SDK history.test.ts 重写（+对抗组：无效枚举/超长 504 与边界 500/非法 JSON/负耗时/跨用户批量删除隔离/clearAll 无参）
- **conftest 并行隔离**：PYTEST_MYSQL_DATABASE 环境变量 opt-in 独立库 + GET_LOCK 按库名区分（compare-squad 先例沉淀），main 建议写入测试规范
- **文档**：图像输入五文档同步——API 错误码表重写（B0300/B0301 未落地已移除，实际 A0400/A0401；上传链路 B0402/B0403/B0404 归属文件模块）、权限表（image:upload/image:delete 废弃，item-files 用 sys:dataset:edit）、/api/v1/files 列为实际上传入口；后端实现加"实现状态速览"表（缩略图/缓存/定时清理/收藏保护清理/查询参数 isFavorite 时间范围=规划中）；需求规格 §1.4.3 回写链路已接通标注（dehaze-squad 补通）+ §2.4.2 存储策略现状标注；测试用例 §7 安全表+§7.1 已落地自动化清单
- **遗留**：①SDK history.test.ts 待 main 统一重启 8991 后跑（预计 ~30 用例）；②历史回写由 dehaze-squad 在 presentation/dehaze 完成（非我改）；③image_source SSRF 归 dehaze-squad 已修
- 归属裁定记录：image_source.py 归 dehaze（双 squad 发现同一漏洞，后者修）；item_file 越权归 image-input 修（B0407 口径，数据集管理轮复核）；历史回写归 dehaze
- 窗口模式升级：compare 开创独立测试库隔离（dehaze_test_compare），image-input 沿用（dehaze_test_imageinput）+ conftest opt-in 环境变量与按库名重建锁——三成员 pytest 并行，SDK 仍全局互斥；统一重启 8991 后串行跑三家 SDK
- image-input（pytest 中）：三端历史 API 契约修复（配额 100/批量删除真实计数/schema 校验/错误码）；compare（实施中）：越权×4、_is_qualified 小写查大写字典永远不合格、PSNR inf 崩 JSON 列、配额泄漏、死字段清理

## compare-squad 效果对比审查修复（2026-09-12 完成，SDK 测试待 8991 窗口确认）
- **python 修复**：①越权×4——GET /evaluation/{taskId}（router 层 create_by 校验，A0401 防枚举）、GET /evaluation/logs（list_logs 加 user_id 参数+repo get_paginated 加 create_by 过滤，原全量跨用户）、compare get_report_status/get_report_html/_get_owned_log（create_by 校验）、generate_report 的 pred_log 归属校验；②_is_qualified 修复——metrics 以 label("PSNR") 为 key 却查 "psnr" 小写，永远判不合格（改小写归一化比对）；③metrics.py 边界口径——calculate 前置 PIL 尺寸比对（不一致 fail-fast ValueError）、PSNR=inf（全同图 MSE=0）钳制 100.0（与雷达图固定上限口径一致）、其余非有限值（NaN）raise → 任务 failed 不落脏数据、_to_tensor 改收 PIL Image；④配额泄漏——evaluate 扣减后算法不存在/图片下载失败在日志创建前，异常路径 restore_quota；⑤报告 XSS——_build_report_html 对 algorithm_name/URLs html.escape（存储型 XSS：报告以 text/html 同源返回）；⑥死代码清理——CompareReportForm 删 format/includeMetrics/includeFilters（文档权威"仅 logId，固定 HTML"），update_result 直接收 dict，str 兼容分支全删
- **Java**：EvaluationController.getTaskStatus 归属校验（Objects.equals createBy）、getEvalLogPage(query, userId) 用户隔离、CompareController/CompareServiceImpl 归属校验、CompareReportForm 删死字段（@NotBlank format 一并删）。mvn -o compile 过
- **Go**：evaluation GetTaskStatus/GetLogPage 加 userID（repo FindPage 签名改 + create_by 过滤）、compare GenerateReport 归属/GetReportTaskStatus/GetReportHTML 归属、CompareReportForm 清理、buildReportHTML html.EscapeString、创建日志失败 refundQuota。镜像 dehaze-squad 的 prediction 同款口径。go build/vet/test 全绿
- **front-vue**：①雷达图固定上限（需求 §2.5.3：PSNR=100/SSIM=1/LPIPS=1/NIQE=10/NIMA=10/BRISQUE=100，原动态 max*1.3 违规）+ METRIC_META 表补方向标记/描述列（原 Contract 永远缺 better/description 字段，箭头列死代码）；②报告导出真 bug——overlap 页把 evaluateAndWait 返回的 eval logId 当 pred logId 传给 /compare/report（后端查的是 sys_pred_log！）；parallel 页 logId 恒 0；修：imageShowStore 加 predLogId（dehaze 页 predictAndWait 成功后写入，单图+批量），两对比页改用 store；③删 PDF/PNG 假选项弹窗（后端只出 HTML，下载名原还带 .pdf 后缀）、下载名改 dehaze-report.html；vue-tsc 0 错
- **SDK**：CompareReportForm 类型+factory 清理；model.test.ts 末尾加「越权访问控制（用户隔离）」4 用例（评估任务/报告生成/报告状态跨用户 A0401 + 评估日志列表双向隔离断言，TEST 账号切换）；顺手修正 dehaze-squad 新增用例的 expectBizError 传 lambda 签名笔误（已私信知会）。sdk build + tsc 通过（ai-skill.ts 存量错误非本域）
- **测试**：pytest 新增 34 用例全绿（tests/algorithm/test_metrics.py 9：尺寸/0字节/截断PNG/inf钳制/NaN/确定性不变量 + tests/service/test_evaluation_service.py 8 + tests/service/test_compare_report_service.py 11 + tests/router/test_evaluation_router.py 6，独立库 DB_NAME=dehaze_test_compare）；大回归 tests/service+router+algorithm 2514 过（1 失败为已知 flaky test_send_expires_at_by_type，单独复跑通过）
- **文档**：需求规格（§2.5.2 加边界口径、§2.8.3 指标区/参数区标规划中、§9.2 缓存标规划中）、API接口（logs 按当前用户、report 仅 logId、归属校验口径）、后端实现（§1.1 补 dehaze-go 三端实现、§4.2 边界口径行、§4.3/§7 缓存标规划中、§8.1 归属校验口径）、测试用例（T-EV-052/T-EV-079 标规划中）
- **待跑**：SDK model.test.ts（含去雾/图像输入两家的新用例）待 main 确认 8991 重启窗口后执行——python 端有新代码必须重启才生效
- **SDK 代跑第一轮 31/38 → 根修**：7 失败全在 evaluation 端点（ResponseValidationError 500）。根因是 `EvalLogRepository.update_result` 对 JSON 类型列手动 `json.dumps` → SQLAlchemy 再序列化 → 双重编码字符串标量（新行也复现，非仅存量）；修复=写入直传 dict（根修）+ 读取统一经 `evaluation_service.parse_eval_result`（str→loads、非法 warn→None，接入 /logs、/metrics、GET /{taskId} metrics、compare get_report_html 四处消费点）；连带失败（metrics undefined/socket hang up/越权前置空）预期自愈。pytest +5（parser 分支+round-trip 回归）本域 59/59。**教训：pytest 服务层测试拿实体断言不会触发 response_model 校验，VO 契约类缺陷必须在 router/SDK 层覆盖**。观察项待决策：/metrics 会混入报告任务行（result={reportHtml,...} 包装，completed 状态），如需过滤用 result NOT LIKE '%"reportHtml"%'

## dataset-squad 数据集管理审查修复（25/28，2026-09-12）
- **item_file 权限复核结论**：image-input 轮加的 sys:dataset:edit 与数据集域自洽（种子 menu 41 存在），但权威 API 文档 §2.3 规定图片删除= sys:dataset:delete——python 单删/批删已从 edit 修正为 delete；Go 原用 sys:file:edit/sys:file:delete（种子中不存在的权限码，等于无人可写）已改为 sys:dataset:edit/delete；**Java 数据集/数据项/图片文件三个 Controller 全域无 @PreAuthorize**（任意登录用户可增删改）已补齐
- **python 修复**：①文件安全——item-files 上传与配对上传补扩展名白名单（jpg/jpeg/png/gif/bmp/webp）+ PIL 解析校验（原 hazy-only 路径完全无校验，可传 .html/.svg → 存储型 XSS）+ MAX_UPLOAD_SIZE 上限（原整包读内存无上限）；②type 硬校验枚举 clear/hazy/trans/depth/segment（对齐 SDK）；③表单列宽对齐——dataset/item name 255/200→64（varchar(64)，超长原本 DB 500）；④get_children_count 补 deleted==0（列查询不走全局软删过滤器）；⑤delete_datasets 清理无效 with_deleted=True（全局过滤器恒注入，死参数）；⑥删死代码 imgUrl（response_model 本就剥离）
- **uk 软删复核**：数据集域删除全为物理 delete，无 deleted==1 旧读法残留
- **测试**：pytest 160/160（新增 tests/service/test_dataset_upload_security.py 15 用例 + test_item_file_permission_fix.py 重写 10 用例 + 文件域回归，独立库 dehaze_test_dataset）；SDK dataset-item.test.ts +4（名称超长×2/html 伪装/非法 type）+ item-file.test.ts「两种行为均可接受」收紧为必须 A0400——**待 8991 重启后 main 代跑**
- **文档**：需求规格 §4.2 与后端实现 §7.2 的「create_by 数据隔离」表述三端均未实现且与权威 API 文档矛盾，已按权限模型口径修正；§7.1 文件校验、§7.4 批量标注=规划中 同步
- **遗留观察**：①批量上传某组存储异常时可能残留无图空数据项（校验前置已覆盖绝大多数失败路径）；②Go build 当前失败源于 algo-squad WIP（algorithm_select.go 未用 import），非本域
- **教训（第二次踩）**：同一文件多个 replace_in_file 并行会竞态覆盖（丢编辑），必须串行

## 图像处理链三连收官（21-23/28，2026-09-12）
- 三个 commit + 收尾：图像输入 5a63b4213（24 文件）、去雾处理 25667e8a9（17 文件）、效果对比 2a419fb1c（35 文件）+ c6310d251（result 双重编码根修）+ ca1975663（格式化扫过）
- SDK 终验（main 代跑，统一重启 8991）：model.test.ts 38/38（去雾越权组+compare 越权×4）+ history.test.ts 25/25（两处测试侧口径由 main 修正：空数组 A0400 对齐全项目、algorithmParams 按解析对象比较）
- **eval result 双重编码根因**（值得记住）：SQLAlchemy JSON 列手动 json.dumps + 绑定再序列化 = 库存字符串标量，新写入也复现——根修直接传 dict + parse_eval_result 存量兼容（str→loads/非法 warn None）
- 【待决策小项】①/metrics 指标历史混入报告行（result 为 reportHtml 包装对象，语义不纯净；NOT LIKE hack 不可取，正解是 task_type 列）；②recommendedBy 未消费；③evaluation 页本地 TXT 导出与文档 HTML 报告不一致（无 predLogId 入口）
- 进度：23/28。核心模块 4/8（AI对话+图像输入+去雾处理+效果对比），剩 MCP/数据集管理/算法管理/算法选择。今日累计 6 commit。

## 最后一批启动（24-27/28，三成员并行覆盖剩余全部核心模块）
- mcp-squad（MCP 内部能力网关 24/28）：重点网关安全（凭证/参数注入/端口暴露）、OpenAPI→元 tool 正确性、错误传播；外部 Server 管理已审只做边界核对；网关由 run.py 管理（mcp:8082）
- dataset-squad（数据集管理 25/28，F-M06-001）：重点越权/删除级联/批量导入导出联动；**必做：复核图像输入轮的 item_file sys:dataset:edit 权限修复与数据集权限体系自洽性**；uk 新语义残留核对
- algo-squad（算法管理+算法选择 26-27/28，两域共享 sys_algorithm 合并）：重点算法状态机/被引用下架保护/选择入口过滤口径一致；**必做：get_algorithm_options 软删过滤（推荐轮登记待办）**；测试勿硬编码算法 ID（开发库算法 1 草稿态）
- 三批完成后 28/28 全部收官

## MCP 网关模块收官（24/28，commit 3f8926ece）
- 两大重磅：①**内部网关通道生产从未工作过**（McpGatewayClient 缺 await session.initialize()，mcp 1.24 未协商会话网关一律 400；list_tools 静默空列表致 prefilter 退化仅标题搜索、execute 以 CancelledError 炸推理链 except Exception 接不住）——修复属能力恢复；②SSRF/凭证外泄（x-backend-url 头覆盖删除 + 绑定 0.0.0.0→127.0.0.1）
- binary 过滤根修（原比对描述文本首词恒 "string"）、路径参数 URL 编码、递归 schema 深度上限、OpenAPI 冷启动重试+按需补载
- 测试：gateway 单测 27/27 + live 15/15（mcp1.24↔mcp2.0 握手）+ 客户端回归 7/7 + e2e 4/4
- 8991 客户端修复激活待 dataset/algo 完成后统一重启
- 【待决策】①网关跨机部署需共享密钥鉴权；②M2M key 硬编码默认值迁 .env（secrets env-only）；③M2M key 映射 owner=admin 权限面（LLM 经网关可达 admin API，DELETE 已滤 POST/PUT 未滤）
- java(8989)/mcp(8082) 已由 main 启动在线。今日累计 7 commit。剩 dataset(25)/algo(26-27)。

## 最后一批收官（25-27/28，2026-09-12）——28/28 全部完成
- dataset 60aa91d1f（17 文件）：item_file 复核（Java 三 Controller 12 处 @PreAuthorize 补齐、Go 幽灵权限码修正）、存储型 XSS 防护（hazy-only 路径原无任何校验）、pytest 160/160
- algo dcb46b104（27 文件）+87ae35270：python 状态机死锁根修（已发布算法永远无法删除）、版本/回滚越权、软删语义三端统一、算法选择四端契约收敛；**空分类过滤缺陷连 Java 原实现都有**（parentId=0 扁平数据整树清空，无人消费从未暴露，pytest 把缺陷当规格）——三端撤销过滤改全量返回
- compare 补遗 e6609e119（evaluation router 漏提交）
- SDK 收官全绿：algorithm 72/72 + dataset 86/86（统一重启后实跑；MCP 客户端修复随重启激活）
- 【批次待决策】①predict 下载失败配额泄漏（归去雾域登记）；②Go 缺 audit/version/rollback、Java 缺 select/recommend 三端能力差异（文档标规划中）；③MCP 三项（跨机鉴权/M2M key 迁 .env/权限面收敛）；④历史累积项（metrics 混报告行/recommendedBy/TXT 导出口径/评估缓存规划中等）
- 今日累计 12 commit。**28/28 模块审查全部完成**（基础 19 + 核心 8 + AI对话四小队模式）。工作区干净（仅 .codebaze-memory 垃圾文件待删）。

## misc-fixes 三项零散修复（2026-09-12）
- **A1 FileDTOFactoryTest**：3 个失败根因是测试夹具用 "test content" 伪造 jpg 内容，被魔数校验（对齐 python 权威口径）拒绝；实现本身无缺陷。修测试侧：新增 JPEG_CONTENT 常量（FF D8 FF E0 头），3 用例改用真实魔数。4/4 通过。
- **A3 子 Agent 生命周期钩子**：agent_hooks.py 的 _memory_extraction_hook/_title_update_hook 增 is_subagent 分流（子图跳过记忆提取与标题更新，与计费/trace 钩子同口径）。新增 tests/service/ai/middleware/test_subagent_lifecycle_hooks.py（4 用例），middleware 全量 96 passed。文档同步：上下文管理 §7.3、多步推理 §9.2、架构公共钩子设计要点。
- **C4 evaluation 页报告入口**：核实 /compare/report 入参是 pred_log.logId；sys_eval_log 无 pred_log_id 字段（不需要后端改动）；imageShowStore.predLogId 已存在（去雾页跳转时写入），compare 两页已有同款报告流程。evaluation 页头部新增"生成报告"按钮（复用 predLogId → generateReport → 轮询 getReportStatus → 下载 HTML），TXT 导出保留。vue-tsc 0 错。注意：直接上传图片（非去雾页跳转）时 predLogId=0，按钮提示"当前图片非去雾处理结果"。
- 教训：replace_in_file 报成功但改动落盘丢失（memory hook 首次编辑），复跑验证 grep 确认后发现并重做——编辑后务必验证落盘。

## pred-fixes 去雾/预测域两项修复（2026-09-12，用户决策 A2+C2+C3 执行）
- **A2 predict 配额泄漏**：python predict 扣配额后（文件查询/拦截器/下载/缓存/建日志）前置失败经 try/except `restore_quota` 归还后原样抛出（照 evaluate 模式，quota_deducted 门控防 m2m 误还）；Go 端补 repo.Create 失败 refundQuota（Java 此路径无扣减无泄漏）。新增 tests/service/test_pred_recommend_trace.py 14 用例（网络错/SSRF 拒绝/文件不存在归还、m2m 不扣不还、成功不还）。
- **C2 recommended_by**：sys_pred_log 加列+索引；三端 predict/batch 接收 recommendedBy 落库（python predict/batch_predict/_write_completed_log 签名加参，Java SysPredLogServiceImpl 三路径 set、Go Predict 签名加 *int64）；PredLogVO（py/java）+SDK PredLogVO 透出 recommendedBy。推荐报表 get_report 采纳率口径改为「带 recommended_by 预测数/推荐总数」，满意度=有用反馈占比，趋势=当日采纳预测数/当日推荐数（python 两查询 service 合并、Java SysPredLogMapper.selectDailyRecommended+SysRecommendationMapper.selectDailyTotal、Go recRepo CountRecommended/FindDailyRecommended/FindDailyTotal+mock 手工同步；旧 selectDailyAdoptionRate/FindDailyAdoptionRate 删除）。旧测试 test_get_report_counts_recommendations_and_feedback 断言同步新口径。
- **C3 task_type**：sys_eval_log 加 task_type varchar(20) default 'evaluation'+索引；评估创建写 evaluation（py repo create_pending_log/Java/Go），compare 报告创建写 report；/evaluation/metrics 三端按 task_type='evaluation' 过滤（py list_completed_metrics、Java EvaluationController、Go FindPageByUser）。存量报告行迁移 SQL 用 result LIKE '%"reportHtml"%' 回填。
- **schema+迁移**：config/sql/schema/{sys_pred_log,sys_eval_log}.sql DDL 更新；config/sql/migration/2026-09-12_pred_eval_log_columns.sql（开发库增量执行交 main 协调）。
- **测试**：PYTEST_MYSQL_DATABASE=dehaze_test_pred 新增 14 用例+回归 117 全绿；go build/vet/test 全绿（testutil 自动重建 dehaze_test 顺带验证新 schema 可导入）；mvn -o compile 过；SDK pnpm build 过。Java 无 predict 单测（编译验证）；SDK 集成测试待 8991 重启（predict/batch 传 recommendedBy、metrics 不含报告行、报表采纳率三个断言点需实跑）。
- **再踩同文件并行编辑竞态**（第 3 次）：prediction_service.py batch_predict 签名与 get_report 两处丢失，靠回归测试/复跑暴露——同文件多编辑必须串行+编辑后 grep 验证落盘。
- 文档：03-数据库设计 §4.3、去雾处理 后端实现（§3.1 配额归还+§4.3 recommendedBy 落库）与 API接口（logs/batch）、效果对比 后端实现+API接口（task_type）、推荐管理 后端实现 §4.5（采纳口径）。


## 决策执行批次收官 + 开发库重建（2026-09-12）
- 第二轮决策批次 4/4 全部闭环：misc dc31944ac / mcp-hardening b72e81da2 / kb-billing ad0228c4b / pred-fixes 3df5ea027+499c20899
- **开发库重建（用户指示）**：三日测试残留严重（推荐规则 118 条、算法 3000+、软删行、USER 成长值）；重建前备份 backups/dev_db_backup_20260912.sql（96 表 39345 行）；重建 = DROP+CREATE + config/sql schema(sys_*.sql)+data 全量导入 613 语句零失败；**注意 xxl_job.sql 是独立库 schema（同目录混放），glob 必须用 sys_*.sql 过滤（conftest 同款），且该文件自带删库重建语义会把 xxl_job 恢复种子态**；M2M key 哈希与种子匹配（sha256(.env 值)==种子 key_hash）链路无影响
- 重建后连带修复：①pred-fixes 遗漏的 Java 测试构造器适配（曾阻断 8989 启动——run.py 经 mvn spring-boot:run 启动会先 test-compile）；②推荐防重两项修正（三端顺序 A0401→A0501；判重仅 enabled=1 规则——规则无删除接口仅启停，禁用规则不占位）；③推荐测试三处适配（tie-breaker 单规则/对抗语料独立场景/集成独占 building）
- 重建后验证全绿：recommendation 39/39 + role 59/59 + model 38/38 + MCP live 20/20
- 今日累计 18 commit。全部审查工程 + 两轮决策执行完毕，工作区干净（backups/ 未入库）。**待用户指示：团队关闭（team_delete）与代码 push。**

## api-key 页 clearValidate 报错修复（2026-09-12）
- front-vue `views/system/api-key/index.vue`：`formRef = ref(ElForm)` 把组件定义当 ref 初始值，首次点"创建"（el-dialog 内容未挂载、模板 ref 未覆盖初始值）时 `formRef.value?.clearValidate` 命中组件定义 → not a function。改为 `ref<FormInstance>()`（element-plus type import，项目 6 个较新文件已有此范式）。注意：全项目仍有 ~30 个 `ref(ElForm)` 旧写法，它们只在表单已挂载后才调方法所以未暴露，属潜在同款隐患，未擅改。

## ai-agents 编辑智能体 400 报错修复（2026-09-12）
- `AgentConfigForm.vue loadOptions`：listSkills/listServers 传 `pageSize: 200` 撞后端 `BasePageQuery` 全局上限 le=100 → A0400。改为 100（项目选项加载惯例：ai-billing/endpoint 等处均 pageSize:100）。vite 热更新即生效，无需重启后端。

## admin KB 向量化"配额不足"误报根修（2026-09-12，方案 A+B 用户拍板）
- **根因**：admin（user_id=2，注意 root 是 user_id=1）无 sys_member 行 → quota_service.get_limits 返回 None → fail-closed 拒绝（文案"配额不足"实为"无会员权益档案"）；且 sys_user.credits_balance=0（外部 embedding 模型 estimated>0 时会再撞余额不足；当前 sys_ai_model_price_detail 全 0 价=免费）。会员行唯一自动初始化入口是注册——种子账号与后台建用户永不走注册。
- **方案 A（治本）**：三端登录成功后兜底 ensure 会员档案（幂等）：python auth_service.login 加 get_or_init_member；Java MemberService 新增 ensureMemberProfile（**先查活跃行再 initMember——initMember 的 upsertByUser 冲突分支会降级 level_0 清零 total_consumption，绝不可对活跃会员调用**）；Go 新增 EnsureMemberProfile（FindByUserID→nil 才 initDefaultMember；Go Upsert 只更新 deleted/update_time 天然幂等）。Go mock 经 mockery 再生成（坑：带 --name/--dir 参数的调用会触发全量生成污染目录，应直接裸跑 `mockery` 走 .mockery.yaml；误生成文件已清理）。测试：python +1 用例 15/15、Go auth 适配 fixture（原 setupTest memberService 传 nil 会空指针）+新增 2 用例、member 6.4s 全绿、mvn -o compile 过、回归 93/93。
- **方案 B（数据）**：sys_member.sql 加 admin level_0 行 + sys_user.sql 加 credits_balance 列（admin=100000，其余 0，root 不给——用户明确只修 admin）；开发库已 INSERT/UPDATE 立即生效，get_limits=(10000,100000) check_quota=True 已验证，用户无需重新登录即可重试文档处理。
- **文档**：认证管理 §4.1.1 登录流程加第 3 步会员档案兜底；会员管理 §4.6 补初始化两入口 + Java initMember 降级陷阱注记 + admin 种子说明；AI 计费管理 §4.1 fail-closed 口径补"无档案≠额度耗尽"注记。
- **待办**：python 8991 重启后登录兜底代码才生效（数据侧已通，不阻塞用户）；无 API 契约变更 SDK 不动。

## 模型可用性测试功能落地（2026-09-12，方案 A+B 用户确认）
- **架构事实（重要）**：AI 能力（模型/供应商/对话/知识库等 33 张 sys_ai_* 表）**仅 python 端承载**，Java/Go 无任何 AI 模块（nginx /py-api/ → python），AI 域改动无需三端同步（此前 kb-billing/obs 同款）。
- **A 模型级测试**：`POST /api/v1/ai/models/{modelId}/test`（ai:model:manage）→ `app/service/ai/service/model_test_service.py`：按 model_type 发最小真实推理（chat: openai_compat /chat/completions max_tokens=1 非流式；anthropic /v1/messages；embedding /embeddings（cohere /v1/embed 特判）；rerank /rerank 单文档）；认证复用 build_auth_headers + provider_key_selector；local 供应商探测前 ensure_running。结果落 sys_ai_model 三列 last_test_status(0/1/2)/last_test_at/last_test_error(含HTTP状态+响应摘要,截断500)，失败不抛异常仅缺模型/供应商(A0401)/无Key(A0503)抛；测试后清 ai:model:list 缓存。
- **B 近24h实调**：list_models 聚合 get_usage_stats_24h——chat 按 sys_ai_llm_call（status 1成功/2失败/3超时，算真实成功率）；embedding/rerank 按 sys_ai_billing（成功才落账恒100%）；无调用 null。注意 text() IN :models 必须 bindparam(expanding=True)。
- **SDK/前端**（子代理完成）：AiModelVO +6 字段 + testModel；models/index.vue 最近测试列（三态tag+tooltip）+ 近24h实调列 + 测试按钮（testingIds Set 防重）；mock 补字段。SDK build+test:unit 61 过、vue-tsc 0 错。
- **SQL**：schema sys_ai_model.sql 三列 + migration/2026-09-12_ai_model_test_columns.sql；开发库已 ALTER。
- **测试**：pytest 新增 test_ai_model_test_service.py 12 用例（探测分支×5/成功落库/HTTP错误详情/超时/三个业务异常/24h聚合真库SQL）+ 模型域回归 53 过。
- **文档**：需求规格（生命周期表加"可用性测试"行+模型管理页列表区块）、后端实现（§2.5 三列说明、§2.7 管理端聚合表、新增 §2.7.1 测试服务）、测试用例 T-MF-091~097、前端实现 ModelTable、API接口（子代理）。
- **待办**：8991 重启后新接口才可用；SDK 集成测试（ai-model.test.ts 补 test 接口用例）未做待窗口。

## KB 创建 embeddingProvider 根修（2026-09-12）
- **根因**：`KnowledgeBaseCreateForm.embedding_provider` 默认 "openai"，前端创建表单只传 embeddingModel 不传 provider → 选 bge-m3（注册在 local）时默认 openai 与注册表不一致 → A0400。属设计缺陷：注册表本就是单一事实源，provider 不该由前端/默认值指定。
- **根修**：schema 删 embedding_provider 字段；`_resolve_embedding_model` 返回 (dimension, provider_code)——provider 由注册表模型行 provider_id 反查 get_by_id 推导；create 直接写入。注册表供应商行不存在（数据异常）→ A0400。
- **同步**：SDK CreateForm 删 embeddingProvider（VO 保留回显）；test_kb_service 桩改 get_by_id + mismatch 用例改为"注册表推导写入"正向用例 + 供应商缺失拒绝用例；SDK factory/集成测试注释同步。pytest KB 域 118 过、SDK build+test:unit 61 过、vue-tsc 0 错。其他前端项目无 embeddingProvider 引用（已 grep）。
- **文档**：AI知识库 文档管理 §创建校验、架构与公共 下线迁移行——provider 口径改"注册表推导"。
- **待办**：8991 重启后生效；~~迁移面板遗留~~（已决策下线，见下条）。

## Embedding 迁移面板下线决策（2026-09-12，用户确认"不需要迁移"）
- **决策**：评估后确认现阶段不需要存量迁移功能（无存量数据、低频场景、删库重建即可替代），下线不可用的迁移入口而非实现它——符合"不为不存在的需求写代码"。
- **根因记录**：EmbeddingMigratePanel 借 update 表单携带 embeddingModel 提交，被 update 的"创建后不可修改 embedding 模型或分块策略"校验**直接拒绝**（非静默不生效），从未能用。
- **改动**：删 EmbeddingMigratePanel.vue 组件 + KbConfigPanel 挂载；adminKb store 删 migrateDialog/submitEmbeddingMigrate 及残留 import；vue-tsc 0 错。
- **文档**：前端实现-管理端（组件树/组件表/store 状态操作/权限收敛行）、前端实现-公共（职责边界句）、后端实现-架构与公共 §13 改"规划中"——记录不可用根因 + 立项触发条件（有存量数据 + 供应商停服/成本压力）+ 已评估的实现路径（专有接口：注册表校验→更新库配置→新维度重建索引→BackgroundTasks 逐文档复用 _process_document，进度经 processing_status 可见，单文档失败可 reprocess 自愈）与当前替代路径（删库重建，源文件不丢）。

## KB 分块乱码+句子割裂根修（2026-09-12）
- **乱码根因（已实验证实）**：tiktoken cl100k_base 是字节级 BPE，token 可跨多字节字符字节边界；中文+符号混合语料 ~1/5 token 边界 decode 半个字符产生 U+FFFD（`�"]势` 样例吻合）。`_chunk_fixed`/`_hard_split` 按 token 硬切窗口 decode 即中招。修复：`_decode_window` 窗口首尾遇 � 借相邻 token（上限3次）对齐字符边界，返回 (content, win_start, win_end)。
- **句子割裂根因**：旧 `_cut_at_sentence` 要求"边界后仅空白"（rstrip 后恒假）形同虚设。修复：`_sentence_cut` 在尾部 25% 从后向前找句末/换行边界截断，游标推进用 `_tokens_upto_chars`（窗口内二分 decode 长度定位 token 位置）。
- **三个实现陷阱（连环踩）**：①`len(enc.encode(head))` 孤立编码 ≠ 上下文编码 token 数 → 游标漂移雪崩（必须二分映射）；②`_decode_window` 借用改了 start 但没返回 → 映射偏移；③尾部小窗口（剩余<chunk_size）句切+overlap 回退每轮净前进→0 → 无穷碎块（脏语料 50 个递减碎块被 _merge_small 并成 1290 巨块）——**尾部窗口必须整窗收尾不句切**。
- **测试**：test_kb_chunking 47 过（+5 用例：混合语料全策略零乱码/块尾句边界/短文本单块/无标点滑窗防死循环/不变量放宽 560——软预算=借用3+合并49）；KB 域回归 106 过。
- **文档**：文档管理 §4.1 fixed 行为重写。**用户侧**：已有坏 chunk 需 reprocess 文档重新分块；8991 重启生效。

## dehaze-test 定位章程 + kb_eval 评估体系（2026-09-12，用户确认）
- **dehaze-test 定位（已写入 README）**：开发工作台（Developer Workbench）——面向"人的开发活动"（调试/数据操作/跨端对比/质量评估），人工触发、产物是数据报告非 pass/fail；不承载 CI/产品回归/SDK 契约测试。治理规则：危险操作白名单化、一次性脚本用完即删（migrate_observability.py 已删）、评估产物归档 reports/、"评估发现硬性不变量必须沉淀回各端 pytest，不许留在这里当长期断言"。
- **kb_eval 离线评估套件（已完成）**：corpus_selector（221 篇 md 按规模×结构分层抽样 29+8 对抗，manifest 落盘供检索阶段复用）+ metrics（9 指标）+ offline_eval（5 策略×512/64）。首份基线 reports/offline_baseline_20260912_1815.md：repl=0/coverage=1 验证通过。
- **基线发现的 4 个分块引擎缺陷（量化锚点，待修）**：①recursive 细化时丢 `。`/`，` 分隔符（超长行 coverage=0.978 丢 2.2%）；②semantic _merge_small 无界合并（对抗样本 1199/1600 token 单块）；③semantic _split_at_sentences 贪心重组丢换行（表格行粘连）；④fixed/qa 不感知代码围栏（篇均 2.69 次切断）。fixed sentence_complete_rate 仅 0.155（md 块尾多为列表/标题形态）= 结构感知分块的改进空间证据。
- **待办**：①检索链路评估（testset_builder+retrieval_eval，走 8991 真实链路）——**前置：8991 重启**（今天累积：登录兜底/模型测试接口/KB provider 修复/分块修复）；②建议先跑完检索基线再统一修 4 缺陷（完整 before/after 对比）；③修复后硬性不变量沉淀回 test_kb_chunking。

## kb_eval 检索基线 + 两阶段成本重构（2026-09-12 完成，8991 已重启）
- **成本教训（用户批评）**：首轮 subagent 用"全量重跑"调阈值，fixed 策略建了 4 个库重复执行（单轮 10-15 分钟）。重构为两阶段：**collect**（唯一有成本：建库→6 篇分层代表→逐条检索逐条 flush 落 reports/raw/{strategy}.jsonl，raw 完整即断点跳过）+ **evaluate**（读 raw 离线重放，--threshold 参数化，0.5s 出报告）。总成本：collect 399s（两策略各一轮）+ 重放 0.5s/次。**铁律：调参只重跑 evaluate，严禁重新 collect**。
- **检索基线（39 用例，threshold=0.5）**：fixed Recall@5=0.712/MRR=0.450 全面碾压 semantic 0.305/0.151；semantic 最差 long×code_dense=0.017。**反直觉发现**：semantic 小块化（token_p50=145 vs fixed 472）单块信息量不足→向量表达弱，离线结构指标好≠检索好——此前"连贯性选 semantic"的建议被数据推翻，建库推荐应以检索数据为准（fixed 或调大 semantic 块）。
- 离线-检索对照不一致（fixed 围栏切断最多但召回最高）：召回由 分块结构×embedding特性×query形态 多因素决定，单一指标传导论断需谨慎。
- 命中判定 10-gram shingle 重合度≥0.5，分布两极化阈值不敏感（0.3/0.4/0.5 差异≤0.02）。
- **待用户指示**：①补全 recursive/table collect（各 ~3.5 分钟）；②统一修复 4 缺陷（recursive丢分隔符/semantic无界合并/semantic丢换行/fixed-qa围栏不感知）后重跑两基线做 before/after。

## KB 分块 4 缺陷统一修复 + before/after 基线（2026-09-12 完成）
- **修复**：①recursive 细化分支拼回分隔符（`p if i==0 else sep+p`）；②_merge_small 传 chunk_size 上限（_postprocess 签名加参）；③_split_at_sentences 正则改 `(?<=[。！？!?.])[ \t]*|(?<=\n)(?=\S)`（句号后只吃空格Tab、换行留前段）+ 无标点长段 _hard_split 兜底（注意：往 str 列表塞 Chunk 的类型坑）；④_chunk_fixed 重构为结构感知（_collect_fence/_atomic_chunks 按行贪心/_fixed_token_windows 原 token 窗口主体），table 的 _fixed_text_chunks 别名自动获益。pytest 51（+4 缺陷不变量用例）+ KB 域 106 全过。
- **离线 after（2001 报告）**：fixed fence_brk 2.69→0.41（↓85%）、table_row_brk 2.14→0.86、semantic 超限块清零、coverage 全 1.0。
- **检索 after（意外发现，重要）**：fixed Recall@5 0.712→**0.464**（下降！），recursive 0.594 持平、semantic 0.318 微升、table 0.486 微降。raw 数据归因：**块大小是检索主导因素**——before fixed Top1 块 P50=568 字符 vs after 323（结构感知切碎），semantic Top1 块 P50 仅 32 字符（其检索差的直接证据）；分数非因素（after 均分 0.813 反而略高）。机制：大块含完整节内容→重合命中；小块纯但信息量不足→高分不命中。
- **下轮迭代方向（待用户决策）**：原子块（围栏/表格）与相邻文本块在 chunk_size 预算内贪心合并（保结构完整+恢复块大小），collect 一轮 ~7 分钟验证。

## KB 分块装箱式重构 + 三轮基线对比（2026-09-12 完成，迭代闭环）
- **装箱实现**：_chunk_fixed 重构为流式装箱——_scan_atomic_segments（行扫描产出 text/atomic 段序列）+ settle/parts 累积 + _take_budget_text（预算内句边界优先截取，剩余用字符切片防 � 污染）+ _MIN_TEXT_STEP=50（剩余空间过小即结算防碎块）；超预算原子 _atomic_chunks 按行独立；**装箱模式放弃 token overlap**（原子为天然锚点，test_overlap 断言改无重叠语义）；删除 _fixed_token_windows（无引用）。
- **三轮检索基线（Recall@5 / MRR）**：fixed 0.712/0.450（原始·围栏切断2.69）→ 0.464/0.391（原子独立·块p50=323）→ **0.659/0.479（装箱·块p50=432+围栏0.41）**；recursive 0.621 / semantic 0.332 / table 0.547 均为装箱版最优或持平。fixed 残余差距 0.053 = 原始版跨节大块的 containment 红利（代价是代码被切断）——装箱版 MRR 反超（排名质量更高），质量取舍更优。
- **离线装箱版**：块数/篇 28.4→15.8、token_p50 206→432、fence_brk 0.41、coverage 1.0。pytest 157 全过（overlap 用例改无重叠断言：重复语料下 count==1 会误报，用句计数+去空白长度）。
- **三轮 raw 归档**：reports/raw_before_fix（原始）、raw_atomic_split（原子独立）、raw（装箱），可随时重放对比。
- **评估体系价值验证完毕**：基线→修复→重测→发现权衡→再迭代，每轮成本 collect ~400s/两策略 + evaluate 1s。文档管理 §4.1 fixed 行为已同步装箱口径。

## 父子分块方案定稿（2026-09-12，已写入文档待实施）
- **模拟验证**（零成本，raw 数据 loose 0.1 阈值 vs strict 0.5）：fixed 命中 0.462→0.744（**+0.282**）、recursive +0.263、table +0.205、semantic 仅 +0.051（瓶颈是零召回非扩展）。增益全部来自装箱块与节重合 0.1~0.5 的"部分命中"场景。
- **方案核心**：parent=Markdown 小节（虚拟化不物化，(document_id, section_index) 逻辑分组）；child=节内装箱（复用现有装箱逻辑）；检索 Top-2K child→ES 按 (document_id, section_index) 分组→组内 max 作节代表分→节排序 top_k→拉节内容注入（锚点 `【文档名 > 节路径】`，超预算按 child 分数截断）。sys_knowledge_chunk +section_index/section_path 两列，ES mapping 加 section_index(keyword)。
- **设计决策（写入文档 D1-D5）**：虚拟 parent vs LangChain 物化；parent=结构化节边界（与评估命中单元对齐）；section_index=分组键/section_path=锚点单跳冗余；不引入 LangChain 检索框架（分块算法复杂度在细节、ParentDocumentRetriever 无分组去重逻辑且集成成本>自建增量；对话侧 deepagents 已是正确复用）；child 不跨节。
- **基准文档**：`dehaze-doc/docs/03-模块设计/基础模块/AI知识库/父子分块设计.md`（XX设计.md 命名沿认证管理先例；含三轮基线数据、模拟依据、5 个决策记录、2 个可调点（节分打分/截断策略——评估数据定参数）、2.5 天工作量估算）。实施后设计要点同步至文档管理/检索引擎/测试用例三文档，设计文档转决策记录。
- **待用户指示**：开始实施（可派 subagent 并行 schema/引擎/检索三条线，评估收尾）。

## 父子分块实施完成（2026-09-12，四策略历史最优）
- **引擎（主 agent）**：Chunk/Section dataclass（section_index/section_path 契约字段）；parse_sections 四级降级——标题树（`_sections_by_heading` **行扫描带围栏跟踪**，防代码注释 `# 注释0` 被误判为标题——finditer 版的坑）/段落聚类/句子聚类（`_split_spans`+`_cluster_sections_by_spans` **原文区间切片**，split+join 重组会吃句间空格破坏结构——第二个坑）/单节；所有策略节内执行**除 qa**（Q/A 标记是比标题更强的结构信号，段落聚类会拆散问答对——第三个坑）；_SECTION_TARGET_TOKENS=1500。
- **数据+检索（subagent）**：schema/migration/entity/ES mapping(section_index integer)；document_service 写入带节字段；docx Heading 1-9→`#`标记（_docx_paragraph_text）；repository.list_by_document_sections（(doc_id,section_index) IN 单查询）；search_service 候选×2 + _group_sections（组内 max 节代表分）+ _fill_section_contents（MySQL 拉节拼接，失败降级 ES 候选还原）；VO +chunkIds/sectionPath 只增不删；test_set 命中按 chunkIds 并集；knowledge_base_tool `【文档名 > 节路径】`锚点注入。subagent 还修引擎 3 遗留：_clean_whitespace/_force_split 重建丢 section 字段、_merge_small 跨节合并（加同节约束）。pytest 264 过。
- **开发库**：migration 已执行；活跃文档 0（索引重建无损失）；8991 已重启。
- **检索基线（Recall@5/MRR）四代对比**：fixed 0.712→0.659→**0.725**/0.433（超原始版+结构完整）；recursive 0.594→0.621→**0.720**/0.434；semantic 0.305→0.332→**0.479**/0.451（+0.147 翻倍——父子分块正好救小块策略：小块 child 检索准+节扩展补上下文）；table 0.548→0.547→**0.585**/0.521。fixed/recursive 逼近模拟上限 0.744（97%）；MRR 的 fixed 略降（0.479→0.433）= 节代表分排序特性（设计文档可调点 A 的观察对象）。
- **raw 四代归档**：raw_before_fix/raw_atomic_split/raw_packed_v1/raw（父子版）。设计文档状态转"已实施"（subagent 已同步检索引擎/文档管理实现文档）。

## Qwen3-Reranker-0.6B 本地接入完成（2026-09-12，subagent）
- **选型**：Qwen3-Reranker-0.6B GGUF（Mungert/Qwen3-Reranker-0.6B-GGUF Q8_0，639,150,432 字节，Qwen 官方无 GGUF；QuantFactory 同量化 804MB 异常弃用）——三本地模型全 Qwen3-0.6B 家族。bge-reranker 分类头在 llama_cpp python 绑定不支持（不选）。
- **三个实测驱动的实现决策**（任务书原方案被实测推翻，都是坑）：①`create_completion(logprobs=)` 要求 logits_all=True（16k×15万词表≈10GB 内存）不可行 → 低层 `reset()+eval()+llama_get_logits_ith(-1)` 取末位 logits 做 yes/no softmax（=官方 Transformers 用法）；②端点挂 `/v1/rerank`（rerank_service 由 base_url(.../v1)+/rerank 派生，且 /v1/rerank 是 Jina/SiliconFlow 约定）；③官方 assistant 后缀**必须含空思考块 `<think>\n\n</think>`**，缺失则 top-1 是 <think> 打分全错。
- **触发设计**：不自动下载（600MB，rerank 可选功能）；首次 /v1/rerank 缺模型→后台线程 ensure（fcntl 跨进程互斥）+503 提示，稍后重试自愈；手动预下载命令已备。
- **验证**：真实验证（18992 临时端口）打分单调性完美（相关 0.9989/弱相关 0.0039/无关 0.00017）；单测 9 + seeder 契约 + 回归 273 全过；播种 status=0 保守禁用（已在开发库启用）。e2e：建库接受 enableRerank+rerankModel（注册表校验通过）。
- **待办**：kb_eval rerank 开启对比轮（CPU 预filling 慢——16k token 单对 ~20s，kb_eval 节内容 ~430 token 单对 1-2s、全量 39 用例×10 对约 10 分钟/策略）；生产建议 GPU 构建。
- **后续规划待用户确认**：①query 形态单一（模糊 query 测试集 + query 改写）；②hybrid 自适应权重；③BEIR/C-MTEB 锚点。
- **RAG 质量优化路线已定稿**（2026-09-12，用户确认写入基准文档）：`dehaze-doc/docs/03-模块设计/基础模块/AI知识库/RAG质量优化路线.md`——四代基线数据 + 差距表 + 四阶段路线（1 评估扩展[多形态测试集+rerank对比轮，一切前置]→2 规则版自适应[含"为什么不是 LTR"决策记录：训练数据约束/规则版三重角色/≥80% 收益即终态]→3 数据驱动演进[query改写/外部embedding实测/节分加权/LTR，全部按触发证据立项]→4 C-MTEB/BEIR 锚点）+ 决策原则（数据先行/证据边界/成本纪律/不变量沉淀）+ 基线只升不降红线（回退>0.02 须定位）。
- **设计质询修正（2026-09-12 用户指出过拟合 Markdown）**：架构事实是 Markdown 为中间表示（document_parser 统一产出 blocks，docx 表格已转 md 语法），分块层格式无关。但命中两个真实缺口并已修入设计文档：①`_parse_docx` 丢弃 Heading 样式（type 恒 text，paragraph.style 没读）——补 Heading 1-9 → `#`标记，分块层零改动；②退化路径从"无标题→单节"改为**语义分段节**（双换行+聚类，复用 _chunk_semantic 段落逻辑）。§4.2 增加格式矩阵（md/docx/xlsx/pptx/pdf-fast/hi_res 规划），D2 改写为"parent=语义结构单元，Markdown 为第一实现"。
- **二次质询修正（纯文本/长段落能否正确分割）**：fallback 逐级降级表（4 级：标题树→段落聚类→**句子贪心聚类节**（新增：连续长文本无空行时按句聚合 ~1500 token 切节，复用 _split_at_sentences）→单节+截断兜底）。最坏情况（无标点无换行）child 仍可用（token 硬切字符对齐），仅节扩展收益归零——"不坏"声明。
- **评估流程闭环已验证**：基线→修复→重跑→发现新权衡→再迭代，两阶段架构（collect/evaluate 解耦）全程可控成本（本轮 collect 2×~400s + evaluate 1s）。


## 重新生成 gitee patchset.xz

- gitee/master 基准 feb783f07，本地 HEAD a326036d3（领先 309 提交）
- patchset.xz 672K（--binary diff），worktree 验证 APPLY OK
