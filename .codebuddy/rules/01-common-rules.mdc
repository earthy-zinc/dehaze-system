---
description: 全局通用规则，适用于所有项目。包含回复风格、subagent 策略、注释/文档规范、代码修改原则。
alwaysApply: true
enabled: true
updatedAt: 2026-07-11T01:03:00.000Z
provider: 
---

## 代码修改原则

### 禁止兼容历史烂逻辑
- 发现新逻辑与历史逻辑矛盾、或历史设计不规范时，**直接修改为新逻辑**，不要做无谓的兜底和兼容
- 不保留旧字段名、旧格式、旧接口的向后兼容（如 `user_id` vs `userId`，不要同时保留两者）
- 不写 `or payload.get("old_field")` 之类的 fallback 兜底
- 不保留已废弃的旧代码路径
- 如果改动影响调用方，同步修改所有调用方，而不是在改动处加兼容层

### 修改时要同步所有受影响的位置
- 修改字段名/格式时，全局搜索所有引用点一并修改
- 不要只改一处而期望其他地方"兼容"新的变化
