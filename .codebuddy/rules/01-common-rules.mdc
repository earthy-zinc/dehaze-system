---
description: 全局通用规则，适用于所有项目。
alwaysApply: true
enabled: true
updatedAt: 2026-07-11T01:03:00.000Z
provider: 
---

## 代码修改原则

当代码改动影响了模块接口、数据流或架构设计时，同步更新 `dehaze-doc` 中对应的设计文档。

### 禁止兼容历史烂逻辑
- 发现新逻辑与历史逻辑矛盾、或历史设计不规范时，**直接修改为新逻辑**，不要做无谓的兜底和兼容
- 不保留旧字段名、旧格式、旧接口的向后兼容（如 `user_id` vs `userId`，不要同时保留两者）
- 不写各种不符合业务逻辑的 fallback 兜底逻辑
- 不保留已废弃的旧代码路径
- 如果改动影响调用方，同步修改所有调用方，而不是在改动处加兼容层，禁止仅仅为了方便，重构时不改引用方，而是仅仅把变量/函数重新转发出去
- 禁止薄层封装，如只是一个转发函数，或者仅有一两行逻辑
- 禁止定义复用度不高的常量，如只被一两个地方引用，或者几乎不会经常修改的值，更偏向直接使用而非定义常量
- 禁止撰写任何注释
- 不要定义一堆用处不大的别名，看似有用，但实际用处不大的工具函数，把逻辑撰写的越简单越好，不要把简单的问题搞复杂化，禁止过度设计代码

- 再次强调严格禁止过度设计，能一个函数解决的，不要拆分多个函数，多个变量，多个文件来回引用，将本就简单的需求复杂化

### 修改时要同步所有受影响的位置
- 修改字段名/格式时，全局搜索所有引用点一并修改
- 不要只改一处而期望其他地方"兼容"新的变化
- 修改代码需要同步修改文档对应位置，确保文档和代码一致
- 遇到项目架构、代码细节设计、代码规范方面的问题，务必不要直接改，请查阅dehaze-doc了解整体架构后才可修改

## 开发规则

开发前应查阅 `dehaze-doc` 中的相关文档，仅检索与当前任务直接相关的部分，避免全量阅读：

- **系统架构**：`dehaze-doc/docs/02-系统架构/` — 总体架构、环境兼容性、数据库设计、API 规范、测试架构、部署架构
- **模块设计**：`dehaze-doc/docs/03-模块设计/[模块]/` — 需求规格、API 接口、后端/前端实现、测试用例
- **项目实现**：`dehaze-doc/docs/04-项目实现/` — 各技术栈（Vue/React/Taro/Flutter/Android/Java/Go/Python）的架构文档

对于一个功能的变更，如果涉及到API请求/响应参数增删改、数据流、架构设计，需要同步检查和变更
- dehaze-java、dehaze-python、dehaze-go 三个后端、以及单元测试
- dehaze-sdk-js、dehaze-android/sdk SDK层，以及SDK接口测试 dehaze-sdk-js/test
- dehaze-front-react、dehaze-front-vue、dehaze_flutter、dehaze-react-native、dehaze-android、dehaze-uniapp、dehaze-taro使用到这个API的地方
- dehaze-doc 相关文档

- 其中后端逻辑的变更需要同步修改三个后端、以及单元测试，SDK接口测试
- 前端变更需要同步修改所有前端项目
- 每次变更都需要更新 dehaze-doc 相关文档，确保文档反应最新状态

如需启动/重启/停止 dehaze-java、dehaze-python、dehaze-go 应用，三端后端统一通过 `scripts/run.py` 管理生命周期（启动/停止/重启/查看状态/查看日志），无需手动在各子项目目录下执行启动命令，日志统一存放在各服务 `logs/{yyyy-MM-dd}/` 目录下。可查阅项目根目录 README.md 获得启动指引，并告知用户，切勿私自重启和停止